# BACKLOG-seo — SEO/インフラ班のタスクキュー（並列ループ・2026-06-13〜）

担当領域・契約・絶対ルールは loop-prompt-seo.txt を参照。所有ファイル=sitemap*/robots/manifest/seo-lib/JSON-LDヘルパー/横断検索(search-index・fuzzy-search・notice-search・/search)/app-shell(検索UIのみ)/両layout(metadataのみ)。**着手前に必ず本番とコードの現状を確認**（走行中の全領域ループが先に消化している項目があるため、済みなら[x]にして次へ）。マスター BACKLOG.md は参照専用。

## 未着手（上から処理）

### 2026-07-02 Fable診断注入（診断書: docs/fable-diagnosis-2026-07-02/05-search-egov.md）
（未着手の実装タスクなし。O17/T6 は下記 完了欄のとおり Path A 設計ドラフト化済み＝実装はオーナー承認待ち。3件未満のため次イテレーションは補充の指針§に従い自領域から補充。）

## 完了
- [x] 【柱C-3・S DRY 補充（自領域）ルート layout.tsx metadata のドメイン/ロケール直書きを seo-metadata.ts 単一ソースへ集約＝サイトルート canonical・metadataBase の取り残しドリフト源を是正】(seo/root-metadata-domain-single-source) sitemap/robots/og-image/json-ld/page-json-ld は既に SITE_URL 集約済みだったのに、**全ページの og:url/canonical 解決の基点かつサイトルート canonical という最重要箇所**であるルート `layout.tsx` の `metadataBase: new URL("https://www.anzen-ai-portal.jp")` と `alternates.canonical: "https://…"` だけがドメイン直書きの取り残しで、`SITE_URL` を別ドメインへ替えてもホームページの canonical と全ページの og:url 基点が旧ドメインへ無言ドリフトする構造上の穴だった。あわせて `openGraph.locale="ja_JP"`・`alternateLocale=["en_US"]`・`siteName="安全AIポータル"` の直書きも `SITE_LOCALE`/`SITE_ALTERNATE_LOCALES`/`SITE_NAME` へ集約。各定数は従来リテラルと同値のため **metadata 出力は byte-identical**（挙動変更0）。回帰: `layout-metadata-single-source.test.ts` 新設＝(1)layout.tsx にドメイン直書き（`bareDomain`）が残らず SITE_URL を import (2)locale/siteName も直書きせず単一ソース定数使用・旧リテラル消滅 (3)単一ソース定数が従来値と同値＝byte-identical の前提固定。sitemap-domain-single-source.test.ts と同型の source-scan ガードで再発を機械封止。tsc0・lint errors0・関連テスト緑・build成功・再生成データ復元。
- [x] 【柱C-2・S 横断検索の化学物質ヒットを canonical 個別詳細 /chemical-database/[cas] へ深リンク＝サイト最大級コーパスへ内部リンクを通す（補充タスク／PR #786）】(seo/chemical-search-canonical-detail-deeplink) 横断検索(/search・⌘K)の化学物質ヒットは mock50・MHLW約3,954件とも一覧クエリページ `/chemical-database?q=<名称>` へ落ちており、`sitemap-chemicals.xml` 収載の**3,515本の個別詳細**（濃度基準値・GHS区分・関連法令を出典付きで持つ自己canonicalな実在ページ＝サイト最大級の独自コンテンツ）を素通りしていた。事故 `/accidents/[id]`・保護具 `/equipment/[id]`・通達 `/circulars/[id]` は既に個別詳細へ深リンク済みなのに、より大きい化学物質DBだけが取り残された内部リンク(発見性)＋UX(詳細ページ vs 名称検索一覧)の二重の穴。正本＝濃度基準DB `concentration-limits.json` のキー集合を単一ソースに使う（詳細ページは `CONCENTRATION_LIMITS.substances[normalizeCas(cas)]` が無いCASを `notFound()` で弾くため、この集合こそ「実在の詳細ページに解決するCASの全体」）。ただし本体は約 **2.0MB** で client の検索チャンクへ載せられないため、**キーのみ（数字とハイフンのみ・40KB / gz13KB）** を軽量スナップショット `src/lib/cross-search/chemical-detail-cas.json` として抽出（`scripts/gen-chemical-detail-cas.mjs`。出力は data 班所有の `src/data/` を避け当班 lib 配下）。純関数 `chemicalDetailUrl(cas,name)` が membership を判定＝収載CAS（実測 compact **2481/2595**・mock **48/50** ≒ **95%**）は `/chemical-database/<cas>`（sitemap収載URLと1:1一致）へ、未収載の残り約4%は従来の一覧クエリページへフォールバック＝**幽霊URL 0**（存在しない詳細へは決してリンクしない）。ドリフト検出: `chemical-detail-cas.test.ts` がスナップショットと濃度基準DBキー集合の**完全一致**をCIで固定＝data班が濃度基準DBを更新して乖離したら赤化して再生成を強制（silent soft404 を封止）。回帰: 深リンク先CASが全てキー集合に解決／未収載はq=フォールバック／全角・空白CASの正規化／URLエンコードを検証。tsc0・lint errors0・全**2562**テスト緑（新規13）・build成功・再生成データ復元。
- [x] 【柱C-2・S 補充（自領域）横断通達検索に現場口語・旧法令名・障害名の橋渡しシノニムを追加＝現場語彙で通達 0 件になる取り逃しを是正】(seo/notice-search-field-vocab-synonyms) `notice-search.ts`（安衛法チャットボットが回答に添付する関連通達・告示・指針を実データ `@/data/mhlw-notices` から選ぶ横断検索・当班 C-2 所有）の `TOPIC_SYNONYMS` は熱中症/化学物質/石綿等 14 群を持っていたが、**現場・一人親方が実際に打つ口語・旧称・障害名が通達タイトルの正式表記と食い違い 0 件になる穴**があった。実測: 「酸欠」は通達タイトルに **literal で 0 件**（正式表記は「酸素欠乏」＝4 件）・「振動障害」も **0 件**（原因作業の正式表記は「チェーンソー」＝7 件）で、そのまま打つと関連通達がまったく surface しなかった（石綿↔アスベストと同型の語彙ギャップ）。実データ裏付けを取った上で 3 群を新設＋1 群を拡張＝(1)`酸素欠乏:[酸素欠乏,酸欠,硫化水素]`（現場口語「酸欠」→酸素欠乏症・硫化水素中毒通達）(2)`墜落制止用器具:[墜落制止用器具,安全帯,フルハーネス,墜落,転落]`（2019 改称前の旧称「安全帯」→現行の墜落防止通達群・タイトルに安全帯 7/墜落制止用器具 6 実在）(3)`チェーンソー:[チェーンソー,振動障害,白ろう病,振動工具]`（障害名→林業の振動ばく露通達）(4)`教育` 群へ「職長」追記（「職長」単独打鍵→安全衛生教育群へ展開）。**捏造 0**＝各語は mhlw-notices の実在タイトル表記に裏付け（酸素欠乏/硫化水素/チェーンソー/墜落/転落 は全て実タイトルに存在）。**既存破壊 0**＝展開は加点方向のみ（トークン追加でrecall拡張）で、既存テストの熱中症/アスベスト/化学物質/石綿クエリはどれも新群のトリガ語を含まず無影響。回帰: `notice-search.test.ts` に「現場口語・旧称・障害名の橋渡し」describe 5 it 追加＝(前提固定)「酸欠」「振動障害」がタイトルに literal 0 件＝hits>0 が橋渡し由来である証明・「酸欠」→酸素欠乏/硫化水素 surface・「振動障害」→チェーンソー surface・「安全帯」→墜落/転落/墜落制止用器具 surface・「職長」展開、全 hit が正本 mhlwNotices の実 id に解決（幽霊通達 0）。tsc0・lint errors0・全2557テスト緑（新規5含む）・build成功。
- [x] 【柱C-3-3・S 補充（自領域）RSS フィード逆カバレッジガード新設＝実在 /feed/*.xml ルートの SITE_FEEDS 登録漏れ（自動発見不能な孤立フィード）を機械検知】(seo/feed-reverse-coverage-guard) `SITE_FEEDS` 登録簿（feeds.ts）は全ページ `<head>` の `<link rel="alternate" type="application/rss+xml">` の単一ソースで、既存 `feeds.test.ts` は「登録path→route.ts 実在」「title↔channel title 一致」の**forward**ドリフトは守っていたが、逆向き「実在 route→登録簿」が皆無だった＝当班/他班が `src/app/feed/<slug>.xml/route.ts` を新設しても `SITE_FEEDS` へ登録し忘れると、そのフィードはどのページ `<head>` にも広告されず RSS リーダー・クローラから**自動発見不能な孤立フィード**のまま放置される穴（#750 の sitemap 逆カバレッジと同型）。`feeds.test.ts` に逆カバレッジ describe（1 it・**コード変更0のガード**）を追加＝`readdirSync` で `src/app/feed` 直下を走査し GET ハンドラを持つ `<slug>.xml/route.ts`（route group `(x)`/private `_` 除外）を全て `/feed/<slug>.xml` へ射影し、各々が `SITE_FEEDS` 登録済みであることを必須化（走査サニティ＝既知4本以上）。ミューテーション実測＝serious-cases を SITE_FEEDS から外すと当該 it が `[ '/feed/serious-cases.xml' ]` で赤化することを確認。現状の実在4フィードは全登録済み（コード修正不要＝純ガード）。tsc0・lint errors0・全2539テスト緑（新規1含む）・build成功・再生成データ復元。
- [x] 【柱C-2・S 補充（自領域）横断検索に対象4類型のペルソナ別ポータル /for/<persona> を収載＝立場名で0件だった発見性の穴を是正】(seo/cross-search-persona-portals-for・PR #812) サイトの唯一のゴール（対象ユーザー4類型＝現場監督・一人親方・安全担当・労働安全コンサルにとって日本一使いやすい）に直結するペルソナ別実務ポータル `/for/{construction,solo,manager,consultant}`（sitemap priority 0.85〜0.9・自己canonical・index:true・OGP付の実在 indexable ハブ）が、横断検索(/search・⌘K)から丸ごと0件だった＝「職長」「一人親方」「安全衛生担当」「社労士」と**自分の立場名**で探した対象ユーザーが、自分専用の入口ハブへ検索経由で着けない発見性の穴（#561 等と同型）を是正。FLAGSHIP ナビ非掲載のため `site-pages-search-source.ts` の `EXTRA_DESTINATION_PAGES`（助成金ハブと同じ手当て補充枠）へ4件追加。title/subtitle/keywords は各ページ metadata の title/description が自ら advertise する**立場・役割名のみ**に絞り、墜落/足場/化学物質等のハザード語・法令名は入れない＝条文・通達の権威クエリを汚さない（保護具/機能ページ/疾患ガイドと同方針）。feature 扱いで既存カテゴリへ収載＝**新規タブ/カテゴリ追加なし**（4件に比例的）。url は全て sitemap 実在（drift ガードで機械固定＝幽霊URL0・薄い/noindex混入0）・FLAGSHIP ナビと非重複。回帰: (1) `site-pages-search-source.test.ts` に「/for/<persona> が立場名 keyword で結線」describe を追加 (2) `search-index.test.ts` に buildSearchIndex 経由で立場名クエリ（職長/現場代理人/一人親方/安全衛生担当/社労士/労働安全コンサルタント）が当該ポータルへ着地する統合テストを追加。feature 件数一致断言は getSitePageSearchEntries().length 参照のため自動追従。tsc0・lint errors0・全2640テスト緑・build成功・再生成データ復元。**要・他班（注記のみ）**: `/for/<persona>` を FLAGSHIP ナビへ載せるか否かはナビ config 所有の ux-hub 班判断。
- [x] 【柱C-2・S 補充（自領域）横断検索に助成金・補助金ハブ（/subsidies・/subsidies/calculator）を収載＝「助成金」「補助金」で0件だった中小企業/一人親方向け発見性の穴を是正】(seo/cross-search-subsidies-hub) 横断検索(/search・⌘K)の `feature` カテゴリは FLAGSHIP ナビ由来の目的地ページのみ収載していたが、**sitemap 収載済み・indexable な独立ハブ `/subsidies`（助成金・補助金ガイド＝中小企業の安全投資ROIと労災経済損失試算）と `/subsidies/calculator`（支給額試算ツール）は FLAGSHIP ナビ非掲載のため横断検索から丸ごと0件**だった＝「助成金」「補助金」と打った中小事業主/一人親方（本サイトの中核ユーザー）が公的助成金ハブへ検索経由で着けない発見性の穴を是正。`site-pages-search-source.ts` に `EXTRA_DESTINATION_PAGES`（ナビ外の indexable 目的地ページの手当て補充・ナビ config は当班所有外のため触らず検索の発見性だけ是正）を新設し、`getSitePageSearchEntries()` の末尾でパス衝突時ナビ優先（先勝ち）で merge。keyword は個別助成金の金額/条件を写経・捏造せず、ページ metadata description が自ら advertise する3制度（エイジフレンドリー補助金・働き方改革推進支援助成金・建退共）＋一般語（助成金/補助金/支援金/中小企業/一人親方）に限定。**新規タブ/カテゴリ追加なし**（既存 `feature` カテゴリへ2件を比例的に収載）。回帰: `site-pages-search-source.test.ts` に describe「補充: FLAGSHIP 外の indexable 目的地ページ」4 it 追加＝(1)全補充URLが `sitemap()` に実在＝indexable（薄い/noindex 混入0・幽霊URL0より強い保証）(2)ナビ正本パスと非重複（二重管理回避）(3)助成金ハブが「助成金」「補助金」keyword 付きで結線 (4)全補充URLが最終射影に含まれる。`search-index.test.ts` の feature describe に1 it 追加＝「助成金」「補助金」で /subsidies・「助成金 試算」で /subsidies/calculator が着地。既存 feature 件数一致断言(`page-*`射影源=getSitePageSearchEntries().length)は動的比較のため自動追従。tsc0・lint errors0・全2602テスト緑(新規5含む)・build成功・再生成データ復元。**要・他班（注記のみ）**: `/subsidies` を FLAGSHIP ナビ subItems へ載せるか否かはナビ config 所有の ux-hub 班判断。個別助成金の詳細ページ `/subsidies/[id]` は未実装（現状ハブが唯一の目的地）。
- [x] 【柱C-2・S 補充（自領域）横断検索に「治療と仕事の両立支援 病態別ガイド」6疾患を収載＝疾患名0件だった発見性の穴を是正】(seo/cross-search-illness-guides・PR #803) 横断検索(/search・⌘K)の `feature` カテゴリは FLAGSHIP ナビの目的地ページを収載するが、親ハブ `/treatment-work-balance`（subItem 収載済み）配下の**疾患別ガイド** `/treatment-work-balance/illness-guide/[illness]`（がん/脳卒中/心疾患/糖尿病/メンタルヘルス/難病の**6疾患**＝自己canonical・OGP付・PageJsonLd・sitemap収載済みの実在 indexable ページ）が横断検索から丸ごと**0件**だった＝「がん 両立支援」「脳卒中 復職」「糖尿病 就業配慮」と疾患名を打った安全担当/産業医が個別ガイドへ検索経由で着けない発見性の穴（#561 等と同型）を是正。`search-index.ts` に `ILLNESS_CATEGORIES` を動的import する射影ブロックを新設（signs/equipment/faq と同型のブラウザ安全な直import・遅延検索チャンク側にのみ載る）。title=疾患短縮名+両立支援ガイド・subtitle=病態サマリ・keywords=正式疾患名+両立支援頻用語+病態別リスク（症状語で引ける）。url は `generateStaticParams`(dynamicParams=false)が全 id を解決＝**収載集合＝解決集合で必ず着地（幽霊URL 0）**。関連法令は条文の権威クエリ汚染回避で keywords へ入れない（保護具/機能ページと同方針）。目的地ページ扱いで既存 `feature` カテゴリへ収載＝**新規タブ/カテゴリ追加なし**（6件に比例的）。locked不変条件（就業制限1位=安衛法61条・石綿事前調査=石綿則3条）は疾患名が当該クエリに非出現のため無影響。回帰: `search-index.test.ts` に describe 2本追加＝(1)全6疾患が illness-guide 深リンクへ着地・解決集合一致・幽霊URL0・親ハブ止まりでない (2)疾患名クエリ(がん/脳卒中/糖尿病)で個別ガイド着地。既存 feature describe の件数一致断言を `page-*` 射影源に限定（2源共存に追従）。tsc0・lint errors0・全2588テスト緑(新規2含む)・build成功(6ガイド全SSR prerender確認)・再生成データ復元。
- [x] 【柱C-2・S T4後段の ⌘K パリティ＝横断検索の第2入口(⌘K)の0件フォールバックにも「法令名＋条番号→e-Gov 条アンカー直リンク」を結線】(seo/cmdk-egov-article-anchor-parity) `egovArticleAnchor`（#767 で新設・lib/cross-search/egov-fallback.ts）による「抄録未収載の条番号を打っても当該法令の e-Gov 該当条(#Mp-At_N)へ1タップ着地」は #767 時点で `/search` NoResults にのみ結線され、**⌘K(CommandPalette) 側は在庫 PR #743(⌘K 0件パリティ)と同一ファイルの二重編集を避けるため後続へ保留**されていた。#743 マージ後は overlap が解消したため本 PR で結線＝⌘K の0件ブロックでも `egovArticleAnchor(debouncedQuery)`(useMemo)が非nullなら teal の直リンク「e-Gov で『{正式名称} {第N条}』を開く」(44px標的・`trackEvent('search_zero_result_egov_article')` で /search と同一イベント名)を amber 収録範囲注記とポータルトップ導線の間に描画。**両検索入口(/search・⌘K)で0件UXが完全一致**＝curated 未収載の条を打った現場ユーザーがどちらの入口でも原文へ最短導線で着地。lib 純関数を再利用し実装重複0・可視の検索UI以外は無改変(既存破壊0・捏造0・幽霊リンク0＝法令トップは必ず実在)。回帰: `CommandPalette.test.tsx` に 1 it 追加＝「安衛則 第9999条」(法令名+抄録未収載条)で0件→ #Mp-At_9999 直リンク(href=`https://laws.e-gov.go.jp/law/{安衛則egovLawId}#Mp-At_9999`・target/rel)を RTL 実描画で駆動＝`SearchResults.egov-anchor.test.tsx`(/search)と同型固定。tsc0・lint errors0・全2573テスト緑(新規1含む)・build成功・再生成データ復元。
- [x] 【柱C-3-3・S 補充（自領域）PWA manifest のメディア資産（icons/screenshots）実在ガード新設＝幽霊アイコンでインストール不可になる穴を CI で封止】(seo/manifest-asset-existence-guard) `manifest.test.ts` は既に**ショートカット URL の実在**（幽霊ショートカット防止）を機械突合していたが、manifest の静的メディア参照（`icons[].src`＝/icon-192x192.png・/icon-512x512.png、`screenshots[].src`＝home/ky/chatbot × desktop/mobile 6枚）は**未ガード**で、他班が public/ の画像をリネーム・削除しても参照文字列は残りリンク切れは PWA インストール時にしか露見しない穴だった。とりわけ **192px+512px の png icon が揃うことは Chrome のインストール可能性（beforeinstallprompt）の必須要件**で、512 が消えるとアプリが丸ごとインストール不可になるのに CI 無検知（発見性＝ホーム画面設置導線の静かな死）。ショートカット実在ガードと同型の `publicAssetExists(src)`（`public/` 配下へ existsSync 突合）で describe「メディア資産 実在ガード」4 it を新設＝(1)全 icon src が実ファイルへ解決＝幽霊アイコン0 (2)インストール必須要件の 192/512 png icon が実在 (3)全 screenshot src が実ファイルへ解決＝幽霊スクショ0 (4)リッチインストール UI 用に wide/narrow 両 form_factor 具備。**コード変更0**＝現状全資産は実在（緑）で、既存挙動の特徴づけ固定＝将来の資産削除/改名を発見性側で赤化させる回帰ガードのみ（水増し・捏造0・既存破壊0）。resolver のミューテーション実測＝存在しない src で false・実ファイルで true を確認。tsc0・lint errors0・manifest.test.ts 9 it 全緑（新規4）・build成功。
- [x] 【柱C-2・S 0件フォールバックを e-Gov 条アンカー直リンクへ格上げ＝T4 後段「収録外条番号→当該法の e-Gov 条アンカーへ誘導」を実装（診断書 05-search-egov.md T4/G7）】T4 の 0件フォールバックは #743 までで /search・⌘K 両入口に「収録範囲の明示＋e-Gov ポータルトップ導線＋クエリのクリップボード引き継ぎ」を備えたが、**後段の「収録外の条番号を打ったユーザーを当該法令の e-Gov 該当条へ直接誘導する」は未実装**で、抄録未収載の条を打った現場ユーザーは e-Gov トップで貼り付け→検索→目次スクロールの複数ステップを踏まされていた。当班 lib の純関数 `egovArticleAnchor(query)` を `lib/cross-search/egov-fallback.ts` に新設＝クエリを条番号パーサ(`normalizeArticleQuery`)＋かな読み展開(`expandLawAliases`)で正規形へ寄せてから、**法令名（略称/正式名称）が明示され かつ e-Gov 法令番号(`LAW_METADATA.egovLawId`)を持つ法令＋基条番号**のときだけ `https://laws.e-gov.go.jp/law/{id}#Mp-At_{N}`（O18 の条文参照リンカーと同形・e-Gov 新UIで実測有効）を返す。**法令正確性は不可侵**＝(1)裸の条番号から法令を推測しない(誤誘導回避で null) (2)枝番「第N条のM」は e-Gov Mp-At_N が基条しか指せず誤着地するため対象外(リンカーと同方針) (3)法令番号を持たない/未知の法令は null＝従来のポータルトップ導線に委譲。法令トップURL自体は必ず実在＝到達可能で、仮に当該条が存在しなくても 404 ではなくアンカー不発になるだけ＝NoResults の amber 注記「条文の有無・原文は e-Gov でご確認ください」と整合(幽霊リンク0)。`/search` の NoResults に teal の直リンク「e-Gov で『{正式名称} 第N条』を開く」を追加(44px標的・`trackEvent('search_zero_result_egov_article')` で計測分離)。**⌘K 側は在庫PR #743(⌘K の0件パリティ)と同一ファイルを二重に触る overlap 回避のため本PRでは触れず後続で結線**。回帰: `egov-fallback.test.ts` に 8 it 追加＝略称/正式名称/かな読み/漢数字の解決・最長一致(施行令>親法)・枝番null・裸条番号null・未知法令/条番号なしnull・**生成番号が LAW_METADATA 実在(捏造0)**。`SearchResults.egov-anchor.test.tsx` 新設＝法令名+抄録未収載条(安衛則 第9999条)で NoResults を実描画し #Mp-At_9999 直リンク(target/rel)を RTL で駆動。tsc0・lint errors0・全2529テスト緑(新規9含む)・build成功・再生成データ復元。**要・他班（注記のみ）**: 条文カード本文への参照リンカー結線(O18)は law-search-panel.tsx 所有の ux-tools 班。
- [x] 【柱C-3-3・S 逆カバレッジガード新設＝実在 indexable ページの sitemap 欠落を機械検知＋発見された /profile を収載】(seo/sitemap-reverse-coverage-guard) 既存のゴーストURLガード（#724）は「sitemap→ルート」（載っているURLが実在するか＝デッドURL0）を守るが、逆向き「ルート→sitemap」（**実在する公開ページがちゃんと載っているか＝欠落0**）のガードが無く、当班以外（ux-records/tools/hub）が新しい公開ページを追加して sitemap.ts のハンド保守URL列へ収載し忘れると、そのページは検索エンジンから発見されにくい孤立ページのまま放置される穴だった（柱C-3-3 の個別追加テストは既知ページを固定するだけで、未知の新規欠落は検知できない）。`sitemap.test.ts` に逆カバレッジガード describe（4 it）を新設＝`src/app` 配下の静的ルートを全走査し、各々が (a) robots.ts の Disallow 配下（**robots() 出力から単一ソース参照**＝二重管理せず /admin/ ・/auth/ ・/lms 等を自動追従）、(b) ページ自身の `robots:{index:false}` 宣言、(c) redirect/permanentRedirect スタブ、(d) 当班の意図的非収載 allowlist（`/organization`＝正式リリース前デモ・理由必須）、のいずれにも当たらない＝**indexable な実ページ**なら sitemap 収載を必須化。所有UI班が noindex 宣言すればガードは自動追従する（当班がページ本文の index 方針を決めるのではない）。本ガード新設で全169静的ルートを機械突合し、`/profile`（自社プロファイル登録＝index:true・自己canonical・OGP付き・全機能の初期表示最適化の入口）が**どの sitemap にも載っていなかった発見性の穴**を検出→ sitemap.ts へ収載（lastmod=2026-05-29 git履歴由来・priority0.6・monthly）。`/organization` は既存の非収載境界テスト（デモ版モック）と対で allowlist へ理由付き明示。ミューテーション実測＝/profile を sitemap から外すと当該 it が赤化することを確認。tsc0・lint errors0・全2479テスト緑（新規4含む）・build成功・再生成データ復元。
- [x] 【柱C-2・S ⌘K 横断検索の0件フォールバックを /search と同一の安全ガードへパリティ化＝未収載を「規定なし」と誤読させる穴を第2の検索入口でも塞ぐ】(#743) 横断検索は /search（結果ページ）と ⌘K（CommandPalette）の**2入口が同一インデックス（search-index.ts）を共有**するが、0件時のUXが乖離していた＝/search は T4（診断書 05-search-egov.md G7）で「収録範囲の明示（未収載≠規定なし）＋e-Gov 法令検索への逃がし＋trackEvent」を備えるのに、**⌘K は 0件で素の『結果が見つかりませんでした』のみ**＝curated 抄録に無い条番号を打った現場ユーザーに「その規定は存在しない」と誤読させる安全上のリスクが第2入口に残存していた（footerの『すべての結果を見る』→/search で間接救済は可能だが直接の一次情報導線が無い）。`CommandPalette.tsx` の 0件ブロックに /search の NoResults と同一方針の**安全ガード（amber の「見つからない＝規定がないではありません」収録範囲注記）＋e-Gov 法令検索リンク（`@/lib/cross-search` の `EGOV_LAW_SEARCH_URL`＝到達可能なポータルトップに固定＝幽霊リンク0）＋`trackEvent('search_zero_result_egov')`（/search と同一イベント名で両入口の0件計測を統一）**を追加。lib ヘルパーを再利用し実装重複なし・可視の検索/メタ以外は無改変（既存破壊0・捏造0）。回帰: `CommandPalette.test.tsx` 新設（2 it・RTL で実コンポーネントを 0-hit クエリまで駆動）＝(1)e-Gov リンクが EGOV_LAW_SEARCH_URL・target=_blank・rel に noopener (2)「見つからない＝規定がない ではありません」の安全コピー描画。jsdom 未実装の scrollIntoView を no-op stub。tsc0・lint errors0・build成功・全2469テスト緑（新規2含む）。
- [x] 【柱C-2・S 補充（自領域）横断検索に「機能・目的地ページ」を収載＝機能名を打っても検索0件だった発見性の穴を是正】横断検索(/search・⌘K)はこれまで**コンテンツレコード**（条文・通達・判例・化学物質・事故・教育・用語・FAQ・標識・保護具・記事）のみ収載し、サイトの主要な**目的地ページそのもの**（サイネージ・KY用紙・化学物質RA・作業環境測定・事故DB・法改正…）が丸ごと0件ヒットだった＝「サイネージ」「KY」「作業環境測定」と機能名を打った現場ユーザーが目的機能へ検索経由で着けない発見性の穴（⌘K の空クエリ用ショートカット4件は検索対象外・サイドバー回遊のみが導線だった）。**ブラウザ安全な射影源 `lib/site-pages-search-source.ts` を新設**＝機能ナビ正本 `FLAGSHIP_FEATURES`(`@/config/flagship-nav`＝サイドバー/トップが参照)を単一ソースに、主要機能＋配下機能(subItems)をベースパスで重複除去して射影(アイコン絵文字は検索不要＝射影せず検索チャンク軽量化)。search-index からは**動的import**され遅延検索チャンク側のみに載る。`search-index.ts` に `feature` カテゴリ新設(CATEGORY_META=slate・SEARCH_CATEGORIES・SEARCH_CATEGORY_PRIORITY=保護具の直上=コンテンツ系より下位ティアで権威を奪わない・countByCategory・全カテゴリ合計テストに c.feature 追加)。title=機能名(打鍵一致「サイネージ」「化学物質RA」)・subtitle=カード見出し/配下説明(2語AND補助)。url はハッシュ/クエリ除去のベースパスで実在ルートへ解決(**幽霊URL 0**)。cross-search 既定優先度が元々 `feature` を先頭に持つ設計(死蔵時の想定)と整合。UI(SearchResults/CommandPalette)に 機能 タブ＋LayoutGrid アイコン追加。**drift ガード**＝`site-pages-search-source.test.ts`(6 it)で(1)非空・app走査サニティ(2)全 url が実在 page ルートへ構造解決＝幽霊URL0(sitemap ゴーストガードと同 resolver)(3)`/industries/<slug>` が generateStaticParams の `INDUSTRY_CONTENT_SLUGS` へ台帳一致(4)全 FLAGSHIP 主要機能 href の収載漏れ0(5)url重複0(6)title/subtitle/絶対path 具備＝機能追加を「検索へ結線し忘れる穴」を CI で強制。`search-index.test.ts` に feature describe(2 it)＝射影源件数一致・機能名クエリ(サイネージ/化学物質RA/作業環境測定)着地・2語AND(事故 分析)。**locked不変条件(就業制限1位=安衛法61条・石綿事前調査3位=石綿則3条)は無回帰**(機能エントリは当該語で高スコアにならず下位ティア＝T1/T2/T3 回帰緑)。tsc0・lint errors0・全2500テスト緑(新規8含む)・build成功。**要・他班（注記のみ）**: 非flagshipのユーティリティpage(/favorites・/about・/goods 等)や個別記事本文SSRは対象外＝当班は主要機能導線の発見性のみ是正。
- [x] 【柱C-2・S 補充（site-critique 01 S-1）横断検索に法改正記事(10本)を収載＝/articles・sitemap掲載なのに検索0件だった発見性の穴を是正】(#738) 法改正記事（`src/data/articles/*.json`＝監修済みの法改正・実装ガイド解説 10本）は `/articles` 一覧と `sitemap-articles.xml` には載っているのに横断検索(/search・⌘K)から**丸ごと0件ヒット**だった（critique 01「法改正記事という検索需要のあるコンテンツ枠が丸ごと死んでいる」）。原因は正本 `getPublishedArticleIndex` が `node:fs`(readdirSync) 依存でブラウザ非安全＝client 検索インデックスから import 不可（過去 iteration で「要・他班」保留だった穴）。**ブラウザ安全な射影源 `lib/articles-search-source.ts` を新設**＝10本の記事 JSON を静的import(fs非依存)し本文(sections/sources)除外の軽量エントリへ射影。search-index からは**動的import**され遅延検索チャンク側にのみ載りメインバンドル非肥大(court-cases 全件・compact.json 919物質を既にロードする同じチャンク)。`search-index.ts` に `article` カテゴリ新設(CATEGORY_META=violet・SEARCH_CATEGORIES・SEARCH_CATEGORY_PRIORITY=FAQ の次点で条文/教育/FAQ には譲る・countByCategory)。url は `/articles/<slug>` 深リンク＝`/articles/[slug]` の generateStaticParams(`getPublishedArticleSlugs`)が公開済み全 slug を解決するため必ず着地(**幽霊URL 0**)。時限公開(publishedAt が未来)は実行時 now で除外＝正本と同セマンティクス。UI(SearchResults/CommandPalette)に 記事 タブ＋Newspaper アイコン追加。`/search` メタ description を実カバレッジ(法令条文・法改正記事…)へ更新(旧5カテゴリの陳腐化是正)。**drift ガード**＝`articles-search-source.test.ts`(3 it)で実在ファイル集合(readdirSync)と slug 集合の双方向一致を機械固定＝data班の記事追加を「横断検索へ結線し忘れる穴」を CI で発見性側へ強制(sitemap ゴーストURL回帰ガードと同方針)。`search-index.test.ts` に article describe(3 it)＝公開済み全件収載・深リンク slug が `getPublishedArticleSlugs` へ解決(soft404 0)・タグ/キーワードヒット(熱中症/フルハーネス)。tsc0・lint errors0・全2467テスト緑(新規6含む)・build成功。**注**: 個別記事の JSON-LD(NewsArticle)や本文SSRは /articles ページ所有UI班の担当。
- [x] 【柱C-4・S JSON-LD 低頻度 provider/creator ノード＋WebSite.author を @id 集約＝#704 follow-up を単一PRへ統合完遂（#719 を統合・supersede）】(#704 の明記 follow-up 候補) #704 で Organization/WebSite/Article/NewsArticle/WebApplication/WebPage の主要ノードは正準 `ORG_ID`/`WEBSITE_ID` へ @id 集約済みだが、**低頻度スキーマ 5 種（`serviceSchema.provider`・`courseListSchema` の Course.provider・`datasetSchema.creator`・`quizSchema.provider`・`dataCatalogSchema.creator`）が name/url をインライン再宣言したまま取り残されて**おり、検索エンジンから「同一の自サイト組織」が別ノードに分裂＝ナレッジグラフのブランド同定シグナルを希釈する #704 と同型の残穴だった。加えて `qaPageSchema` の `isPartOf` WebSite は `@id` 未同定（さらに publisher 欠落）・`webSiteSchema.author` は別 url（`/about`）のインライン Organization を宣言し**全ページで**別ノードへ分裂していた。`json-ld.tsx` に軽量参照 `ORG_REF`（`@id:ORG_ID`）/`WEBSITE_REF`（`@id:WEBSITE_ID`）を新設し上記 provider/creator を全て `ORG_REF` 参照へ、`qaPageSchema.isPartOf` を `WEBSITE_REF`＋`publisher:ORG_REF` へ、`webSiteSchema.author` を sibling publisher と同形の bare `{ "@id": ORG_ID }` へ集約（`webPageSchema` の既存インライン @id ブロックも同 REF へ DRY＝出力不変）。全ノードが `PUBLISHER_REF`（ロゴ付き発行主体）と同一 @id を共有＝1 つの正準 Organization/WebSite ノードへ収斂。**重複PR #719（同一 follow-up の別実装＝webSiteSchema.author のみ担当）を本PRへ統合し supersede＝水増し回避で単一の正準実装に一本化**。挙動は JSON-LD の意味論的リンク付けのみ＝可視UI/ルーティング/メタ本文は無改変（捏造0・既存破壊0・投機的拡張なし＝provider/creator 追加なし）。回帰: `json-ld.test.ts` に describe「低頻度 provider/creator ノードの @id 集約」4本追加（Service.provider・Dataset/DataCatalog.creator・Quiz.provider・QAPage.isPartOf/publisher が全て正準 @id を指す）＋webSiteSchema の @id テストに author=ORG_ID 断言を追加＋既存 CourseList provider テストを @id 付きへ更新。tsc0・lint errors0・全テスト緑・build成功。
- [x] 【柱C-3・S sitemap ゴーストURL回帰ガード新設＝ページ削除/改名時のデッドURL無言残留（404のsitemap掲載）を機械検知】`sitemap.ts` の静的URL列（172静的+動的6家系）は現状クリーン（本監査で全193ルートを実在照合＝ゴースト0・欠落0・`/search`はnoindexで正当除外を確認）だが、既存テストは「特定ページが載っている／非収載境界」だけを固定し**「sitemap の全URLが実在ルートへ解決する」逆方向のガードが皆無**だった＝当班以外（ux-records/tools/hub）がページを削除・改名すると sitemap.ts のハンド保守された静的URL列に旧URLが取り残され、検索エンジンへ404のデッドURLを提出し続けクロールバジェット浪費・Search Consoleエラーを招く穴。`sitemap.test.ts` に describe 追加（3 it・**コード変更0のガード**）＝(1)`readdirSync` で `src/app` 配下を再帰走査し `page.*` を持つルートのセグメント列（route group `(x)`/並列`@`/プライベート`_`除去・動的`[x]`保持）を収集（走査サニティ＝150超・ルート`/`検出） (2)`sitemap()` の全URLを `new URL().pathname` で正規化し、各々が静的一致 or 動的セグメント構造一致でルートへ解決＝デッドURL0 (3)解決ロジックの偽陽性ガード（未知1階層/3階層paths は未解決・実在動的配下は解決）。動的パラメータ値の実在（court-cases 1:1 等）は既存 柱C-3-3 describe が別途固定のため二重管理せず構造のみ突合。ゴースト注入で当該itが赤転→復元で緑を実測。tsc0・lint errors0・全2442テスト緑（新規3含む）・build成功。
- [x] 【柱C-3・S sitemap（本体+子6本）と robots のドメインを seo-metadata.ts 単一ソース化＝発見性インフラのドメイン直書き取り残しを是正】(PR #711) og-image(#710)・json-ld(#530)・page-json-ld(#698) は既に `SITE_URL` 集約済みだが、**発見性インフラの中核である sitemap/robots だけ取り残されていた**＝`web/src/app/sitemap.ts` の `const base` ・子sitemap 6本（sitemap-index/articles/accidents/chemicals/circulars/equipment.xml）の `const BASE` ・`robots.ts` の Sitemap 行、計**8箇所に `https://www.anzen-ai-portal.jp` を直書き**しており、`SITE_URL` 変更時に全 `<loc>` と robots の Sitemap 参照が**旧ドメインへ無言ドリフト**する（＝新ドメインで sitemap 全滅＝クロール全停止）最も痛いクラスの穴だった。8ファイル全てで `SITE_URL` を import しエイリアス（base/BASE）値のみ差し替え。`SITE_URL` は末尾スラッシュ無し＝従来の直書き値と同値のため**出力は byte-identical**（各 route の既存 route.test.ts 全緑が無回帰を担保）。回帰: `sitemap-domain-single-source.test.ts` 新設＝(1)対象8ファイルにドメイン直書きリテラルが無く `@/lib/seo-metadata` から `SITE_URL` を import（it.each ハードコードガード） (2)`sitemap()` の全 `<loc>` が SITE_URL オリジン始まり (3)`robots()` の Sitemap 行が `${SITE_URL}/sitemap-index.xml`。tsc0・lint errors0・全2423テスト緑（新規10 it 含む）・build成功。
- [x] 【S・柱C-4類 OGP画像(/api/og)の透かしドメインを SITE_URL 単一ソース化＋og-url.ts 回帰テスト新設＝ハードコード直書きの取り残しを是正】動的OGP画像 route `web/src/app/api/og/route.tsx`（全ページ共通の 1200×630 SNSリンクカード画像を `next/og` の ImageResponse で生成）が右下の透かしに `anzen-ai-portal.jp` を**ドメイン直書き**しており、`SITE_URL` を別ドメインへ替えても透かしだけ旧ドメインへ**無言ドリフト**する穴だった（#530/#698 で JSON-LD 系は SITE_URL 単一ソース化済みだが OGP画像 route が取り残されていた同型の穴）。`seo-metadata.ts` に `SITE_DISPLAY_HOST`（＝`SITE_URL` からプロトコル・`www.`・末尾スラッシュを除いた表示用ホスト＝現行 `anzen-ai-portal.jp`）を新設し route の透かしを `{SITE_DISPLAY_HOST}` へ差し替え。**挙動不変**＝`SITE_DISPLAY_HOST`==="anzen-ai-portal.jp" のため描画される透かしは byte 相当で不変（dev server で `/api/og`・`/api/og?title=…&lang=en` を実測＝ともに HTTP200・image/png・1200×630・透かし `anzen-ai-portal.jp` を目視確認）。併せて**無テストだった** OGP画像URL生成ヘルパー `og-url.ts`（layout.tsx metadata・json-ld.tsx が全ページの og:image に使用）に `og-url.test.ts`(9 it)を新設＝(1)ogImageUrl: title のみ/desc付与/lang='ja'は省略(route既定=jaと整合)/lang='en'のみ付与/記号・空白の URLエンコード往復 (2)SITE_DISPLAY_HOST: SITE_URL 由来の導出・プロトコル/www./スラッシュ/パス不在・現行本番ドメイン一致で挙動固定 (3)og route ソースの**ハードコード回帰ガード**＝SITE_DISPLAY_HOST を seo-metadata から import・`{SITE_DISPLAY_HOST}` 使用・裸ドメインリテラル不在。tsc0・lint errors0・全2411テスト緑（新規9含む）・build成功。
- [x] 【柱C-4・S JSON-LD 主要エンティティ（Organization/WebSite）を @id グラフ化＝重複ノード分裂を集約】(#704) 全ページ `<head>` で `organizationSchema()`/`webSiteSchema()` を別々の JSON-LD として出力しているのに両者が `@id` 未同定・相互リンク無し＝検索エンジンから「無関係の2ノード」に見え、さらに Article/NewsArticle/WebApplication/WebPage の author/publisher/provider/isPartOf が自サイト組織・サイトを name/url インライン再宣言＝同一の実世界エンティティが複数の別ノードに分裂しナレッジグラフ/ブランド同定シグナルを希釈していた穴を是正。`json-ld.tsx` に安定 @id を新設（`ORG_ID=${SITE_URL}/#organization`・`WEBSITE_ID=${SITE_URL}/#website`＝SITE_URL 単一ソース由来・ハードコード無し）し、Organization/WebSite に @id 付与＋`WebSite→Organization` を `publisher:{@id}` 参照で結線。共有 `PUBLISHER_REF`（→Article/NewsArticle リスト・WebApplication provider へ自動波及）・`newsArticleSchema` publisher・`webPageSchema` の isPartOf/publisher にも @id を付し全「自サイト組織/サイト」ノードを 1 つの正準ノードへ集約。自己参照 `sameAs:[SITE_URL]`（同定価値ゼロのバリデータ smell・外部権威プロフィール未整備）を除去。挙動は JSON-LD の意味論的リンク付けのみ＝可視UI/ルーティング/メタ本文は無改変（捏造0・既存破壊0）。回帰: `json-ld.test.ts` に @id 集約 describe 追加（@id 形式・WebSite.publisher が ORG_ID 参照・sameAs 不在・各発行主体ノードが ORG_ID/WEBSITE_ID を指す）＝既存 SearchAction(/search 正規化) は不変。tsc0・lint errors0・全2400テスト緑（新規含む）・build成功。**follow-up候補**: serviceSchema.provider・courseListSchema.provider・datasetSchema.creator 等の低頻度ノードの @id 化（primary グラフ確立後）。
- [x] 【柱C-4・S PageJsonLd のサイトURLを seo-metadata.ts 単一ソース化＋回帰テスト新設＝ドメイン直書き取り残しを是正】(#698) `json-ld.tsx` は #530 でサイトURL/名を `seo-metadata.ts`(`SITE_URL`/`SITE_NAME`) の単一ソースへ集約済みだが、sibling の汎用ヘルパー `page-json-ld.tsx`（全ページ共通の WebPage + BreadcrumbList + 可視パンくずを出力）は `const SITE_BASE = "https://www.anzen-ai-portal.jp"` と**ドメイン文字列を直書きしたまま取り残されて**おり、`SITE_URL` 変更時に WebPage `@id`/`url` と BreadcrumbList の `item` 絶対URLが旧ドメインへ**無言ドリフト**する構造化データ整合の穴だった。`SITE_BASE` を `SITE_URL` 由来へ差し替え（エイリアス名のみ残し値の直書きは撤去）。回帰: `page-json-ld.test.tsx`(5 it)を新設＝(1)既定パンくず（ホーム→ページ名）が `SITE_URL` 由来の絶対URLを出力・position 1始まり連番 (2)明示パンくずの全 item が `SITE_URL` prefix (3)keywords/contributor(Person)/isPartOf(SITE_NAME/SITE_URL) の WebPage 伝播 (4)可視パンくず `<nav aria-label="パンくずリスト">` を1つ描画 (5)**ソースにドメイン直書きリテラルが無い**ハードコード回帰ガード（`SITE_URL` の裸ドメインを include しない＋`from "@/lib/seo-metadata"` を含む）。**挙動不変**＝出力 JSON-LD は SITE_URL が同値のため byte 相当で不変（水増し0・既存破壊0）。tsc0・lint errors0・全2382テスト緑（新規5含む）・build成功。
- [x] 【柱C-2・S 横断検索 法令名かな読みエイリアスを現場頻用12法令へ拡張＝音声/うろ覚え入力の取り逃しを是正】法令名かな読み→正略称の展開表 `lib/cross-search/law-alias.ts::LAW_NAME_ALIASES`（O8-c）は 安衛法/安衛則/石綿則 等**20法令のみ**で、**現場監督・一人親方・人事が最も検索する 建設業法・労災保険法・均等法・育介法・最賃法・職安法・職能法・女性則・年少者則・作環測法・四アルキル鉛則・機械等検定規則 の12法令にかな読みエイリアスが無かった**＝「けんせつぎょうほう」「ろうさいほけんほう」等の音声/うろ覚え入力が0件になる取り逃しを是正。全12法令が curated 条文インデックス(`allLawArticles`/`mhlwLawArticles`)に**実在の検索可能条文を持つ**ことを機械確認（建設業法4件・労災保険法8件・均等法4件・女性則3件…）してから収載＝空振りエイリアス0。読みは略称の機械的音読み(けんせつぎょうほう=建設(けんせつ)業(ぎょう)法(ほう)等)で**コンテンツに literal では現れず既存ヒットを奪わない**（本モジュールの不変条件を厳守）。**実測検証**＝raw かな読みは 0 件→展開後に建設業法/労災保険法/均等法/女性則の実条文が4/8/4/3件ヒットを確認。同期ガード(LAW_ALIAS_SHORTS⊂KNOWN_LAW_SHORTS)により全12略称が LAW_METADATA 実在も担保。locked不変条件(就業制限1位=安衛法61条・石綿事前調査1位=石綿則3条)は新読みトークンが当該クエリに非出現のため無影響（全2351テスト緑で確認）。回帰: law-alias.test.ts に頻用12法令の展開＋条番号分解後段の他語保持2本を追加。tsc0・lint errors0・全2351テスト緑・build成功。
- [x] 【S DRY・回帰 横断検索カテゴリタブの単一ソース化＝二重管理ドリフト源を是正】横断検索の2UI（`/search` 結果ページの `SearchResults.tsx` と ⌘K パレットの `CommandPalette.tsx`）が**同一の順序付きカテゴリ配列 `const CATEGORIES: SearchCategory[] = ['law','faq',…,'sign']` をハンド重複**で持ち、カテゴリ追加（過去の faq/sign/equipment 追補で毎回両ファイルを手更新）の度に片方を忘れるとそのUIだけ新カテゴリのタブが欠落するドリフト源だった。`search-index.ts` に表示順の単一ソース `SEARCH_CATEGORIES: readonly SearchCategory[]` を export し、両UIが import してローカル重複 const を撤去（`CATEGORIES.map`→`SEARCH_CATEGORIES.map`・URL cat 検証も同配列へ）。**両方向ドリフトの機械検知**＝`search-index.test.ts` に describe 3本追加＝(1)`SEARCH_CATEGORIES` 集合＝`Object.keys(CATEGORY_META)` の等価（メタに足してタブ出し忘れ／タブにあるのにメタ無しの双方を1等価で検知）(2)重複カテゴリ0（同一タブ二重描画防止）(3)`countByCategory` の初期化キーと整合（未集計カテゴリ防止）。**挙動不変**＝表示順・タイブレーク順（別軸の `SEARCH_CATEGORY_PRIORITY`）・アイコン switch（各UIのJSX固有・tsc網羅済）は無改変で、二重管理だけを解消（水増し0・既存破壊0）。tsc0・lint errors0・全2314テスト緑（新規3含む）・build成功。
- [x] 【柱C-2・S 横断検索 notice-search.ts の回帰テスト新設＝チャットボット出典提示ロジックの無テスト穴を是正】当班 C-2 領域の `web/src/lib/notice-search.ts`（125行・チャットボット回答へ添付する関連通達/告示/指針を実データ `@/data/mhlw-notices` から選ぶ横断検索。api/chatbot・gemini・chatbot-notice-attachment が依存）が**テストファイル皆無**だった＝法令隣接の出典提示に直結するのに無言退行しても検知不能な穴を、`notice-search.test.ts`(9 it)を新設して固定（sitemap 静的ルート機械突合／search-index 実データ照合と同方針＝モック不使用で正本 mhlwNotices をそのまま流す）。不変条件: (1)入力ガード＝空/空白/全角空白/正規化後1文字語(「の」「？」)は0件(幽霊ヒット無し) (2)トピック語「熱中症」で>0件かつ全 id が正本 mhlwNotices に解決(捏造0)・detailUrl 非空 (3)シノニム展開＝「アスベスト」がタイトル「石綿…」の通達を surface(TOPIC_SYNONYMS 石綿↔アスベスト) (4)助詞・空白混じり自然文「熱中症 対策について」でも正規化して拾う (5)k で上限制御(既定3・k指定≤k) (6)ランキング決定性＝小k は大k の先頭部分と一致・冪等 (7)NoticeHit は正本レコードの射影(写経でない=全フィールド一致) (8)NOTICE_BINDING_LABELS 3種の日本語ラベル網羅。**コード変更0**＝既存挙動をそのまま特徴づけ固定（回帰ガードのみ・水増し無し）。tsc0・lint errors0・全2317テスト緑(新規9含む)・build成功。
- [x] 【S 発見性・実在RSSフィード4本を全ページ<head>で自動発見可能化＝alternates.typesで広告する穴を是正】(#674) `/feed/{news,law-revisions,accident-reports,serious-cases}.xml` の**4本の公開RSSフィード**（`lib/rss.ts` 生成・無料/登録不要/出典リンク付き・robots で /feed を非Disallow＝クロール可）は実在するのに、どのページ `<head>` からも `<link rel="alternate" type="application/rss+xml">` で広告されておらず、**RSSリーダー・ブラウザ・クローラからのフィード自動発見が不能**だった発見性の穴を是正（サイトマップ・OGP は整備済みだが feed alternate だけ欠落）。`lib/seo/feeds.ts` を単一ソースに新設＝`SITE_FEEDS` 登録簿(path+title)＋`rssAlternateTypes()`。ルート `layout.tsx`（当班 core/shell custodian）の `alternates` に `types: application/rss+xml` を追加＝全ページ共通 `<head>` に4本の `<link rel="alternate">` を出力（`metadataBase` により相対path→絶対URL解決）。既存 `canonical`/`openGraph`/`twitter` は無改変（openGraph 浅マージ回避のため types のみ追加）。**幽霊フィードリンク0＋drift ガード**＝`feeds.test.ts` 5本：(1)登録簿2本以上・path形式`/feed/*.xml` (2)path重複0 (3)**全 path が実在 `route.ts`(GETハンドラ)へ解決**＝幽霊リンク0 (4)**登録 title が各 RSS channel title と一致**＝route との drift ガード (5)`rssAlternateTypes()` 網羅。prerendered HTML で4本の `rel=alternate` 出力（絶対URL・正 title）を実測確認。tsc0・lint errors0・全2304テスト緑（新規5含む）・build成功。
- [x] 【柱C-2 追補・S 横断検索に保護具(安全用品DB 1,050件)を収載＝現場頻用の保護具名が0件だった発見性の穴を是正】横断検索(/search・⌘K)のカテゴリは law/notice/chemical/education/accident/precedent/glossary/faq の8型のみで、**保護具（フルハーネス・防じんマスク・安全帯・保護帽…）が丸ごと0件ヒット**だった。詳細ページ `/equipment/[id]`（約1,050件・自己canonical・indexable・JIS/検定・関連法令付）が実在し `sitemap-equipment.xml` に個別収載済みなのに、現場が最も検索する保護具名で横断検索から引けない穴（#561 accident・化学物質と同型）を、`search-index.ts` に `equipment` カテゴリを新設して是正。正本 `getAllEquipment()`（＝詳細 /equipment/[id] の generateStaticParams が解決する eq-NNNN 集合そのもの）を read-only import し title=製品名・subtitle=カテゴリ名＋規格・keywords=カテゴリ名/小分類/メーカー/JIS規格（industries/hazards は英語コードのため除外＝ノイズ回避、regulations は法令権威クエリ汚染回避のため除外）・url=`/equipment/<id>`（正本由来のため必ず解決＝幽霊URL0・データ追加に自動追従）。**タイブレーク最下位**＝保護具は商品レコメンド（アフィリエイト）で法令・通達・判例より権威が低いため `SEARCH_CATEGORY_PRIORITY` の末尾に置き、同点時も権威コンテンツの上位を決して奪わない（locked不変条件「就業制限」1位=安衛法61条・「石綿 事前調査」1位=石綿則3条 が不変であることを既存T1-T3回帰で確認）。UI(SearchResults/CommandPalette)に保護具タブ＋HardHat アイコン(amber)を追加、CATEGORY_META/countByCategory/CATEGORIES 配列も拡張。回帰: search-index.test.ts に equipment describe 3本追加＝(1)正本 getAllEquipment と件数・ID集合一致＋500件超の非空虚性(2)全件 /equipment/<id> 深リンク・裸/equipment・/equipment-finder 不在・url↔id 対応(幽霊URL0)(3)フルハーネス/墜落制止用器具/メーカー/JIS でヒット＋subtitle非空。既存 countByCategory 合計テストに c.equipment を追加。tsc0・lint errors0・全2267テスト緑（新規3含む）・build成功。**要・他班（注記のみ）**: 一覧 `/equipment-finder`（保護具ファインダー）本文の改善は所有UI班(ux-tools)の担当＝当班は横断検索インデックスの発見性のみ是正。
- [x] 【柱C-2 追補・S 横断検索に安全標識(JIS Z 9101・約110種)を収載＝現場頻用の標識名が0件だった発見性の穴を是正】横断検索(/search・⌘K)は law/notice/chemical/education/accident/precedent/glossary/faq の**8型のみ**で、`@/data/safety-signs` の JIS Z 9101 準拠 **安全標識（禁止/警告/指示/安全状態/防火の5分類・約110種）** が丸ごと0件ヒットだった。「立入禁止」「感電注意」「保護めがね着用」等の標識名は現場で頻用されるのに、発見手段が `/safety-signs` ハブの回遊のみで、用語(glossary)・FAQ とも別軸の**視覚標識の直接照会**が欠落していた（#561 accident・FAQ 収載と同型の発見性の穴）。`search-index.ts` に `sign` カテゴリを新設し `SAFETY_SIGNS`(node:fs非依存=ブラウザ安全) を read-only import＝title=標識名・subtitle=分類ラベル+意味・keywords=英名(nameEn)+分類ラベル+関連法令(statute+article)。url=`/safety-signs/sign/<id>` で個別詳細へ深リンク。**幽霊URL0の保証**＝詳細 `/safety-signs/sign/[id]` の `generateStaticParams` が `SAFETY_SIGNS` 全件 id を返し未知 id は `notFound()` で弾く＝収載集合＝解決集合で必ず着地（詳細ページは既に sitemap 収載済み＝発見手段だけが欠落）。UI(SearchResults/CommandPalette)に 標識 タブ＋Signpost アイコン(amber)を追加、CATEGORY_META/countByCategory/CATEGORIES も拡張。同点タイブレークは用語と同じ参照系ティア（法令・教育・FAQ より下位・化学物質より上位）＝**locked不変条件「就業制限」1位=安衛法61条(O8-a/T8) は標識名と非衝突** のため無影響。回帰: search-index.test.ts に sign describe 3本追加＝(1)全SAFETY_SIGNS収載(正本と件数一致・裸/safety-signs不在・深リンク)(2)id集合が正本に解決(幽霊URL0=generateStaticParams一致)(3)標識名/英名/関連法令ヒット＋意味subtitle。tsc0・lint errors0・全2241テスト緑・build成功。
- [x] 【柱C-3-3 追補5・S 化学物質 個別詳細ページ約3,515本を子サイトマップ収載＝サイト最大級の未収載コンテンツを是正】横断検索・sitemap の全領域監査で、`/chemical-database/[cas]`（濃度基準DBの物質個別詳細＝物質ごとに濃度基準値・GHS区分・関連法令＝化管法/PRTR・化審法・毒劇法等を出典付きで持つ自己canonical・indexable な実在ページ・`revalidate=2592000` の on-demand ISR）が**全sitemap不在**だった発見性の穴を是正（事故 `/accidents/[id]`・保護具 `/equipment/[id]` は子サイトマップ収載済みなのに、より大きい**約3,515物質**の化学物質DBだけ漏れていた＝サイト最大級の未収載独自コンテンツ）。子サイトマップ `sitemap-chemicals.xml` を新設（accidents/equipment と同形）し、正本 `CONCENTRATION_LIMITS.substances` のキー集合を単一ソースに全件 `/chemical-database/<cas>` を深リンク収載。**幽霊URL0の保証**＝詳細ページは `CONCENTRATION_LIMITS.substances[normalizeCas(cas)]` に無い CAS を `notFound()` で弾くため、このキー集合こそが「実在の詳細ページに解決する CAS の全体」＝全URLが必ず解決しデータ追加にも自動追従。**canonical乖離0の保証**＝全キーが `[0-9-]` のみ（ノーマライズ済み・全角/空白なし）で URL 安全＝詳細ページ canonical `/chemical-database/${cas}`（decodeURIComponent・非エンコード）と 1:1 一致することを回帰で固定。lastmod は当日打ち（lastmodスパム）回避のため濃度基準DBスナップショット生成日 `CONCENTRATION_LIMITS.generatedAt`（2026-05-24）を全URL共通で使用＝`lib/sitemap/freshness.ts` に `chemicalsDataUpdated` を追加し index と単一ソース化。`sitemap-index.xml` に本子を登録（本体→記事→事故→通達→保護具→化学物質の6子構成）。robots は sitemap-index を広告済みのため自動発見。priority 0.6・monthly。回帰: route.test.ts 5本（正本キー全件一致・1000件超の非空虚性・裸/chemical-database不在・全キー`[0-9-]`のみ＝canonical一致・lastmod=chemicalsDataUpdated当日固定でない）＋freshness.test 1本＋index route.test を6子構成へ更新。tsc0・lint errors0・全2233テスト緑・build成功。**要・他班（注記のみ）**: 一覧 `/chemical-database`(index) の client は `getAllMergedChemicals()` 全件へ「開く」リンクを出すが、`CONCENTRATION_LIMITS.substances` 外の CAS は詳細で `notFound()`＝潜在ソフト404。sitemap は解決集合のみ収載で正だが、client 側のリンク条件是正は所有UI班(ux-tools)の担当。
- [x] 【S 発見性・PWA 横断検索/AI質問をホーム画面ショートカット化＋幽霊ショートカット回帰ガード】App Router 動的 `manifest.ts` のショートカット（ホーム画面ロングタップのクイックアクション）が KY/法改正/事故DB の3本のみで、当班の担当領域である**発見性の入口＝横断検索(/search)** と **安衛法の即答＝AIチャットボット(/chatbot)** がインストール済みユーザーの1タップ導線から欠落していた穴を是正。両ルートの実在を確認（`src/app/(main)/{search,chatbot}/page.tsx`）のうえ、現場でよく使う順（KY→横断検索→AI質問→法改正→事故DB）で収載。**`/search` は noindex**（クエリ毎の薄い重複ページ回避）だが、ショートカットはインストール済みユーザー向け導線でクローラの indexability とは無関係のため収載してよい点を回帰コメントで明示。**幽霊ショートカット防止**＝新設 `manifest.test.ts`（5 it）で全ショートカット URL を `page.tsx` の実在へ機械突合（sitemap 孤立ページ突合と同方針）＝(1) shortcuts 3本以上・必須フィールド (2) 全 URL が (main)/直下 app の page.tsx へ解決＝実在0欠落 (3) URL 重複0 (4) /search・/chatbot を含む (5) start_url も実在ルート。tsc0・lint errors0・全2224テスト緑（新規5含む）・build成功。**投機的拡張なし**＝ショートカット追加のみで既存アイコン/スクショ/カテゴリは無改変。
- [x] 【S 調査・却下＝負の結果記録】横断検索の共有正規化 `lib/fuzzy-search.ts::normalizeSearchText` に**かな折り畳み（ひらがな→カタカナ正準）**を試作したが、**RAG 100問ベンチマーク退行のため revert**。理由: `normalizeSearchText` は当班の横断検索(cross-search/score.ts)だけでなく**チャットボット RAG 検索(lib/rag-search.ts)も直接依存**する共有関数で、部分一致スコアラに blanket かな折り畳みを入れると異script衝突で精度が希釈＝`chatbot-phase2-metrics`(Citation@1 66%→59%・Recall@5 100%→88%)・`rag-100q-fresh`(90%→85%)が悪化。cross-search 側の locked 不変条件テストは通ったが**同種の日本語キーワード検索の ground truth である RAG ベンチが5pt退行**＝cross-search 精度も同様に希釈する強い証左のため、`既存破壊0/水増し0`原則に基づき不採用。将来かな入力ゆらぎを拾うなら共有正規化ではなく cross-search 専用パス限定＋cross-search 独自の精度ベンチ整備が前提（次回検討）。コード変更0で working tree clean 復帰。
- [x] 【柱C-2 追補・S 横断検索に FAQ(200問) を収載＝質問インテントの発見性の穴を是正】横断検索(/search・⌘K)は law/notice/chemical/education/accident/precedent/glossary の7型のみで、`@/data/faqs` の **キュレーション済み FAQ 200問**（4バッチ=法令/管理体制/化学物質/健康・教育）が丸ごと 0 件ヒットだった。用語(glossary=「○○とは」)とは別軸の**質問インテント**（「衛生管理者は何人？」「SDS交付は義務？」「特別教育はオンライン可？」）が発見不能な穴を、`search-index.ts` に `faq` カテゴリを新設して是正。`ALL_FAQS`(node:fs非依存=ブラウザ安全) を read-only import し title=質問文・subtitle=回答冒頭80字（結果一覧で即答）・keywords=tags＋関連法令（条番号・分類語からも引ける）・url=`/faq/<category>`（sitemap収載・自己canonical の実在カテゴリ一覧ページ＝law-system/management/chemical/health-education のいずれかで必ず解決＝幽霊リンク0）。UI(SearchResults/CommandPalette)に FAQ タブ＋HelpCircle アイコン(sky)を追加、CATEGORY_META/countByCategory/CATEGORIES 配列も拡張。**locked不変条件の保全**＝bare「就業制限」1位=安衛法61条(O8-a/T8)が、概念名で始まる FAQ 質問のタイトル前方一致(65点)に条文キーワード完全一致(55点=articleTitle「就業制限」)が負けて奪われる退行を検知＝FAQ結果タイトルに慣用の「Q. 」接頭を付し前方一致(65)→部分一致(45)へ落として権威条文の1位を堅持（FAQは下位で発見可能）。回帰: search-index.test.ts に FAQ describe 3本追加＝(1)全ALL_FAQS収載(正本と件数一致・裸/faq不在・/faq/深リンク)(2)category slug が実在4一覧に解決(幽霊リンク0)(3)質問文/関連法令/タグヒット＋回答subtitle。tsc0・lint errors0・全2213テスト緑・build成功。**要・他班**: 個別FAQへの深リンク(/faq/<category>#<id>)は /faq ページ本文(UI班)の FAQItem にアンカー＋hashオープン実装が前提のため今回はカテゴリ一覧へ寄せた(glossaryと同方針)。
- [x] 【柱C-2 追補・S 横断検索の教育(Eラーニング)を全テーマ源へ拡張＋個別テーマへ深リンク化】横断検索(/search・⌘K)の education カテゴリが (1) `ELearningPanel` が実際に描画する 9 テーマ源（入門コース＋汎用カタログ＋追補＋業種別6分野=製造/医療福祉/運輸/林業/食品/小売）のうち `elearningThemesCatalog` **1 源だけ**を import しており、業種別・入門・追補テーマが横断検索から丸ごと欠落（近年追加分が引けない発見性の穴＝#561 の accident と同型）、(2) 全件が一覧トップ `/e-learning` へリンクし検索したテーマへ到達不能、の2点を是正。`search-index.ts` の education ブロックを panel の `allThemes` と同じ 9 源の union（`theme.id` 重複除去）へ寄せ、url を panel が受け取る深リンク `/e-learning?theme=<id>#el-quiz` へ変更＝panel 側で `allThemes.some(t=>t.id===themeParam)` により id 検証済み・収載源が allThemes と一致するため必ず解決（未知id は panel が無視して先頭表示＝幽霊リンク 0）。`el-quiz` アンカーは panel line 177 に実在を確認。keywords に `sourceType`/`level`/`industry_detail` を補い業種語（「製造業」等）からも引ける。回帰: `search-index.test.ts` に education describe 3本追加＝(1) index の ID 集合が panel 全テーマ源(allThemes)と一致（1源import欠落是正・業種別 el-mfg/el-hc/el-rt 収載を明示）(2) 各件が `/e-learning?theme=<id>#el-quiz` 深リンク・裸 /e-learning 不在・深リンク先が allThemes に実在（幽霊リンク0）(3) 業種語ヒット。tsc0・lint errors0・全2178テスト緑・build成功。CATEGORY_META/UI(SearchResults/CommandPalette)は既存 education タブのため無改変。**要・他班メモ**: 法改正記事(/articles)の横断検索収載は、正本 `getPublishedArticleIndex` が `node:fs`(readdirSync)依存でブラウザ非安全＝data班が `src/data/articles/` にブラウザ安全な集約モジュールを公開するのが前提（当班の client 検索インデックスからは import 不可）。
- [x] 【柱C-3-3 追補4・S 孤立していた実在ツールページ /ky/workers を sitemap 収載】静的ルート全172本を sitemap.ts と機械突合し、40本の差分を「robots index:false / redirect・notFound スタブ / robots.ts Disallow 配下(/admin,/auth,/dev,/api-docs,/lms,/dpa) / リリース前デモ / ツール状態」で分類。唯一の真の孤立ページ `/ky/workers`（作業員マスター＝KY用紙の参加者選択ツール・robots上書き無し=index:true・自己canonical・PageJsonLd・OG完備）を収載＝兄弟 `/ky/paper`（追補3）と同じ KY全面再設計 #285 で追加されたのに漏れていた発見性の穴を是正。lastmod=2026-05-25(#285・lastmodスパム回避)・priority 0.7・monthly。回帰: sitemap.test.ts 追補4 describe 3本（/ky/workers収載・lastmod固定・境界 /ky/list=robots index:false は非収載）。sitemap.test 24 it 緑・全2178テスト緑・tsc0・lint errors0・build成功。**要・他班（注記のみ）**: /organization(デモ)・/profile(ユーザ個別)が page 側 noindex 未付与で潜在 indexable＝sitemap除外は正だが根治は所有UI班のgenerateMetadata担当。
- [x] 【O17/T6・P2・L 設計ドラフトのみ=Path A】条文パーマリンク `/laws/[law]/[art]` ＋束ねパネル（関連通達・判例・用語・教育資格・e-Gov＝G6「e-Govに構造的不可能な最大の勝ち筋」）の設計書を **`docs/fable-diagnosis-2026-07-02/T6-law-permalink-bundle-design.md`** に起票。**設計のみ・コード/データ生成0**。**Path A の根拠**＝本タスクは数百〜約1,065本の**新規 indexable URL名前空間 `/laws/*` の新設**＝CLAUDE.md「必ずオーナーに確認すること＝ページ構成の大幅変更（URL変更を伴うもの）」に該当し、URL/canonical は一方通行のドア。sibling T7（外部API）と同じく実装はオーナー明示指示待ち。要点: (1) **URL設計**＝推奨案A `/laws/<egovLawId>/<artSlug>`（ASCII安定・e-Gov原文と1:1・改称に不変・slug衝突ゼロを回帰固定）、(2) **捏造ゼロの境界監査**＝条文↔各DBの構造化相互参照を実データ精査した結果、通達は `lawRef`＋title地の文の条番号を**O18リンカで機械抽出＝捏造なしで導出可**、判例/教育資格は**条文キー無し＝keyword推測で埋めるのは捏造＝data班のマップ前提（要・他班）**、(3) **段階투입**＝Phase1骨格(SEO班単独＝route基盤/self-canonical/**O18リンカ本文でG4条間リンクをライブ化**/e-Gov透明性/sitemap-laws.xml受け皿/Legislation JSON-LD/回帰)→Phase2通達束ね(リンカ導出)→Phase3判例/資格(data班マップ前提)、(4) **thin/duplicate対策**＝`/law-search`(ツール面・自身canonical)と競合させず `/laws/*` を条文の唯一 indexable home に、(5) **JSON-LD**＝`json-ld.tsx` に schema.org `Legislation` ヘルパー新設案（既存 LegalDocument=通達 とは別型）、(6) **O18リンカのライブ化**＝これまで描画面ゼロで dormant だったリンカが本ページで初の描画面を得る。**オーナー確認事項**: URL名前空間新設可否＋スキーム確定・新規ページの所有班(当班の発見性infra page か UI班 feature page か＝loop-prompt enumerated route は `/search` のみ)・束ねデータの構造化マッピング所有(data班)・ビルド予算。env/外部API/課金は不要(全て read-only の既存 curated＋LAW_METADATA)。コード変更0のため tsc/lint/vitest/build は非改変で全緑維持。
- [x] 【柱C-3-3 追補3・S /ky/paper をsitemap収載＋二重着手(#623)の統合＝/organization は非収載を堅持】並走していた自班の別ブランチ #623（同じ「孤立ページ収載」を古い base で着手）を精査したところ、#623 は6本を追加していたが、うち4本（/accident-news・熱中症3本）は本ブランチ #627（追補2）の**改良版と重複**（本ブランチは lastmod を `accidentsDataUpdated` へ動的追従・非収載境界を回帰ロック）、1本 `/organization` は**追補2の非収載境界テストが明示的に false 固定した「正式リリース前のデモ版」モック**（架空の拠点名・社員数・修了率のデモ値＝indexさせるべきでない）で #623 の追加は誤り。残り1本 `/ky/paper` のみが**正当な未収載孤立ページ**（`robots: index:true`・自己canonical・HowTo JSON-LD・`/pdf` の permanentRedirect 先＝KY入力の正規ページ）だったため、これを本ブランチへ取り込み統合。#623 は誤収載(/organization)を含むため superseded として close 予定（二重掲載＝水増し回避）。`/ky/paper` の lastmod は page.tsx 最終更新 2026-05-25・priority 0.75・monthly。回帰: `sitemap.test.ts` 追補2 describe に2本追加＝(1) /ky/paper 収載 (2) 非収載境界＝リダイレクト元 /pdf は false（実体URL /ky/paper のみ収載）。tsc0・lint errors0・sitemap.test 21本緑・build成功。
- [x] 【柱C-3-3 追補2・S 孤立していた実在indexableページ4本をsitemap収載＋非収載境界を回帰固定】静的ルート173本を sitemap.ts と機械突合し、page.tsx として実在・自己canonical・indexable なのに全sitemap不在だった孤立ページ4本を追加＝(1) `/accident-news`（重大災害事例ブラウザ・死亡災害DB類型検索・`revalidate=86400`・OG付・サイト最大級の独自コンテンツ）、(2)(3)(4) `/heat-illness-prevention/{acclimatization,log,poster}`（令和7年6月改正安衛則 第612条の2 対応の実在ツール3本・自己canonical・PageJsonLd付＝親 /heat-illness-prevention と wbgt-calculator/industry-risk/r7-compliance は収載済みなのに後発の3本だけ漏れ）。lastmod は `/accident-news`＝`accidentsDataUpdated`（= `SERIOUS_CASES_META.generatedAt`＝当該ページのデータ源そのもの）に追従、熱中症3本は兄弟ページと同節の静的日付を踏襲（lastmodスパム回避）。**非収載境界も明示ロック**＝機械的全ページ追加による誤収載を防ぐため、redirect スタブ（`/about/cases`→/about・`/quick-start`→/quick）・リリース前デモ（`/organization`「正式リリース前のデモ版」）・印刷専用（`/accident-news/print`）・noindex（admin/auth/dev/account/favorites/stats 等）は収載しないことを回帰で固定。回帰: `sitemap.test.ts` に describe「追補2」5本追加（追加4本収載／accident-news lastmod==accidents／redirect・デモ・印刷は非収載）＝全19本。tsc0・lint errors0・全2142テスト緑・build成功。子sitemapは無改変（単一URLのため本体 pages 配列に直書き）。
- [x] 【決裁A・確認のみ=既達】robots.ts の `facebookexternalhit` を許可リスト（`SOCIAL_LINK_PREVIEW_BOTS`）へ移し FB/Messenger/IG の OGP リンクカードを復活済み・学習用 FacebookBot 等は `AI_TRAINING_CRAWLERS` で遮断維持を確認（2026-06-11 決裁A で実装済みのため再実装せず現状維持＝水増し回避）。
- [x] 【T7・P2 設計ドラフトのみ=Path A】e-Gov API v2 全文取込（抄録1,065条→全文カバレッジ）の設計書作成。診断 05-search-egov.md T7/G7 の中期策を **`docs/fable-diagnosis-2026-07-02/T7-egov-fulltext-ingest-design.md`** に起票。**設計のみ・コード/データ生成0**（外部API本番取込みはオーナー承認待ち＝Path A の境界を §8/§10 で明示）。要点: (1) 既存インフラ再利用＝`scripts/etl/egov-revisions-fetch.ts`（e-Gov API v2 ETL・認証不要・政府標準利用規約2.0・実在検証済み lawId×20）＋`egov-caption-snapshot.ts`（チェックイン済みスナップショット＋整合テストの先例）＋`LAW_METADATA.egovLawId`＋`LawArticle` 型を土台にする＝新規基盤ではなく既存パターンの拡張、(2) 方式＝「ビルド時に外部APIを叩く」ではなく**オフライン取得→チェックイン→静的import**（CI/デプロイ再現性・main 常時デプロイ可能を担保）、(3) **新規 env 不要**（API キー無し）、(4) 段階投入＝安衛法/令/則→主要特別則→その他、(5) 全文化のバンドル肥大対策＝検索インデックスと本文の分離・遅延ロード・PWA プリキャッシュ見直し（診断 G8 の宿題）＝SEO/ux-tools/PWA 班の横断合意要、(6) 品質＝原文サンプル照合・削除条ガード・出典必須。**オーナー確認事項**: 外部API本番取込みの是非・データ所有（`web/src/data/laws/**` は data 班）・取得頻度（単発 or Cron）。コード変更0のため tsc/lint/vitest/build は非改変で全緑維持。
- [x] 【O18・P1 診断T5 条文本文の参照自動リンク】条文カード本文に素テキストで現れる条番号参照（「第30条」「安衛則第36条」「クレーン等安全規則第23条」等）を、収録済みなら当サイト深リンク `/law-search?law=<正式名称>&art=<条番号>`（内部）、収録外でも法令番号があれば e-Gov 条アンカー（`https://laws.e-gov.go.jp/law/<id>#Mp-At_<N>`）へ変換する純粋関数 `linkifyArticleReferences` を `lib/law-links/article-ref-linkify.ts` に新設（診断書 比較 h＝参照ジャンプの完敗返上）。**表示非依存のセグメント配列**（`{text}` / `{text,href,external}`）を返し、条文カードへの結線は law-search-panel.tsx（ux-tools 所有）側の 1 行に委譲＝**要・他班**（当班はリンカーと解決保証テストまで＝loop-prompt「跨りは自領域分だけ実施」）。**法令正確性は不可侵**のため解決不能・曖昧な参照は一切リンク化しない設計＝(1)「令第6条」「法第43条」「同法第20条」等 直前が別法令を示す助詞（令/法/則/例/同/附/別…）の裸参照はスキップ、(2) 収録外の枝番（第○条の△）は e-Gov 条アンカーが基条しか指せず誤着地するため非リンク、(3) 未知の法令名接頭も非リンク。参照先解決は read-only import のみ（捏造0）＝`allLawArticles`(curated・mhlw補完除外＝search-index.ts と同方針) の収録集合＋`@/data/law-metadata` の LAW_METADATA（略称→正式名称・e-Gov 法令番号）。漢数字/全角/枝番/項の数値化は O8-b の kanjiToNum ロジックを `lib/law-links/kanji-numerals.ts` へ切り出し `article-query.ts` と共有化（DRY・挙動不変）。**完了条件充足**: `article-ref-linkify.test.ts` 12本＝コーパス全文にリンカーを流し「生成リンクの解決率100%・幽霊リンク0」（内部リンク→収録集合に存在／e-Gov リンク→既知法令番号かつ Mp-At_正整数）＋非空虚性（実生成リンク>20）＋セグメント連結==入力（欠落・重複0）を恒久固定。全2033テスト緑・tsc0・lint errors0・build成功。UI/深リンク/カバレッジは非改変。**注**: 「令第○条」「同法第○条」の一意解決は参照先法令コンテキストの追跡が要るため今回は意図的に非対象（誤リンク回避＝隠すのは可）＝O17 束ねパネルで条文コンテキスト付きの解決を検討。
- [x] 【S6・P1 診断T4+T8 0件時のe-Govフォールバック＋収録範囲明示＋0件クエリ運用ループ】`/search` の 0 件画面（`NoResults`）に (1) **収録範囲の明示**＝「見つからない＝規定がないではない」の安全コピー（本サイトは主要法令の条文抄録＋通達・判例で未収載条文あり／原文は e-Gov で確認）、(2) **e-Gov 法令検索フォールバック**＝到達可能なポータルトップ（`https://laws.e-gov.go.jp/`・実測200・検索ボックス有）への外部リンク＋**検索語コピー**でクエリ引継ぎ、を追加。e-Gov 新 UI は SPA でキーワードのディープリンク URL が非公開のため、誤ったクエリ付きURL＝幽霊リンクを避け「常に到達可能なトップ＋コピー引継ぎ」に固定（`lib/cross-search/egov-fallback.ts`：`EGOV_LAW_SEARCH_URL`/`egovHandoffQuery`）。計装 `search_zero_result_egov`（0件→e-Gov逃がしクリック）を追加＝既存 `search_results_view{result_count}` と併せ 0 件率と機会損失を計測。**0件クエリ週次確認手順**を `docs/fable-diagnosis-2026-07-02/s6-zero-result-runbook.md` に新設（GA4で result_count=0 抽出→同義語/エイリアス/条番号ゆらぎ/真の未収載の4分類トリアージ→回帰固定）。**ランキング調整（「就業制限」1位=安衛法61条）は O8-a で既達＝`search-index.test.ts` の it.each(rank1) で既にロック済みのため再実装せず現状維持を確認**（水増し回避）。回帰: `egov-fallback.test.ts`(2 it)＋`SearchResults.test.tsx`(3 it・no-hit クエリで 0 件描画→e-Gov/収録範囲/コピー導線を検証)。tsc0・lint errors0・全1994テスト緑・build成功。**要・他班**: `/law-search`（`law-search-panel.tsx`）の 0 件画面・収録外条番号→e-Gov 条アンカー(`#Mp-At_N`)誘導は当該 UI 班所有のため対象外。
- [x] 【O8-c・P0 診断T3 法令名かな読みの正略称展開】(#TBD) e-Gov も当サイトも 0 件だった**法令名かな読み**（「あんえいほう」「くれーんそく」等＝現場のうろ覚え・音声入力で頻発）を該当条文へ着地させた（05-search-egov.md T3・比較 c）。着手前の本番インデックス実測で切り分け: 正式名称「労働安全衛生規則 第563条」→既に 1 位＝安衛則563条、別略称「労安衛法 61条」→既に 1 位＝安衛法61条（keyword「安衛法」への部分一致で拾える）＝**正式名称・別略称は O8-a で解決済み**。残る真の穴は**かな読み**（あんえいほう/あんえいそく/くれーんそく/ゆうきそく/とっかそく/さんけつそく/せきめんそく/ふんじんそく/じんぱいほう…が全て 0 件＝インデックスにもコンテンツにも literal で現れない）。`lib/cross-search/law-alias.ts` に純粋関数 `expandLawAliases` を新設し、`searchItems` の cross-search 委譲直前へ `expandLawAliases(normalizeArticleQuery(query))` として O8-b の後段に配線。かな読み・稀な漢字別表記（塵肺法）トークンを正略称（安衛法・じん肺法…）へ**完全一致で差し替え**＝差し替えても失うヒットが無い語だけに限定し既存 AND 検索を無改変。エイリアス表のキー（正略称）は data/law-metadata.ts の `LAW_METADATA`（read-only import）に実在する lawShort であることを同期ガードテストで担保（診断書の「law-metadata 連携」要件）。完了条件充足（本番インデックス回帰）: 「あんえいほう」→安衛法条文ヒット・「あんえいほう 88条」→1位 安衛法88条・「労働安全衛生規則 第563条」→1位 安衛則563条は不変。回帰: `law-alias.test.ts` 10本＋`search-index.test.ts` T3 8本。全1981テスト緑・tsc0・lint errors0・build成功。UI/深リンク/カバレッジは非改変。O8-b(#591) 上にスタック。**注**: e-Gov API `abbrev` フィールド由来の別略称は既存の部分一致で概ね解決済みのため辞書化を見送り、0 件の読みに絞った（水増し回避）。
- [x] 【O8-b・P0 診断T2 条番号クエリパーサ（法令名＋条番号の複合）】(#591) e-Gov でも 0 件になる「安衛法61条」等の**地続き「法令名＋条番号」クエリが横断検索で該当条文へ着地しなかった**穴を是正（05-search-egov.md T2）。`lib/cross-search/article-query.ts` に純粋関数 `normalizeArticleQuery` を新設し、`searchItems` の cross-search 委譲直前へ前処理配線。(1) 地続き「安衛法61条」→「安衛法 第61条」（AND エンジンは空白区切り各語で扱うため分解が必須）、(2) 漢数字「第六十一条」→「第61条」（/law-search の `kanjiToNum` と同一ロジックを再実装＝コンポーネント内非公開関数で import 不可）、(3) 全角「６１条」・枝番「61-2条」「第10条-3」→「第61条の2」「第10条の3」、(4) 末尾「条」必須で裸の数字・日付範囲「2024-2026」・「第一種」「三大災害」を素通し＝O8-a の 2 語 AND 検索を無改変。完了条件充足（本番インデックス回帰）: 「安衛法61条」「安衛法 88条」「安衛則563条」「第六十一条」がいずれも 1 位に該当条文(law・`/law-search?law=&art=` 深リンク)。回帰: `article-query.test.ts` 11本＋`search-index.test.ts` T2 4本。全1964テスト緑・tsc0・lint errors0・build成功。UI/深リンク/カバレッジは非改変。**注**: 法令エイリアス（正式名称⇄略称・かな読み）展開は O8-c、0 件時の e-Gov フォールバックは S6 で別途。
- [x] 【O8-a・P0 診断T1 /search・⌘K を死蔵 cross-search エンジンへ載せ替え】旧 `searchItems` はクエリ全体を 1 つの部分文字列として扱い、2 語クエリ（「石綿 事前調査」等）が全滅していた（診断書 05 の敗因）。6月実装済みだが未配線の `lib/cross-search/`（空白AND＋シノニム展開 query-expansion＋keywords 重み付け＋カテゴリ優先タイブレーク）を配線。実装: (1) `cross-search/score.ts` の `searchCrossIndex` を `ScorableItem` で汎化しジェネリック化＋`categoryPriority` オプション追加（cross-search.test は既定優先度で不変）、(2) `search-index.ts` の `SearchItem` に任意 `keywords` を追加し全カテゴリ（law=条文キーワード+articleTitle+法令名+略称+条番号 / precedent=field+issues+court / accident=workCategory+type+severity+industry_detail / chemical=cas+name_en / notice=docType+番号+category+issuer / glossary=reading）へ供給、(3) `searchItems` を `searchCrossIndex` へ委譲（law/education 優先の SEARCH_CATEGORY_PRIORITY）。**深リンク・カバレッジ・glossary は一切非改変**（既存の /circulars/<id>・/accidents/<id>・/law-search?law=&art= を維持）。UI(SearchResults/CommandPalette)は API 不変で無改修。本番インデックス実測: 「石綿 事前調査」→1位 石綿則第3条、「クレーン 過負荷」→1位 クレーン則第23条、「足場 作業床」→1位 安衛則第518条(第563条も3位以内)、ボーナス「就業制限」→1位 安衛法61条(T8意図も充足)。全クエリ<25ms(≪200ms)。回帰: search-index.test.ts に AND/keywords/シノニム単体3本＋本番2語クエリ収束4本(it.each)を追加。既存 40 テスト＋全1940テスト緑。
- [x] 【柱C-2・S 横断検索の通達を個別詳細へ深リンク化】横断検索(/search・⌘K)の通達カテゴリが全件 `/resources?q=<title>` へリンクしていたが、`resources-client.tsx` は `q` を読まない＝検索した個別通達へ到達できず全1,158件一覧へ落ちていた（#561 と同型の発見性の穴）。`search-index.ts` の通達 url を正本 `/circulars/<id>` へ深リンク化＝詳細 `/circulars/[id]` の generateStaticParams が mhlwNotices 全件 id を解決するため必ず着地（幽霊URL 0）しデータ追加にも自動追従。回帰2本追加（全 notice が /circulars/<id>・/resources?q= 不在・url id と item.id 対応／深リンク先 id 集合 == 正本 mhlwNotices 集合・件数一致）計14 it。
- [x] 【柱C-3-3・S 事故事例 個別ページのサイトマップ収載】`/accidents/[id]`（事故事例 個別詳細・約290件・固有 canonical/title/OG を持つ実在ページ）が全sitemap不在だった発見性の穴を是正（判例 /court-cases/[id] は収載済みなのに事故DB＝サイト最大級の独自コンテンツだけ漏れていた）。子サイトマップ `sitemap-accidents.xml` を新設（circulars/equipment/articles と同形）し、正本 `getAccidentCasesDataset()` から `/accidents/<id>` を全件深リンク収載（重複ID除去・幽霊URL0）。lastmod は当日打ち（lastmodスパム）回避のため `accidentsDataUpdated`（事故DB生成日）を全URL共通で使用＝occurredOn(災害発生日)はページ更新日でないため不採用。`sitemap-index.xml` に本子を登録（本体→記事→事故→通達→保護具の5子構成）。robots は sitemap-index を広告済みのため自動発見。回帰：route.test.ts 4本（正本ID全件一致・裸/accidents不在・lastmod=accidentsDataUpdated当日固定でない）＋index test を5子構成へ更新。
- [x] 【柱C-2・S 横断検索に法令条文を収載】高意図の最頻クエリ「安衛則 第○条」「足場 規則」等の**法令本文が横断検索(/search・⌘K)で 0 件ヒット**だった穴を是正（従来の検索手段は専用ページ /law-search のみ）。`search-index.ts` に `law` カテゴリを追加し、`@/data/laws` の `allLawArticles`（curated 中核・厚労省PDF補完=mhlwLawArticles は文書バンドル名のため除外）を read-only import で数百条規模収載。title=略称+条番号（例「安衛則 第518条」）、subtitle=正式名称＋条文見出し＋本文冒頭で、略称・正式名称・条番号・見出し語のいずれからも引ける。各結果は `/law-search?law=<正式名称>&art=<条番号>` で当該条文へ深リンク（パネルの filter `a.law===selectedLaw` と同形）。`/search` SearchResults と ⌘K CommandPalette に法令タブ＋アイコン(BookText)を先頭追加、CATEGORY_META(teal)/countByCategory も拡張。回帰テスト search-index.test.ts に law 統合テスト4本（300条以上収載/全件深リンク/略称・正式名称・見出し語ヒット/深リンク同形・id一意・PDF補完混入なし）を追加。**注**: mhlwLawArticles（厚労省PDF抽出の補完）は条文単位の深リンクUXに合わず除外＝必要なら別途 law-search の mhlw モード。
- [x] 【柱C-2・S 横断検索の事故事例を正本へ寄せ全件収載＋深リンク化】(#561) 横断検索(/search・⌘K)の事故カテゴリが (1) 7事故データファイル中5つだけを手importしており `real-accident-cases-2024-2026`・`real-accident-cases-2025-preliminary` の2ファイルが丸ごと欠落（近年の事故が検索で引けない発見性の穴）、(2) 全件が一覧トップ `/accidents` へリンクし検索した個別事故へ到達不能、の2点を是正。`search-index.ts` の事故ブロックを正本 `getAccidentCasesDataset()`（＝詳細 `/accidents/[id]` が `findAccident` で解決する集合そのもの）単一ソースへ寄せ、url を `/accidents/<id>` へ深リンク化＝検索結果→詳細が必ず解決（幽霊URL 0）しデータ追加にも自動追従。subtitle に severity・occurredOn を追加し件数増後の同名事故を区別可能に。回帰2本追加（index と正本のID集合一致＝欠落是正／全件 `/accidents/<id>` 深リンク・裸 `/accidents` 不在）計14 it。
- [x] 【柱C-3-4・S DRY後追い】本体 `sitemap.ts` 冒頭の freshest 群（siteFreshest/freshestArticle/freshestNotice 等8変数）を、`lib/sitemap/freshness.ts` の `computeSitemapFreshness(buildToday)` 分割代入へ置換し sitemap-index.xml と単一ソース化。#537 マージ確認後に着手。不要 import 6件撤去（COURT_CASES/latestIsoDate は courtCasePages で継続使用）。出力 byte-identical・全1828テストpass・build成功（#556）。
- [x] 【決裁A拡張・柱C robots AI学習専用UAの遮断拡張】2026-06-11 決裁「学習系は遮断継続／検索引用系は許可」を、その後各社が分離・新設した AI学習専用UAへ機械的に拡張（新規方針判断ではない）。`robots.ts` の `AI_TRAINING_CRAWLERS` に Google-Extended・Applebot-Extended・Meta-ExternalAgent・cohere-ai(+training-data-crawler)・PanguBot・AI2Bot・Timpibot・Webzio-Extended・FriendlyCrawler・ImagesiftBot・img2dataset・Kangaroo Bot を追加。**重要**: `*-Extended` は学習オプトアウト専用UAで Googlebot/Applebot（検索インデックス）とは別物＝検索順位・流入に影響しない。あわせて許可リストへ Anthropic 現UA（Claude-User／Claude-SearchBot）を追記（学習用 ClaudeBot は遮断のまま）。robots.test.ts に拡張回帰3本（後発学習UA=disallow:/／Googlebot・Applebot に専用遮断ルール無し＝UA:* Allow:/／Claude-SearchBot・Claude-User=許可・ClaudeBot=遮断）を追加し計8 it。
- [x] 【柱C-2・S 横断検索に用語集を収載】高意図の「○○とは」語が横断検索(/search・⌘K)で 0 件ヒットだった穴を是正。`search-index.ts` に `glossary` カテゴリを追加し、`@/data/glossary` の 4 バッチ 152 語（read-only import）を収載。subtitle に読み(かな)＋定義冒頭60字を載せ、用語名・読み・定義語のいずれからも引け、結果一覧に定義が即表示される。`/search` の SearchResults と ⌘K CommandPalette にカテゴリタブ＋アイコン(BookMarked)を追加。CATEGORY_META/countByCategory も拡張。回帰テスト search-index.test.ts に glossary 統合テスト3本(152語収載/読み・定義ヒット/all合計一致)を追加。**注**: /glossary 本体に直書きされた基礎語約99語は当班所有外(ux-hub ページ)のため対象外＝完全網羅は別途要・他班(用語データの @/data/glossary 一元化)。
- [x] 【柱C-3-4 / A-3・S サイトマップ index/equipment の lastmod 動的化】`sitemap-index.xml` は全子サイトマップに当日（new Date()）を打っており、中身不変でも lastmod が毎日動く lastmod スパム（Google に無視され再クロール遅延）だった点を是正。新設 `lib/sitemap/freshness.ts` の純粋関数 `computeSitemapFreshness(buildToday)` で各セクション実データ最新日（本体=siteFreshest/記事=freshestArticle/通達=freshestNotice/保護具=equipmentDataUpdated、未来日はビルド日 cap）を導出し、index 4子＋ `sitemap-equipment.xml` の per-URL lastmod（同じく当日打ちだった）を実データ日へ差し替え。fallback 値は `sitemap.ts` と一致。freshness.test.ts(6 it)＋ index route.test.ts(4 it) で「cap値そのものを返さない＝当日固定でない」回帰を固定。`sitemap.ts`/circulars route は #537 と競合回避のため非改変。
- [x] 【柱C-2・S 404 横断検索ボックス】グローバル 404（app-shell 外＝ナビ/⌘K 無し）の `not-found.tsx` に、JS 非依存のネイティブ GET フォームで `/search?q=` へ送る横断検索ボックスと主要機能ランチャー（法令/事故/判例/通達/KY/Eラーニング/チャット/問い合わせ）を新設。site-critique C-2「404どん詰まり（本文199字・リンク4本・検索手段なし）」を是正＝旧URL/タイポ流入の取りこぼしを回収。入力/ボタンは min-h-11（44px）。回帰テスト not-found.test.tsx（5 it）。
- [x] 【柱C-3-2/A-3・S サイトマップ役割分担の是正】sitemap-circulars.xml が別系統データ officialNotices(nt-* ID)を出力し /circulars/[id] で全件404する幽霊URL(soft404)だった点を、正本 mhlwNotices(mhlw-notice-NNNN)へ差し替え。あわせて本体 sitemap.xml に直書きしていた個別通達/保護具URLを撤去し、専用子サイトマップ(sitemap-circulars/-equipment.xml＝sitemap-index 列挙)へ一本化（同一URLの二重掲載＝役割崩壊を解消）。route.test.ts(6 it)＋sitemap.test.ts に二重掲載なし回帰(3 it)を追加。
- [x] 【柱C-4・S（メタ部分のみ）】JSON-LDヘルパー lib整備 — 済（seo/c4-json-ld-lib-consolidate / #530）。`json-ld.tsx` のハードコードしたサイトURL(22)/サイト名(13)を `seo-metadata.ts` の SITE_URL/SITE_NAME 単一ソースへ集約（出力JSON byte-identical・既存破壊0）。ロゴURLは LOGO_URL に一本化。これまでゼロだった JSON-LDヘルパー約20関数の回帰テスト `json-ld.test.ts`(23 it) を新設＝@context/@type・position 1始まり・slice上限(ItemList10/FAQ20/用語50/Quiz10)・OG画像フォールバック・SearchActionの/search正規化・任意フィールド条件出力を固定。generateMetadataヘルパー(withSiteAlternates/withSiteOpenGraph/withSiteTwitter)・BreadcrumbList可視化(PageJsonLd/Breadcrumb)・og:imageフォールバック(layout.tsx /api/og)は既存敷設済みを確認。各routeのページ別 generateMetadata/本文SSRは各route所有UI班の担当。
- [x] 【柱C-4・S og:image欠落フォールバック(lib層)】#528 — withSiteOpenGraph/withSiteTwitter に DEFAULT_OG_IMAGE(/api/og 1200x630)を既定付与。Next.js は openGraph を浅マージ(丸ごと置換)するため、ページが独自 openGraph を export すると root の og:image が消える退行(実測 /bcp・/insurance 等=critique C-1)を、helper 利用 32 ページ一括で修復。route本文は不変。
- [x] 【柱C-2・S 横断検索】(a) search-index 構築済(#514)。(b) 検索UI=⌘K コマンドパレット既存＋モバイル検索トリガを 44px タップ標的化(#524, WCAG 2.5.5)。(c) /search 結果ページ追加(#518)＝カテゴリタブ＋件数バッジ・空/0件状態・URL共有可・全件表示。searchItems に limit 引数(既定10で⌘Kは不変)＋countByCategory＋ユニットテスト固定。⌘K フッタに「すべての結果を見る→/search?q=」導線(2タップ到達)。webSiteSchema の SearchAction を /search に正規化。/search は noindex,follow。
- [x] 【柱C-3-3・S】欠落ページのsitemap追加 — 済（#513 マージ済。/court-cases・/whats-new・/site-records 系を sitemap.ts へ収載、sitemap.test.ts で回帰固定）。
- [x] 【柱C-3-4・S】lastmod動的化 — 済（#516 マージ済。各データ実更新日から生成・未来日capで阻止、lib/sitemap/lastmod）。
- [x] 【決裁A・柱C】SNSリンクプレビュー復活 — 済（seo/robots-fb-link-preview / #522）。facebookexternalhit を AI_TRAINING_CRAWLERS から専用 SOCIAL_LINK_PREVIEW_BOTS（Allow:/＋共通非公開パス除外）へ移動。FacebookBot等の学習クローラ遮断は維持。og:image/og:title/twitter card は layout.tsx で /api/og フォールバック敷設済みを確認。robots.test.ts で回帰固定（5 it）。

## 補充の指針（未着手3件未満で起こす）
- docs/site-critique-2026-06-11/01-seo-technical.md・05-lighthouse.md の SEO/構造化データ系で自領域に閉じるもの。
