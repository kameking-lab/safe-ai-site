# 06 スマホ実機検証（375px相当）

> **未検証の明記**: ブラウザ実行不可のため **実機タップ・表示崩れは未確認**。以下はコードのTailwindクラスからの静的推定＋打合せ書の構造的課題。実機確認は社長がプレビューで。

調査時刻: 2026-05-25

---

## 現状の安全衛生日誌（スマホ）— 静的所見
- 一覧 `diary-list-client`: 統計が `grid-cols-2 sm:grid-cols-4`、本体 `max-w-2xl`＝**375pxでも破綻しにくい**（推定良好）。
- 必須フォーム `diary-form-required`: ボタン `flex-wrap`、`min-h-[40px]`＝**タップ領域は確保**（推定良好）。音声入力ボタンはスマホ前提で◎。
- 詳細フォーム `diary-form-detail`: `grid cols-2 gap-3` のフィールド配置＝375pxで2列が窮屈な可能性（**要実機確認**）。リスク5×5マトリクスのスマホ操作性は未確認。

## 打合せ書（新規）のスマホ課題＝ここが本丸
- 打合せ書の中央テーブルは **業者×9列（業者名/作業/機械/資格/人員/災害/リスク/指示/サイン）**。**375pxに横並びは不可能**。
- **横スクロール入力はNG**（教訓3「現場にめんどくさいをさせない」に反する。指で横スクロールしながら9列入力は地獄）。
- **正解＝業者ごとの「カード縦積み入力」**: 1業者＝1カード（KYの危険行カードと同型）。カード内は縦1列でフィールドを積む。閲覧時のみ表形式に。
- 点検項目8カテゴリは **アコーディオン**（カテゴリ折りたたみ）＋AI事前チェック済みなので「確認してタップ修正」だけ＝スマホでも軽い。

## 強い判定
- スマホは **「現場で当日実績を返す／確認する」用途に限定**し、**前日のフル作成はタブレット/PC前提**にする（元請現場代理人は事務所作成が主）。これは教訓3に沿う（現場の負荷最小）。
- 打合せ書スマホUIは **横スクロール表を絶対に作らない**。業者カード縦積み＋アコーディオン点検＋AI事前チェックで「タップ確認」に寄せる。
- KYパターンB（ズーム）はスマホ閲覧（印刷プレビュー確認）に流用、入力はカード型。
