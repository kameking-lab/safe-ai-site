# Astra independent UX recheck — PR #1001

Application head: `6425056110fb722da16f84eb4c58012405a6f1bf`. Reviewed 2026-09-24 JST with anonymous Chromium against the exact local worktree (`next dev --webpack`, localhost:3327; installed Next 16.3.5). **Local acceptance: PASS. Prior R1/R2 are resolved.** No application change or merge performed. This does not replace clean CI or production/Preview acceptance.

- **Initial discovery PASS:** at 1280×900, search y=222–270 and category y=242–290; at 320×900, search y=293.39–341.39 and category y=377.69–425.69. Both are visible and usable without scrolling. 320×640 also passes. Removed internal QA badges are absent. Screenshots independently inspected; compact heading/filters now precede the results and featured showcase.
- **Return-state PASS, eight journeys:** at each width, expanded 60 results → detail → browser Back; expanded 100 → detail → explicit library link; all filters → detail → Back; change query/sort after returning → detail → explicit link. DOM controls and count retain query/category/use/signFormat/numericOnly/documentOnly/quickFilter/sort; saved visibleCount is 60/100 or 20 for filtered lists. All eight returned scroll positions differ from the position captured on card activation by **0px** (criterion ≤50px). Mobile deep positions restored: y=24984 and y=37836. Comparing against activation matters because clicking/bringing a tall card into view can first scroll the page.
- **Non-default filter coverage:** query `荷重`, category `editable-numeric`, use `報告書`, signFormat `数値表示板`, both boolean filters true, quickFilter `numeric`, sort `new`; then query `制限` and sort `order` for the next journey. Values survive both return paths at both widths. Direct detail entry without the return marker still returns to a default 20-card, empty-query hub.
- **Editor/export regression PASS:** Japanese alone and `プレビューどおり` initially selected. Add the four foreign languages in reverse order; fields/preview retain `ja, vi, zh-CN, en, id`. Synthetic Japanese text `再審査用の保護帽を着用` stays out of URL/filename. A4縦 and A4横 both update preview and size; actual browser button downloads JPEG/PNG/PDF for both orientations, **six files**. Rasters are 2480×3508 or 3508×2480, 300dpi. Visual text/order/wrapping matches preview; each PDF embeds the exact downloaded JPEG and has the corresponding A4 MediaBox. This is a helmet-sign regression sample, not visual certification of all 100 exports/translations.
- **Layout/runtime PASS:** zero horizontal overflow at 320px for hub, restored lists and five-language editor; final full run has zero page/console errors and zero HTTP ≥400 responses.

At 2026-09-23 15:45 UTC, PR is draft and head remains the reviewed SHA. Vercel and repository-hygiene are SUCCESS; e2e, smoke and safety-before-performance remain IN_PROGRESS; full is SKIPPED. **Release gate remains HOLD for unfinished CI; no Preview-page acceptance is claimed.** Earlier Preview SSO restriction is not cleared by a green deployment status.

Raw independent script, JSON, screenshots and six downloads are kept only in `%TEMP%\astra-pr1001-64250561` (`review.mjs`, `results.json`, `extra.json`); no image/download is added to Git. The initial harness attempt corrected an exact-label selector before the successful complete run. Only this compact Markdown records the recheck in the repository.
