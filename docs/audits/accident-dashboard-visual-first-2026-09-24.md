# 事故分析ダッシュボード Visual-first 実装証跡

実装日: 2026-09-24 JST
基点: `origin/main` `7df7a44fd1158dfc4cf20d81daa1fbedd5d47ce8`

## 受入結果

- 収録事例と全国速報を別タブ・別母集団として表示。全国速報は収録事例の2019〜2024年系列へ接続しない。
- 既定の死亡災害比率100%メーターを廃止。既定表示は墜落・転落 `1,248 / 4,782 = 26.1%`。事故型指定時の分母は、業種・年・データ源などを維持し、事故型条件だけを外した比較母集団。
- 初期導線を「業種・事故型・年」→「事故型構成比・月別ヒートマップ・年別推移」に短縮。詳細条件、KPI、詳細図、出力操作は後段へ配置。
- フィルタ変更は履歴を積み、URL再読込とブラウザの戻るで状態を復元。
- 0件時は構成比を算出せず、空状態を表示。月別セルは月名と件数を併記し、図の代替表はキーボードで展開可能。
- 初期説明文は動的な速報名・件数を実値に置いた計測で224字。出典範囲、期間、件数、分母、発生率ではない旨を常設。

## ブラウザ実測

Cookieを「拒否する」で確定後、スクロール0からのDOM座標。

- 390×844px: 構成比メーター上端 `801.30px`、月別ヒートマップ見出し `897.89px`、年別推移カード上端 `1,129.28px`、横はみ出し `0px`。
- 1440×900px: 構成比メーター上端 `569px`、月別ヒートマップ見出し `505px`、年別推移カード上端 `492px`。3図が同じ初期画面内に並ぶ。
- 建設業→2024年→墜落・転落、ブラウザ戻る、0件条件、全国速報タブを実ブラウザで検証。

## 検証

- `npm run lint`: 0 errors（既存29 warnings）
- `npx tsc --noEmit`: PASS
- 事故分析Vitest: 7 files / 23 tests PASS
- Playwright `accident-dashboard-visual-first.spec.ts`: 4 tests PASS
- `npx next build`: PASS（3,450 static pages）
- `npm run build` の前段source/public identity guardはPASS。ローカルPlaywright生成物を検出するstorage guardは作業環境固有で停止したため、Next本体ビルドを別途完走。クリーンなPR CIでは生成物をGitへ含めない。
