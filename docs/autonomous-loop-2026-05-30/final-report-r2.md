# 第2ラウンド 最終レポート — 機能別 実務深掘りシナリオ巡回（2026-05-30）

> **【最重要・独立行】**
> - 第2ラウンド稼働: 2026-05-30 09:21 JST 開始 / 本レポート 10:05 JST（約45分）/ 完遂サイクル **1**（採用1・却下0）+ 全9機能の実機シナリオ巡回
> - **安全地点タグ（R1から継続）: `autonomous-loop-start-2026-05-30`（SHA `86542480`）**
> - 全否定の復元: `git fetch --tags && git reset --hard autonomous-loop-start-2026-05-30 && git push -f origin main`（個別は各PRを `git revert <SHA>`）
> - R2 マージPR: **#319** / R2 本番デプロイ回数: **1**
> - R2 後 main HEAD: `29fcf639`
> - サイト完成度: R1終了 **92** → R2終了 **92.5**（横断は出し切り済のため、R2は精度・検証中心）
> - 既存機能 回帰: **なし**（最終テスト **968 pass / 123 files**、CI e2e/smoke pass）

---

## 0. R2 の方法（静的レビューでなく実機シナリオ）
Playwright + ローカル本番ビルド(`next start`)で、9機能をモバイル(iPhone13)実機操作。初見〜入力〜エッジ(空欄/APIキー無/クラウド無)〜印刷(printメディア)〜多言語まで「通しで」操作し、詰まり・欠落・不正確を探索。スクリーンショットで実画面を確認（`web/.r2-screens/`、非コミット）。

## 1. 9機能 シナリオ巡回の結果
| 機能 | 通したシナリオ | 詰まり/発見 | 判定 |
|------|----------------|-------------|------|
| KY `/ky/paper` | 現場でフォーム入力→危険(1R)/対策(3R)→AI提案→保存/共有/印刷 | 現場語ラベル・各欄音声・KYT4R・自動保存・空欄AIは「先に作業内容を」と明示・共有失敗は代替提示 | 実務レベル |
| 打合せ書 `/safety-diary` | 重層下請(元請/1〜3次/下位)＋行ごとリスク評価＋AI推論 | 縦積み入力可・AI/公式版/KY転記あり | 実務レベル |
| 化学物質RA `/chemical-ra` | 物質autocomplete→換気/取扱量を質的選択→A4結果 | **数値不要で非専門家到達可**・混合物RA・SDS取込・Gemini表示 | 実務レベル |
| 事故DB `/accidents` | 業種×作業内容で検索→AI注意喚起→出典確認 | データ内訳/出典明示・AccidentHubNav。**件数表記の過大表現を発見→r2-01で是正** | 実務レベル(是正後) |
| チャットボット `/chatbot` | 法令質問→該当なし/キー無時の応答 | **キー無でもハード失敗せず関連条文(安衛則563条等)＋一般原則の寄り添い応答** | 実務レベル |
| サイネージ `/ky/morning` | 朝礼大画面・6言語切替・空状態 | 大きな空状態＋6桁コード導線・**6言語翻訳切替動作**(Tagalog見出しのみ英語fallback=軽微) | 実務レベル |
| 法改正 `/laws` | 施行前/済・影響業種・自社対応要否 | **業種/規模/対象属性/影響度の多軸フィルタ**・AI要約・施行当時/現行のe-Gov誘導 | 実務レベル |
| 新着 `/whats-new` | 法改正・事故速報一望・RSS | ハブ＋RSS3本＋メール購読 | 実務レベル |
| 重大災害 `/accident-news` | 類型把握・似た事例・説明資料印刷 | AccidentHubNav・print出力はchrome無しクリーンA4 | 実務レベル |

エッジ/エラーUXは**全て graceful degradation**（chatbot/KY-AI/KY共有/空欄validation）。これは本サイトの完成度の高さを示す。

## 2. R2 実験
- **実験r2-01（採用 / PR#319）**: 件数表記の正確化。
  - `/accidents`(詳細事例292件)を R1自作のナビ文言が「約5,000件を全件検索」と過大表現していたのを是正（約5,000件=統計ダッシュボード/accidents-analyticsの5,026に正しく帰属）。
  - `/for/construction`「3,515物質」(stale)を canonical 定数 `MHLW_MERGED_CHEMICAL_COUNT`(=3,695)参照に変更しドリフト排除。
  - 回帰ガード2件追加。lint0/tsc0/test968/build OK、実機確認済。
- **却下/見送り（理由付き）**:
  - モバイル3層ナビ重複(R1監査指摘)の解消: 1行分のchrome削減 vs オーナー意図設計の尊重で価値が主観的→「迷ったら捨てる」適用、不介入。
  - Tagalog見出しの英語fallback: 翻訳作文は捏造リスクのため不介入。
  - 33 vs 50 法令の不一致: 内部仕様依存で正値を確証できず、誤置換は捏造リスク→**オーナー判断事項として申し送り**。

## 3. R2 実験数 / 採用 / 却下
- 実験 **1** / 採用 **1**（#319）/ 却下(merge見送り) **0**（＋確証不能で不介入の申し送り3件）。

## 4. マージPR・デプロイ回数
- PR **#319** のみ。本番デプロイ **1回**。R2後 main HEAD `29fcf639`。

## 5. 完成度推移・各機能
- R1終了92 → R2終了92.5。各機能は元々実務レベルで、R2は事故導線の**正確性**を+0.5。

## 6. 既存機能 非破壊
- 最終テスト **968 pass / 123 files**（R1終了966 + r2-01ガード2）。lint0/tsc0/build OK。CI e2e/smoke pass。

## 7. 社長が朝イチで見るべき変更トップ5（本番URL）
1. `/accidents` 上部「事故情報ナビ」 — **「事故DB検索＝出典付き事例検索」**に文言是正（誤誘導解消）。「約5,000件」は統計ダッシュボード側へ。
2. `/for/construction` 最下部の化学物質リンク — 「**3,695** 物質を検索」（stale 3,515 を是正・以後自動追従）。
3. トップ `/` 主要機能グリッドの事故カード — 説明文を実態整合に微修正。
4. （R1分）`/` ペルソナバンド・`/for/{solo,manager,consultant}`・各サイドバー「立場から探す」。
5. （確認）9機能のエラー時挙動 — APIキー/クラウド未設定でも親切に代替提示（chatbot・KY-AI・KY共有）。

## 8. まだ実務レベル未満で残った点（正直に）
- **コア9機能は実機巡回でも詰まりが少なく成熟**。R2で見つかった実害は件数表記の1件のみ（是正済）。
- オーナー判断が要る申し送り: (a) **33 vs 50 法令** の表記統一（RAGコーパスの正値はオーナーが既知）、(b) 化学物質件数の正規定義(292/3,695/3,984等は意味が異なるため定義の明文化)、(c) UI多言語の方針(現状 SUPPORTED_LANGUAGES=ja、Tagalog一部英語fallback)。
- 横断的に唯一気になるのはモバイルの上部chrome密度(ヘッダ＋表示支援行＋主要機能行)だが、対象が現場ふりがな利用者ゆえ a11y を削るのは逆効果で、安全な改善余地は小。
- データ拡充(事故DB/法改正の実ソース取込)は捏造禁止のため実ソース要＝本セッション対象外。

## 9. ビルドコスト改善: なし（R2デプロイは1回に抑制。docs はVercel Ignored Build Stepで本番ビルド不発生を確認済）

---
## 総評
社長の「完璧なわけがない」は正しく、実機巡回で**実害1件(件数過大表現)を発見し是正**できた。一方、9機能を初見〜エッジ〜印刷〜多言語まで通しで操作した結果、コア機能は graceful degradation を含め**総じて実務レベル**であることが、静的レビュー(R1)より強い根拠(実画面)で確認できた。残課題はオーナーの仕様判断か実データ提供を要するものが中心で、ここで主観的・捏造的なマージを積むより、**確証ある是正のみ通し事実を正直に残す**方針を採った。新たな実データ・方針指示があればその範囲で深掘りを継続する。

---
## 追補（10:30 JST）: 割り込みタスク完了 + 追加検証
### 割り込みタスク: 利用統計ダッシュボードのサンプル除去（PR#321 採用）
- `/stats` の GA4/GSC 未接続時のモック（サンプル）数値表示を廃止し、**正直な空状態「利用統計は準備中です」**へ。
- GA4 live 時も、GA4 単体で取得できずモック混入していた指標（機能別利用・フロー・CV・チャット・前期間比）は**非表示**にし、実測指標のみ表示。
- 「サンプル」ラベル・モック表示用デッドコード約227行を削除。env 新規追加なし（実接続はオーナー判断）。stats-mock/API は温存。
- 検証: lint0/tsc0/test968/build OK・実機(Playwright)で空状態確認。CI e2e/smoke pass。

### データ整合性の横断確認（捏造の有無）
- かつて監査が指摘した**虚偽UGC（/qa-knowledge・community-cases・架空authorAlias）は既に撤去済**（ルート/データ/参照とも存在せず）。
- 他に「displayされる捏造メトリクス」は検出されず（mock フォルダの多くは curated 実コンテンツ）。/stats のみが該当し是正済。

### 追加の機能シナリオ検証（実機）
- **KY 途中保存・復元**: 現場名・作業内容を入力→リロード→**復元される**（localStorage autosave 動作）。
- **whats-new RSS**: `/feed/{news,law-revisions,accident-reports,serious-cases}.xml` 全て 200・正XML。
- **accident-news 似た事例**: `?focus=<id>` で「似た事例」セクション描画。
- → 全9機能を初見〜入力〜エッジ〜印刷〜多言語〜永続化〜配信まで実機巡回し、機能的な詰まりは解消済を確認。

### R2 累計（追補時点）
- 採用 PR: #319（件数正確化）, #321（統計サンプル除去）, #320（R1 docs）。本番デプロイ R2 計 2回（製品）。
- 完成度: R1終了92 → R2 92.5（正確性・整合性の底上げ中心）。
- 残るオーナー判断: 33 vs 50法令の統一 / 化学物質件数の定義明文化 / GA4・GSC・Supabase の実接続（env）/ UI多言語方針。

---
## 追補2（10:42 JST）: 利用統計サンプル除去の本番実機検証（重要）
本番 www.anzen-ai-portal.jp/stats を Playwright で実機確認した結果、**本番では GA4・GSC が既に接続済（「GA4 ライブ / GSC ライブ」）**だった（ローカルは env 無しで空状態）。
- 表示中の実データ: DAU 155 / MAU 155 / PV 260（実測）・ページ別アクセス（実GA4）・流入元（実GA4）・SEO効果（実GSC）。
- **以前「サンプル」ラベルで併記されていたモックセクション（機能別利用・離脱フロー・コンバージョン・チャット指標・前期間比）は本番から消滅を確認**（"Amazon クリック"・"質問数（合計）"・機能別利用テーブル いずれも不在）。
- → 社長の狙い「全て本番データにしたい・サンプル表記を消す」は**本番で達成・実機検証済**。捏造表示はゼロ。
- 補足: 未接続環境では空状態「準備中」を表示する分岐も実装済（将来 env が外れても捏造しない）。

---
## 追補3（12:20 JST）: エスカレ4項目の処理結果 + 件数監査
社長エスカレ回答（数字は自分でコードから確定して統一・捏造禁止）に対応:
1. **法令数（#325）**: 実計数 → curated中核 **55法令等**（法令規則47＋指針通達8。compact.json由来の文書バンドル11件は水増しのため除外）。computed定数 `LAW_SOURCE_COUNT` で9ファイル統一。「33」「50以上」はいずれも不正確だった。
2. **化学物質件数（#326）**: 追跡 → **3,695**（MHLW告示/NITE/PRTR/化審等をCAS統合した規制対象物質数）が正準。3,984は非表示の取込件数（別物・温存）。「約3,700」4箇所を定数統一。stale内訳(251+2,729…=3,633≠3,695)は除去。
3. **サイネージ6桁共有/Supabase（#327）**: 障害は**既に解決済**（2026-05-26 fix.sql適用、本番read-onlyで再確認）。私の領域として共有失敗時の正直なエラー＋同一端末フォールバック誘導＋回帰テストを実装。fix.sql再適用手順をdocs化。
4. **多言語の整え（#328）**: 6言語ラベルは完成（かな混入0）。Tagalogも本文ネイティブ（英語は2語のみ＝妥当・日本語残存なし）。捏造翻訳せず、未翻訳(日本語かな)混入を防ぐ回帰ガードを追加。

### 追加の件数監査（productive accuracy vein）
- 主要件数を実データと照合: FAQ 200=正、13業種×39テンプレート=正(13×3=39)、10業種(industries)/13業種(plan)は別機能で非矛盾。
- **glossary「272語」(app-shellナビ)は stale**（実体 TERMS.length≈251＝BASE99+EXTRA152。/glossaryページは computed で正確）。ナビの stale 数値を除去（ページ側の正確な計数に委ねる）。
