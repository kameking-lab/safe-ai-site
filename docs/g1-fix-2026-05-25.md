# 本番Supabase障害 修正ログ（2026-05-26）

## 診断の確定（前回仮説の訂正）

前回（G-1検証）は「service_role鍵がanon等に誤設定」を最有力仮説としたが、本セッションで**訂正**。

社長がクリップボード提供した service_role secret(Legacy) を検証:
- JWT payload: `role=service_role` / `ref=klqbfudraljartikwseq`（正しいプロジェクト）/ `iss=supabase` / exp=2036年。→ **鍵は正しい service_role**。

この正しい鍵で Supabase REST を直接叩いた結果:
- `GET /rest/v1/ky_records` → **HTTP 403 / 42501 permission denied**、hint: `GRANT SELECT ON public.ky_records TO service_role;`
- `GET /rest/v1/signage_sessions` → **HTTP 403 / 42501 permission denied**（同上）
- `GET /rest/v1/meeting_records` → HTTP 404 / PGRST205（テーブル未作成）

→ **根本原因の確定: 障害2 は「鍵の誤り」ではなく「テーブルへの service_role GRANT不足（原因B）」**。
　ky_records / signage_sessions が標準Supabaseの自動GRANTを経ずに作成されたため、service_roleロールに
　table権限が無い。Vercelの鍵差し替え（当初Step3）は**不要・無効**（鍵は元々正しい）。

## 結論: 必要な修正は fix.sql のみ

3障害すべてが `docs/g1-verification-2026-05-25/fix.sql` の実行で解消する:
- (1) meeting_records 作成 → 障害1解消
- (2) ky_records / signage_sessions / meeting_records への service_role GRANT → 障害2解消
- Vercel env 変更は不要。

## 実行上の制約

fix.sql は GRANT / CREATE TABLE（DDL）を含み、これは **service_role REST鍵（PostgREST用JWT）では実行不可**。
DB superuser 権限が要る。当環境に psql / supabase CLI は無く、Supabase の DB パスワード/アクセストークンも未保有。
→ 実行手段は次のいずれか:
- (A) 社長が Supabase SQL Editor で fix.sql を実行（postgres権限・秘密共有不要・最速）。【推奨】
- (B) 社長が Supabase Personal Access Token(sbp_) を提供 → CLIが Management API `POST /v1/projects/{ref}/database/query` で実行。
- (C) 社長が DB接続パスワードを提供 → CLIが psql/supabase CLI 導入の上で実行。

## 実行結果（2026-05-26 確定）

選択手段: (B) Supabase Personal Access Token(sbp_) を Management API で使用。
- 鍵・トークンは社長クリップボード経由でプログラム取得し、チャット/ログ/コミットへ一切出力せず、検証後に一時ファイルを削除。

実行:
- `POST https://api.supabase.com/v1/projects/klqbfudraljartikwseq/database/query` に fix.sql 全文 → **HTTP 201 成功**。
- 確認SELECT結果: ky_records / signage_sessions / meeting_records の3テーブルとも service_role に
  SELECT/INSERT/UPDATE/DELETE/TRUNCATE/REFERENCES/TRIGGER 付与を確認。meeting_records 新規作成済み。
- service_role鍵で直接REST再テスト: 3テーブルとも 403→**200**（空配列）に回復。

Vercel: **変更なし**（根本原因はGRANT。鍵は元々正しいservice_roleと判明。差し替え・再デプロイ不要）。

## G-1 本番再検証結果（修正後）
- KY: `POST /api/ky/records` → ok:true(200)、GETで保存確認。`POST/GET /api/ky/signage`（6桁コード生成993624→取得）→ ok:true。**YES**
- 打合せ書: `POST/GET /api/meeting/records` → ok:true、meeting_records に行追加確認。`/api/meeting/suggest`(Gemini) → 正常。**YES**
- サイネージ: 別端末想定（localStorageなし）で `/ky/morning?code=993624` を実ブラウザ表示 → API 200・本日の作業/リスクTop3/対策/行動目標/「共有コード993624のKYを表示中」を描画。**YES**（証跡: docs/g1-verification-2026-05-25/signage-share-verified.png）

検証で作成したテストデータ（device_id=test-cli-2026-05-26-fix の ky_records/meeting_records、signage code 993624）は service_role で全削除済み（HTTP 204、確認クエリ空）。

## オーナー後始末（推奨）
- 今回使用した Supabase Personal Access Token は用済み。https://supabase.com/dashboard/account/tokens で Revoke 推奨（CLI側のコピーは削除済み）。
