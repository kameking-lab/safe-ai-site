# 10 3デバイス検証（軸10）

検証手段: Playwright(Chromium) で本番 https://www.anzen-ai-portal.jp を実レンダリング。スクショ: `screenshots/`。

## 実測（/chemical-database/50-00-0 ホルムアルデヒド）

| デバイス | 横スクロール | 主要セクション |
|---|---|---|
| スマホ 375px | なし ✓ | 濃度基準値・発がん性 / 政府版GHS(NITE) / 特別則 / 関連法令(3件) |
| タブレット 768px | なし ✓ | 同上 |
| PC 1920px | なし ✓ | 同上 |

- 詳細ページは `PageContainer width="full"` で全幅。3デバイスとも横スクロールなし＝レイアウト崩れゼロ。
- 検索（/chemical-database）: 「ベンゼン」入力 → **745ms** で結果表示（サブ秒・スマホ現場利用に十分）。
- 注: Lighthouseは当環境にCLI/Chrome無く未測定（捏造せず）。本番でのLighthouse実行を推奨。HTMLは詳細1ページ約151KB（やや大きめ、画像なし・テキスト主体）。

## 想定ユース
- スマホ375px: 現場でSDS確認・危険物名検索→規制一覧。実用的。
- タブレット768px: 現場事務所でのRA記録作成。
- PC1920px: 本社・本部での一括確認。

## 課題
- DV-1 スマホでの詳細ページは情報密度が高い（多数の法令セクション）。アコーディオン/折りたたみで初見の圧迫感を軽減する余地。
- DV-2 1ページ151KBはモバイル回線でやや重い。遅延読込・セクション分割の検討余地（Lighthouse実測後に判断）。

## 結論
3デバイスとも崩れなし・検索サブ秒で、モバイル現場利用は既に実用的。改善は密度緩和（アコーディオン）と実測ベースの軽量化（中優先）。
