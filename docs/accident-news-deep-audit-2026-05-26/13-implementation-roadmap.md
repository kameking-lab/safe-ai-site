# 13. 実装ロードマップ

## Stage 1（本Phase B）— 安全コア
- P0-1 重大災害事例ブラウザ `/accident-news`（匿名・類型・同種頻度・出典・AI解説）
- P0-2 出典・引用ルール徹底
- P1-1 既存機能連携の動線
- 検証: 純関数（フィルタ/集計）の単体テスト＋既存930維持＋本番動作確認

## Stage 2 — 通知・仕上げ（P1/P2）
- P1-2 /whats-new・RSS・メールダイジェスト統合
- P1-3 説明資料印刷ビュー
- P2-1/P2-2 報道構造化補助・サジェスト高度化

## Stage 3 — 社長判断後（P3・要弁護士確認）
- P3-1 会社名（送検公表）安全モード（送検公表限定・出典必須・約1年expiry・発注者非掲載）
- P3-2 発注者名は非実装（非推奨）

## 完了条件（共通）
1. `tsc`/`lint` 0、`test` 既存930維持＋新規、`build` 成功
2. **全事例で出典必須・推測禁止・本文非転載・会社名/発注者なし**（法的要件）
3. branch→PR→CI緑→squash merge→本番確認

## 停止・エスカレ条件
- 会社名・発注者の掲載要求＝法的「高」→ 社長判断（弁護士確認推奨）。本Phaseは実装しない。
- スクレイピング規約懸念→ 公式は取込せず出典リンク誘導。

## ビルドコスト
- 既存匿名データの再構成＋既存ETL流用。新規env・新規SaaS・新規cronなし（通知は既存基盤に相乗り）。ビルド影響極小。
