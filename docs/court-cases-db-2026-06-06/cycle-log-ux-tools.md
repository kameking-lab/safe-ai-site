# cycle-log — 機能UX班-B（判定・法令・統計系）並列ループ

各イテレーションの1段落。マスターの cycle-log.md は参照専用。

## 2026-07-03 /accidents-analytics・/accident-news フィルタselect/button 44px化（ux-tool/accidents-analytics-news-select-44px）

イテレーション頭でPR #662（mental-health-managementサブガイド結論カード）がCI緑・mergeable=MERGEABLEだったためsquashマージ→mainをff-only同期（clean）。lane backlog未着手が次点1件（/law-search・/chemical-databaseデータ鮮度表示、data班確認待ちでブロック中）のみ＝3件未満のため補充。診断07残タスク一覧・自己記憶（ux-tools-domain-pillar0-saturated）を確認したところ、2026-06-14のタップ標的監査（PR #570）で「フォームselectがまだ33〜40px」が将来候補として申し送りされたまま未着手と判明。実コード確認＝`AnalyticsDashboardImpl.tsx`の軸G業種select（#quick-industry）・フィルタバー2 select（#industry-filter/#type-filter）・絞り込みをクリアボタン・詳細分析トグルボタンはいずれも`min-h`指定が皆無でpx-2.5〜3 py-1.5前後≒30px、`accident-news-filter.tsx`の3 select＋キーワード入力＋検索/クリアボタンは`min-h-[40px]`と44px未満で据え置かれていた。是正=全て`min-h-[44px]`を付与（寸法のみ、文言・色・href・ロジック不変＝既存破壊0）。無読テスト新設8/8 PASS（own prod 3100・390×844・serviceWorkers:block・domcontentloaded＝/accidents-analyticsの軸G select・フィルタバー2 select・絞り込みをクリア・詳細分析トグル、/accident-newsの3 select・キーワード入力・検索ボタン）。ゲート全通過（tsc0/lint errors0/vitest 2288 pass/build成功）。ビルド再生成JSON（docs/rag-metrics-latest.json・chatbot-eval-fresh-results.json・ky-print-sheet snapshot）は契約どおりcheckoutで復元＝コミットせず。スクリプト=docs/third-party-reviews/scripts/accidents-analytics-news-select-44px-noread-2026-07-03.mjs。残=次点1件（/law-search・/chemical-databaseデータ鮮度表示はdata班確認待ち）のみ＝3件未満のため次イテレは柱0/柱3レビューから補充が必要。

## 2026-07-03 /mental-health-management サブガイド2本に結論カード新設（ux-tool/mental-health-mgmt-subguide-conclusion-card）

イテレーション頭で自班未マージPRを確認＝#648（Q39 PIN trigger拡充）はマージコンフリクト（cycle-log-ux-tools.md・BACKLOG-ux-tools.mdのみ、並行イテレーションの#651・#643と同時多発）を手解消してからsquashマージ。続けて#656（chemical-ra product-search ゼロヒットCTA）がe2e/smoke共に緑化していたためsquashマージ。mainをff-only同期。lane backlog未着手を確認したところ最上位1件（/law-search・/chemical-databaseデータ鮮度表示）はdata班確認待ちでブロック中のため、次点リストから実行可能な1件（/mental-health-management配下small-business・interview-guidanceサブガイドの結論カード欠如）に着手。実コード確認＝両ページとも`PageHeader`直後が`AlertCircle`の制度説明プローズ（努力義務の位置付け等）のみで、色帯・デカ数字・「次にやること」の視覚結論（`ConclusionCard`）を持たなかった一方、姉妹ページ`/stress-check`は`ReadinessForm`内で`ConclusionCard`を既に使用しておりパターンが確立済みと判明。是正=`PageHeader`直後に`ConclusionCard`を新設（既存プローズは補足としてそのまま残置、水増し回避のため新規判定ロジックは追加せず既存データ配列の件数を転記のみ）。small-business=tone warning（既存amber基調と整合）・value 9(`SMALL_BUSINESS_STEPS.length`)・unit ステップ・title「努力義務」・action「実施ロードマップへ」(#roadmap)。interview-guidance=tone info（既存sky基調と整合）・value 8(`INTERVIEW_FLOW_STEPS.length`)・unit ステップ・title「実務フロー」・action「フローを見る」(#flow)。`ConclusionCard`・`safety-tone.ts`はux-records custodian所有のためimportのみで変更せず、`data/mental-health-rules`（data班所有）も既存exportの件数を読み取るのみで一切変更せず。無読テスト12/12 PASS（own prod 3100・390×844＝結論カードがファーストビュー内y=489・デカ数字48px・アクション導線44px以上・DOM順で制度説明プローズより前・h1=1、両ページとも）。ゲート全通過（tsc0/lint errors0/vitest 2242 pass/build成功）。ビルド再生成JSON（docs/rag-metrics-latest.json・chatbot-eval-fresh-results.json・ky-print-sheet snapshot）は契約どおりcheckoutで復元＝コミットせず。BACKLOGの次点リストからも本件と#656で完了済みのchemical-ra CTA項目を削除。残=次点1件（/law-search・/chemical-databaseデータ鮮度表示はdata班確認待ち）のみ＝3件未満のため次イテレは柱0/柱3レビューから補充が必要。スクリプト=docs/third-party-reviews/scripts/mental-health-mgmt-subguide-conclusion-card-noread-2026-07-03.mjs。

## 2026-07-03 /chemical-ra/product-search ゼロヒット専用CTA新設（ux-tool/chemical-ra-product-search-zero-hit-cta）

イテレーション頭で自班未マージPR2件を確認＝#651（法令名正規化テーブル単一ソース化）はCI全緑のためsquashマージ、#648（Q39 PIN trigger拡充）はmain先行によりmerge conflict(DIRTY)＝cycle-log-ux-tools.mdのみ（並行イテレーションの#643 law-search-corpus-lazy-loadと同時多発）だったため origin/main を通常マージで手解消（append-onlyログの両エントリを保持する形で解消）、ゲート再実行（tsc0/lint errors0/vitest 2225 pass/build成功）後pushしCI待ちへ（本イテレでは未マージ・次回回収）。mainをff-only同期（#651反映を取込・clean確認）。**本題**=lane backlogの未着手2件（Q39=#648で対応中のため重複回避・データ鮮度表示=data班確認待ちでブロック）のみ＝3件未満のため補充指針「自領域の柱0/柱3レビュー」に従いExploreで2候補を実コード調査：(1)/chemical-ra/product-search のゼロヒットCTA欠如、(2)/mental-health-management サブガイド(small-business/interview-guidance)の結論カード欠如。両方とも実コードで欠陥を確認しbacklogへ補充した上で、スコープが小さく単一ファイルで完結する(1)に着手。**実装**=`product-search-panel.tsx`の`handleSearch`が0件ヒットを実エラーと同一の`error`stateに流し込み、汎用赤バナー（「該当する製品が見つかりませんでした（主要10製品収録）」）のみで次の一手が無かった。是正=`hasSearched`stateを新設して0件検知をerrorから分離、amber色の専用空状態ブロックに「化学物質RA（手入力）」(/chemical-ra)・「化学物質検索DB」(/chemical-database)への具体的CTAリンクを隣接表示（既存`RelatedPageCards`はページ下部に常時固定表示でゼロヒットに連動しないため対象外と判断）。文言は既存メッセージの転記のみ＝**捏造ゼロ・既存破壊0**。単体テスト3件新設（`product-search-panel.test.tsx`＝ゼロヒット時CTA表示・ヒットありでCTA非表示・未入力時は入力エラーのみでCTA非表示、fetchはvi.stubGlobalでモック）。無読テスト8/8 PASS（own prod 3100・390×844・SW block・domcontentloaded＝実在製品名"5-56"→内蔵DBの「KURE 5-56」に別名一致してヒット・架空製品名→CTA2本のhref健全性・h1=1・ヒット時はCTA非表示の非退行）。ゲート全通過（tsc0/lint errors0/vitest 2230 pass/build成功）。ビルド再生成JSON（docs/rag-metrics-latest.json・chatbot-eval-fresh-results.json）・ky-print-sheet snapshotは契約どおりcheckoutで復元＝コミットせず。**注**=無読スクリプトはPlaywrightのESM解決がスクリプト自身の物理パス基準（cwd非依存）のためdocs/third-party-reviews/scripts配下からは直接実行不可＝web/scripts/へ一時コピーして実行後に削除（記録用の恒久コピーはdocs側に保持）。スクリプト=docs/third-party-reviews/scripts/chemical-ra-product-search-zero-hit-cta-noread-2026-07-03.mjs。残=#648 CI回収→squashマージ、次点1件（/mental-health-management サブガイド結論カード）をbacklogで継続。

## 2026-07-03 法令名正規化テーブルの単一ソース化（ux-tool/law-name-registry-single-source）

イテレーション頭で自班未マージPRを確認＝#648（Q39 PIN trigger拡充）はe2e/smoke IN_PROGRESSのため次回回収、#643（law-search遅延読み込み）はCI全緑のためsquashマージ→mainをff-only同期（clean確認）。lane backlog最上位＝診断04 §5.1-1「法令名正規化テーブルの単一ソース化」に着手。実コード確認で想定より根が深い重複を発見：`chatbot-enrichment.ts`が参照する`LAW_METADATA`は`data/laws/law-metadata.ts`（50法令・data班所有）とは**別物**の`data/law-metadata.ts`（33法令・issuer/施行日用）で、しかも`KNOWN_LAW_SHORTS`は手動38件の三重目の重複リスト＝50法令体制拡張（2026-06-10）で追加された12法令（過労死防止法・化審法・毒劇法など）がここに未登録のまま残り、正当な引用でも「範囲外」の偽警告が出うる実害ドリフトだった。**是正**=新規`web/src/lib/law-name-registry.ts`を作成し、data班所有の`data/laws`（50法令）を単一ソースに`LAW_ALIAS_GROUPS`・`LAW_SHORT_SET`・`LAW_FULL_NAME_SET`・`isLawNameEquivalent`を自動生成、公式名と字面が異なる通称のみ`EXTRA_ALIASES`（21法令分）で補う設計に統一。`chatbot-enrichment.ts`の`KNOWN_LAW_SHORTS`/`KNOWN_LAW_FULL_NAMES`と`rag/synonyms.ts`の`LAW_ALIASES`（56行のべた書き）をこのregistry参照に置換、`isLawShortEquivalent`は`isLawNameEquivalent`へ委譲（export名・シグネチャは既存テスト互換のため維持）。`notice-search.ts`のTOPIC_SYNONYMSは法令名⇄略称表ではなくトピック語彙（熱中症→WBGT等）であり対象外と判断、無理に共用せず見送り。rag-search.tsは自班chatbot専属ロジック（seo班横断検索とは無関係）と確認した上で編集、data班所有の`data/laws/**`・`data/law-metadata.ts`は読み取りのみで一切変更せず。ゲート全通過（tsc0/lint errors0/vitest 2219 pass/build成功）。fresh eval非退行を確認（recall5: main 1.0→1.0・fresh 0.99→0.99 とも維持、mrr/該当条文countはisLawShortEquivalentの網羅範囲拡大により微増＝改善方向で悪化なし）。再生成JSON（docs/rag-metrics-latest.json・chatbot-eval-fresh-results.json）は契約どおりcheckoutで復元＝コミットせず。本タスクは画面を持たない内部ロジック統合のため無読テスト（ブラウザ/画面）は対象外と判断＝上記フル自動ゲート（既存のrag-100q/rag-100q-fresh/chatbot-enrichment/chatbot-phase2-metrics各testが本変更を直接ピン留め）とfresh eval非退行差分を代替エビデンスとした。残=#648（Q39）CI回収→squashマージ、次点2件（/law-search・/chemical-databaseデータ鮮度表示はdata班確認待ち、mental-health-managementサブガイド結論カード等）をbacklog補充要。

## 2026-07-03 fresh eval Q39 足場PIN trigger拡充（ux-tool/q39-ashiba-henkougo-tenken-pin）

イテレーション頭で自班未マージPR確認＝#643（law-search-corpus-lazy-load）はe2e/smoke IN_PROGRESSのため次イテレーションで回収、他にux-tool/ prefixの自班PRなし。mainをff-only同期（clean確認、他班マージ分10コミット取込）。lane backlog未着手を確認したところ最上位の2026-07-03補充枠3件のうち最初の1件（fresh eval Q39 PIN trigger拡充）に着手。調査＝Q39「足場の組立て・変更後に必要な点検と記録の根拠条文は？」は既存「足場の点検」PIN（rag-search.ts、567条+566条を紐付け）の4trigger（足場の点検/足場点検/足場の作業開始前点検/足場用墜落防止設備の点検）いずれとも不一致で発火せず、キーワードスコアのみで安衛則565条ほか無関係5件を誤取得していた。是正＝同PINのtriggersに「組立て・変更後」「組立て、変更後」を追加。実体根拠は安衛則567条2項（"足場の組立て、一部解体若しくは変更の後において...点検させ...補修"＝ashiba-sagyo-kisoku.ts実データで確認）。gold側のもう一方=568条（つり足場の点検）はegov-caption-snapshot.tsにcaptionのみ存在し全文条文がcorpus未収録（`allLawArticles`に無く`applyPinnedTopics`のfind()が必ずnullになるためPIN化しても無意味）と判明したためdata班領域には触れず対象外＝ただしrag-100q-fresh.test.tsのisMatchはgold配列に対しOR判定（いずれか1件のヒットで正答）のため567条のみのヒットで完了条件を満たせることを確認してから実装。他fixture（chatbot-fresh-100.json/chatbot-basic-100.json）に「組立て・変更後」を含む別問題が無いことを事前grepで確認し誤爆リスクを排除。回帰テスト1件追加（rag-search.test.ts）。完了条件を実測で確認＝fresh eval Recall@5 99→100/100（Q39含め全問正答・failures配列が空に）・main eval 124/124=100%非退行（PIN group拡張の副作用でMRRは0.8297→0.8257へ僅かに変動するがrecall5は不変）。ゲート全通過（tsc0／lint errors0／vitest 2211 pass・261 files／build成功）。生成物のうちdocs/rag-metrics-latest.json・web/src/data/chatbot-eval-fresh-results.jsonは本タスクの正答率向上そのものを表す実データのためコミット対象（PR #632 S7の先例と同様）、ky-print-sheet snapshotは無関係差分（改行コードのみ）だったためgit checkoutで復元。残=補充枠の残り2件（診断04 §5.1-1法令名正規化テーブル単一ソース化／law-search・chemical-databaseデータ鮮度表示はdata班確認待ち）と#643のCI回収を次イテレへ。**マージ時追記**＝origin/mainとのマージコンフリクト（cycle-log-ux-tools.mdのみ・並行イテレーションの#643と同時多発）を手解消。

## 2026-07-03 /law-search 法令コーパスの遅延読み込み分離（ux-tool/law-search-corpus-lazy-load）

lane backlog未着手0件（全[x]）のため補充指針「パフォーマンス改善」（CLAUDE.md自律作業ルールで自己判断可）から起こす。実コード確認＝`law-search-panel.tsx` が `web/src/data/laws` の全条文データ（`allLawArticles`・約1.4MB/全1,070条文）と検索UI・カード描画・AI要約モーダル・漢数字正規化ロジックを1ファイルに同居させていた。他ページの共有チャンクには元々含まれていなかった（Next.jsのルート単位コード分割）が、同ページ内で検索入力欄などの軽量UIとコーパス本体が不可分に結合しており、ページ自身の初期スクリプト解析コストを不要に押し上げていた。**是正**=検索結果描画（`ArticleCard`・`AiSummaryModal`・`LawSearchResults`・`kanjiToNum`/`normalizeQuery`等）を新規 `law-search-results.tsx` へ切り出し、`law-search-panel.tsx` 側は既存の `MhlwLawArticlesPanel` と同じ `next/dynamic` パターン（SSR維持＝初期HTMLに検索結果を残しLCPを落とさない）で読み込むよう変更。検索ロジック・UI文言・条文データはいずれも無変更（**捏造・水増しゼロ、既存破壊0**）。ビルド成果物 `react-loadable-manifest.json` で法令コーパス本体が独立チャンク（552KB）に分離されたことを確認し、`build-manifest.json` の `rootMainFiles`（全ページ共有の初期ロード対象）を直接grepして条文テキスト（「クレーン等安全規則」等）が混入していないことも実測（ブラウザでの検証はNext.js Linkのアイドル時プリフェッチにより/law-search遷移前でも該当チャンクがフェッチされてしまい判定が不安定だったため、ビルド成果物の静的検査に切替＝[[noread-test-prod-server-port]]の"own prod 3100"定石に加え今回学んだ追加知見）。無読テスト7/7 PASS（law-search-corpus-lazy-load-2026-07-03＝own prod 3100・390×844・domcontentloaded＝rootMainFilesにコーパス本体不含・h1=1・結論カード48px・出典の見分け方が初期折りたたみ・検索語入力で件数変化・条文カード表示・MHLWタブ切替非破壊）。ゲート全通過（tsc0／lint errors0／vitest 2175 pass／build成功）。ビルド再生成JSON（docs/rag-metrics-latest.json・chatbot-eval-fresh-results.json・ky-print-sheet snapshot）は契約どおりcheckoutで復元＝コミットせず。**イテレーション頭の作業**＝自班CI緑未マージPR3件を確認・処理：#625（音声完結モードのマイク権限エラー握りつぶし是正）はCI全緑のためsquashマージ。#632（S7小粒4本＝T6+T8+T9+T10）と#620（S7 T6+T8+T9のみ）は両方CI緑だが、ブランチ分岐後に並行イテレーションが同一の診断04 S7タスクを独立実装した重複PRと判明（rag-search.ts/chatbot-fallback-logic.ts/chatbot-no-hit-response.tsを別々に修正）＝#598/#603と同種の重複（[[lane-backlog-stale-vs-main]]同系統の判断）。T10まで完全カバーする#632を選び、origin/mainとのマージコンフリクト（cycle-log-ux-tools.mdのみ）を解消のうえゲート再実行（tsc0/lint errors0/vitest 2194 pass/build成功）してsquashマージ、#620はマージせずクローズ（PRコメントで重複理由を明記）してブランチ削除。mainをff-only同期。スクリプト=docs/third-party-reviews/scripts/law-search-corpus-lazy-load-2026-07-03.mjs。

## 2026-07-03 S7: チャットボット小粒4本＝一般語PINガード・adjacent誤ヘッダ・no-hitノイズ・週次プローブ（ux-tool/s7-chatbot-pin-guard-noise-suppression・PR #632）

イテレーション頭で自班PR #625（音声完結モードのマイク権限エラー握りつぶし是正）はCI進行中のため契約どおりsquashマージせず次イテレ回収し、main を ff-only 同期（clean確認）。lane backlog の未着手は S7（診断04のT6+T8+T9+T10）の1件のみ＝過去2回の直近イテレでは「F3=Fable班のチャットボット生成品質eval確立待ちでブロック中」として見送っていたが、診断書04を再読すると T6/T8/T9/T10 の完了条件はいずれも独立した実測基準（fresh eval Recall@5・単体テストの回帰・「天気」質問で無関係法令0件・週次プローブの実行）で、F3（結論キーフレーズ判定の生成品質eval）に依存していないと判明＝O4/O5でも同様にF3非依存でローカルevalのみで完了させてきた前例（[[lane-backlog-stale-vs-main]]と同系統の判断）に倣い着手。**T6**=`rag-search.ts`のPinnedTopicに`excludeTriggers`を新設し、一般語「換気」PINが酸欠/坑内/有機溶剤文脈を乗っ取る誤発火を抑止（fresh eval Q79「酸素欠乏症等の防止措置」が酸欠則5条ではなく安衛則600/601条を誤取得していた事故を実測確認→是正後は酸欠則ファミリーに収束、加えて該当PINのtriggerへ言い回しを追加してQ79を完全正答化）。fresh eval Recall@5 98→99/100（残る足場Q39は換気PINと無関係の別欠陥のため対象外・backlogへ次点申し送り）。**T8**=`decideFallbackTier`/`buildFallbackDecision`に`hadPins`信号を追加し、curated PIN確定時はscore0.7台でも`direct`扱いにして、正答（診断04 Q7＝職長教育、施行令19条直撃・score0.73）なのに「直接的に答える条文は限定的です」と誤ヘッダが付く問題を解消。**T9**=no-hit時の関連条文提示に`NO_HIT_NOISE_FLOOR`(0.08)を導入し、診断04 Q21「明日の東京の天気」で無関係な港湾労働法第2条が提示されていたノイズを抑止（0.04は除外・0.12=解雇予告→労働契約法16条の正当な関連提示は維持）。あわせて関連情報が0件のときの`buildNoHitTemplate`文言を「ただし関連をご案内します」という予告倒れではなく「収録範囲内に該当する関連情報は見つかりませんでした」と正直に述べるよう是正（欠落明示）。実装中、GEMINI_API_KEY未設定時のdegradedパス（route.ts、通常フローのnoise floorを経由しない別コードパス）でも同じノイズが再現することを発見し同floorを追加適用。**T10**=本番週次スモークプローブを`web/scripts/chatbot-prod-smoke-probe.mjs`として実装（`npm run smoke:chatbot-prod`）。T1〜T3・T8・T9で実際に修正した5つの事故（偽の範囲外警告／プレースホルダ漏出／派遣元誤答／adjacent誤ヘッダ／related法令ノイズ）を回帰カナリアとして本番APIへ直接プローブし、フラグをJSON出力（gitignore済・実行の度に上書き）。**週次cron自動化はGemini APIの継続課金を伴う運用変更のため、CLAUDE.mdの「必ずオーナーに確認すること」に照らしオーナー確認事項として申し送り**、スクリプト単体は完成・手動実行可の状態に留めた（GitHub Actions等への自動ワイヤリングは未実施）。ローカルprod(3100・APIキー無しdegradedパス込み)で実行し5問5問ともフラグ0件を確認。回帰テスト9件新設（decideFallbackTier/buildFallbackDecisionのhadPins分岐・buildNoHitTemplateの欠落明示・NO_HIT_NOISE_FLOORの閾値検証）。ゲート全通過（tsc0／lint errors0／vitest 2144 pass／build成功）。ビルド再生成JSON中、fresh eval結果とrag-metrics-latest.jsonはT6の改善を正しく反映した実測値のためcheckout復元せずコミット（O5と異なりスコア改善そのものが今回の成果物）。BACKLOG-ux-tools.mdのS7を[x]化、在庫3件未満のため診断04 §5.1-1（法令名正規化テーブル単一ソース化）・fresh Q39（足場PIN trigger拡充）・law-search/chemical-databaseのデータ鮮度表示（data班連携要）の3件を補充。**マージ時追記**: 本PRのブランチ分岐後、並行イテレーションが同一のS7（T6+T8+T9、T10除く）を独立実装したPR #620（ux-tool/s7-chatbot-pin-tier-nohit）を作成済みと判明。両者は同じrag-search.ts/chatbot-fallback-logic.ts/chatbot-no-hit-response.tsを別実装で修正しており、本PRがT10まで含めS7を完全カバーするため、#598/#603の前例（[[lane-backlog-stale-vs-main]]同系統の重複PR処理）に倣い#620はマージせずcloseし重複と明記、ブランチも削除。残=#625 CI回収・補充3件の着手・#620 close。

## 2026-07-03 /chatbot 音声完結モードのマイク権限エラー握りつぶしを是正（ux-tool/chatbot-voice-error-feedback）

イテレーション頭で自班未マージPR2件を確認＝#612（circulars鮮度表示）はCI全緑（e2e/smoke/Vercel SUCCESS）のため契約どおりsquashマージ→mainをff-only同期（clean確認、他班マージ分も取込）。#620（S7 T6+T8+T9）はe2e/smoke IN_PROGRESSのため次イテレーションで回収。**本題**=lane backlog の未着手が S7残（F3=チャットボット生成品質eval確立待ちで継続ブロック中）の1件のみ＝3件未満のため補充指針「チャットボットの深掘り」に従いExploreで調査。`chatbot-panel.tsx` の音声完結モード「話して質問する」ボタンが呼ぶ `startVoiceInput` は `recog.onerror = () => {}` でWeb Speech APIのエラーを完全に握りつぶす実装だったと判明＝マイク権限拒否・無音タイムアウト・ネットワーク断のいずれでもUIが無反応に見え、ユーザーは「聞き取れていないのか壊れているのか」を無読で判断できない柱0違反（診断書未記載の新規発見）。**是正**=同コンポーネント内で既に使われている `voice-input-field.tsx` の `useSpeechToText` フックが持つ日本語エラーメッセージマッピング関数を `describeVoiceError` としてexportし、`chatbot-panel.tsx` 側で新規に重複実装せず再利用（onerrorイベント・`new Ctor()`/`recog.start()`の例外catch双方に接続）。ボタン脇に role="alert" でエラー文言（例:「マイクが許可されていません。ブラウザのアドレスバー🔒から...」）を表示し、音声完結モードのトグルをOFFにすると文言も消え次回に持ち越さない設計。文言・分岐ロジックは既存 `voice-input-field.tsx` の実績パターンをそのまま踏襲＝新規デザイン判断なし・既存破壊0。単体テスト4件新設（`voice-input-field.test.ts`＝マイク拒否/無音/マイク未検出/ネットワーク/未対応ブラウザ/未知エラーコードの各分岐）。無読テスト3/3 PASS（own prod 3100・390×844・domcontentloaded＝`window.webkitSpeechRecognition`をモックしonerror("not-allowed")を発火させ検証。OFF時は文言なし→ON+話して質問するタップでエラー発火時「マイクが許可されていません」可視化→OFF復帰で文言消滅。Next.jsの`#__next-route-announcer__`が常時role=alertを持つ点に注意しセレクタで除外）。ゲート全通過（tsc0／lint errors0／vitest 2104+4=2108 pass／build成功、1回目はTurbopackのセグフォルトで再実行）。ビルド再生成JSON（docs/rag-metrics-latest.json・chatbot-eval-fresh-results.json・ky-print-sheet snapshot）は契約どおりcheckoutで復元＝コミットせず。スクリプト=docs/third-party-reviews/scripts/chatbot-voice-error-feedback-noread-2026-07-03.mjs。残=#620（S7 T6+T8+T9）CI回収→squashマージ、S7残（F3待ちで継続ブロック）、次点2件（mental-health-managementサブガイド結論カード・chemical-ra product-searchゼロヒットCTA）をbacklogに追記。

## 2026-07-03 /circulars 一覧にデータ鮮度表示を追加＋未達成のフッター文言を是正（ux-tool/circulars-latest-issued-date-freshness）

イテレーション頭で自班未マージPR2件を確認＝#603（S8-a監修者バイライン）は main先行によりconflict(DIRTY)だったため契約どおり origin/main を通常マージで解消（cycle-log append-onlyの衝突を手解消）、ゲート再実行後CI緑を確認しsquashマージ。#598（O5 synonyms是正）も同様に確認したところ、#603のブランチ履歴に#598のコミット3本が既に含まれておりsquash内容が完全一致（diff無し）と判明＝#598は#603のマージで既にmain反映済みの重複PRだったためマージせずcloseし、両ブランチをリモート削除。main を ff-only 同期（clean確認）。**本題**=lane backlog の未着手が S7（F3=チャットボット生成品質eval確立待ちで継続ブロック中）の1件のみ＝3件未満のため補充指針に従いExploreで自領域の柱0/柱3ギャップを調査。診断07 柱C-11「データ鮮度表示」の観点で law-search・circulars(一覧)・chemical-database の3ページを実コードで確認した結果、いずれも「最終更新」「取得日」の類が皆無と判明。うち circulars(一覧) は既存フッターが「本一覧は...最終確認日を付与したものです」と、実際には表示されていない日付の存在を暗黙に主張する未達成の文言不整合を検出（法令内容の捏造ではないが、サイト自身の鮮度表示に関する説明文が事実と不一致）。**是正**=`CircularsFilterableList` の結論カード description に、呼び出し元(page.tsx)で発出日降順に整列済みの配列先頭（＝収録通達中で最も新しい発出日）を「収録最新発出: {issuedDateRaw|issuedDate}」として追加＝新規データの捏造ゼロ・既存フィールドの転記のみ。フッター文言を実態に一致するよう「収録通達のうち最も新しい発出日を上部に表示しています」に是正（法令の発出者・本文表記は無変更）。law-search・chemical-database の2ページは基礎データに機械可読な鮮度フィールドが無く、かつ web/src/data/** はdata班所有で新規フィールド追加が独断でできないため、捏造リスクを避けて本イテレは対象外とし次点へ申し送り（backlogに明記）。単体テスト3件新設（最新日付の表示・issuedDateRaw欠落時のISOフォールバック・全件日付null時の非クラッシュ）。無読テスト1/1 PASS（own prod 3100・390×844・domcontentloaded＝鮮度文言表示・h1=1・フッター文言整合・旧文言の残存なし）。ゲート全通過（tsc0／lint errors0／vitest 2029 pass／build成功）。ビルド再生成JSON（docs/rag-metrics-latest.json・chatbot-eval-fresh-results.json）は契約どおり checkout で復元＝コミットせず。スクリプト=docs/third-party-reviews/scripts/circulars-freshness-noread-2026-07-03.mjs。残=S7（F3待ちで継続ブロック）・law-search/chemical-databaseのデータ鮮度表示（data班連携要否の確認から）。

## 2026-07-02 O4残＝派遣法45条要約の元/先誤答＋SYSTEM_PROMPTのYYYYプレースホルダ漏出是正（ux-tool/haken-education-yyyy-placeholder）

イテレーション頭で自班PR #583（O4のT1＝チャットボット偽「範囲外」警告の根絶）を確認したがCI（e2e/smoke）が走行中で未マージのため契約どおりsquashマージせず次イテレ回収。作業は main を ff-only 同期して新ブランチで実施（PR #583自体には積み増さない＝独立レビュー可能にするため）。lane backlog 最上位=2026-07-02注入のFable診断書04「O4残: 派遣法45条要約の是正＋SYSTEM_PROMPTプレースホルダ対策（T2+T3。T1はPR #583で対応済み）」に着手。**T2**=`haken-anzen-eisei.ts` の派遣法第45条要約が、派遣先が負う安全衛生義務の列挙（一〜四）に「作業環境の整備・安全衛生教育（法59条の一部）」とだけ書き、雇入れ時教育（59条1項＝派遣元の義務）と作業内容変更時教育・特別教育（59条2項・3項＝派遣先の義務）を区別していなかった構造的欠陥が真因＝本番実機プローブ（診断書04 Q20）でGeminiが「派遣労働者の雇入れ時教育は派遣先の義務」と誤断定（正しくは派遣元）。是正=項目二を「作業内容変更時教育及び特別教育（法第59条第2項・第3項）」に限定し、本文末尾に「雇入れ時教育（59条1項）及び一般定期健康診断（66条1項）は派遣元の義務」と明記。itemNumberMapとkeywordsも追従。rag-100qに回帰問(id124「派遣労働者の雇入れ時教育は派遣元と派遣先のどちらが行いますか？」→gold=派遣法第45条)を追加し検索面を固定。**T3**=`route.ts` の SYSTEM_PROMPT ルール10「（施行：YYYY年MM月、所管：厚生労働省）」という**文字通りのテンプレート例**をGeminiがそのまま出力する事故を本番プローブQ10で実測（1問中3箇所素通し）。真因はプロンプト内の書式例自体がプレースホルダ文字列だったこと。是正は二段構え＝①プロンプト側の例を実在例「安衛則第518条（施行：2020年12月、所管：厚生労働省）」に差し替え＋「YYYY年MM月」「第XX条」のようなプレースホルダ出力を明示的に禁止する文言を追加（根治）、②保険として `chatbot-enrichment.ts` に `sanitizePlaceholderCitations()` を新設し、Gemini応答にYYYY/第XX条トークンが残った場合に該当箇所（施行日句または丸括弧ごと）を除去する後処理サニタイザを追加、`route.ts` の非stream経路（callGemini）で全応答パスに適用。stream経路はチャンクを逐次クライアントへ送信する設計上、事前サニタイズが構造的に不可能（プロンプト側の根治で対応）のため対象外と判断（水増し回避）。単体テスト5件追加（プレースホルダ除去・所管等の実データ保持・実日付は無変更・no-op確認）。ゲート全通過（tsc0／lint errors0／vitest全pass／build成功）。BACKLOG-ux-tools.mdのO4項目をT1(#583)+T2+T3(#588)まとめて[x]化。残=#583 CI回収→squashマージ、O5(synonyms.ts:166気積是正)が次点。

## 2026-06-14 /chatbot 常時表示クイック質問チップの44px化（ux-tool/chatbot-quick-chips-44px）

イテレーション頭で main を ff-only 同期（cac10af3→8c94ca2f＝他班 faq/safety-signs/notifications 44px・data コーパス是正等を取込・clean確認）。自班 PR #570（chemical-ra クイックスタート44px）はCI pending のため契約どおり squashマージせず次イテレ回収。lane backlog の未着手は0件（全[x]）＝3件未満のため補充指針「柱0未適用箇所・無読不合格・チャットボット深掘り」に従う。まず自領域25ルートの無読スウィープを実機再計測（prod 3100・390×844・SW block・tools-noread-sweep）＝screening-fail 13件だが**h1は全25ルート各1個**（多重/欠落なし）でスクショ直読の結果、判定/法令/統計ページはいずれも結論カード・色帯・件数を既に保持＝新規カードは水増し（禁止）と再確認。代わりに**チャットボットを深掘り**したところ、入力欄直下の「クイック質問例」チップ（C-001＝初心者が打たずにタップで質問する常時動線）が `px-3 py-1 text-[11px]` ≒**高さ約24px で44px未満**＝指タップ標的として不足を発見。会話開始後は空状態の大きな質問例ボタン（既に min-h-[44px]）が消えるため、この行が"打たずに追質問"の唯一の動線になるのに最小タップ標的を満たしていなかった（自班 #570 が chemical-ra クイックスタートを44px化したのと同じ柱C-6論点の取りこぼし）。**是正**=空状態ボタンと同基準 `min-h-[44px]`＋`px-4 py-2`＋可視ラベル「質問例：」を付与（文言・件数=EXAMPLE_QUESTIONS.slice(0,3) は不変＝**捏造/水増しゼロ**、タップ標的拡大とラベル付与のみ＝既存破壊0）。ゲート全通過（tsc0／lint errors0／vitest 1874 pass／build成功）。無読テスト新規5/5 PASS（390×844・own prod 3100・SW block・domcontentloaded＝群が常時可視・「質問例：」ラベル在・チップ3個・各44px以上・タップで送信され履歴に文言）。スクリプト=docs/third-party-reviews/scripts/chatbot-quick-chips-44px-noread-2026-06-14.mjs。残=#570 CI回収・lane backlog 全[x]＝次イテレも補充指針（チャットボット/化学RAの深掘り継続）。
## 2026-06-14 柱0補充 /accidents-analytics 統計DB規模ヘッドライン（ux-tool/analytics-headline-conclusion）

イテレーション頭で自班の `ux-tool/` 未マージPRを確認＝#557（柱3② /risk 見通しストリップ地域名）のみで CI 進行中のため次イテレ回収とし、main を ff-only 同期（clean）。lane backlog は全 [x]（在庫<3件）のため補充指針「柱0未適用箇所・無読不合格画面」から起こす。まず自領域の主要判定/件数/法令ページ（risk-prediction=TopSummaryCard・stress-check=ConclusionCard・chatbot=空状態の例示チップ6本44px・chemical-ra=A4印刷/台帳）を実コードで再確認し**いずれも結論ファースト済＝水増し回避で新規カードは作らない**判断。代わりに**自領域25ルートの無読スウィープを実機再実行**（tools-noread-sweep・prod 3100・390×844・SW block・domcontentloaded）。h1は全25route各1個（多重/欠落の回帰なし）を再確認。screening-fail 13件の大半は status系/検索系/ハブ系/チャット空状態/印刷/admin など**画面種別由来の偽陽性**だったが、**`/accidents-analytics` は実害**＝事故統計ダッシュボードなのにファーストビュー maxFont=22・デカ数字なしで、初期ビューがナビ＋h1＋出力ツールバー＋「業種を選んで」案内のみ。肝心の「このDBは何件の労働災害を集計しているか」は fold 下（サマリーKPI は AnalyticsDashboardImpl line441・現状の role=status は出力ツールバーの sr-only ライブ領域のみで視覚的な結論ヘッドラインは皆無）。**是正**＝h1直下に統計DB規模ヘッドライン（data-testid=analytics-headline）を新設＝デカ数字 **5,074件**（収録総件数＝meta.curatedCases＋meta.mhlwDeathsCount）＋期間 2007〜2026＋内訳チップ（厚労省死亡災害DB 4,782件＋curated詳細 292件）。全値は既存 `aggregates.meta` の転記のみ＝**捏造ゼロ**。当初 死亡災害比率(kpi.fatalRatePercent)96% をチップに出したが、統合構成が死亡災害DB寄りで高く出る値を文脈注記なしに3秒読者へ見せると「労働災害の96%が死亡」と誤読される懸念があり、**誤読回避でヘッドラインからは外し**文脈注記つきのサマリーKPI側（「全事例に占める死亡災害」）に残置。ConclusionCard は安全トーン(緑=安全/赤=危険)の判定意味を帯び「1画面1枚」規約もあるためデータ規模の中立ヘッドラインには不適と判断し、role=status を付けない中立デカ数字ストリップで実装（既存の結論カード意味論と非衝突）。無読テスト7/7 PASS（analytics-headline-noread-2026-06-14＝ヘッドラインFV内 y=477・デカ数字 36px・単位件・漢字短ラベル収録・DOM順で最初の入力(業種select)より前＝結論ファースト・h1=1・デカ数字がサマリーKPI「収録総件数」と一致＝転記検証）。ゲート全通過（tsc0／lint errors0※対象ファイル・フルlintはCI委譲／vitest 1836 pass／build成功）。ビルド再生成JSON（docs/rag-metrics-latest.json・chatbot-eval-fresh-results.json）は契約どおり checkout で復元＝コミットせず。スクリプト=docs/third-party-reviews/scripts/analytics-headline-noread-2026-06-14.mjs。残=#557 CI回収・柱3 /risk 警報防災の追加実用化レビュー。
## 2026-06-14 /chemical-ra クイックスタート物質チップを44px化（ux-tool/chemical-ra-quickstart-44px）

イテレーション頭で自班CI緑PR #557（/risk 見通しストリップに該当地域名）を squashマージ→main を ff-only 同期（risk-outlook-region-names 等を取込・clean確認）。#562（accidents-analytics 統計DB規模ヘッドライン）はCI進行中（e2e/smoke 走行中）で次イテレ回収。**本イテレ**＝lane backlog の未着手が0件（全[x]）＝3件未満のため補充指針に従い自領域の柱0/無読不合格を捜索。まず自領域25ルートの無読スウィープ（tools-noread-sweep・prod 3100・390×844・SW block）を実機再計測したところ screening-fail 13件だが、スクショ直読で大半は**結論カードが既済の判定/状態ページ**（/insurance＝amber「未加入」カード＋CTA・/accidents-analytics＝#562で対応中）・**入力前の検索/会話ツール**（chatbot/law-search/chemical-ra＝入力後に結論を最上部表示済）・**運営用ダッシュボード**（/stats＝GA4未接続の準備中）で、いずれも結論カード追加は水増し（禁止）と判断。代わりに**実機タップ標的監査**（ファーストビュー内の a/button/input/select/summary 寸法を計測）を新規実施し、**genuine な柱0「タップしやすいサイズ」違反**を抽出。ホームパンくず（20×16）と footer 導線（34px×7）は共有部品＝他班所有のため対象外、prose内インラインリンクも対象外として除外したうえで、**自lane所有の /chemical-ra「職種別クイックスタート」物質チップ13個が高さ約30px（px-2.5 py-1）と44px未満**を検出＝これは「タップするとその物質でRAを即開始する主要CTA」であり、現場の指タップには小さすぎた。**是正**=チップの className を `inline-flex min-h-[44px] min-w-[44px] items-center justify-center px-3.5 py-2`（pill形状・amber色・text-xs文言・href `?name=…&run=1` はいずれも不変）に変更し、全13チップを44×44px以上に。値・導線・捏造ゼロ＝**寸法のみの非破壊変更**。回帰無読スクリプト新設（chemical-ra-quickstart-44px-noread-2026-06-14＝ブロック存在・13チップ全て44px以上・href健全性）を実機prod(3100・390×844)で **28/28 PASS**（旧ビルドは別プロセスが3100を占有しEADDRINUSE→該当PIDをkillし新ビルドで再計測して確認）。スクショで pill の見た目・余白・折返しが崩れず大型化のみを目視確認。ゲート全通過（tsc0／lint errors0／vitest 1856 pass／build成功）。残=lane backlog 全[x]＝次イテレも補充指針で起こす（#562 accidents-analytics ヘッドラインのCI回収・chemical RA/チャットボットの深掘り余地を継続調査）。

## 2026-06-14 /risk 見通しストリップに該当地域名を表示（ux-tool/risk-outlook-region-names）

イテレーション頭で自班CI緑PR #550（件数結論カードの無読テスト敷設）を squashマージ→main を ff-only 同期（c4d36091 等を取込・clean確認）。自班の他PR（#535/#540/#545）は既に main へ反映済みでリモートブランチは自動削除済み＝stale tracking ref を prune。**本イテレ**＝lane backlog の未着手が0件（全[x]）＝3件未満のため補充指針に従い自領域の柱0/柱3/無読不合格を捜索。まず自領域25ルートの無読スウィープを実機再計測（prod 3100・390×844・SW block）したところ screening-fail 12件だが、その大半は **入力前の検索/会話ツール**（risk-prediction/chatbot/law-search＝結論は入力後に scrollIntoView で最上部に出す既済）・**ハブページ**（mental-health 系＝単一判定を持たない）・**運営用 noindex ダッシュボード**（/stats）で、いずれも結論カードを足すと水増し（禁止）になると直読で判断。代わりに**自lane cycle-log が「残」として明記していた柱3 follow-up＝/risk 警報防災の追加実用化**に着手。前回追加した「明日からの見通し」ストリップは最悪レベル＋「N地域」の件数しか出さず、「台風前日の元請安全担当」が一番知りたい**自現場（例:九州）が明日荒れるか**は1週間予報を開いて地図を目視するまで分からなかった（前倒し判断が3秒で完結しない）。**是正**=ストリップのセル本文と aria-label に**最悪レベル該当の地域名**（例「四国・九州」「関東」）を直接表示。地域名は既存 Open-Meteo 予報の regions[].regionLabel をそのまま並べるだけ＝**捏造ゼロ**。3地域以上は先頭2件＋「他N」で1行に省略（レイアウト不変）。純関数 `buildRiskWeatherOutlook` に `worstRegions`（最悪レベル該当地域名のみ・注意報地域を警報日に混ぜない）を追加し、UI は `{count}地域`→`formatOutlookRegions()` の1行差し替えのみ＝**既存破壊0**。テスト+4（weather-outlook.test 8→12件＝警報日は警報地域のみ／注意報のみの日／良好日は空／地域名なし入力はフォールバック）。ゲート全通過（tsc0／lint errors0／vitest 1816 pass／build成功）。無読テスト新規 7/7 PASS（risk-outlook-region-names-noread＝四国・九州が本文とaria-labelに出る・件数のみでない・注意報地域混入なし・他N省略・良好日は地域名なし）＋既存 risk-outlook-noread を地域名表示に追従（「2地域」→該当地域名チェック）し 9/9 維持。スクリプト=docs/third-party-reviews/scripts/risk-outlook-region-names-noread-2026-06-14.mjs。残=lane backlog 全[x]＝次イテレも補充指針で起こす（chemical RA/チャットボットの深掘り余地を継続調査）。

## 2026-06-14 /chemical-ra 多重h1の是正（ux-tool/chemical-ra-multiple-h1-fix）

イテレーション頭で自班CI緑PR #531（バッチ9第2弾）を squashマージ→main を ff-only 同期。#535（newsletter）はマージ衝突（自班 backlog/cycle-log のみ）を origin/main マージで手解消→再push（CI再走を次イテレ回収）。#540（risk台風ペルソナ）はCI待ち。backlog 未着手は #540 在庫の1件のみ（<3件）のため補充規定に従い自領域の柱0/無読不合格を捜索。判定・結果系ページ（risk-prediction/chemical-ra/stress-check/chatbot 等）を実コードで確認＝いずれも結論カードを入力直後に最上部へ scrollIntoView で出す柱0処理が既済（捏造的な"水増し"を避けるため新規結論カードは作らない判断）。代わりに **自領域25ルートの無読スウィープを実機計測**（prod server 3100・390×844・serviceWorkers:block・domcontentloaded、noread-sweep-2026-06-11 の自領域版）。その結果 **`/chemical-ra` のみ h1=2** を機械検出＝ページ側 `PageHeader`（TranslatedPageHeader 経由）と `ChemicalRaPanel` 内の画面用イントロが**同一文言「化学物質リスクアセスメント」で h1 を二重**に出していた（WCAG/SEO上の多重h1＝柱C-7「多重h1是正」の取りこぼし）。修正は最小・非破壊＝パネル側イントロを `h1`→`h2` に降格（同パネル内の他セクション見出しと同階層・文言と見た目 text-xl は不変、ページの h1 は PageHeader が単独保持）。回帰テスト2本新設（`chemical-ra-panel.h1.test.tsx`＝パネルは h1=0／イントロ h2 に「化学物質リスクアセスメント」を保持）。再ビルド後の無読スウィープで `/chemical-ra` は h1=1 に是正、他24ルートに副作用なし。ゲート全通過（tsc0／lint errors0／vitest pass／build成功）。スウィープスクリプト=docs/third-party-reviews/scripts/tools-noread-sweep-2026-06-14.mjs。残=柱3リスクマップ実機レビュー（#540 在庫）。

## 2026-06-14 柱0回帰固定 件数結論カードの無読テスト敷設＋自領域柱0監査（ux-tool/noread-batch9-count-cards）

冒頭で自班緑PR #535（柱0バッチ9 第3弾 /newsletter）を squashマージ→main を ff-only 同期。PR #540（柱3 /risk 台風前日レビュー）は main 先行により conflict(DIRTY) だったため契約どおり origin/main を当該ブランチへ通常マージ＝BACKLOG-ux-tools.md の衝突を手解消（バッチ9を#535反映の[x]・台風行を当PRの[x]に統合）し再push（CI次イテレ回収）。PR #545（/chemical-ra 多重h1是正）はCI進行中で次イテレ回収。**本イテレ実作業**＝lane backlog の未着手が typhoon 1件（#540で在庫消化中）のみ＝3件未満のため補充指針「柱0未適用箇所・無読不合格・第三者指摘」から起こす。まず自領域route を Explore 2本で総点検したところ、判定/件数/法令/統計の主要ページは**いずれも既に結論ファーストの視覚結論を最上部に保持**（risk-prediction=TopSummaryCard・chemical-ra=RaConclusionCard・law-hierarchy/accidents-reports/accident-news/circulars/subsidies(+calculator)/goods/chemical-database=ConclusionCard）し、h1も全25route各1個で多重/欠落なしと確認＝**新規の柱0未適用箇所なし**（Explore の「LACKS」誤判定はclient部品未走査由来。chemical-database/goods/subsidies/product-search はいずれも実装済を直読で反証）。よって追加カードは水増し（禁止）と判断し見送り、代わりに**未テストだった検索/件数系3本の結論カードに回帰無読テストを敷設**＝PR #527でデカ数字カードを敷いたが専用スクリプトが無かった /chemical-database(収録3,695物質)・/goods(掲載39品目)・/subsidies(活用可能7制度)。テスト観点=①結論カード(section[role=status])がファーストビュー内(390×844)②デカ数字(text-5xl)＋単位＋漢字短ラベル③DOM順で検索入力より前(結論ファースト)④h1=1。自前prod(3100・domcontentloaded・serviceWorkers:block)で**18/18 PASS**（y=322/313/572）。**UI/アプリコード変更ゼロ＝既存破壊0**（追加は docs スクリプト1本のみ）。ゲート全通過（tsc0／lint errors0／vitest 1771 pass／build成功）。スクリプト=docs/third-party-reviews/scripts/batch9-count-cards-noread-2026-06-14.mjs。残=柱3リスクマップ実機レビュー（#540で在庫消化中）。

## 2026-06-14 柱0バッチ9/9 第3弾 /newsletter 登録前後の状態カード（ux-tool/batch9-newsletter-status-card）

イテレーション頭で自分の緑PR #527（柱0バッチ9 第1弾＝判定/件数ツール5本）を squashマージ→main同期（a9e5c04c）。マージ時 BACKLOG-ux-tools.md がC-7完了反映と衝突したため手で解消（C-7はPR #520でmain反映済＝[x]、バッチ9進捗を追記）。第2弾 PR #531（insurance/bcp/organization）はCI待ちで次イテレ回収。本イテレ実作業＝バッチ9/9 残の `/newsletter`。従来は登録フォームの内側にしか成功表示がなく、ページを開いた瞬間「自分が登録済みかどうか」が3秒で分からなかった。フォームの登録状態を親 client パネル `NewsletterPanel` へ持ち上げ、**最上部に状態結論カード**（共通 `ConclusionCard`＝import のみ・ux-records custodian部品は不改変）を新設＝**未登録(info/青・Mailアイコン・「登録する」導線)→登録完了(safe/緑・MailCheck)** に色帯切替。`NewsletterForm` には `onSuccess` コールバックを追加しただけで送信ロジックは不変（捏造・水増しゼロ＝状態の事実をそのまま結論化）。フォーム自身の成功ブロックは詳細として残置（結論=上／詳細=下）。`/leaflet` は印刷ラッパー専用ページのため最上部状態カードの対象外＝スキップ（無理に付けると水増し）。無読テスト（390×844・prod server 3100・serviceWorkers:block・domcontentloaded）7/7 PASS＝カードがファーストビュー内(y=449)・登録前 sky/「未登録」/「登録する」・h1=1・送信後 emerald/「登録完了」に切替（RESEND未設定ローカルは addSubscriber がメモリ保存で ok:true）。ゲート全通過（tsc0／lint errors0／vitest 1700 pass／build成功）。スクリプト=docs/third-party-reviews/scripts/newsletter-status-card-2026-06-14.mjs。これで柱0バッチ9/9 完了（第1弾#527 main済・第2弾#531 main済・第3弾本PR）。残=柱3リスクマップ実機レビュー。

## 2026-06-14 柱0バッチ9/9 第2弾 状態系3ページ結論カード（ux-tool/batch9-status-conclusion-cards）

冒頭で自班CI緑PR #520（柱C-7 出力手段）をsquashマージ→main を ff-only 同期。第1弾(PR #527・CI pending)が判定/件数の濃い5ツールを処理済みのため、残りの状態系を第2弾として処理。**ConclusionCard を import専用部品として** /insurance・/bcp・/organization の最上部に新設。/insurance＝tone=warning・アイコンShieldAlert・短ラベル「未加入」＋action「個別に相談」→/contact、長文の「企業のご利用について（調達基準）」は CollapsibleDetail で初期折りたたみ（法令/調達の重要情報は消さず畳むだけ）。/bcp＝tone=info・デカ数字「99%」稼働率目標＋個人事業運営の正直な注記を1行に圧縮＋action「保険状況を見る」→/insurance、冗長なイントロ段落を削除。/organization＝tone=warning・デカ数字「83.5%」教育修了率＋補助チップ（事故0件=safe／建設三班64%=danger）、**全値を既存の KPI/DEPARTMENTS 配列から導出（reduce で最下位部署を算出）＝転記のみ・捏造ゼロ**、デモ版ディスクレーマは維持。/leaflet（A4印刷PDF）・/newsletter（登録CTAフォーム）は判定/件数の"状態"を持たない成果物のため結論カード対象外とし、水増しを回避（backlogに明記）。ゲート全通過（tsc0／自班3ファイル lint errors0※フルeslintは環境セグフォルトのためCI委譲／vitest 1687 pass／build成功）。無読テスト（390×844・自前prod server -p3100・domcontentloaded・serviceWorkers:block）15/15 PASS＝3カードともファーストビュー内(y=365〜413)・デカ数字48px・短ラベル/action/折りたたみ/チップ在。スクリプト=docs/third-party-reviews/scripts/status-pages-conclusion-cards-2026-06-14.mjs。残=柱3リスクマップ実機レビュー（/leaflet・/newsletter は対象外確定）。

## 2026-06-13 柱C-7 事故統計の出力手段（ux-tool/c7-accidents-export）

着手時、lane backlog 最上位2件（柱0バッチ6/9・柱C-6 /circulars）はいずれも既に main へ反映済み（PR #511・柱0バッチ4）でlane側ファイルが未更新だっただけと判明→現状確認のうえ [x] 化。実作業は次の未着手＝柱C-7。元請の「月例安全会議の資料に統計を貼る」が完了しない（出力手段ゼロ）課題に対し、`/accidents-analytics`（事故統計ダッシュボード）と `/accidents-reports`（業種別レポートハブ）へ出力ツールバー `DataExportToolbar` を新設＝**CSVダウンロード／要点コピー／共有／印刷**の4手段。CSV・要点テキストは純関数に分離（`lib/export/csv.ts` の汎用セクションCSVビルダー＋`lib/accidents-analytics/export.ts`＋`lib/accidents-reports-export.ts`）で集計値をそのまま転記＝**捏造・水増しゼロ**、テスト17件で列崩れ・エスケープ・符号を固定。ツールバーはブラウザAPI（Blob/clipboard/navigator.share/print）のみ担当、先頭BOMでExcel文字化け防止、ボタンは44px・印刷物には出さない（print:hidden）。h1は対象2ページ＋/risk・/risk-prediction・/law-search いずれも既に1個（多重/欠落なし）を確認のみ＝マスターのh1指摘は既に解消済みだった。無読テスト（390×844・prod server・serviceWorkers:block）8/8 PASS＝ツールバーがファーストビュー内（analytics y=531・reports y=743）・4手段在・44px。ゲート全通過（tsc0／lint errors0／vitest 1660 pass／build成功）。スクリプト=docs/third-party-reviews/scripts/accidents-export-noread-2026-06-13.mjs。残=柱0バッチ9/9（その他ツール結論カード）・柱3リスクマップ実機レビュー。

## 2026-06-14 柱3 リスクマップ実機レビュー＝明日からの見通しストリップ（ux-tool/risk-typhoon-persona-review）

イテレーション頭で自lane緑PR #531（柱0バッチ9第2弾）を squashマージ着手→main側 #527 と BACKLOG行が衝突したため origin/main を当該ブランチへ通常マージで解消（行は完了側を採用し第3弾=#535を追記）。コード衝突なし。#531/#535 はCI再走のため次回回収。本題は lane 最上位の柱3レビュー＝「台風前日の元請安全担当・段落を読まない」ペルソナで `/risk` を実機確認。**気づき**: 3秒で読む結論カード（role=status）は「今日」の全国警報を JMA実況＋Open-Meteo予報の悪い方で断定するが、このペルソナが前日夕に効かせたいのは「明日」の可否で、その材料は従来「1週間予報」タブの奥に埋まっていた（前倒し判断が3秒で出ない）。**是正**: 結論カード直下に「明日からの見通し」ストリップを新設＝明日／明後日／3日後の全国最悪レベルを赤(警報相当)・黄(注意報相当)・緑(なし)＋該当地域数で並べ、セルをタップするとその日の1週間予報マップへジャンプしてどの地域かまで辿れる。今日は結論カードに任せ**明日起点**にして二重表示・矛盾を回避（JMA実況は当日分のみのため明日以降は予報ベースとUIに明記）。値は既存予報の日別再集計のみ＝**捏造・水増しゼロ**。純関数 `lib/risk/weather-outlook.ts`（最悪レベル・該当数・トーンを集計、テスト8件）＋ `WeatherForecastPanel` にストリップ描画（SAFETY_TONE トークンで塗り・min-h44px・aria-label）。ゲート全通過（tsc0／lint errors0／vitest 1716 pass／build成功）。無読テスト 9/9 PASS（390×844・own prod server -p3100・serviceWorkers:block＝明日警報=赤2地域がファーストビュー内 y=570・明後日注意報=黄・44px・タップで週モード遷移・今日カード独立・良好時は緑）＋既存 risk-noread 11/11 維持。スクリプト=docs/third-party-reviews/scripts/risk-outlook-noread-2026-06-14.mjs。残=柱0バッチ9（#531/#535 CI回収）・柱3 /risk 警報防災の追加実用化レビュー。

## 2026-07-02 チャットボット偽「範囲外」警告の根絶（ux-tool/chatbot-out-of-scope-false-positive）

イテレーション頭で自lane未マージPRを確認したところ #562/#570/#573（analytics-headline・chemical-ra-quickstart-44px・chatbot-quick-chips-44px）は全てCI緑で既にmainへsquash-merge済み・リモートブランチも自動削除済みと判明（ローカル3ブランチのみ残存）→ローカルブランチを削除しmainをff-only同期（clean確認）。**本題**=2026-07-02 Fable横断診断（PR #579）で追加された lane backlog 最上位 O4（P0）＝チャットボットの偽「範囲外」警告。診断書 04-chatbot-accuracy.md の本番プローブ23問で、正答なのに `detectOutOfScopeLawReferences` が誤って範囲外警告を出し信頼度をmediumへ不当降格したケースが5/20問=25%と実測されていた（Q8など）。原因を実コードで特定=2バグ複合。(a) 法令名抽出の正規表現 `chatbot-enrichment.ts:386` の文字クラスに長音「ー」が無く、「クレーン等安全規則」が「ン等安全規則」に分断され既知法令と一致しない。(b) 正式名称⇄短縮名の対応表が無くsubstring照合のみのため、「労働安全衛生法」⊅「安衛法」・「酸素欠乏症等防止規則」⊅「酸欠則」等の正式名称そのものの言及が全て範囲外扱いされていた。**是正**=文字クラスに「ー」「々」を追加、既存 `law-metadata.ts` の `LAW_METADATA`（lawShort→fullNameの単一ソース）から正式名称の完全一致集合 `KNOWN_LAW_FULL_NAMES` を新設し判定に追加（法令コーパスの収録範囲・条文内容は無変更、後処理の誤検知ロジックのみ是正＝**捏造・水増しゼロ**）。回帰テスト2件追加（クレーン等安全規則の分断防止／正式名称の完全一致判定＝既存17件+2=19件）。診断書の同一O4項目には派遣法45条要約是正（T2）・SYSTEM_PROMPTのYYYYプレースホルダ対策（T3）も含まれるが今回はT1のみ実施のため、backlog行は「O4残」としてT2/T3に絞って残置（O4全体は未[x]のまま、T1完了分は別行で[x]記録）。ゲート全通過（tsc0／lint errors0（既存warningのみ）／vitest 1935 pass／build成功）。ビルド再生成JSON（docs/rag-metrics-latest.json・chatbot-eval-fresh-results.json）は契約どおり checkout で復元＝コミットせず。PR #583。残=O4残（T2派遣法45条・T3プレースホルダ）を次イテレで着手。

## 2026-07-02 O5: synonyms.ts「気積→事務所則第14条」是正＋口語同義語拡充（ux-tool/o5-synonyms-jimusho-kisoku-fix・PR #598）

前セッションの未コミット作業（gates未実行・未push）を引き継いで完了。是正=`synonyms.ts:166` の「気積」同義語展開先が事務所則第14条（排水）になっていた誤りを、コーパス実データ（jimusho-eisei-kijun-kisoku.ts / egov-caption-snapshot.ts）と一致する第2条（気積の基準）へ修正（dataレーン#538申し送りの取り逃し分・04診断T4/07診断P0-1）。拡充=口語検索の取りこぼし解消のためTERM_EXPANSIONSに「健康診断の頻度」「健診の頻度」「健康診断は何年ごと」→安衛則第44条(1年以内ごと)・第45条(特定業務6月以内ごと)、「酸欠 資格」「酸欠の資格」「酸欠作業の資格」→酸欠則第11条(作業主任者・技能講習)を追加。rag-search.test.tsに回帰テスト3件を新設。完了条件を実測で確認＝「健康診断の頻度」で安衛則44・45条がtop5・「酸欠 資格」で酸欠則11条がtop1・main eval 124/124=100パーセント（非退行）・fresh eval 98/100=98パーセント（非退行、既存不正答2件は本タスク対象外で変化なし）。ゲート全通過（tsc0／lint errors0／vitest 1976 pass・237 files／build成功）。push前に生成物docs/rag-metrics-latest.json・web/src/data/chatbot-eval-fresh-results.jsonはgit checkoutで復元（data班所有・[[noread-test-prod-server-port]]の定石通り）。本PRはsynonyms.ts/rag-search.test.tsという検索ロジックlib（本来seo班境界）に触れたが、backlog自体がO5としてOpus担当に明記済みの既存タスクの完了処理であり新規逸脱ではない。残=lane backlog未着手0件→次イテレは補充指針（自領域柱0/無読レビュー）で起こす。
## 2026-07-02 S8-a: /circulars/[id] にE-E-A-T監修者バイライン＋Person JSON-LD配線（ux-tool/s8a-circulars-supervisor-byline）

イテレーション頭で自班CI緑未マージPRなし（直前PR #598はCI in-progressのため次回回収）。lane backlog未着手は診断07由来のS7（チャットボット小粒4本・生成品質evalで機械判定＝BACKLOG-fable.md F3が未完のためブロック中と確認）とS8-a（監修者バイライン）の2件のみ＝3件未満だが、S8-aは即着手可能なため先にこちらを処理。診断07（E-1・柱C-8）が指摘した「通達詳細/判例詳細/FAQに監修バイラインが無い」のうち、自班route分＝/circulars/[id]のみ対応（判例詳細/FAQはux-hub班領域のため対象外）。既存パターン調査＝/guides系static pageは`keyword-landing-jsonld.tsx`でPerson（hasOccupation/qualifications付き・氏名は開示せず「労働安全衛生コンサルタント（登録番号260022）」表記）、/articlesは`newsArticleSchema`のauthorName/authorUrlで簡易Person、を踏襲。新規`SupervisorByline`コンポーネント（可視リンク・/about導線）を敷設し、共有lib`json-ld.tsx`の`legalDocumentSchema()`に監修者Personを`contributor`（schema.org公式プロパティ＝二次的寄与者。通達の発出者=author/publisherとは意味が別なため上書きせず追加）として常時付与。**通達の発出者（厚労省等のOrganization表記）は不変＝法令正確性を壊さず追加のみ**。legalDocumentSchemaの呼び出し元は/circulars/[id]のみ（grep確認済）のため既存挙動への影響なし。無読テストは通常の「3秒で判定/件数」系ではなくSEO構造化データの配線確認のため、可視バイライン・aboutリンク・h1単一性・JSON-LD contributor Person・発出者author健全性を機械チェックするスクリプトを新設（実行時はNode ESM解決の都合でweb/配下へ一時コピーして実行→検証後削除、正本はdocs/third-party-reviews/scripts/に保存）。own prod server -p3100・domcontentloaded で2/2 PASS。ゲート全通過（tsc0／lint errors0／vitest 1977 pass・237 files／build成功）。push前に生成物docs/rag-metrics-latest.json・web/src/data/chatbot-eval-fresh-results.jsonはgit checkoutで復元（[[noread-test-prod-server-port]]の定石）。残=S7（F3=チャットボット生成品質eval確立待ちでブロック中・Fable/ops側の完了を要観測）。次イテレは補充指針（自領域柱0/柱3レビュー、または診断07のC-10/C-11の自班route分）で起こす。
