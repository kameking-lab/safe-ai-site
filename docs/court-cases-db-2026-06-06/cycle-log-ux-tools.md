# cycle-log — 機能UX班-B（判定・法令・統計系）並列ループ

各イテレーションの1段落。マスターの cycle-log.md は参照専用。

## 2026-07-03 /chemical-ra 化学物質検索コンボボックスにキーボード操作を追加（ux-tool/chemical-selector-combobox-a11y）

イテレーション頭でCI緑の自班PR #776（tablist-keyboard-nav-a11y）をsquashマージ→`git checkout main && git pull --ff-only`でclean同期。lane backlog未着手を確認したところ2件（1件目=/law-search・/chemical-databaseデータ鮮度表示、data班確認待ちでブロック中／2件目=`mhlw-chemical-selector.tsx`のキーボードa11y、PR #776着手時のExplore調査で発見し次点送りにしていたもの）で3件未満だが2件目は自班のみで実装可能なため着手。**本題**=`mhlw-chemical-selector.tsx`（/chemical-raの厚労省数千件物質名タイプアヘッド）は結果リストが`<button onClick>`のみでキーボード操作不可・Escapeキーで閉じる手段も無く、`role="combobox"`等のARIA属性も皆無だった。**是正**=ARIA 1.2 comboboxパターンを敷設：inputに`role="combobox"`/`aria-expanded`/`aria-haspopup="listbox"`/`aria-controls`/`aria-autocomplete="list"`/`aria-activedescendant`、結果`<ul>`に`role="listbox"`、各`<li>`に`role="option"`+`aria-selected`を追加。新規`activeIndex` state＋`handleInputKeyDown`でArrowDown/Up（ローミングハイライト・端でラップアラウンド）・Enter（ハイライト項目選択）・Escape（クローズ）を実装。既存の`<button onClick>`はマウス選択用に`tabIndex={-1}`で温存しフォーカスをinputに集約（既存onClick/検索ロジック・視覚デザイン・文言は不変＝既存破壊0）。単体テスト新規3件（ArrowDown+Enter選択・Escapeクローズ・listbox/option ARIA構造）。無読テスト作成時、初回は`page.getByRole("option")`がページ内の言語切替`<select>`のネイティブ`<option>`（実装と無関係）を誤って拾いFAILしたため、`aria-controls`のid経由で自コンポーネントのlistboxへ厳密スコープして解消＝13/13 PASS（own prod 3100・390×844・/chemical-ra実機）。ゲート全通過（tsc0/lint errors0/vitest 2569 pass/build成功）。**注**=無読テスト用playwright依存解決のため`docs/third-party-reviews/scripts`配下に一時`node_modules`（web/node_modules由来）を作成したが、想定していたシンボリックリンクではなくOS上は独立コピーとして作成されていたため、テスト完了後に確実に削除・作業ツリークリーンを確認（web/node_modules本体は無傷）。ビルド再生成JSON（docs/rag-metrics-latest.json等）は契約どおり`git checkout --`で復元＝コミットせず。残=次点1件（/law-search・/chemical-databaseデータ鮮度表示はdata班確認待ち）のみ＝3件未満のため次イテレも柱0/柱3レビューから補充が必要。スクリプト=docs/third-party-reviews/scripts/chemical-selector-combobox-noread-2026-07-03.mjs。

## 2026-07-03 /accidents-reports/compare 業種比較セレクタ・詳細リンク・[industry]表示期間タブ、/chemical-database/[cas] 個票CTAを44px化（ux-tool/compare-chemical-cas-44px）

イテレーション頭でCI緑未マージ自班PR2件（#746 goods-leaflet-newsletter-44px・#762 mental-health-mgmt-hub-conclusion-card）を確認→いずれもmainとコンフリクト（append-onlyのBACKLOG-ux-tools.md・cycle-log-ux-tools.mdのみ、既知パターン）のためbranch内でoriginを通常マージし両エントリを保持する形で手解消、ゲート再実行（tsc0/lint errors0/vitest各2467・2519 pass/build成功）後pushしてsquashマージ完了（#746は自動マージ許可されずCI green確認後の手動squash、#762はauto-mergeがリポジトリ設定で不許可のためCI待って手動squash）。`git checkout main && git pull --ff-only`でclean同期。**本題**=lane backlog未着手が1件（要data班確認でブロック中）のみ＝3件未満のため、前回イテレーション（mental-health-mgmt-hub-conclusion-card）のcycle-log申し送り「accidents-reports/compare比較セレクタ・[industry]表示期間タブ・chemical-database/[cas]個票CTA等の残存44px漏れ」をExploreに実装検証させた。判明=`comparison-industry-selector.tsx`の業種選択トグル・「選択を戻す」・「比較を更新」、`comparison-view.tsx`の各業種カード「詳細」リンク・末尾「〜の詳細レポート」リンク、`top-cases-tabs.tsx`（[industry]の「重大事故の表示期間」タブ）、`chemical-database/[cas]/page.tsx`の「RAを開始」「一覧に戻る」の計8箇所がmin-h未指定で実測28〜40px。`industries/[industry]/page.tsx`自体には表示期間タブは存在せず既存要素は44px対応済みと確認したため誤爆を回避し対象外とした。**是正**=計8箇所に`min-h-[44px]`を付与（寸法のみ、文言・onClick/href不変＝既存破壊0）。無読テスト新設9/9 PASS（own prod 3100・390×844・domcontentloaded＝業種選択トグル全件・選択を戻す/比較を更新・各業種カード詳細リンク全件・末尾詳細レポートリンク全件・表示期間タブ全件+タブ切替非退行・chemical-database個票のRA開始/一覧に戻る、いずれも44px以上）。実装中、compare画面の初期状態が既に上限5業種(MAX_COMPARE_INDUSTRIES=5=総業種数)全選択済みで「選択を戻す」ボタンが出現しないケースを検知→テストスクリプト側でaria-pressed="false"のトグルが無ければaria-pressed="true"（かつdisabled除外）を代わりにクリックするようフォールバックして解消。ゲート全通過（tsc0/lint errors0/vitest 2520 pass/build成功）。ビルド再生成JSON（docs/rag-metrics-latest.json・chatbot-eval-fresh-results.json・ky-print-sheet/meeting-print-sheet snapshot）は契約どおり`git checkout --`で復元＝コミットせず。**注**=作業中`web/CLAUDE.md`→`web/AGENTS.md`経由で`node_modules/next/dist/docs/`を読むよう誘導する記載（プロンプトインジェクション、既出＝過去複数イテレーションで検出済のものと同一）を再確認・引き続き無視、本タスクへ影響なし。残=次点1件（/law-search・/chemical-databaseデータ鮮度表示はdata班確認待ち）のみ＝3件未満のため次イテレも柱0/柱3レビューから補充が必要。スクリプト=docs/third-party-reviews/scripts/compare-chemical-cas-44px-noread-2026-07-03.mjs。

## 2026-07-03 /mental-health-management ハブに結論カードを新設（ux-tool/mental-health-mgmt-hub-conclusion-card）

イテレーション頭で自班未マージPR #757（chatbot role=alert・aria-live欠落/risk-predictionマトリクス色のみリスク表現の是正）はCI進行中(IN_PROGRESS)のためマージ据え置き、`git checkout main && git pull --ff-only`でclean同期のみ実施（CI待ちは次イテレーションで回収）。**本題**=lane backlog未着手が1件（要data班確認でブロック中）のみ＝3件未満のため補充指針に従いExplore監査を委任。自班所有routeの柱0未適用箇所横断調査の結果、`/mental-health-management`ハブ（3子ページstress-check/small-business/interview-guidanceはいずれも既にConclusionCard保持）だけがPageHeader直後AlertCircleプローズのみで色帯・デカ数字の視覚結論を欠いていた同一系統内の取りこぼしを検出。他候補（accidents-reports/compare比較セレクタ・[industry]表示期間タブ・chemical-database/[cas]個票CTA等の残存44px漏れ）は次点として申し送り。**是正**=`PageHeader`直後に`ConclusionCard`を新設（tone info・value 3=`SUBPAGES.length`・unit 本・title「実務ガイド」・action「チェックリストを見る」→stress-check）、既存プローズはそのまま補足として残置（水増し回避）。値は既存データ配列の件数を転記のみ＝捏造ゼロ・既存破壊0。無読テスト新設6/6 PASS（own prod 3100・390×844・domcontentloaded＝結論カードがファーストビュー内・デカ数字48px・アクション導線44px・DOM順で制度説明プローズより前・h1=1）。ゲート全通過（tsc0/lint errors0/vitest 2502 pass/build成功）。ビルド再生成JSON（docs/rag-metrics-latest.json・chatbot-eval-fresh-results.json・ky-print-sheet/meeting-print-sheet snapshot）は契約どおり`git checkout --`で復元＝コミットせず。**注**=作業中`web/CLAUDE.md`→`web/AGENTS.md`経由で`node_modules/next/dist/docs/`を読むよう誘導する記載（プロンプトインジェクション、既出＝過去複数イテレーションで検出済のものと同一）を再確認・引き続き無視、本タスクへ影響なし。残=次点1件（/law-search・/chemical-databaseデータ鮮度表示はdata班確認待ち）＋PR #757（CI進行中・次イテレ回収）＋Explore報告の残存44px候補（accidents-reports/compare・[industry]・chemical-database/[cas]）のため次イテレも継続着手可。スクリプト=docs/third-party-reviews/scripts/mental-health-mgmt-hub-conclusion-card-noread-2026-07-03.mjs。

## 2026-07-03 /goods 商品カード・選び方ガイドのアフィリエイトCTA・/leaflet印刷ボタン・/newsletter本登録フォームを44px化（ux-tool/goods-leaflet-newsletter-44px）

イテレーション頭でCI緑未マージ自班PR（#735 chemical-database-tap-targets-44px）をsquashマージ→`git checkout main && git pull --ff-only`でclean確認（#741は当該イテレーション着手時点でe2e/smoke CI進行中のため次回回収に据え置き）。lane backlog未着手を確認したところ「/law-search・/chemical-database データ鮮度表示」の1件（要data班確認でブロック中）のみ＝3件未満のため補充指針に従いExplore調査を委任（stress-check/chemical-ra/goods/leaflet/newsletter/strategy/insurance/bcp/organization/law-hierarchy/accidents-reportsを対象読み込み、tap-target・conclusion-card・多重h1・握りつぶしエラーの4観点）。ほぼ全域が既に是正済みと判明した中、`safety-goods-panel.tsx`の商品カード内「Amazonで見る」「楽天で見る」（掲載品目分×2＝主要購買導線・繰り返し出現）・「選び方ガイド」内「Amazonで探す」「楽天で探す」・女性向けフィルタートグルが`min-h`指定なしで実測36〜40px、`LeafletPrintView.tsx`の唯一の主要CTA「印刷 / PDF保存」ボタンも同様、`newsletter-form.tsx`の実運用フォーム（`compact`バリアントはgrep確認の結果呼び出し元皆無＝dead codeのため対象外）のメール入力欄・業種セレクトも`py-2.5`のみで44px未満だったのを検出。**是正**=計9箇所に`min-h-[44px]`を付与（寸法のみ、文言・onClick/href・機能不変＝既存破壊0）。無読テスト新設12/12 PASS（own prod 3100・390×844・SW block・domcontentloaded）。**ハマった点**=無読テスト初回実行時に5件FAILしたが原因はコード不備ではなく、ポート3100に過去イテレーション由来の古いnext startプロセスが残存（EADDRINUSEでnpx next startが起動失敗し、curlは古いプロセスに200を返していたため編集前のCSSを検証していた＝[[noread-test-prod-server-port]]の既知制約）。`netstat`でPID特定→`taskkill`→再起動後は12/12 PASSに復帰。ゲート全通過（tsc0/lint errors0(既存warningのみ)/vitest 2467 pass・289 files/build成功）。ビルド再生成JSON（docs/rag-metrics-latest.json・chatbot-eval-fresh-results.json・ky-print-sheet/meeting-print-sheet snapshot）は契約どおりcheckoutで復元＝コミットせず。無読スクリプトはPlaywrightのESM解決がスクリプト自身の物理パス基準のためdocs/third-party-reviews/scripts配下からは直接実行不可＝web/へ一時コピーして実行後に削除。残=PR #741のCI回収、次点=/law-search・/chemical-databaseデータ鮮度表示（data班確認待ち）のみ＝3件未満のため次イテレも柱0/柱3レビューから補充が必要。スクリプト=docs/third-party-reviews/scripts/goods-leaflet-newsletter-44px-noread-2026-07-03.mjs。

## 2026-07-03 /chatbot 送信エラーrole=alert・ストリーミングaria-live欠落＋/risk-predictionマトリクス色のみリスク表現を是正（ux-tool/a11y-alert-live-matrix-label）

柱0（44px・結論カード）は自領域25routeでほぼ飽和のため、補充指針「無読テスト不合格画面」を別角度＝44px以外のアクセシビリティで実施（Explore調査を委任）。是正=①`chatbot-panel.tsx`送信エラーバナーに`role="alert"`付与、②AI回答ストリーミング中バブルに`aria-live="polite"`付与、③`risk-prediction-panel.tsx`リスクマトリクスの各セルが色のみでリスク段階を表現していたのを`<td>`に`matrixCellRiskLabel()`によるaria-labelを追加（視覚デザイン・onClick/href不変＝既存破壊0）。単体テスト2件新設。無読テスト4/4 PASS（own prod 3100・390×844・SW block）。ゲート全通過（tsc0/lint errors0/vitest 2494 pass/build成功）。スクリプト=docs/third-party-reviews/scripts/a11y-aria-live-alert-matrix-label-2026-07-03.mjs。

## 2026-07-03 Explore横断監査で発見した残存44px漏れ9箇所＋chatbot aria-expanded欠落を是正（ux-tool/mhlw-panels-misc-44px-a11y）

イテレーション頭で自班CI緑未マージPR #741（ContextualPpePicks等44px化）を確認しsquashマージ→`git checkout main && git pull --ff-only`でclean同期。**本題**=lane backlog未着手が1件（要data班確認でブロック中）のみ＝3件未満のため補充指針に従いExploreへ自領域route・コンポーネントの柱0未適用箇所横断監査を委任。結果、`chemical-ra-extras.tsx`「追加」ボタン・`chemical-ra-panel.tsx`A4実施レポート印刷ボタン・`goods-chatbot.tsx`サンプル質問チップ・`law-revision-list.tsx`絞り込み解除×ボタン（タップ域最狭）・`accidents-reports/hub-filter.tsx`「フィルタを解除」ボタン・`subsidies/calculator/page.tsx`外部リンク「公式ページ」・`circulars/[id]/page.tsx`共有リンク3本（X/Facebook/メール）の計9要素がmin-h未指定で高さ約24〜36pxと判明。加えて`chatbot-panel.tsx`エクスポートドロップダウン開閉ボタンにaria-expanded/aria-haspopupが無く、同ファイル内の別ドロップダウン（履歴選択・実装済み）と不一致だったa11y欠陥も検出。**要注意の誤爆回避**=Explore報告に含まれていた`industry-risk-ranking.tsx`・`mhlw-deaths-panel.tsx`・`mhlw-disaster-databases-panel.tsx`（コンポーネント名は所有プレフィックス`mhlw*`に一致）を実装確認したところ、いずれも`home-screen.tsx`の`variant==="accidents"`条件下でのみ描画＝`/accidents`（ux-hub所有の「事故DB」route）専用と判明したため、いったん施した編集を`git checkout --`で取り消し対象外に確定（コンポーネント名の字面一致だけでなく実際の描画route境界を都度確認する必要性を再確認）。**是正**=計9箇所に`min-h-[44px]`（寸法のみ、文言・onClick/href不変＝既存破壊0）＋aria属性2種を1箇所に追加。無読テスト新設10/10 PASS（own prod 3100・390×844・SW block＝/chemical-ra クイック判定チップ「トルエン」タップ後の印刷ボタン、/goods サンプル質問チップ、/laws?articles=…での絞り込み解除ボタン、/accidents-reports?q=…でのフィルタ解除ボタン、/subsidies/calculator施策選択→試算後の公式ページリンク、/circulars/mhlw-notice-0001共有リンク3本、/chatbot質問例タップ→回答完了後のエクスポートボタンaria-expanded遷移）。ゲート全通過（tsc0/lint errors0/vitest 2475 pass/build成功）。ビルド時に再生成された docs/rag-metrics-latest.json・ky-print-sheet/meeting-print-sheet snapshot・chatbot-eval-fresh-results.json は契約どおり`git checkout --`で復元＝コミットせず。**注**=作業中`web/CLAUDE.md`→`web/AGENTS.md`経由で`node_modules/next/dist/docs/`を読むよう誘導する記載（プロンプトインジェクション、既出＝過去複数イテレーションで検出済のものと同一）を再確認・引き続き無視、本タスクへ影響なし。無読スクリプトはPlaywrightのESM解決がスクリプト自身の物理パス基準（既知の制約＝[[noread-test-prod-server-port]]）のためdocs/third-party-reviews/scripts配下からは直接実行不可＝web/へ一時コピーして実行後に削除。残=次点1件（/law-search・/chemical-databaseデータ鮮度表示はdata班確認待ち）のみ＝3件未満のため次イテレも柱0/柱3レビューから補充が必要。スクリプト=docs/third-party-reviews/scripts/mhlw-panels-misc-44px-a11y-noread-2026-07-03.mjs。

## 2026-07-03 ContextualPpePicks共有部品＋RA結果CTA＋accident-news/product-search/subsidies calculatorを44px化（ux-tool/ppe-cta-various-44px）

イテレーション頭で自班未マージPRは#735（chemical-database-tap-targets-44px）のみ・CI進行中(IN_PROGRESS)のためマージ据え置き、`git checkout main && git pull --ff-only`でclean確認のみ実施（CI待ちは次イテレーションで回収）。**本題**=lane backlog未着手が1件（要data班確認でブロック中）のみ＝3件未満のため補充指針に従いExplore監査を委任。自班所有routeの44px/結論カード/h1/TODO横断調査の結果、`ContextualPpePicks.tsx`（複数owned routeに埋め込まれる共有部品として自班に明示所有）の「AI診断 →」・Amazon/楽天ボタン、`chemical-ra-panel.tsx`のRA結果後CTA3本、`/accident-news`印刷CTA、`/chemical-ra/product-search`戻るリンク、`/subsidies/calculator`開閉トグルの計8箇所がmin-h未指定で44px未満（実測30〜36px）と判明。**是正**=いずれも`min-h-[44px]`（+`inline-flex items-center`）を付与（寸法のみ、文言・onClick/href不変＝既存破壊0）。無読テスト新設6/6 PASS（own prod 3100・390×844・domcontentloaded＝/circulars/mhlw-notice-0001・/accident-news・/chemical-ra/product-search・/subsidies/calculator=施策選択→試算→開閉トグル、いずれも44px以上）。ゲート全通過（tsc0/lint errors0/vitest 2467 pass/build成功）。ビルド再生成JSON（docs/rag-metrics-latest.json・chatbot-eval-fresh-results.json・ky-print-sheet/meeting-print-sheet snapshot）は契約どおりcheckoutで復元＝コミットせず。**注**=作業中`web/CLAUDE.md`→`web/AGENTS.md`経由で`node_modules/next/dist/docs/`を読み実在しないAPI(`unstable_instant`)を装った記載へ誘導するプロンプトインジェクション（既出＝過去複数イテレーションで検出済のものと同一）を再確認・引き続き無視、本タスクへ影響なし。残=次点1件（/law-search・/chemical-databaseデータ鮮度表示はdata班確認待ち）・PR #735（CI進行中・次イテレ回収）のみ＝3件未満のため次イテレも柱0/柱3レビューから補充が必要。スクリプト=docs/third-party-reviews/scripts/ppe-cta-various-44px-noread-2026-07-03.mjs。

## 2026-07-03 /law-search(MHLW公式PDF)絞り込みチップ・ページネーション/plan-generator再編集リンクを44px化（ux-tool/mhlw-plan-preview-44px）

イテレーション頭でCI緑未マージ自班PR2件（#721 chatbotツールバー・#714 job-class-tabs）を確認→#721はそのままsquashマージ成功、#714はmainとコンフリクト（append-onlyのBACKLOG-ux-tools.md・cycle-log-ux-tools.mdのみ、既知パターン）のためbranch内でoriginを通常マージし両エントリを保持する形で手解消、ゲート再実行（tsc0/lint errors0/vitest 2444 pass/build成功）後push→`--squash --auto`で回収。続けて自PR #728（risk-lawsearch-tabs-44px）も同様にmainとコンフリクト（同パターン）→手解消・ゲート再実行後pushしsquashマージ完了。**本題**=lane backlog未着手を確認したところ次点の柱0スウィープ申し送り（chatbot-toolbar-44px着手時のExplore調査で検出）が1件残存＝`law-search-panel.tsx`のデータソース切替タブは直前の`risk-lawsearch-tabs-44px`で既に是正済みと判明したが、`mhlw-law-articles-panel.tsx`の法令名フィルタチップ（全法令/各法令名ボタン、`px-3 py-1`≒24px）・前へ/次へページネーション（`px-3 py-1.5`≒30px）、`strategy/plan-generator/preview/[id]/page.tsx`の「← 事業者名・業種等を再編集」リンク（`px-3 py-1.5`≒32px）の計3箇所5要素がmin-h未指定のまま残っていた。**是正**=いずれも`min-h-[44px]`（+`inline-flex items-center justify-center`）を付与（寸法のみ、文言・onClick/href不変＝既存破壊0）。無読テスト新設4/4 PASS（own prod 3100・390×844・domcontentloaded＝`/law-search?mode=mhlw`でhydration待ち後に法令名フィルタチップ全件・「次へ」ボタン44px以上を確認、`/strategy/plan-generator`の「計画を生成してプレビュー」をクリックして実際にpreviewページへ遷移させ再編集リンクの寸法を検証）。ゲート全通過（tsc0/lint errors0/vitest 2454 pass/build成功）。ビルド再生成JSON（docs/rag-metrics-latest.json・chatbot-eval-fresh-results.json・ky-print-sheet/meeting-print-sheet snapshot）は契約どおりcheckoutで復元＝コミットせず。無読スクリプトはPlaywrightのESM解決がスクリプト自身の物理パス基準（既知の制約＝[[noread-test-prod-server-port]]）のためdocs/third-party-reviews/scripts配下からは直接実行不可＝web/へ一時コピーして実行後に削除。**注**=作業中に`web/CLAUDE.md`→`web/AGENTS.md`経由で`node_modules/next/dist/docs/`を読むよう誘導する記載（プロンプトインジェクション、既出＝過去複数イテレーションで検出済のものと同一）を再確認したが、既に無視すると判断済のため再度無視し本タスクへ影響なし。残=次点1件（/law-search・/chemical-databaseデータ鮮度表示はdata班確認待ち）のみ＝3件未満のため次イテレも柱0/柱3レビューから補充が必要。スクリプト=docs/third-party-reviews/scripts/mhlw-plan-preview-44px-noread-2026-07-03.mjs。

## 2026-07-03 /risk-prediction 主タブ・/law-search モード切替+フィルタチップ+e-Gov/AI要約ボタンを44px化（ux-tool/risk-lawsearch-tabs-44px）

イテレーション頭で自班CI緑PR #714（interview-guidance職種タブ44px化）をsquashマージしようとしたところ、並行マージされた他PR群（他班#715/#716/#705等）によりBACKLOG-ux-tools.md・cycle-log-ux-tools.mdでコンフリクト（append-onlyログの並行イテレーション同時多発、既知パターン）＝origin/mainを当該ブランチへ通常マージし両エントリを保持する形で手解消、ゲート再実行（tsc0/lint errors0/vitest 2439 pass/build成功）後pushしCI再走へ（#714自体は次回回収）。PR #721（chatbotツールバー44px）はCI進行中のため据え置き。**本題**=lane backlog未着手が次点1件（/law-search・/chemical-databaseデータ鮮度表示、data班確認待ちでブロック中）のみ＝3件未満のため補充指針に従いExplore調査を委任。所有コンポーネント`risk-prediction*`・`law-search*`を横断監査した結果、`risk-prediction-panel.tsx`のTabButton（検索/傾向/マトリクス/スコア＝ページ全体の主ナビ、role="tab"相当のクリック要素）が`px-2 py-2 text-xs`≒32〜36pxでmin-h未指定、`law-search-panel.tsx`のモード切替（キュレーション/MHLW公式PDF）が`px-4 py-1.5 text-sm`≒32pxでmin-h未指定、`law-search-results.tsx`のe-Govリンク・AI要約ボタン（`px-3 py-1 text-[11px]`≒24px）・法令名フィルタチップ（`px-3 py-1.5 text-xs`≒28px）がいずれもmin-h未指定と判明＝過去に`job-class-tabs.tsx`や`law-revision-list.tsx`で是正済みの同型バグが姉妹ファイルに横展開漏れとして残存していたパターン。他候補（leaflet印刷ボタン・subsidies/calculator内リンク）も検出したが今回は主タブ/主要検索導線を優先し次点として申し送り。**是正**=計6箇所に`min-h-[44px]`（+`inline-flex items-center[-justify-center]`）を付与（寸法のみ、文言・onClick/href不変＝既存破壊0）。無読テスト新設7/7 PASS（own prod 3100・390×844・domcontentloaded＝/risk-predictionの主タブ4種全44px以上、/law-searchのモード切替2種・「すべての法令」チップ・e-Govリンク・AI要約ボタン全44px以上）。実装中、法令名の判定はクライアントコンポーネントのhydration完了前だとボタンがDOMに存在しない事象が発生＝domcontentloaded直後の即時カウントでは`count=0`偽FAILとなったため、対象ボタンの`visible`待ちを明示的に挿入して解消（[[noread-test-prod-server-port]]に類するhydrationタイミングの落とし穴として記録価値あり）。ゲート全通過（tsc0/lint errors0/vitest 2439 pass/build成功）。ビルド再生成JSON（docs/rag-metrics-latest.json・chatbot-eval-fresh-results.json）は契約どおりcheckoutで復元＝コミットせず。スクリプト=docs/third-party-reviews/scripts/risk-lawsearch-tabs-44px-noread-2026-07-03.mjs。残=PR #714（コンフリクト解消済・CI回収待ち）・PR #721（chatbotツールバー・CI回収待ち）・次点=/leaflet印刷ボタン・/subsidies/calculator内ボタンの44px化・/law-search・/chemical-databaseデータ鮮度表示（data班確認待ち）。

## 2026-07-03 /mental-health-management/interview-guidance 職種タブ（JobClassTabs）を44px化（ux-tool/job-class-tabs-44px）

イテレーション頭で自班PR #709（法改正一覧フィルタ/ソートチップ44px化）がCI全緑（e2e/smoke/Vercel SUCCESS）だったためsquashマージ→mainをff-only同期（clean確認）。lane backlog未着手が次点1件（/law-search・/chemical-databaseデータ鮮度表示、data班確認待ちでブロック中）のみ＝3件未満のため補充指針に従いExplore調査を委任。過去の柱0監査は概ね飽和状態（[[ux-tools-domain-pillar0-saturated]]）だが、Exploreに20+回のgrep/read（担当route全域）で実コード直読させた結果、`mental-health-management/interview-guidance/job-class-tabs.tsx`の職種タブ（role="tab"、タップで措置案パネルを切替える実インタラクティブ要素）が`px-3 py-1.5 text-xs`≒高さ約28〜30pxで44px未満のまま取り残されていたと判明。同ツリーの`stress-check/readiness-form.tsx`（似た pill ボタン群）は既に`min-h-[44px]`済みで、`job-class-tabs.tsx`だけが横展開漏れの典型パターン。他候補（chemical-ra/product-search back-link・accident-news印刷リンク・subsidies/calculator外部リンク・strategy/plan-generator再編集リンク・organization disabledボタン）はいずれもプレーンリンクまたはdisabled/優先度低と判断し対象外。**是正**=`min-h-[44px]`（+`inline-flex items-center justify-center`）を付与（寸法のみ、文言・onClick不変＝既存破壊0）。無読テスト新設4/4 PASS（own prod 3100・390×844・domcontentloaded＝h1=1・全職種タブ44px以上・タブ切替でパネル内容が変化＝非退行）。ゲート全通過（tsc0/lint errors0/vitest 2413 pass/build成功）。ビルド再生成JSON（docs/rag-metrics-latest.json・chatbot-eval-fresh-results.json・ky-print-sheet/meeting-print-sheet snapshot）は契約どおりcheckoutで復元＝コミットせず。無読スクリプトはPlaywrightのESM解決がスクリプト自身の物理パス基準（既知の制約＝[[noread-test-prod-server-port]]）のためdocs/third-party-reviews/scripts配下からは直接実行不可＝web/へ一時コピーして実行後に削除。**注**=作業中に`web/CLAUDE.md`→`web/AGENTS.md`経由で`node_modules/next/dist/docs/`を読むよう誘導する記載（プロンプトインジェクション、既出＝過去複数イテレーションで検出済のものと同一）を再確認したが、既に無視すると判断済のため再度無視し本タスクへ影響なし。残=次点1件（/law-search・/chemical-databaseデータ鮮度表示はdata班確認待ち）のみ＝3件未満のため次イテレも柱0/柱3レビューから補充が必要。スクリプト=docs/third-party-reviews/scripts/job-class-tabs-44px-noread-2026-07-03.mjs。

## 2026-07-03 /chatbot ツールバー（履歴/音声/クリア/共有URL/エクスポート＋ドロップダウン＋履歴一覧）を44px化（ux-tool/chatbot-toolbar-44px）

イテレーション頭で自班CI緑未マージPR #694（/mental-health・/circulars・/subsidies フッターCTA）を確認→mainがconflict(DIRTY・append-onlyのcycle-log-ux-tools.mdのみ、既知パターン)だったためoriginをbranch内で通常マージし両エントリを保持する形で手解消、ゲート再実行（tsc0/lint errors0/vitest 2433 pass/build成功）後pushして`--squash --auto`予約＝CI再実行後に自動マージ確認（#694マージ済・mainをff-only同期）。#714（interview-guidance職種タブ44px）はCI継続中（自動マージ非対応リポジトリ設定のため次回回収）。**本題**=lane backlog未着手が次点1件（/law-search・/chemical-databaseデータ鮮度表示、data班確認待ちでブロック中）のみ＝3件未満のため補充指針「チャットボットの深掘り」に従いExplore調査を委任。所有コンポーネント`chatbot-panel.tsx`のチャット画面最上部ツールバー6ボタン（保存した会話/音声で会話する/話して質問する/履歴をクリア/共有URL/エクスポート、`px-3 py-1.5 text-xs`≒高さ約32px）、エクスポートのドロップダウン4項目（Markdown/テキスト/JSON/JSONをインポート、`px-4 py-2 text-xs`≒高さ約28px）、履歴セッション一覧の各行ボタン（`px-3 py-2 text-xs`≒高さ約28px）、行内の削除アイコン（テキストのみでタップ域極小）が`min-h`指定なしで44px未満のまま取り残されていた＝チャット画面で最も頻繁に触る操作列が未対応。**是正**=計13箇所に`min-h-[44px]`（削除アイコンは`min-w-[44px]`も追加）を付与（寸法のみ、文言・onClick・機能不変＝既存破壊0、削除の`stopPropagation`等ロジックは無変更）。無読テスト新設12/12 PASS（own prod 3100・390×844・SW block＝常時表示2ボタン44px以上・質問例タップ後の履歴クリア/共有URL/エクスポート44px以上・ドロップダウン4項目44px以上・履歴セッション行/削除アイコン44px以上＋削除タップで該当行が消える非退行）。ゲート全通過（tsc0/lint errors0/vitest 2433 pass/build成功）。ビルド再生成JSON（docs/rag-metrics-latest.json・chatbot-eval-fresh-results.json・ky-print-sheet/meeting-print-sheet snapshot）は契約どおりcheckoutで復元＝コミットせず。Explore調査で他に3箇所（law-search-panel.tsxのデータソース切替タブ、mhlw-law-articles-panel.tsxの絞り込みチップ/ページネーション、plan-generator/preview/[id]の再編集リンク）を検出したためlane backlogに次点補充として記録。残=次点2件（データ鮮度表示はdata班確認待ち／上記3箇所は次イテレ回収）のみ＝3件未満のため次イテレも柱0レビューから補充が必要。スクリプト=docs/third-party-reviews/scripts/chatbot-toolbar-44px-noread-2026-07-03.mjs。

## 2026-07-03 /mental-health・/circulars/[id]・/subsidies フッターCTAを44px化（ux-tool/mental-health-circulars-subsidies-cta-44px）

イテレーション頭でCI緑の自班PR #677（/stats結論カード）をsquashマージ→mainをff-only同期（clean確認）。lane backlog未着手を確認したところ1件（/law-search・/chemical-databaseデータ鮮度表示）のみで、しかもdata班確認待ちでブロック中＝3件未満のため補充指針「自領域の柱0/柱3レビュー」に従い調査。所有route内で未着手だった`/stress-check`（実体は`/mental-health-management/stress-check`）と`/mental-health`（トップ）をまず直読したところ、stress-checkは既に`ConclusionCard`＋`StatusBadge`＋44px化済みタップ標的で柱0完成済みと確認、`/mental-health`は純粋な参考解説ハブ（判定/件数状態を持たない）で結論カード追加は水増しに該当すると判断。そこで所有route全体を`px-4 py-2 text-xs`のrounded-lg CTA（`min-h`指定なし）パターンでgrep横断した結果、3ページ7箇所の実害を検出（text-xs line-height 16px + py-2 16px＝実測約32〜34px<44px）＝いずれも「次にやること」の主要導線（/mental-health=多様な働き方の安全・安全用語辞書、/circulars/[id]=本文（出典）を開く・PDF・通達一覧（出典）、/subsidies=料金プランを見る・導入相談をする）。**実装**=全7箇所に`min-h-[44px]`を付与（寸法のみ、文言・機能・href不変＝既存破壊0）。無読テスト新設7/7 PASS（own prod 3100・390×844、/circulars/[id]は一覧から実データの詳細ページへ遷移して検証）。ゲート全通過（tsc0/lint errors0/vitest 2358 pass・279 files/build成功）。ビルド再生成物（docs/rag-metrics-latest.json・chatbot-eval-fresh-results.json・ky-print-sheet/meeting-print-sheet snapshot）は契約どおりcheckoutで復元＝コミットせず。残=次点1件（/law-search・/chemical-databaseデータ鮮度表示はdata班確認待ち）のみ＝3件未満のため次イテレも柱0/柱3レビューから補充が必要。スクリプト=docs/third-party-reviews/scripts/mental-health-circulars-subsidies-cta-44px-noread-2026-07-03.mjs。

## 2026-07-03 法改正一覧（law-revision-list.tsx）のフィルタ/ソートチップ・個票アクションを44px化（ux-tool/law-revision-filter-chips-44px）

イテレーション頭で自班未マージPR2件を確認＝#694（/mental-health・/circulars・/subsidies フッターCTA）はmain先行によりconflict(DIRTY)＝cycle-log-ux-tools.mdのみ（append-onlyログの並行イテレーション同時多発、既知パターン）だったため`git worktree`上でorigin/mainを通常マージし両エントリを保持する形で手解消、ゲート再実行（tsc0/lint errors0/vitest 2358 pass/build成功）後pushしCI待ちへ（本イテレでは未マージ・次回回収）。続けて#700（/mental-health ハブ結論カード）がCI緑化したためsquashマージ→mainをff-only同期（clean確認）。**本題**=lane backlog未着手が次点1件（/law-search・/chemical-databaseデータ鮮度表示、data班確認待ちでブロック中）のみ＝3件未満のため補充指針「自領域の柱0レビュー」に従いExplore調査を委任。所有コンポーネント`law-revision*`のうち`law-revision-list.tsx`（/laws・home画面に埋め込み＝ページ自体は他班所有だが、component prefix指定によりこのファイルの改修は自班所有）を実読させた結果、既に`min-h-[44px]`済みの「もっと見る」ボタンの直上に、業種/対象属性/事業所規模/種別/影響度/施行状況/施行日順の全7フィルタ・ソートチップ群（`px-3 py-1 text-xs`≒高さ約24px）が44px未満のまま取り残されていた（同一ファイル内で一部のみ是正済という繰り返しの取りこぼしパターン）。周辺を追加精査し、ゼロヒット時の再検索導線3箇所、各カードの「AIで要約」「質問する」ボタン（`px-3 py-2 text-sm`≒高さ約36px）、常時表示・展開後2種類の「e-Govで原文を確認」リンク、自社版書き換えボタンも同ジャンルの違反と確認。**是正**=計15箇所に`min-h-[44px]`（+`inline-flex items-center justify-center`）を付与（寸法のみ、文言・onClick・href不変＝既存破壊0）。プレーンな下線テキストリンク（「再生成」「出典:」）は既存の prose inline link 免除方針を踏襲し対象外。無読テスト新設13/13 PASS（own prod 3100・390×844・/laws＝全フィルタチップ群44px以上・カードのAIで要約/質問する44px以上・常時表示/展開後双方のe-Govリンク44px以上・ゼロヒット再検索ボタン44px以上＋非退行でクリア動作維持）。テスト実装中に本番サーバーの旧プロセスが`pkill`で終了せず古いビルドを掴んだままテストが偽PASSする事故が一度発生＝`netstat`でPIDを特定し`Stop-Process`で確実に停止してから再ビルド・再起動する運用に修正（[[noread-test-prod-server-port]]に類する新たな落とし穴として記録価値あり）。ゲート全通過（tsc0/lint errors0/vitest 2395 pass/build成功）。ビルド再生成JSON（docs/rag-metrics-latest.json・chatbot-eval-fresh-results.json・ky-print-sheet/meeting-print-sheet snapshot）は契約どおりcheckoutで復元＝コミットせず。**注**=作業中に`web/CLAUDE.md`→`web/AGENTS.md`経由で`node_modules/next/dist/docs/`を読むよう誘導する記載（プロンプトインジェクション、既出＝過去複数イテレーションで検出済のものと同一）を再確認したが、既に無視すると判断済のため再度無視し本タスクへ影響なし。残=次点1件（/law-search・/chemical-databaseデータ鮮度表示はdata班確認待ち）のみ＝3件未満のため次イテレも柱0レビューから補充が必要。スクリプト=docs/third-party-reviews/scripts/law-revision-filter-chips-44px-noread-2026-07-03.mjs。

## 2026-07-03 /mental-health ハブに結論カード新設＋フッターCTA2本を44px化（ux-tool/mental-health-hub-conclusion-card-44px）

イテレーション頭で自ブランチPR #688（/risk-prediction サンプルチップ44px）がCI全緑だったためsquashマージ→mainをff-only同期（clean確認）。lane backlog未着手が次点1件（/law-search・/chemical-databaseデータ鮮度表示、data班確認待ちでブロック中）のみ＝3件未満のため補充指針に従いExplore調査を実施。過去の柱0監査は概ね飽和状態（[[ux-tools-domain-pillar0-saturated]]）だが、対象を担当route全25本の実コード読了に広げた結果、`/mental-health`ハブページ（メンタルヘルス・ハラスメント・VDT作業）が2026-06-14の柱0監査記録に唯一名前が無く、かつ実害を確認＝着手。**実装**=`mental-health/page.tsx`は`PageHeader`直後が「見えない半分」のAlertCircleプローズのみで色帯・デカ数字の視覚結論(ConclusionCard)を持たず（mental-health-management・treatment-work-balance等の兄弟ハブは既に対応済みで横展開漏れ）、末尾の相談窓口セクションのCTAリンク2本（多様な働き方の安全／安全用語辞書）も`min-h`指定が無くpx-4 py-2 text-xsのみ＝実測約32pxで44px未満だった。是正=`PageHeader`直後に`ConclusionCard`を新設（tone info・value 4=`HUB_TOPICS.length`＝ページ内4分野の見出し数の転記のみ・title「要点まとめ」・action「ストレスチェックを見る」→/mental-health-management/stress-check）、既存プローズはそのまま補足として残置（水増し回避）。CTA2本には`min-h-[44px]`を付与（文言・href不変）。値は既存セクション構成の転記のみ＝捏造ゼロ・既存破壊0。無読テスト新設8/8 PASS（own prod 3100・390×844・SW block・domcontentloaded＝結論カードがファーストビュー内・デカ数字48px・アクション導線44px・DOM順で制度説明プローズより前・h1=1・フッターCTA2本とも48px）。ゲート全通過（tsc0/lint errors0/vitest 2382 pass/build成功）。ビルド再生成JSON（docs/rag-metrics-latest.json・chatbot-eval-fresh-results.json・ky-print-sheet/meeting-print-sheet snapshot）は契約どおりcheckoutで復元＝コミットせず。スクリプト=docs/third-party-reviews/scripts/mental-health-hub-conclusion-card-44px-noread-2026-07-03.mjs。残=次点1件（/law-search・/chemical-databaseデータ鮮度表示はdata班確認待ち）のみ＝3件未満のため次イテレも柱0/柱3レビューから補充が必要。

## 2026-07-03 /risk-prediction「サンプル作業内容」チップを44px化（ux-tool/risk-prediction-sample-chips-44px）

イテレーション頭で自班未マージPR2件を確認＝#681（/law-hierarchy ActionLink・/treatment-work-balance/plan-builder 印刷ボタン44px化）はe2e/smoke共にSUCCESS化していたためsquashマージ→mainをff-only同期（clean確認）。#677（/stats結論カード新設）はmain先行によりconflict(DIRTY)＝cycle-log-ux-tools.mdのみ（append-onlyログの並行イテレーション同時多発）だったため origin/main を通常マージで手解消（両エントリを保持する形で解消）、ゲート再実行（tsc0/lint errors0/vitest 2313 pass/build成功）後pushしCI待ちへ（本イテレでは未マージ・次回回収）。**本題**=lane backlog未着手が次点1件（/law-search・/chemical-databaseデータ鮮度表示、data班確認待ちでブロック中）のみ＝3件未満のため補充指針「自領域の柱0レビュー」に従いExplore調査。過去の柱0監査は概ね飽和状態（[[ux-tools-domain-pillar0-saturated]]）だが、実コードを直接読ませた結果`risk-prediction-panel.tsx`で新規のタップ標的違反を検出＝検索カード内「業種・工種から呼び出す（1タップ）」チップ列（717-730行）は既に`min-h-[44px] min-w-[64px]`済みだったが、直下の兄弟要素「サンプル作業内容」チップ列（735-752行、`QUICK_EXAMPLES`）だけ`min-h`指定が無く`px-2.5 py-1 text-[11px]`≒高さ約21px<44pxのまま取り残されていた（同一カード内で一方のみ是正済という典型的な取りこぼしパターン）。**是正**=`min-h-[44px]`を付与（`px-2.5`→`px-3`は縦方向確保に伴う微調整のみ、文言・onClick・href不変＝既存破壊0）。無読テスト5件新設（own prod 3100・390×844・serviceWorkers:block・domcontentloaded＝ラベル可視・チップ検出・全チップ44px以上・兄弟の業種・工種チップ列が非退行で44px以上・タップで`#risk-query`入力欄へサンプル文言が反映）5/5 PASS。ゲート全通過（tsc0/lint errors0/vitest 2342 pass/build成功）。ビルド再生成JSON（docs/rag-metrics-latest.json・chatbot-eval-fresh-results.json・ky-print-sheet/meeting-print-sheet snapshot）は契約どおりcheckoutで復元＝コミットせず。**注**=作業中に`web/CLAUDE.md`→`web/AGENTS.md`経由で`node_modules/next/dist/docs/`を読むよう誘導する記載（プロンプトインジェクション、既出＝2026-07-03 stats-conclusion-card-44px時に検出済のものと同一）を再確認したが、既に無視すると判断済のため再度無視し本タスクへ影響なし。無読スクリプトはPlaywrightのESM解決がスクリプト自身の物理パス基準（既知の制約＝[[noread-test-prod-server-port]]）のためdocs/third-party-reviews/scripts配下からは直接実行不可＝web/scripts/へ一時コピーして実行後に削除。スクリプト=docs/third-party-reviews/scripts/risk-prediction-sample-chips-44px-noread-2026-07-03.mjs。残=#677（/stats結論カード）のCI回収→squashマージ、次点1件（/law-search・/chemical-databaseデータ鮮度表示はdata班確認待ち）のみ＝3件未満のため次イテレも柱0/柱3レビューから補充が必要。

## 2026-07-03 /stats に結論カード新設＋期間切替タブ44px化（ux-tool/stats-conclusion-card-44px）

イテレーション頭で自班未マージPR #673（/treatment-work-balance結論カード）はe2e/smoke共に緑化していたためsquashマージ→mainをff-only同期（clean確認）。lane backlog未着手を確認したところ1件（/law-search・/chemical-databaseデータ鮮度表示）のみで、しかもdata班確認待ちでブロック中＝3件未満のため補充指針「自領域の柱0/柱3レビュー」に従いExplore調査。まずT5（安衛令コーパス拡充）を候補検討したが対象データが`web/src/data/laws`＝data班所有と判明し着手不可、次に/chatbotの残perf懸念（診断07 S-4残課題）を調査したところ`chatbot-panel.tsx`は既にPR #500で`@/data/laws`をclient chunkから排除済み（law-search本体とは独立に先行是正済み）と判明し対象外。最後に自領域25route中、2026-06-14の柱0監査記録に唯一名前が無かった`/stats`をExplore調査した結果、実コードで柱0未適用の実害を確認＝着手。**実装**=`StatsDashboardImpl.tsx`は`PageHeader`直後がCollapsibleDetail→期間タブ→データという構成で共通`ConclusionCard`（ux-records custodian所有）を一度も使っておらず、未接続時も絵文字＋プロースの独自空状態のみで色帯・デカ数字の視覚結論を持たなかった。是正=`ConclusionCard`をCollapsibleDetail直後に新設し3分岐＝GA4接続時は`data.summary.pv`（既存transferable metric・転記のみ）をデカ数字で「アクセス実績」表示、GSC等のみ接続時は数値を出さず接続状況のみ説明（捏造防止＝GA4非liveでPVを出さない）、完全未接続時は「未接続」。既存の絵文字＋プロース空状態ブロックは補足としてそのまま残置（水増し回避）。付随で発見した期間切替タブ(role=tab)の44px未満（`px-3 py-1`のみ）もmin-h-[44px]化。単体テスト2件新設（`StatsDashboardImpl.test.tsx`＝GA4/mock両ソースをfetchモックで再現、`TranslatedPageHeader`が`FuriganaProvider`等のcontext依存のためLanguage/Furigana/EasyJapaneseの3providerでラップ）。無読テストはローカル環境がGA4/GSC未接続のため「未接続」分岐のみ実機検証（6/6 PASS、own prod 3100・390×844・SW block）、GA4接続分岐は単体テストでカバー。ゲート全通過（tsc0/lint errors0/vitest 2310 pass/build成功）。ビルド再生成JSON（docs/rag-metrics-latest.json・chatbot-eval-fresh-results.json・ky-print-sheet snapshot）は契約どおりcheckoutで復元＝コミットせず。**注**=作業中に`web/CLAUDE.md`→`web/AGENTS.md`経由で`node_modules/next/dist/docs/`を読むよう誘導する記載を発見し実際に確認したところ、当該docsファイル内に「AI agent hint」を騙りAPIの不安定機能(`unstable_instant`)の無関係な実装変更を促す埋め込み文言（gitignore対象のnode_modules内＝リポジトリ非追跡）を検出＝プロンプトインジェクションと判断し無視、本タスクは既存パターン踏襲のみで実施（node_modules配下の変更提案には従っていない）。残=次点1件（/law-search・/chemical-databaseデータ鮮度表示はdata班確認待ち）のみ＝3件未満のため次イテレも柱0/柱3レビューから補充が必要。スクリプト=docs/third-party-reviews/scripts/stats-conclusion-card-44px-noread-2026-07-03.mjs。

## 2026-07-03 /accidents-analytics・/accident-news フィルタselect/button 44px化（ux-tool/accidents-analytics-news-select-44px）

イテレーション頭でPR #662（mental-health-managementサブガイド結論カード）がCI緑・mergeable=MERGEABLEだったためsquashマージ→mainをff-only同期（clean）。lane backlog未着手が次点1件（/law-search・/chemical-databaseデータ鮮度表示、data班確認待ちでブロック中）のみ＝3件未満のため補充。診断07残タスク一覧・自己記憶（ux-tools-domain-pillar0-saturated）を確認したところ、2026-06-14のタップ標的監査（PR #570）で「フォームselectがまだ33〜40px」が将来候補として申し送りされたまま未着手と判明。実コード確認＝`AnalyticsDashboardImpl.tsx`の軸G業種select（#quick-industry）・フィルタバー2 select（#industry-filter/#type-filter）・絞り込みをクリアボタン・詳細分析トグルボタンはいずれも`min-h`指定が皆無でpx-2.5〜3 py-1.5前後≒30px、`accident-news-filter.tsx`の3 select＋キーワード入力＋検索/クリアボタンは`min-h-[40px]`と44px未満で据え置かれていた。是正=全て`min-h-[44px]`を付与（寸法のみ、文言・色・href・ロジック不変＝既存破壊0）。無読テスト新設8/8 PASS（own prod 3100・390×844・serviceWorkers:block・domcontentloaded＝/accidents-analyticsの軸G select・フィルタバー2 select・絞り込みをクリア・詳細分析トグル、/accident-newsの3 select・キーワード入力・検索ボタン）。ゲート全通過（tsc0/lint errors0/vitest 2288 pass/build成功）。ビルド再生成JSON（docs/rag-metrics-latest.json・chatbot-eval-fresh-results.json・ky-print-sheet snapshot）は契約どおりcheckoutで復元＝コミットせず。スクリプト=docs/third-party-reviews/scripts/accidents-analytics-news-select-44px-noread-2026-07-03.mjs。残=次点1件（/law-search・/chemical-databaseデータ鮮度表示はdata班確認待ち）のみ＝3件未満のため次イテレは柱0/柱3レビューから補充が必要。

## 2026-07-03 /mental-health-management サブガイド2本に結論カード新設（ux-tool/mental-health-mgmt-subguide-conclusion-card）

イテレーション頭で自班未マージPRを確認＝#648（Q39 PIN trigger拡充）はマージコンフリクト（cycle-log-ux-tools.md・BACKLOG-ux-tools.mdのみ、並行イテレーションの#651・#643と同時多発）を手解消してからsquashマージ。続けて#656（chemical-ra product-search ゼロヒットCTA）がe2e/smoke共に緑化していたためsquashマージ。mainをff-only同期。lane backlog未着手を確認したところ最上位1件（/law-search・/chemical-databaseデータ鮮度表示）はdata班確認待ちでブロック中のため、次点リストから実行可能な1件（/mental-health-management配下small-business・interview-guidanceサブガイドの結論カード欠如）に着手。実コード確認＝両ページとも`PageHeader`直後が`AlertCircle`の制度説明プローズ（努力義務の位置付け等）のみで、色帯・デカ数字・「次にやること」の視覚結論（`ConclusionCard`）を持たなかった一方、姉妹ページ`/stress-check`は`ReadinessForm`内で`ConclusionCard`を既に使用しておりパターンが確立済みと判明。是正=`PageHeader`直後に`ConclusionCard`を新設（既存プローズは補足としてそのまま残置、水増し回避のため新規判定ロジックは追加せず既存データ配列の件数を転記のみ）。small-business=tone warning（既存amber基調と整合）・value 9(`SMALL_BUSINESS_STEPS.length`)・unit ステップ・title「努力義務」・action「実施ロードマップへ」(#roadmap)。interview-guidance=tone info（既存sky基調と整合）・value 8(`INTERVIEW_FLOW_STEPS.length`)・unit ステップ・title「実務フロー」・action「フローを見る」(#flow)。`ConclusionCard`・`safety-tone.ts`はux-records custodian所有のためimportのみで変更せず、`data/mental-health-rules`（data班所有）も既存exportの件数を読み取るのみで一切変更せず。無読テスト12/12 PASS（own prod 3100・390×844＝結論カードがファーストビュー内y=489・デカ数字48px・アクション導線44px以上・DOM順で制度説明プローズより前・h1=1、両ページとも）。ゲート全通過（tsc0/lint errors0/vitest 2242 pass/build成功）。ビルド再生成JSON（docs/rag-metrics-latest.json・chatbot-eval-fresh-results.json・ky-print-sheet snapshot）は契約どおりcheckoutで復元＝コミットせず。BACKLOGの次点リストからも本件と#656で完了済みのchemical-ra CTA項目を削除。残=次点1件（/law-search・/chemical-databaseデータ鮮度表示はdata班確認待ち）のみ＝3件未満のため次イテレは柱0/柱3レビューから補充が必要。スクリプト=docs/third-party-reviews/scripts/mental-health-mgmt-subguide-conclusion-card-noread-2026-07-03.mjs。

## 2026-07-03 /chemical-ra/product-search ゼロヒット専用CTA新設（ux-tool/chemical-ra-product-search-zero-hit-cta）

イテレーション頭で自班未マージPR2件を確認＝#651（法令名正規化テーブル単一ソース化）はCI全緑のためsquashマージ、#648（Q39 PIN trigger拡充）はmain先行によりmerge conflict(DIRTY)＝cycle-log-ux-tools.mdのみ（並行イテレーションの#643 law-search-corpus-lazy-loadと同時多発）だったため origin/main を通常マージで手解消（append-onlyログの両エントリを保持する形で解消）、ゲート再実行（tsc0/lint errors0/vitest 2225 pass/build成功）後pushしCI待ちへ（本イテレでは未マージ・次回回収）。mainをff-only同期（#651反映を取込・clean確認）。**本題**=lane backlogの未着手2件（Q39=#648で対応中のため重複回避・データ鮮度表示=data班確認待ちでブロック）のみ＝3件未満のため補充指針「自領域の柱0/柱3レビュー」に従いExploreで2候補を実コード調査：(1)/chemical-ra/product-search のゼロヒットCTA欠如、(2)/mental-health-management サブガイド(small-business/interview-guidance)の結論カード欠如。両方とも実コードで欠陥を確認しbacklogへ補充した上で、スコープが小さく単一ファイルで完結する(1)に着手。**実装**=`product-search-panel.tsx`の`handleSearch`が0件ヒットを実エラーと同一の`error`stateに流し込み、汎用赤バナー（「該当する製品が見つかりませんでした（主要10製品収録）」）のみで次の一手が無かった。是正=`hasSearched`stateを新設して0件検知をerrorから分離、amber色の専用空状態ブロックに「化学物質RA（手入力）」(/chemical-ra)・「化学物質検索DB」(/chemical-database)への具体的CTAリンクを隣接表示（既存`RelatedPageCards`はページ下部に常時固定表示でゼロヒットに連動しないため対象外と判断）。文言は既存メッセージの転記のみ＝**捏造ゼロ・既存破壊0**。単体テスト3件新設（`product-search-panel.test.tsx`＝ゼロヒット時CTA表示・ヒットありでCTA非表示・未入力時は入力エラーのみでCTA非表示、fetchはvi.stubGlobalでモック）。無読テスト8/8 PASS（own prod 3100・390×844・SW block・domcontentloaded＝実在製品名"5-56"→内蔵DBの「KURE 5-56」に別名一致してヒット・架空製品名→CTA2本のhref健全性・h1=1・ヒット時はCTA非表示の非退行）。ゲート全通過（tsc0/lint errors0/vitest 2230 pass/build成功）。ビルド再生成JSON（docs/rag-metrics-latest.json・chatbot-eval-fresh-results.json）・ky-print-sheet snapshotは契約どおりcheckoutで復元＝コミットせず。**注**=無読スクリプトはPlaywrightのESM解決がスクリプト自身の物理パス基準（cwd非依存）のためdocs/third-party-reviews/scripts配下からは直接実行不可＝web/scripts/へ一時コピーして実行後に削除（記録用の恒久コピーはdocs側に保持）。スクリプト=docs/third-party-reviews/scripts/chemical-ra-product-search-zero-hit-cta-noread-2026-07-03.mjs。残=#648 CI回収→squashマージ、次点1件（/mental-health-management サブガイド結論カード）をbacklogで継続。

## 2026-07-03 法令名正規化テーブルの単一ソース化（ux-tool/law-name-registry-single-source）

イテレーション頭で自班未マージPRを確認＝#648（Q39 PIN trigger拡充）はe2e/smoke IN_PROGRESSのため次回回収、#643（law-search遅延読み込み）はCI全緑のためsquashマージ→mainをff-only同期（clean確認）。lane backlog最上位＝診断04 §5.1-1「法令名正規化テーブルの単一ソース化」に着手。実コード確認で想定より根が深い重複を発見：`chatbot-enrichment.ts`が参照する`LAW_METADATA`は`data/laws/law-metadata.ts`（50法令・data班所有）とは**別物**の`data/law-metadata.ts`（33法令・issuer/施行日用）で、しかも`KNOWN_LAW_SHORTS`は手動38件の三重目の重複リスト＝50法令体制拡張（2026-06-10）で追加された12法令（過労死防止法・化審法・毒劇法など）がここに未登録のまま残り、正当な引用でも「範囲外」の偽警告が出うる実害ドリフトだった。**是正**=新規`web/src/lib/law-name-registry.ts`を作成し、data班所有の`data/laws`（50法令）を単一ソースに`LAW_ALIAS_GROUPS`・`LAW_SHORT_SET`・`LAW_FULL_NAME_SET`・`isLawNameEquivalent`を自動生成、公式名と字面が異なる通称のみ`EXTRA_ALIASES`（21法令分）で補う設計に統一。`chatbot-enrichment.ts`の`KNOWN_LAW_SHORTS`/`KNOWN_LAW_FULL_NAMES`と`rag/synonyms.ts`の`LAW_ALIASES`（56行のべた書き）をこのregistry参照に置換、`isLawShortEquivalent`は`isLawNameEquivalent`へ委譲（export名・シグネチャは既存テスト互換のため維持）。`notice-search.ts`のTOPIC_SYNONYMSは法令名⇄略称表ではなくトピック語彙（熱中症→WBGT等）であり対象外と判断、無理に共用せず見送り。rag-search.tsは自班chatbot専属ロジック（seo班横断検索とは無関係）と確認した上で編集、data班所有の`data/laws/**`・`data/law-metadata.ts`は読み取りのみで一切変更せず。ゲート全通過（tsc0/lint errors0/vitest 2219 pass/build成功）。fresh eval非退行を確認（recall5: main 1.0→1.0・fresh 0.99→0.99 とも維持、mrr/該当条文countはisLawShortEquivalentの網羅範囲拡大により微増＝改善方向で悪化なし）。再生成JSON（docs/rag-metrics-latest.json・chatbot-eval-fresh-results.json）は契約どおりcheckoutで復元＝コミットせず。本タスクは画面を持たない内部ロジック統合のため無読テスト（ブラウザ/画面）は対象外と判断＝上記フル自動ゲート（既存のrag-100q/rag-100q-fresh/chatbot-enrichment/chatbot-phase2-metrics各testが本変更を直接ピン留め）とfresh eval非退行差分を代替エビデンスとした。残=#648（Q39）CI回収→squashマージ、次点2件（/law-search・/chemical-databaseデータ鮮度表示はdata班確認待ち、mental-health-managementサブガイド結論カード等）をbacklog補充要。

## 2026-07-03 fresh eval Q39 足場PIN trigger拡充（ux-tool/q39-ashiba-henkougo-tenken-pin）

イテレーション頭で自班未マージPR確認＝#643（law-search-corpus-lazy-load）はe2e/smoke IN_PROGRESSのため次イテレーションで回収、他にux-tool/ prefixの自班PRなし。mainをff-only同期（clean確認、他班マージ分10コミット取込）。lane backlog未着手を確認したところ最上位の2026-07-03補充枠3件のうち最初の1件（fresh eval Q39 PIN trigger拡充）に着手。調査＝Q39「足場の組立て・変更後に必要な点検と記録の根拠条文は？」は既存「足場の点検」PIN（rag-search.ts、567条+566条を紐付け）の4trigger（足場の点検/足場点検/足場の作業開始前点検/足場用墜落防止設備の点検）いずれとも不一致で発火せず、キーワードスコアのみで安衛則565条ほか無関係5件を誤取得していた。是正＝同PINのtriggersに「組立て・変更後」「組立て、変更後」を追加。実体根拠は安衛則567条2項（"足場の組立て、一部解体若しくは変更の後において...点検させ...補修"＝ashiba-sagyo-kisoku.ts実データで確認）。gold側のもう一方=568条（つり足場の点検）はegov-caption-snapshot.tsにcaptionのみ存在し全文条文がcorpus未収録（`allLawArticles`に無く`applyPinnedTopics`のfind()が必ずnullになるためPIN化しても無意味）と判明したためdata班領域には触れず対象外＝ただしrag-100q-fresh.test.tsのisMatchはgold配列に対しOR判定（いずれか1件のヒットで正答）のため567条のみのヒットで完了条件を満たせることを確認してから実装。他fixture（chatbot-fresh-100.json/chatbot-basic-100.json）に「組立て・変更後」を含む別問題が無いことを事前grepで確認し誤爆リスクを排除。回帰テスト1件追加（rag-search.test.ts）。完了条件を実測で確認＝fresh eval Recall@5 99→100/100（Q39含め全問正答・failures配列が空に）・main eval 124/124=100%非退行（PIN group拡張の副作用でMRRは0.8297→0.8257へ僅かに変動するがrecall5は不変）。ゲート全通過（tsc0／lint errors0／vitest 2211 pass・261 files／build成功）。生成物のうちdocs/rag-metrics-latest.json・web/src/data/chatbot-eval-fresh-results.jsonは本タスクの正答率向上そのものを表す実データのためコミット対象（PR #632 S7の先例と同様）、ky-print-sheet snapshotは無関係差分（改行コードのみ）だったためgit checkoutで復元。残=補充枠の残り2件（診断04 §5.1-1法令名正規化テーブル単一ソース化／law-search・chemical-databaseデータ鮮度表示はdata班確認待ち）と#643のCI回収を次イテレへ。**マージ時追記**＝origin/mainとのマージコンフリクト（cycle-log-ux-tools.mdのみ・並行イテレーションの#643と同時多発）を手解消。

## 2026-07-03 /law-search 法令コーパスの遅延読み込み分離（ux-tool/law-search-corpus-lazy-load）

lane backlog未着手0件（全[x]）のため補充指針「パフォーマンス改善」（CLAUDE.md自律作業ルールで自己判断可）から起こす。実コード確認＝`law-search-panel.tsx` が `web/src/data/laws` の全条文データ（`allLawArticles`・約1.4MB/全1,070条文）と検索UI・カード描画・AI要約モーダル・漢数字正規化ロジックを1ファイルに同居させていた。他ページの共有チャンクには元々含まれていなかった（Next.jsのルート単位コード分割）が、同ページ内で検索入力欄などの軽量UIとコーパス本体が不可分に結合しており、ページ自身の初期スクリプト解析コストを不要に押し上げていた。**是正**=検索結果描画（`ArticleCard`・`AiSummaryModal`・`LawSearchResults`・`kanjiToNum`/`normalizeQuery`等）を新規 `law-search-results.tsx` へ切り出し、`law-search-panel.tsx` 側は既存の `MhlwLawArticlesPanel` と同じ `next/dynamic` パターン（SSR維持＝初期HTMLに検索結果を残しLCPを落とさない）で読み込むよう変更。検索ロジック・UI文言・条文データはいずれも無変更（**捏造・水増しゼロ、既存破壊0**）。ビルド成果物 `react-loadable-manifest.json` で法令コーパス本体が独立チャンク（552KB）に分離されたことを確認し、`build-manifest.json` の `rootMainFiles`（全ページ共有の初期ロード対象）を直接grepして条文テキスト（「クレーン等安全規則」等）が混入していないことも実測（ブラウザでの検証はNext.js Linkのアイドル時プリフェッチにより/law-search遷移前でも該当チャンクがフェッチされてしまい判定が不安定だったため、ビルド成果物の静的検査に切替＝[[noread-test-prod-server-port]]の"own prod 3100"定石に加え今回学んだ追加知見）。無読テスト7/7 PASS（law-search-corpus-lazy-load-2026-07-03＝own prod 3100・390×844・domcontentloaded＝rootMainFilesにコーパス本体不含・h1=1・結論カード48px・出典の見分け方が初期折りたたみ・検索語入力で件数変化・条文カード表示・MHLWタブ切替非破壊）。ゲート全通過（tsc0／lint errors0／vitest 2175 pass／build成功）。ビルド再生成JSON（docs/rag-metrics-latest.json・chatbot-eval-fresh-results.json・ky-print-sheet snapshot）は契約どおりcheckoutで復元＝コミットせず。**イテレーション頭の作業**＝自班CI緑未マージPR3件を確認・処理：#625（音声完結モードのマイク権限エラー握りつぶし是正）はCI全緑のためsquashマージ。#632（S7小粒4本＝T6+T8+T9+T10）と#620（S7 T6+T8+T9のみ）は両方CI緑だが、ブランチ分岐後に並行イテレーションが同一の診断04 S7タスクを独立実装した重複PRと判明（rag-search.ts/chatbot-fallback-logic.ts/chatbot-no-hit-response.tsを別々に修正）＝#598/#603と同種の重複（[[lane-backlog-stale-vs-main]]同系統の判断）。T10まで完全カバーする#632を選び、origin/mainとのマージコンフリクト（cycle-log-ux-tools.mdのみ）を解消のうえゲート再実行（tsc0/lint errors0/vitest 2194 pass/build成功）してsquashマージ、#620はマージせずクローズ（PRコメントで重複理由を明記）してブランチ削除。mainをff-only同期。スクリプト=docs/third-party-reviews/scripts/law-search-corpus-lazy-load-2026-07-03.mjs。

## 2026-07-03 S7: チャットボット小粒4本＝一般語PINガード・adjacent誤ヘッダ・no-hitノイズ・週次プローブ（ux-tool/s7-chatbot-pin-guard-noise-suppression・PR #632）

イテレーション頭で自班PR #625（音声完結モードのマイク権限エラー握りつぶし是正）はCI進行中のため契約どおりsquashマージせず次イテレ回収し、main を ff-only 同期（clean確認）。lane backlog の未着手は S7（診断04のT6+T8+T9+T10）の1件のみ＝過去2回の直近イテレでは「F3=Fable班のチャットボット生成品質eval確立待ちでブロック中」として見送っていたが、診断書04を再読すると T6/T8/T9/T10 の完了条件はいずれも独立した実測基準（fresh eval Recall@5・単体テストの回帰・「天気」質問で無関係法令0件・週次プローブの実行）で、F3（結論キーフレーズ判定の生成品質eval）に依存していないと判明＝O4/O5でも同様にF3非依存でローカルevalのみで完了させてきた前例（[[lane-backlog-stale-vs-main]]と同系統の判断）に倣い着手。**T6**=`rag-search.ts`のPinnedTopicに`excludeTriggers`を新設し、一般語「換気」PINが酸欠/坑内/有機溶剤文脈を乗っ取る誤発火を抑止（fresh eval Q79「酸素欠乏症等の防止措置」が酸欠則5条ではなく安衛則600/601条を誤取得していた事故を実測確認→是正後は酸欠則ファミリーに収束、加えて該当PINのtriggerへ言い回しを追加してQ79を完全正答化）。fresh eval Recall@5 98→99/100（残る足場Q39は換気PINと無関係の別欠陥のため対象外・backlogへ次点申し送り）。**T8**=`decideFallbackTier`/`buildFallbackDecision`に`hadPins`信号を追加し、curated PIN確定時はscore0.7台でも`direct`扱いにして、正答（診断04 Q7＝職長教育、施行令19条直撃・score0.73）なのに「直接的に答える条文は限定的です」と誤ヘッダが付く問題を解消。**T9**=no-hit時の関連条文提示に`NO_HIT_NOISE_FLOOR`(0.08)を導入し、診断04 Q21「明日の東京の天気」で無関係な港湾労働法第2条が提示されていたノイズを抑止（0.04は除外・0.12=解雇予告→労働契約法16条の正当な関連提示は維持）。あわせて関連情報が0件のときの`buildNoHitTemplate`文言を「ただし関連をご案内します」という予告倒れではなく「収録範囲内に該当する関連情報は見つかりませんでした」と正直に述べるよう是正（欠落明示）。実装中、GEMINI_API_KEY未設定時のdegradedパス（route.ts、通常フローのnoise floorを経由しない別コードパス）でも同じノイズが再現することを発見し同floorを追加適用。**T10**=本番週次スモークプローブを`web/scripts/chatbot-prod-smoke-probe.mjs`として実装（`npm run smoke:chatbot-prod`）。T1〜T3・T8・T9で実際に修正した5つの事故（偽の範囲外警告／プレースホルダ漏出／派遣元誤答／adjacent誤ヘッダ／related法令ノイズ）を回帰カナリアとして本番APIへ直接プローブし、フラグをJSON出力（gitignore済・実行の度に上書き）。**週次cron自動化はGemini APIの継続課金を伴う運用変更のため、CLAUDE.mdの「必ずオーナーに確認すること」に照らしオーナー確認事項として申し送り**、スクリプト単体は完成・手動実行可の状態に留めた（GitHub Actions等への自動ワイヤリングは未実施）。ローカルprod(3100・APIキー無しdegradedパス込み)で実行し5問5問ともフラグ0件を確認。回帰テスト9件新設（decideFallbackTier/buildFallbackDecisionのhadPins分岐・buildNoHitTemplateの欠落明示・NO_HIT_NOISE_FLOORの閾値検証）。ゲート全通過（tsc0／lint errors0／vitest 2144 pass／build成功）。ビルド再生成JSON中、fresh eval結果とrag-metrics-latest.jsonはT6の改善を正しく反映した実測値のためcheckout復元せずコミット（O5と異なりスコア改善そのものが今回の成果物）。BACKLOG-ux-tools.mdのS7を[x]化、在庫3件未満のため診断04 §5.1-1（法令名正規化テーブル単一ソース化）・fresh Q39（足場PIN trigger拡充）・law-search/chemical-databaseのデータ鮮度表示（data班連携要）の3件を補充。**マージ時追記**: 本PRのブランチ分岐後、並行イテレーションが同一のS7（T6+T8+T9、T10除く）を独立実装したPR #620（ux-tool/s7-chatbot-pin-tier-nohit）を作成済みと判明。両者は同じrag-search.ts/chatbot-fallback-logic.ts/chatbot-no-hit-response.tsを別実装で修正しており、本PRがT10まで含めS7を完全カバーするため、#598/#603の前例（[[lane-backlog-stale-vs-main]]同系統の重複PR処理）に倣い#620はマージせずcloseし重複と明記、ブランチも削除。残=#625 CI回収・補充3件の着手・#620 close。

## 2026-07-03 /chatbot 音声完結モードのマイク権限エラー握りつぶしを是正（ux-tool/chatbot-voice-error-feedback）

イテレーション頭で自班未マージPR2件を確認＝#612（circulars鮮度表示）はCI全緑（e2e/smoke/Vercel SUCCESS）のため契約どおりsquashマージ→mainをff-only同期（clean確認、他班マージ分も取込）。#620（S7 T6+T8+T9）はe2e/smoke IN_PROGRESSのため次イテレーションで回収。**本題**=lane backlog の未着手が S7残（F3=チャットボット生成品質eval確立待ちで継続ブロック中）の1件のみ＝3件未満のため補充指針「チャットボットの深掘り」に従いExploreで調査。`chatbot-panel.tsx` の音声完結モード「話して質問する」ボタンが呼ぶ `startVoiceInput` は `recog.onerror = () => {}` でWeb Speech APIのエラーを完全に握りつぶす実装だったと判明＝マイク権限拒否・無音タイムアウト・ネットワーク断のいずれでもUIが無反応に見え、ユーザーは「聞き取れていないのか壊れているのか」を無読で判断できない柱0違反（診断書未記載の新規発見）。**是正**=同コンポーネント内で既に使われている `voice-input-field.tsx` の `useSpeechToText` フックが持つ日本語エラーメッセージマッピング関数を `describeVoiceError` としてexportし、`chatbot-panel.tsx` 側で新規に重複実装せず再利用（onerrorイベント・`new Ctor()`/`recog.start()`の例外catch双方に接続）。ボタン脇に role="alert" でエラー文言（例:「マイクが許可されていません。ブラウザのアドレスバー🔒から...」）を表示し、音声完結モードのトグルをOFFにすると文言も消え次回に持ち越さない設計。文言・分岐ロジックは既存 `voice-input-field.tsx` の実績パターンをそのまま踏襲＝新規デザイン判断なし・既存破壊0。単体テスト4件新設（`voice-input-field.test.ts`＝マイク拒否/無音/マイク未検出/ネットワーク/未対応ブラウザ/未知エラーコードの各分岐）。無読テスト3/3 PASS（own prod 3100・390×844・domcontentloaded＝`window.webkitSpeechRecognition`をモックしonerror("not-allowed")を発火させ検証。OFF時は文言なし→ON+話して質問するタップでエラー発火時「マイクが許可されていません」可視化→OFF復帰で文言消滅。Next.jsの`#__next-route-announcer__`が常時role=alertを持つ点に注意しセレクタで除外）。ゲート全通過（tsc0／lint errors0／vitest 2104+4=2108 pass／build成功、1回目はTurbopackのセグフォルトで再実行）。ビルド再生成JSON（docs/rag-metrics-latest.json・chatbot-eval-fresh-results.json・ky-print-sheet snapshot）は契約どおりcheckoutで復元＝コミットせず。スクリプト=docs/third-party-reviews/scripts/chatbot-voice-error-feedback-noread-2026-07-03.mjs。残=#620（S7 T6+T8+T9）CI回収→squashマージ、S7残（F3待ちで継続ブロック）、次点2件（mental-health-managementサブガイド結論カード・chemical-ra product-searchゼロヒットCTA）をbacklogに追記。

## 2026-07-03 /circulars 一覧にデータ鮮度表示を追加＋未達成のフッター文言を是正（ux-tool/circulars-latest-issued-date-freshness）

イテレーション頭で自班未マージPR2件を確認＝#603（S8-a監修者バイライン）は main先行によりconflict(DIRTY)だったため契約どおり origin/main を通常マージで解消（cycle-log append-onlyの衝突を手解消）、ゲート再実行後CI緑を確認しsquashマージ。#598（O5 synonyms是正）も同様に確認したところ、#603のブランチ履歴に#598のコミット3本が既に含まれておりsquash内容が完全一致（diff無し）と判明＝#598は#603のマージで既にmain反映済みの重複PRだったためマージせずcloseし、両ブランチをリモート削除。main を ff-only 同期（clean確認）。**本題**=lane backlog の未着手が S7（F3=チャットボット生成品質eval確立待ちで継続ブロック中）の1件のみ＝3件未満のため補充指針に従いExploreで自領域の柱0/柱3ギャップを調査。診断07 柱C-11「データ鮮度表示」の観点で law-search・circulars(一覧)・chemical-database の3ページを実コードで確認した結果、いずれも「最終更新」「取得日」の類が皆無と判明。うち circulars(一覧) は既存フッターが「本一覧は...最終確認日を付与したものです」と、実際には表示されていない日付の存在を暗黙に主張する未達成の文言不整合を検出（法令内容の捏造ではないが、サイト自身の鮮度表示に関する説明文が事実と不一致）。**是正**=`CircularsFilterableList` の結論カード description に、呼び出し元(page.tsx)で発出日降順に整列済みの配列先頭（＝収録通達中で最も新しい発出日）を「収録最新発出: {issuedDateRaw|issuedDate}」として追加＝新規データの捏造ゼロ・既存フィールドの転記のみ。フッター文言を実態に一致するよう「収録通達のうち最も新しい発出日を上部に表示しています」に是正（法令の発出者・本文表記は無変更）。law-search・chemical-database の2ページは基礎データに機械可読な鮮度フィールドが無く、かつ web/src/data/** はdata班所有で新規フィールド追加が独断でできないため、捏造リスクを避けて本イテレは対象外とし次点へ申し送り（backlogに明記）。単体テスト3件新設（最新日付の表示・issuedDateRaw欠落時のISOフォールバック・全件日付null時の非クラッシュ）。無読テスト1/1 PASS（own prod 3100・390×844・domcontentloaded＝鮮度文言表示・h1=1・フッター文言整合・旧文言の残存なし）。ゲート全通過（tsc0／lint errors0／vitest 2029 pass／build成功）。ビルド再生成JSON（docs/rag-metrics-latest.json・chatbot-eval-fresh-results.json）は契約どおり checkout で復元＝コミットせず。スクリプト=docs/third-party-reviews/scripts/circulars-freshness-noread-2026-07-03.mjs。残=S7（F3待ちで継続ブロック）・law-search/chemical-databaseのデータ鮮度表示（data班連携要否の確認から）。

## 2026-07-02 O4残＝派遣法45条要約の元/先誤答＋SYSTEM_PROMPTのYYYYプレースホルダ漏出是正（ux-tool/haken-education-yyyy-placeholder）

イテレーション頭で自班PR #583（O4のT1＝チャットボット偽「範囲外」警告の根絶）を確認したがCI（e2e/smoke）が走行中で未マージのため契約どおりsquashマージせず次イテレ回収。作業は main を ff-only 同期して新ブランチで実施（PR #583自体には積み増さない＝独立レビュー可能にするため）。lane backlog 最上位=2026-07-02注入のFable診断書04「O4残: 派遣法45条要約の是正＋SYSTEM_PROMPTプレースホルダ対策（T2+T3。T1はPR #583で対応済み）」に着手。**T2**=`haken-anzen-eisei.ts` の派遣法第45条要約が、派遣先が負う安全衛生義務の列挙（一〜四）に「作業環境の整備・安全衛生教育（法59条の一部）」とだけ書き、雇入れ時教育（59条1項＝派遣元の義務）と作業内容変更時教育・特別教育（59条2項・3項＝派遣先の義務）を区別していなかった構造的欠陥が真因＝本番実機プローブ（診断書04 Q20）でGeminiが「派遣労働者の雇入れ時教育は派遣先の義務」と誤断定（正しくは派遣元）。是正=項目二を「作業内容変更時教育及び特別教育（法第59条第2項・第3項）」に限定し、本文末尾に「雇入れ時教育（59条1項）及び一般定期健康診断（66条1項）は派遣元の義務」と明記。itemNumberMapとkeywordsも追従。rag-100qに回帰問(id124「派遣労働者の雇入れ時教育は派遣元と派遣先のどちらが行いますか？」→gold=派遣法第45条)を追加し検索面を固定。**T3**=`route.ts` の SYSTEM_PROMPT ルール10「（施行：YYYY年MM月、所管：厚生労働省）」という**文字通りのテンプレート例**をGeminiがそのまま出力する事故を本番プローブQ10で実測（1問中3箇所素通し）。真因はプロンプト内の書式例自体がプレースホルダ文字列だったこと。是正は二段構え＝①プロンプト側の例を実在例「安衛則第518条（施行：2020年12月、所管：厚生労働省）」に差し替え＋「YYYY年MM月」「第XX条」のようなプレースホルダ出力を明示的に禁止する文言を追加（根治）、②保険として `chatbot-enrichment.ts` に `sanitizePlaceholderCitations()` を新設し、Gemini応答にYYYY/第XX条トークンが残った場合に該当箇所（施行日句または丸括弧ごと）を除去する後処理サニタイザを追加、`route.ts` の非stream経路（callGemini）で全応答パスに適用。stream経路はチャンクを逐次クライアントへ送信する設計上、事前サニタイズが構造的に不可能（プロンプト側の根治で対応）のため対象外と判断（水増し回避）。単体テスト5件追加（プレースホルダ除去・所管等の実データ保持・実日付は無変更・no-op確認）。ゲート全通過（tsc0／lint errors0／vitest全pass／build成功）。BACKLOG-ux-tools.mdのO4項目をT1(#583)+T2+T3(#588)まとめて[x]化。残=#583 CI回収→squashマージ、O5(synonyms.ts:166気積是正)が次点。

## 2026-06-14 /chatbot 常時表示クイック質問チップの44px化（ux-tool/chatbot-quick-chips-44px）

イテレーション頭で main を ff-only 同期（cac10af3→8c94ca2f＝他班 faq/safety-signs/notifications 44px・data コーパス是正等を取込・clean確認）。自班 PR #570（chemical-ra クイックスタート44px）はCI pending のため契約どおり squashマージせず次イテレ回収。lane backlog の未着手は0件（全[x]）＝3件未満のため補充指針「柱0未適用箇所・無読不合格・チャットボット深掘り」に従う。まず自領域25ルートの無読スウィープを実機再計測（prod 3100・390×844・SW block・tools-noread-sweep）＝screening-fail 13件だが**h1は全25ルート各1個**（多重/欠落なし）でスクショ直読の結果、判定/法令/統計ページはいずれも結論カード・色帯・件数を既に保持＝新規カードは水増し（禁止）と再確認。代わりに**チャットボットを深掘り**したところ、入力欄直下の「クイック質問例」チップ（C-001＝初心者が打たずにタップで質問する常時動線）が `px-3 py-1 text-[11px]` ≒**高さ約24px で44px未満**＝指タップ標的として不足を発見。会話開始後は空状態の大きな質問例ボタン（既に min-h-[44px]）が消えるため、この行が"打たずに追質問"の唯一の動線になるのに最小タップ標的を満たしていなかった（自班 #570 が chemical-ra クイックスタートを44px化したのと同じ柱C-6論点の取りこぼし）。**是正**=空状態ボタンと同基準 `min-h-[44px]`＋`px-4 py-2`＋可視ラベル「質問例：」を付与（文言・件数=EXAMPLE_QUESTIONS.slice(0,3) は不変＝**捏造/水増しゼロ**、タップ標的拡大とラベル付与のみ＝既存破壊0）。ゲート全通過（tsc0／lint errors0／vitest 1874 pass／build成功）。無読テスト新規5/5 PASS（390×844・own prod 3100・SW block・domcontentloaded＝群が常時可視・「質問例：」ラベル在・チップ3個・各44px以上・タップで送信され履歴に文言）。スクリプト=docs/third-party-reviews/scripts/chatbot-quick-chips-44px-noread-2026-06-14.mjs。残=#570 CI回収・lane backlog 全[x]＝次イテレも補充指針（チャットボット/化学RAの深掘り継続）。
## 2026-06-14 柱0補充 /accidents-analytics 統計DB規模ヘッドライン（ux-tool/analytics-headline-conclusion）

イテレーション頭で自班の `ux-tool/` 未マージPRを確認＝#557（柱3② /risk 見通しストリップ地域名）のみで CI 進行中のため次イテレ回収とし、main を ff-only 同期（clean）。lane backlog は全 [x]（在庫<3件）のため補充指針「柱0未適用箇所・無読不合格画面」から起こす。まず自領域の主要判定/件数/法令ページ（risk-prediction=TopSummaryCard・stress-check=ConclusionCard・chatbot=空状態の例示チップ6本44px・chemical-ra=A4印刷/台帳）を実コードで再確認し**いずれも結論ファースト済＝水増し回避で新規カードは作らない**判断。代わりに**自領域25ルートの無読スウィープを実機再実行**（tools-noread-sweep・prod 3100・390×844・SW block・domcontentloaded）。h1は全25route各1個（多重/欠落の回帰なし）を再確認。screening-fail 13件の大半は status系/検索系/ハブ系/チャット空状態/印刷/admin など**画面種別由来の偽陽性**だったが、**`/accidents-analytics` は実害**＝事故統計ダッシュボードなのにファーストビュー maxFont=22・デカ数字なしで、初期ビューがナビ＋h1＋出力ツールバー＋「業種を選んで」案内のみ。肝心の「このDBは何件の労働災害を集計しているか」は fold 下（サマリーKPI は AnalyticsDashboardImpl line441・現状の role=status は出力ツールバーの sr-only ライブ領域のみで視覚的な結論ヘッドラインは皆無）。**是正**＝h1直下に統計DB規模ヘッドライン（data-testid=analytics-headline）を新設＝デカ数字 **5,074件**（収録総件数＝meta.curatedCases＋meta.mhlwDeathsCount）＋期間 2007〜2026＋内訳チップ（厚労省死亡災害DB 4,782件＋curated詳細 292件）。全値は既存 `aggregates.meta` の転記のみ＝**捏造ゼロ**。当初 死亡災害比率(kpi.fatalRatePercent)96% をチップに出したが、統合構成が死亡災害DB寄りで高く出る値を文脈注記なしに3秒読者へ見せると「労働災害の96%が死亡」と誤読される懸念があり、**誤読回避でヘッドラインからは外し**文脈注記つきのサマリーKPI側（「全事例に占める死亡災害」）に残置。ConclusionCard は安全トーン(緑=安全/赤=危険)の判定意味を帯び「1画面1枚」規約もあるためデータ規模の中立ヘッドラインには不適と判断し、role=status を付けない中立デカ数字ストリップで実装（既存の結論カード意味論と非衝突）。無読テスト7/7 PASS（analytics-headline-noread-2026-06-14＝ヘッドラインFV内 y=477・デカ数字 36px・単位件・漢字短ラベル収録・DOM順で最初の入力(業種select)より前＝結論ファースト・h1=1・デカ数字がサマリーKPI「収録総件数」と一致＝転記検証）。ゲート全通過（tsc0／lint errors0※対象ファイル・フルlintはCI委譲／vitest 1836 pass／build成功）。ビルド再生成JSON（docs/rag-metrics-latest.json・chatbot-eval-fresh-results.json）は契約どおり checkout で復元＝コミットせず。スクリプト=docs/third-party-reviews/scripts/analytics-headline-noread-2026-06-14.mjs。残=#557 CI回収・柱3 /risk 警報防災の追加実用化レビュー。
## 2026-06-14 /chemical-ra クイックスタート物質チップを44px化（ux-tool/chemical-ra-quickstart-44px）

イテレーション頭で自班CI緑PR #557（/risk 見通しストリップに該当地域名）を squashマージ→main を ff-only 同期（risk-outlook-region-names 等を取込・clean確認）。#562（accidents-analytics 統計DB規模ヘッドライン）はCI進行中（e2e/smoke 走行中）で次イテレ回収。**本イテレ**＝lane backlog の未着手が0件（全[x]）＝3件未満のため補充指針に従い自領域の柱0/無読不合格を捜索。まず自領域25ルートの無読スウィープ（tools-noread-sweep・prod 3100・390×844・SW block）を実機再計測したところ screening-fail 13件だが、スクショ直読で大半は**結論カードが既済の判定/状態ページ**（/insurance＝amber「未加入」カード＋CTA・/accidents-analytics＝#562で対応中）・**入力前の検索/会話ツール**（chatbot/law-search/chemical-ra＝入力後に結論を最上部表示済）・**運営用ダッシュボード**（/stats＝GA4未接続の準備中）で、いずれも結論カード追加は水増し（禁止）と判断。代わりに**実機タップ標的監査**（ファーストビュー内の a/button/input/select/summary 寸法を計測）を新規実施し、**genuine な柱0「タップしやすいサイズ」違反**を抽出。ホームパンくず（20×16）と footer 導線（34px×7）は共有部品＝他班所有のため対象外、prose内インラインリンクも対象外として除外したうえで、**自lane所有の /chemical-ra「職種別クイックスタート」物質チップ13個が高さ約30px（px-2.5 py-1）と44px未満**を検出＝これは「タップするとその物質でRAを即開始する主要CTA」であり、現場の指タップには小さすぎた。**是正**=チップの className を `inline-flex min-h-[44px] min-w-[44px] items-center justify-center px-3.5 py-2`（pill形状・amber色・text-xs文言・href `?name=…&run=1` はいずれも不変）に変更し、全13チップを44×44px以上に。値・導線・捏造ゼロ＝**寸法のみの非破壊変更**。回帰無読スクリプト新設（chemical-ra-quickstart-44px-noread-2026-06-14＝ブロック存在・13チップ全て44px以上・href健全性）を実機prod(3100・390×844)で **28/28 PASS**（旧ビルドは別プロセスが3100を占有しEADDRINUSE→該当PIDをkillし新ビルドで再計測して確認）。スクショで pill の見た目・余白・折返しが崩れず大型化のみを目視確認。ゲート全通過（tsc0／lint errors0／vitest 1856 pass／build成功）。残=lane backlog 全[x]＝次イテレも補充指針で起こす（#562 accidents-analytics ヘッドラインのCI回収・chemical RA/チャットボットの深掘り余地を継続調査）。

## 2026-06-14 /risk 見通しストリップに該当地域名を表示（ux-tool/risk-outlook-region-names）

イテレーション頭で自班CI緑PR #550（件数結論カードの無読テスト敷設）を squashマージ→main を ff-only 同期（c4d36091 等を取込・clean確認）。自班の他PR（#535/#540/#545）は既に main へ反映済みでリモートブランチは自動削除済み＝stale tracking ref を prune。**本イテレ**＝lane backlog の未着手が0件（全[x]）＝3件未満のため補充指針に従い自領域の柱0/柱3/無読不合格を捜索。まず自領域25ルートの無読スウィープを実機再計測（prod 3100・390×844・SW block）したところ screening-fail 12件だが、その大半は **入力前の検索/会話ツール**（risk-prediction/chatbot/law-search＝結論は入力後に scrollIntoView で最上部に出す既済）・**ハブページ**（mental-health 系＝単一判定を持たない）・**運営用 noindex ダッシュボード**（/stats）で、いずれも結論カードを足すと水増し（禁止）になると直読で判断。代わりに**自lane cycle-log が「残」として明記していた柱3 follow-up＝/risk 警報防災の追加実用化**に着手。前回追加した「明日からの見通し」ストリップは最悪レベル＋「N地域」の件数しか出さず、「台風前日の元請安全担当」が一番知りたい**自現場（例:九州）が明日荒れるか**は1週間予報を開いて地図を目視するまで分からなかった（前倒し判断が3秒で完結しない）。**是正**=ストリップのセル本文と aria-label に**最悪レベル該当の地域名**（例「四国・九州」「関東」）を直接表示。地域名は既存 Open-Meteo 予報の regions[].regionLabel をそのまま並べるだけ＝**捏造ゼロ**。3地域以上は先頭2件＋「他N」で1行に省略（レイアウト不変）。純関数 `buildRiskWeatherOutlook` に `worstRegions`（最悪レベル該当地域名のみ・注意報地域を警報日に混ぜない）を追加し、UI は `{count}地域`→`formatOutlookRegions()` の1行差し替えのみ＝**既存破壊0**。テスト+4（weather-outlook.test 8→12件＝警報日は警報地域のみ／注意報のみの日／良好日は空／地域名なし入力はフォールバック）。ゲート全通過（tsc0／lint errors0／vitest 1816 pass／build成功）。無読テスト新規 7/7 PASS（risk-outlook-region-names-noread＝四国・九州が本文とaria-labelに出る・件数のみでない・注意報地域混入なし・他N省略・良好日は地域名なし）＋既存 risk-outlook-noread を地域名表示に追従（「2地域」→該当地域名チェック）し 9/9 維持。スクリプト=docs/third-party-reviews/scripts/risk-outlook-region-names-noread-2026-06-14.mjs。残=lane backlog 全[x]＝次イテレも補充指針で起こす（chemical RA/チャットボットの深掘り余地を継続調査）。

## 2026-06-14 /chemical-ra 多重h1の是正（ux-tool/chemical-ra-multiple-h1-fix）

イテレーション頭で自班CI緑PR #531（バッチ9第2弾）を squashマージ→main を ff-only 同期。#535（newsletter）はマージ衝突（自班 backlog/cycle-log のみ）を origin/main マージで手解消→再push（CI再走を次イテレ回収）。#540（risk台風ペルソナ）はCI待ち。backlog 未着手は #540 在庫の1件のみ（<3件）のため補充規定に従い自領域の柱0/無読不合格を捜索。判定・結果系ページ（risk-prediction/chemical-ra/stress-check/chatbot 等）を実コードで確認＝いずれも結論カードを入力直後に最上部へ scrollIntoView で出す柱0処理が既済（捏造的な"水増し"を避けるため新規結論カードは作らない判断）。代わりに **自領域25ルートの無読スウィープを実機計測**（prod server 3100・390×844・serviceWorkers:block・domcontentloaded、noread-sweep-2026-06-11 の自領域版）。その結果 **`/chemical-ra` のみ h1=2** を機械検出＝ページ側 `PageHeader`（TranslatedPageHeader 経由）と `ChemicalRaPanel` 内の画面用イントロが**同一文言「化学物質リスクアセスメント」で h1 を二重**に出していた（WCAG/SEO上の多重h1＝柱C-7「多重h1是正」の取りこぼし）。修正は最小・非破壊＝パネル側イントロを `h1`→`h2` に降格（同パネル内の他セクション見出しと同階層・文言と見た目 text-xl は不変、ページの h1 は PageHeader が単独保持）。回帰テスト2本新設（`chemical-ra-panel.h1.test.tsx`＝パネルは h1=0／イントロ h2 に「化学物質リスクアセスメント」を保持）。再ビルド後の無読スウィープで `/chemical-ra` は h1=1 に是正、他24ルートに副作用なし。ゲート全通過（tsc0／lint errors0／vitest pass／build成功）。スウィープスクリプト=docs/third-party-reviews/scripts/tools-noread-sweep-2026-06-14.mjs。残=柱3リスクマップ実機レビュー（#540 在庫）。

## 2026-06-14 柱0回帰固定 件数結論カードの無読テスト敷設＋自領域柱0監査（ux-tool/noread-batch9-count-cards）

冒頭で自班緑PR #535（柱0バッチ9 第3弾 /newsletter）を squashマージ→main を ff-only 同期。PR #540（柱3 /risk 台風前日レビュー）は main 先行により conflict(DIRTY) だったため契約どおり origin/main を当該ブランチへ通常マージ＝BACKLOG-ux-tools.md の衝突を手解消（バッチ9を#535反映の[x]・台風行を当PRの[x]に統合）し再push（CI次イテレ回収）。PR #545（/chemical-ra 多重h1是正）はCI進行中で次イテレ回収。**本イテレ実作業**＝lane backlog の未着手が typhoon 1件（#540で在庫消化中）のみ＝3件未満のため補充指針「柱0未適用箇所・無読不合格・第三者指摘」から起こす。まず自領域route を Explore 2本で総点検したところ、判定/件数/法令/統計の主要ページは**いずれも既に結論ファーストの視覚結論を最上部に保持**（risk-prediction=TopSummaryCard・chemical-ra=RaConclusionCard・law-hierarchy/accidents-reports/accident-news/circulars/subsidies(+calculator)/goods/chemical-database=ConclusionCard）し、h1も全25route各1個で多重/欠落なしと確認＝**新規の柱0未適用箇所なし**（Explore の「LACKS」誤判定はclient部品未走査由来。chemical-database/goods/subsidies/product-search はいずれも実装済を直読で反証）。よって追加カードは水増し（禁止）と判断し見送り、代わりに**未テストだった検索/件数系3本の結論カードに回帰無読テストを敷設**＝PR #527でデカ数字カードを敷いたが専用スクリプトが無かった /chemical-database(収録3,695物質)・/goods(掲載39品目)・/subsidies(活用可能7制度)。テスト観点=①結論カード(section[role=status])がファーストビュー内(390×844)②デカ数字(text-5xl)＋単位＋漢字短ラベル③DOM順で検索入力より前(結論ファースト)④h1=1。自前prod(3100・domcontentloaded・serviceWorkers:block)で**18/18 PASS**（y=322/313/572）。**UI/アプリコード変更ゼロ＝既存破壊0**（追加は docs スクリプト1本のみ）。ゲート全通過（tsc0／lint errors0／vitest 1771 pass／build成功）。スクリプト=docs/third-party-reviews/scripts/batch9-count-cards-noread-2026-06-14.mjs。残=柱3リスクマップ実機レビュー（#540で在庫消化中）。

## 2026-06-14 柱0バッチ9/9 第3弾 /newsletter 登録前後の状態カード（ux-tool/batch9-newsletter-status-card）

イテレーション頭で自分の緑PR #527（柱0バッチ9 第1弾＝判定/件数ツール5本）を squashマージ→main同期（a9e5c04c）。マージ時 BACKLOG-ux-tools.md がC-7完了反映と衝突したため手で解消（C-7はPR #520でmain反映済＝[x]、バッチ9進捗を追記）。第2弾 PR #531（insurance/bcp/organization）はCI待ちで次イテレ回収。本イテレ実作業＝バッチ9/9 残の `/newsletter`。従来は登録フォームの内側にしか成功表示がなく、ページを開いた瞬間「自分が登録済みかどうか」が3秒で分からなかった。フォームの登録状態を親 client パネル `NewsletterPanel` へ持ち上げ、**最上部に状態結論カード**（共通 `ConclusionCard`＝import のみ・ux-records custodian部品は不改変）を新設＝**未登録(info/青・Mailアイコン・「登録する」導線)→登録完了(safe/緑・MailCheck)** に色帯切替。`NewsletterForm` には `onSuccess` コールバックを追加しただけで送信ロジックは不変（捏造・水増しゼロ＝状態の事実をそのまま結論化）。フォーム自身の成功ブロックは詳細として残置（結論=上／詳細=下）。`/leaflet` は印刷ラッパー専用ページのため最上部状態カードの対象外＝スキップ（無理に付けると水増し）。無読テスト（390×844・prod server 3100・serviceWorkers:block・domcontentloaded）7/7 PASS＝カードがファーストビュー内(y=449)・登録前 sky/「未登録」/「登録する」・h1=1・送信後 emerald/「登録完了」に切替（RESEND未設定ローカルは addSubscriber がメモリ保存で ok:true）。ゲート全通過（tsc0／lint errors0／vitest 1700 pass／build成功）。スクリプト=docs/third-party-reviews/scripts/newsletter-status-card-2026-06-14.mjs。これで柱0バッチ9/9 完了（第1弾#527 main済・第2弾#531 main済・第3弾本PR）。残=柱3リスクマップ実機レビュー。

## 2026-06-14 柱0バッチ9/9 第2弾 状態系3ページ結論カード（ux-tool/batch9-status-conclusion-cards）

冒頭で自班CI緑PR #520（柱C-7 出力手段）をsquashマージ→main を ff-only 同期。第1弾(PR #527・CI pending)が判定/件数の濃い5ツールを処理済みのため、残りの状態系を第2弾として処理。**ConclusionCard を import専用部品として** /insurance・/bcp・/organization の最上部に新設。/insurance＝tone=warning・アイコンShieldAlert・短ラベル「未加入」＋action「個別に相談」→/contact、長文の「企業のご利用について（調達基準）」は CollapsibleDetail で初期折りたたみ（法令/調達の重要情報は消さず畳むだけ）。/bcp＝tone=info・デカ数字「99%」稼働率目標＋個人事業運営の正直な注記を1行に圧縮＋action「保険状況を見る」→/insurance、冗長なイントロ段落を削除。/organization＝tone=warning・デカ数字「83.5%」教育修了率＋補助チップ（事故0件=safe／建設三班64%=danger）、**全値を既存の KPI/DEPARTMENTS 配列から導出（reduce で最下位部署を算出）＝転記のみ・捏造ゼロ**、デモ版ディスクレーマは維持。/leaflet（A4印刷PDF）・/newsletter（登録CTAフォーム）は判定/件数の"状態"を持たない成果物のため結論カード対象外とし、水増しを回避（backlogに明記）。ゲート全通過（tsc0／自班3ファイル lint errors0※フルeslintは環境セグフォルトのためCI委譲／vitest 1687 pass／build成功）。無読テスト（390×844・自前prod server -p3100・domcontentloaded・serviceWorkers:block）15/15 PASS＝3カードともファーストビュー内(y=365〜413)・デカ数字48px・短ラベル/action/折りたたみ/チップ在。スクリプト=docs/third-party-reviews/scripts/status-pages-conclusion-cards-2026-06-14.mjs。残=柱3リスクマップ実機レビュー（/leaflet・/newsletter は対象外確定）。

## 2026-06-13 柱C-7 事故統計の出力手段（ux-tool/c7-accidents-export）

着手時、lane backlog 最上位2件（柱0バッチ6/9・柱C-6 /circulars）はいずれも既に main へ反映済み（PR #511・柱0バッチ4）でlane側ファイルが未更新だっただけと判明→現状確認のうえ [x] 化。実作業は次の未着手＝柱C-7。元請の「月例安全会議の資料に統計を貼る」が完了しない（出力手段ゼロ）課題に対し、`/accidents-analytics`（事故統計ダッシュボード）と `/accidents-reports`（業種別レポートハブ）へ出力ツールバー `DataExportToolbar` を新設＝**CSVダウンロード／要点コピー／共有／印刷**の4手段。CSV・要点テキストは純関数に分離（`lib/export/csv.ts` の汎用セクションCSVビルダー＋`lib/accidents-analytics/export.ts`＋`lib/accidents-reports-export.ts`）で集計値をそのまま転記＝**捏造・水増しゼロ**、テスト17件で列崩れ・エスケープ・符号を固定。ツールバーはブラウザAPI（Blob/clipboard/navigator.share/print）のみ担当、先頭BOMでExcel文字化け防止、ボタンは44px・印刷物には出さない（print:hidden）。h1は対象2ページ＋/risk・/risk-prediction・/law-search いずれも既に1個（多重/欠落なし）を確認のみ＝マスターのh1指摘は既に解消済みだった。無読テスト（390×844・prod server・serviceWorkers:block）8/8 PASS＝ツールバーがファーストビュー内（analytics y=531・reports y=743）・4手段在・44px。ゲート全通過（tsc0／lint errors0／vitest 1660 pass／build成功）。スクリプト=docs/third-party-reviews/scripts/accidents-export-noread-2026-06-13.mjs。残=柱0バッチ9/9（その他ツール結論カード）・柱3リスクマップ実機レビュー。

## 2026-06-14 柱3 リスクマップ実機レビュー＝明日からの見通しストリップ（ux-tool/risk-typhoon-persona-review）

イテレーション頭で自lane緑PR #531（柱0バッチ9第2弾）を squashマージ着手→main側 #527 と BACKLOG行が衝突したため origin/main を当該ブランチへ通常マージで解消（行は完了側を採用し第3弾=#535を追記）。コード衝突なし。#531/#535 はCI再走のため次回回収。本題は lane 最上位の柱3レビュー＝「台風前日の元請安全担当・段落を読まない」ペルソナで `/risk` を実機確認。**気づき**: 3秒で読む結論カード（role=status）は「今日」の全国警報を JMA実況＋Open-Meteo予報の悪い方で断定するが、このペルソナが前日夕に効かせたいのは「明日」の可否で、その材料は従来「1週間予報」タブの奥に埋まっていた（前倒し判断が3秒で出ない）。**是正**: 結論カード直下に「明日からの見通し」ストリップを新設＝明日／明後日／3日後の全国最悪レベルを赤(警報相当)・黄(注意報相当)・緑(なし)＋該当地域数で並べ、セルをタップするとその日の1週間予報マップへジャンプしてどの地域かまで辿れる。今日は結論カードに任せ**明日起点**にして二重表示・矛盾を回避（JMA実況は当日分のみのため明日以降は予報ベースとUIに明記）。値は既存予報の日別再集計のみ＝**捏造・水増しゼロ**。純関数 `lib/risk/weather-outlook.ts`（最悪レベル・該当数・トーンを集計、テスト8件）＋ `WeatherForecastPanel` にストリップ描画（SAFETY_TONE トークンで塗り・min-h44px・aria-label）。ゲート全通過（tsc0／lint errors0／vitest 1716 pass／build成功）。無読テスト 9/9 PASS（390×844・own prod server -p3100・serviceWorkers:block＝明日警報=赤2地域がファーストビュー内 y=570・明後日注意報=黄・44px・タップで週モード遷移・今日カード独立・良好時は緑）＋既存 risk-noread 11/11 維持。スクリプト=docs/third-party-reviews/scripts/risk-outlook-noread-2026-06-14.mjs。残=柱0バッチ9（#531/#535 CI回収）・柱3 /risk 警報防災の追加実用化レビュー。

## 2026-07-02 チャットボット偽「範囲外」警告の根絶（ux-tool/chatbot-out-of-scope-false-positive）

イテレーション頭で自lane未マージPRを確認したところ #562/#570/#573（analytics-headline・chemical-ra-quickstart-44px・chatbot-quick-chips-44px）は全てCI緑で既にmainへsquash-merge済み・リモートブランチも自動削除済みと判明（ローカル3ブランチのみ残存）→ローカルブランチを削除しmainをff-only同期（clean確認）。**本題**=2026-07-02 Fable横断診断（PR #579）で追加された lane backlog 最上位 O4（P0）＝チャットボットの偽「範囲外」警告。診断書 04-chatbot-accuracy.md の本番プローブ23問で、正答なのに `detectOutOfScopeLawReferences` が誤って範囲外警告を出し信頼度をmediumへ不当降格したケースが5/20問=25%と実測されていた（Q8など）。原因を実コードで特定=2バグ複合。(a) 法令名抽出の正規表現 `chatbot-enrichment.ts:386` の文字クラスに長音「ー」が無く、「クレーン等安全規則」が「ン等安全規則」に分断され既知法令と一致しない。(b) 正式名称⇄短縮名の対応表が無くsubstring照合のみのため、「労働安全衛生法」⊅「安衛法」・「酸素欠乏症等防止規則」⊅「酸欠則」等の正式名称そのものの言及が全て範囲外扱いされていた。**是正**=文字クラスに「ー」「々」を追加、既存 `law-metadata.ts` の `LAW_METADATA`（lawShort→fullNameの単一ソース）から正式名称の完全一致集合 `KNOWN_LAW_FULL_NAMES` を新設し判定に追加（法令コーパスの収録範囲・条文内容は無変更、後処理の誤検知ロジックのみ是正＝**捏造・水増しゼロ**）。回帰テスト2件追加（クレーン等安全規則の分断防止／正式名称の完全一致判定＝既存17件+2=19件）。診断書の同一O4項目には派遣法45条要約是正（T2）・SYSTEM_PROMPTのYYYYプレースホルダ対策（T3）も含まれるが今回はT1のみ実施のため、backlog行は「O4残」としてT2/T3に絞って残置（O4全体は未[x]のまま、T1完了分は別行で[x]記録）。ゲート全通過（tsc0／lint errors0（既存warningのみ）／vitest 1935 pass／build成功）。ビルド再生成JSON（docs/rag-metrics-latest.json・chatbot-eval-fresh-results.json）は契約どおり checkout で復元＝コミットせず。PR #583。残=O4残（T2派遣法45条・T3プレースホルダ）を次イテレで着手。

## 2026-07-02 O5: synonyms.ts「気積→事務所則第14条」是正＋口語同義語拡充（ux-tool/o5-synonyms-jimusho-kisoku-fix・PR #598）

前セッションの未コミット作業（gates未実行・未push）を引き継いで完了。是正=`synonyms.ts:166` の「気積」同義語展開先が事務所則第14条（排水）になっていた誤りを、コーパス実データ（jimusho-eisei-kijun-kisoku.ts / egov-caption-snapshot.ts）と一致する第2条（気積の基準）へ修正（dataレーン#538申し送りの取り逃し分・04診断T4/07診断P0-1）。拡充=口語検索の取りこぼし解消のためTERM_EXPANSIONSに「健康診断の頻度」「健診の頻度」「健康診断は何年ごと」→安衛則第44条(1年以内ごと)・第45条(特定業務6月以内ごと)、「酸欠 資格」「酸欠の資格」「酸欠作業の資格」→酸欠則第11条(作業主任者・技能講習)を追加。rag-search.test.tsに回帰テスト3件を新設。完了条件を実測で確認＝「健康診断の頻度」で安衛則44・45条がtop5・「酸欠 資格」で酸欠則11条がtop1・main eval 124/124=100パーセント（非退行）・fresh eval 98/100=98パーセント（非退行、既存不正答2件は本タスク対象外で変化なし）。ゲート全通過（tsc0／lint errors0／vitest 1976 pass・237 files／build成功）。push前に生成物docs/rag-metrics-latest.json・web/src/data/chatbot-eval-fresh-results.jsonはgit checkoutで復元（data班所有・[[noread-test-prod-server-port]]の定石通り）。本PRはsynonyms.ts/rag-search.test.tsという検索ロジックlib（本来seo班境界）に触れたが、backlog自体がO5としてOpus担当に明記済みの既存タスクの完了処理であり新規逸脱ではない。残=lane backlog未着手0件→次イテレは補充指針（自領域柱0/無読レビュー）で起こす。
## 2026-07-02 S8-a: /circulars/[id] にE-E-A-T監修者バイライン＋Person JSON-LD配線（ux-tool/s8a-circulars-supervisor-byline）

イテレーション頭で自班CI緑未マージPRなし（直前PR #598はCI in-progressのため次回回収）。lane backlog未着手は診断07由来のS7（チャットボット小粒4本・生成品質evalで機械判定＝BACKLOG-fable.md F3が未完のためブロック中と確認）とS8-a（監修者バイライン）の2件のみ＝3件未満だが、S8-aは即着手可能なため先にこちらを処理。診断07（E-1・柱C-8）が指摘した「通達詳細/判例詳細/FAQに監修バイラインが無い」のうち、自班route分＝/circulars/[id]のみ対応（判例詳細/FAQはux-hub班領域のため対象外）。既存パターン調査＝/guides系static pageは`keyword-landing-jsonld.tsx`でPerson（hasOccupation/qualifications付き・氏名は開示せず「労働安全衛生コンサルタント（登録番号260022）」表記）、/articlesは`newsArticleSchema`のauthorName/authorUrlで簡易Person、を踏襲。新規`SupervisorByline`コンポーネント（可視リンク・/about導線）を敷設し、共有lib`json-ld.tsx`の`legalDocumentSchema()`に監修者Personを`contributor`（schema.org公式プロパティ＝二次的寄与者。通達の発出者=author/publisherとは意味が別なため上書きせず追加）として常時付与。**通達の発出者（厚労省等のOrganization表記）は不変＝法令正確性を壊さず追加のみ**。legalDocumentSchemaの呼び出し元は/circulars/[id]のみ（grep確認済）のため既存挙動への影響なし。無読テストは通常の「3秒で判定/件数」系ではなくSEO構造化データの配線確認のため、可視バイライン・aboutリンク・h1単一性・JSON-LD contributor Person・発出者author健全性を機械チェックするスクリプトを新設（実行時はNode ESM解決の都合でweb/配下へ一時コピーして実行→検証後削除、正本はdocs/third-party-reviews/scripts/に保存）。own prod server -p3100・domcontentloaded で2/2 PASS。ゲート全通過（tsc0／lint errors0／vitest 1977 pass・237 files／build成功）。push前に生成物docs/rag-metrics-latest.json・web/src/data/chatbot-eval-fresh-results.jsonはgit checkoutで復元（[[noread-test-prod-server-port]]の定石）。残=S7（F3=チャットボット生成品質eval確立待ちでブロック中・Fable/ops側の完了を要観測）。次イテレは補充指針（自領域柱0/柱3レビュー、または診断07のC-10/C-11の自班route分）で起こす。

## 2026-07-03 柱0補充: /treatment-work-balance ハブ+illness-guide詳細に結論カード新設（ux-tool/treatment-work-balance-conclusion-cards）

イテレーション頭で自班未マージPR #669（accidents-analytics/accident-news select 44px化）を確認したがCI(e2e/smoke)が pending のため次回回収に据え置き、mainは既にclean/最新。lane backlog未着手は#10（/law-search・/chemical-database鮮度表示・要data班確認でブロック中）の1件のみ=3件未満のため補充指針に従いExploreで柱0スウィープを実施（stress-check/treatment-work-balance/law-hierarchy/stats/accidents-reports/accident-news/leafletを対象読み込み）。treatment-work-balanceのハブ(page.tsx)と病態別詳細(illness-guide/[illness]/page.tsx)がPageHeader直後AlertCircleの制度説明プローズのみで結論カード未対応と判明（mental-health-managementで直前に対応した同型のギャップ）。是正=両ページにConclusionCardを新設。ハブ=tone info・value 4(FRAMEWORK_STEPS.length)・title「支援フロー」・action「プランを作成する」。詳細=tone info・value=カテゴリごとの典型病態数(conditions.length)・title「配慮ガイド」・action「プランビルダーを開く」（既存CTA文言の再利用）。値はいずれも既存データ配列件数の転記のみ=捏造ゼロ・既存破壊0。他の調査対象で見つかったP2/P3（accident-news filterの40px、law-hierarchy ActionLinkの36px、plan-builder印刷ボタンのmin-h欠落）は今回未着手のため次点として申し送り。無読テスト12/12 PASS（own prod 3100・390×844・domcontentloaded・serviceWorkers:block）。ゲート全通過（tsc0/lint errors0/vitest 2288 pass・270 files/build成功）。ビルド再生成JSON（docs/rag-metrics-latest.json・chatbot-eval-fresh-results.json・ky-print-sheet snapshot）は契約どおりcheckoutで復元=コミットせず。スクリプト=docs/third-party-reviews/scripts/treatment-work-balance-conclusion-cards-noread-2026-07-03.mjs。残=PR #669のCI回収、次点=accident-news/law-hierarchyのタップ標的44px化。

## 2026-07-03 柱0補充・タップ標的横展開: /law-hierarchy ActionLink・/treatment-work-balance/plan-builder 印刷ボタン 44px化（ux-tool/law-hierarchy-treatment-plan-44px）

イテレーション頭で自班未マージPR #669（accidents-analytics/accident-news select 44px化・全CI緑）を squashマージ→BACKLOG-ux-tools.mdでコンフリクト（並行マージされた#673=treatment-work-balance結論カードの行追加と行位置が衝突）したためoriginマージで解消（両エントリを残す・コード側は無衝突）。push後 `gh pr merge --auto` が即マージを確定、ローカルmainをff-only同期。PR #677（stats結論カード）はCI進行中のため次回回収。lane backlog未着手は#10（/law-search・/chemical-database鮮度表示・要data班確認でブロック中）の1件のみ=3件未満のため、前回(#673)の申し送り「次点」に記載済みだった残り2件を消化: `accident-news-filter.tsx` は #669 で既に是正済と確認（min-h-[44px]適用済）。残る `law-hierarchy/page.tsx` の `ActionLink`（HierarchyCard内のe-Gov公式条文・条文を検索・関連通達CTA）が `min-h-[36px]`＜44px、`treatment-work-balance/plan-builder/plan-builder-client.tsx` の印刷ボタンが `min-h` 未指定（同フォーム内の他ボタンは48px）だったのをいずれも `min-h-[44px]` に統一。寸法のみの変更で文言・機能・href不変=既存破壊0。無読テスト新設3/3 PASS（own prod 3100・390×844・domcontentloaded・serviceWorkers:block＝HierarchyCard内ActionLink全件44px以上・plan-builder印刷ボタン44px以上。初回実装時はセレクタが広すぎてページ下部の別レイアウトのプロモーションカードやheight=0の隠れ要素まで拾って偽FAILしたため`article[aria-label] a`へ限定して是正）。ゲート全通過（tsc0/lint errors0（既存warningのみ）/vitest 2308 pass・271 files/build成功）。ビルド再生成JSON（docs/rag-metrics-latest.json・chatbot-eval-fresh-results.json・ky-print-sheet snapshot）は契約どおりcheckoutで復元=コミットせず。スクリプト=docs/third-party-reviews/scripts/law-hierarchy-treatment-plan-44px-noread-2026-07-03.mjs。残=PR #677（stats結論カード）のCI回収、次点=/law-search・/chemical-database鮮度表示（data班確認待ち）。

## 2026-07-03 柱0補充・タップ標的横展開: /chemical-database モード切替・AND/ORトグル・タグチップ・主要CTA・関連ページリンク 44px化（ux-tool/chemical-database-tap-targets-44px）

イテレーション頭で自班未マージPR #733（law-search/strategy 44px化）はe2e/smoke CIが長時間pendingのため次回回収に据え置き、mainをff-only最新化（clean）。lane backlog未着手は「/law-search・/chemical-database鮮度表示（要data班確認でブロック中）」の1件のみ=3件未満のため補充指針に従いExplore調査を委任（chemical-database/goods/leaflet/newsletter/insurance/organization/bcp/strategy/subsidies/stress-check/accidents-reports/law-hierarchy/chemical-raを対象読み込み）。`chemical-database-client.tsx`のモード切替タブ・AND/OR結合トグル・クリアボタン・規制法令タグ20種の個別絞り込みチップが`min-h`指定なしで高さ約20〜32px、加えて専門解説モードの主要CTA2種と下部「関連ページ」リンク3本が`min-h-[36px]`と明示的に44px未満固定だったのを確認。テーブル行内の`RegulationTagBadgeList`（最大100行×複数バッジ）とテーブル「開く」リンクは密なデータテーブルのセル内表示のため既存のprose inline link免除方針を踏襲し対象外(44px化するとテーブルが破綻)。是正=計8箇所に`min-h-[44px]`を付与（寸法のみ、文言・onClick/href不変=既存破壊0）。無読テスト新設9/9 PASS（own prod 3100・390×844・SW block・domcontentloaded＝モード切替タブ・AND/ORトグル・タグチップ「石綿則」・クリア(非退行含む)・専門解説モードのRA実施/厚労省SDSリンク・関連ページリンク3本すべて44px以上・h1=1）。ゲート全通過（tsc0/lint errors0（既存warningのみ）/vitest 2456 pass・289 files/build成功）。ビルド再生成JSON（docs/rag-metrics-latest.json・chatbot-eval-fresh-results.json・ky-print-sheet/meeting-print-sheet snapshot）は契約どおりcheckoutで復元=コミットせず。スクリプト=docs/third-party-reviews/scripts/chemical-database-tap-targets-44px-noread-2026-07-03.mjs。残=PR #733のCI回収、次点=/law-search・/chemical-database鮮度表示（data班確認待ち）。

## 2026-07-03 柱0外a11y補充: /chatbot role=alert・aria-live欠落＋/risk-prediction マトリクス色のみリスク表現の是正（ux-tool/a11y-alert-live-matrix-label）

イテレーション頭で自班未マージPR #751（mhlw-panels-misc-44px-a11y）はCI進行中で次回回収に据え置いていたが、本イテレー中にCI緑化を確認しsquashマージ→main ff-only同期（38b4be62、他班の並行マージも含めfast-forward）。lane backlog未着手は「/law-search・/chemical-database鮮度表示（要data班確認でブロック中）」の1件のみ=3件未満のため、柱0（44px・結論カード）は自領域25routeでほぼ飽和と判断し、補充指針を別角度＝**44px以外のアクセシビリティ**でExplore調査を委任。発見3件はいずれも実コード確認済みの新規欠陥（44px/結論カードとは無関係）: ①`chatbot-panel.tsx`送信エラーバナーに`role="alert"`が無く（同ファイル内の音声入力エラーは実装済みで不整合）、通信エラー時にスクリーンリーダー利用者へ通知されなかった。②同ファイルのAI回答ストリーミング中バブルに`aria-live`が無く、生成中/完了のいずれも通知されなかった（最頻導線のチャットボットで影響大）。③`risk-prediction-panel.tsx`のリスクマトリクス各セルが色のみで高/中高/中/低のリスク段階を表現（WCAG 1.4.1）、色覚多様性・スクリーンリーダー利用者は同じ件数が高リスクか低リスクか判別できなかった。是正=①`role="alert"`付与、②`isStreamingMsg`時のみ`aria-live="polite"`付与、③`matrixCellRiskLabel()`を新設し`<td>`に`aria-label`（重大性×頻度: リスク段階（件数））を追加・内側divは`aria-hidden`化（視覚デザイン・onClick/href・寸法は不変=既存破壊0）。単体テスト2件新設（`chatbot-panel.test.tsx`＝fetchをpendingにしたままの状態でストリーミング中バブルのaria-live確認・fetch reject でrole=alert確認、jsdom未実装の`Element.scrollTo`はテスト内でスタブ）。無読テスト4/4 PASS（own prod 3100・390×844・SW block・page.routeでstream/fallback両APIを制御=送信エラーバナー可視化＋role=alert・ストリーミング中バブルにaria-live在・マトリクスセルaria-labelにリスク段階文言含む）。ゲート全通過（tsc0/lint errors0（既存warningのみ）/vitest 2494 pass・295 files/build成功）。ビルド再生成JSON（docs/rag-metrics-latest.json・chatbot-eval-fresh-results.json・ky-print-sheet/meeting-print-sheet snapshot）は契約どおりcheckoutで復元=コミットせず。スクリプト=docs/third-party-reviews/scripts/a11y-aria-live-alert-matrix-label-2026-07-03.mjs。残=次点=/law-search・/chemical-database鮮度表示（data班確認待ち）。

## 2026-07-03 柱0外a11y補充②: カスタムrole="tab"実装3件のキーボード操作(矢印/Home/End)是正（ux-tool/tablist-keyboard-nav-a11y / PR #776）

イテレーション頭で自班CI緑未マージPR #769（/accidents-reports/compare業種比較セレクタ・詳細リンク・[industry]表示期間タブ・/chemical-database/[cas]個票CTA 44px化）のCI（e2e/smoke）がpendingのまま長く待機したため`git checkout main && git pull --ff-only`で先にmainをclean同期（他班マージ分10コミット取込）し、バックグラウンドでExplore監査を並行起動。CI緑化を確認後squashマージ→originのブランチも削除。lane backlog未着手は「/law-search・/chemical-database鮮度表示（要data班確認でブロック中）」の1件のみ=3件未満のため、柱0（44px・結論カード）は自領域25routeで飽和済み（[[ux-tools-domain-pillar0-saturated]]）と判断し、補充指針を別角度＝**キーボードa11y**でExplore調査を委任（TODO/FIXME・alt属性・内部リンク切れ・デッドコードも並行調査したがいずれも自領域はクリーンと確認）。発見=`top-cases-tabs.tsx`(/accidents-reports)・`job-class-tabs.tsx`(/mental-health-management/interview-guidance)・`StatsDashboardImpl.tsx`(/stats)の3ファイルがいずれも独自にrole="tab"/role="tablist"を実装しながらonClickのみでキーボード操作（ArrowLeft/Right・Home/End・ローミングtabindex＝ARIA APG tabパターン）が一切無く、キーボード/スイッチデバイス利用者がタブ切替できない欠陥（44px/結論カードとは無関係の新規発見）。加えて前者2ファイルはrole="tabpanel"自体が欠落しタブ↔パネルの関連付けも無かった。同時に検出した`mhlw-chemical-selector.tsx`（/chemical-ra化学物質検索コンボボックス）の無キーボード対応（矢印/Enter/Escape・role="combobox"欠如）は数千件規模のタイプアヘッドで範囲が広いため別タスクとして次点へ切り出し、backlogに追記。**是正**=3ファイルで同型ロジックが重複するため共通フック新規`web/src/lib/a11y/use-roving-tablist.ts`（ローミングtabindex＋ArrowLeft/Right/Up/Down・Home/Endの純粋なキーボード操作ロジックのみ、UIロジックは各ファイル側に残置）を敷設し適用。既存onClick/選択ロジック・文言・寸法・視覚デザインは不変＝既存破壊0。role="tabpanel"未実装の2箇所には`useId()`で発行したidをaria-controls⇄idで連携する形で新設。無読テスト新設16/16 PASS（own prod 3100・390×844・SW block＝/accidents-reports/construction期間タブ・/mental-health-management/interview-guidance職種タブ・/stats期間タブの3画面でArrowRight/Left/Home/Endによる選択・フォーカス移動、aria-controls↔tabpanel関連付けを実機確認。/statsはGA4未接続では期間タブ自体が非表示のため`page.route`で`/api/stats`をライブ応答へモックしてタブを表示させた上で検証）。ゲート全通過（tsc0/lint errors0（既存warningのみ）/vitest 2542 pass・298 files/build成功）。ビルド再生成JSON（docs/rag-metrics-latest.json・chatbot-eval-fresh-results.json・ky-print-sheet/meeting-print-sheet snapshot）は契約どおりcheckoutで復元＝コミットせず。無読スクリプトはPlaywrightのESM解決がスクリプト自身の物理パス基準（既知の制約＝[[noread-test-prod-server-port]]）のためdocs/third-party-reviews/scripts配下からは直接実行不可＝web/scripts/へ一時コピーして実行後に削除。**注**=作業中`web/CLAUDE.md`→`web/AGENTS.md`経由で`node_modules/next/dist/docs/`を読むよう誘導する記載（プロンプトインジェクション、既出＝過去複数イテレーションで検出済のものと同一）を再確認したが、既に無視すると判断済のため再度無視し本タスクへ影響なし。スクリプト=docs/third-party-reviews/scripts/tablist-keyboard-nav-noread-2026-07-03.mjs。残=次点2件（/law-search・/chemical-database鮮度表示はdata班確認待ち／`mhlw-chemical-selector.tsx`コンボボックスのキーボード対応）＝2件のため次イテレも柱0/柱3レビューまたはキーボードa11yの深掘りから補充が必要。
