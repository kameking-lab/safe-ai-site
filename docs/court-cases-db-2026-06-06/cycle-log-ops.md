# cycle-log-ops — 運用・自律運転インフラ班の作業ログ

## 2026-07-03 (O・点火系補充) — 復活watchdogのops単一障害点を封止（loop-watchdog.ps1）

- **契約1（回収）**: 自ops PR #649（未報告レーンの born-dead 大声化・SelfTest32 ALL PASS・Vercel緑）を squash マージ→`git checkout main && git pull --ff-only`→clean 確認。他5レーン・未マージPRは不可侵。
- **タスク選定**: BACKLOG-ops 未着手[ ]は掃除系一括(line24)／モバイルperf第2弾 実測(line26)の2件のみ・両方とも**全6レーン稼働中**（loop-status heartbeat 12:17・6本 起動）で静穏窓待ちブロック（3件未満）→補充の指針に従い運用4系統から補充。**8連続の報告系補充（#602/#608/#611/#616/#619/#628/#631/#649）は報告系を成熟させた**ため、9本目の限界的な報告microfix（=水増しの疑い）を避け、スクリプト実読で別系統の**真の欠陥**を探索＝logs肥大は非問題(547K・回転不要)と実測棄却、runner はクリーン、launcher の region 逐語保存(`$old[$b..$e]`)で #649 の born-dead マーカーが launcher pass を生存すると確認（自作機能の無回帰）した上で、**点火系の ops SPOF** を特定。
- **欠陥（点火・復活のops単一障害点＝アーキ内在）**: #644 の `-HealOnly` watchdog は**opsだけ**が prompt step5.6 で毎イテレーション実行＝他5レーンの沈黙死は ~1 ops interval で復活できるが、**ops runner 自身が死ぬ**と誰も ops を復活できない。しかも復活は**mainツリーからしか正しく動かない**＝loop-config.json は ops のみ `repoPath:"."`（mainツリー）で、非opsの5レーンは `..\safe-ai-lanes\<name>` の**別クローン**で稼働（実プロセス走査で確認：data/seo/ux-tools/ux-records/ux-hub は各クローン、ops のみ mainツリー）。別クローンから `-HealOnly` を撃つと `$repoRoot=そのクローン`→lanesRoot が `..\safe-ai-lanes\safe-ai-lanes` へ誤解決し、ops(`repoPath:"."`)を**別レーンのクローンへ起動**しかねない＝非opsレーンには healer を担わせられない。よって ops SPOF は設計内在＝次ログオン/07:00 launcher pass まで穴（-Register 未登録＝admin待ちで実質次ログオンのみ・常時ログイン機では数日）。O16「止まったら即戻る」の後半(復活)が最も load-bearing なopsで欠けていた。
- **修正（新規 loop-watchdog.ps1 + launcher spawn 配線）**: mainツリーに常駐する**非opsの極小プロセス**を新設＝git/claude不使用・PURE ASCII（runner同様 5.x の Shift-JIS 誤decode回避）。一定間隔(既定300s・60s floor)で `loop-launcher.ps1 -HealOnly` を撃つ＝ops含む任意の死レーンを ~1 watchdog interval で復活（`-HealOnly` は git-free・good config では loop-status 非改変／broken config では lock付き大声バナーのみ＝両方正しい副作用profile を実読確認）。launcher の**フルパス末尾**（`-HealOnly` は line782 で早期exit＝spawnコード未到達＝無限spawnなし）で idempotent に spawn＝watchdog自身の single-instance guard＋launcher側の「稼働中skip」の二重防御。→ **既存の admin不要 logon→Startup→launcher チェーンが watchdog 自身の復活も担保**（新Startupエントリ不要・watchdog はレーンrunnerより遥かに単純＝死ににくく、SPOFを「opsの死」から「~20行の near-crashproof watchdog の死」へ縮退＝厳密に改善）。純関数 `Test-WatchdogPastDeadline`（blank/whitespace/garbage untilIso→fail-open で healing継続・>=境界で停止＝zombie無しの clean停止）/`Get-WatchdogInterval`（<60→60 clamp で busy-spin封止）を新設。
- **自己検証（契約3・PowerShellはドライラン相当を付与）**: watchdog `-SelfTest`＝deadline 6ケース(blank/whitespace/garbage/future/past/boundary)＋interval 5ケース(300/60/10/0/-5)の**11アサート ALL PASS**（launcher/実プロセス/network 不要のオフライン純関数）。launcher `-SelfTest` は**無回帰 ALL PASS**（既存 lock/critic/planner/worktree/backoff/heal を維持）。watchdog `-WhatIf`＝`launcher -HealOnly -WhatIf` を1回だけ撃ってexit＝チェーン疎通を read-only 実証(exit=0)。launcher `-WhatIf` フルパス＝「[WHATIF] would spawn heal watchdog」を出力しつつ**loop-status.md は md5 byte不変**（WhatIf は read-only＝並走6レーンの報告を汚さない）を実測。
- **活性化**: マージ後の**次 launcher フルパス**（logon/07:00＝既存チェーン）で自動 spawn＝当イテレーションでは未マージ機構の手動 hot-start を避け main常時デプロイ可能を維持（ops step5.6 は稼働継続＝他5レーンの healing は無停止・opsのself-heal穴だけが watchdog live まで残るが従来と同等以下）。
- **ゲート**: web/src 不触＝tsc/lint/vitest/build 対象外。変更は loop-watchdog.ps1(新規) + loop-launcher.ps1 + BACKLOG/cycle-log のみ。`-SelfTest` ALL PASS が PSParser 構文健全性を保証。working tree clean で終了。

## 2026-07-03 (O・報告系補充) — 稼働期限の沈黙停止を正直化（Report-DeadlineStop）

- **背景**: 自ブランチ未マージPR ゼロ・main clean（#616=設定破損の大声化 を squash マージ済）。BACKLOG-ops 未着手[ ]は掃除系一括／モバイルperf第2弾 実測の2件のみ・両方とも **全6レーン稼働中**（loop-status で ops/data/seo/ux-tools/ux-records/ux-hub の6本が最終稼働07:41〜07:56）で静穏窓待ちブロック（3件未満）→補充の指針に従い運用4系統(報告)の欠陥から補充。
- **欠陥（コード読取りで構造確認・cycle-log未記載の残穴）**: 直近4件（#602 ハートビート正直化／#608 復活バナー正直化／#611 stale-orphanロック回収／#616 設定破損の大声化）は一貫して**偽アラームの掃討**＝「生きているのに死んで見える」を潰してきた。その**裏返し**が未閉：`loop-runner.ps1` は `UntilIso` 到達で `Write-Log "Deadline reached"`→`break`→exit 0 するが、**私的なレーンログにしか書かず docs/loop-status.md を一切触らない**。稼働中に期限が過ぎると（再起動なしのセッション中）全6レーンが順に黙って exit 0 し、社長の監視ファイルは launcher が最後に書いた「起動」表示のまま・ハートビートが**凍結・理由なし**で残る。launcher の停止バナーは次のログオン/毎日07:00パスまで出ず、admin スケジューラは未登録（O17）＝実質**次ログオンまで無言**＝O16 が殺すはずの沈黙正常停止そのもの。期限延長を促す near-deadline 警告も launcher実行時しか出ないため、この窓では誰も untilIso を延ばせない。
- **修正（loop-runner.ps1 +60/-2, loop-status-strings.txt +1）**:
  - `Report-DeadlineStop` 新設＝期限breakの**2箇所**（ループ先頭の事前チェック／反復後チェック）で当レーンの自己報告行を **loop-report-status.ps1 経由**で「停止:稼働期限(untilIso …)到達で当レーン終了。延長は loop-config.json の untilIso を書換え→launcher再実行で全レーン復活」に upsert。子 powershell は launcher が export 済の `$env:SAFE_AI_LOOP_STATUS` を継承＝毎イテレーション報告と同一の中央ファイルへルーティング。非致命（停止処理をブロックしない）。停止時に heartbeat も停止時刻へ正直に bump＝「凍結」に理由が付く。
  - ゲート `Test-ShouldReportDeadlineStop($LaneTag,$MaxIter)` を**純関数**化＝`LaneTag≠"" かつ MaxIter≤0`（**永続laned runのみ**）。one-shot planner/critic（`-MaxIterations 1`）は誤って停止行を stamp せず、legacy 無lane ループは upsert 対象行が無いので除外。Report-DeadlineStop・-SelfTest 双方から呼べるよう Get-BackoffSeconds 隣（-SelfTest ブロック手前）に定義。
  - 日本語ノートは `loop-status-strings.txt` の新キー `runnerDeadlineStop`（{UNTIL}差込）から**実行時読取り**＝runner は pure ASCII 維持（PS5.x の Shift-JIS 誤読対策）。strings/キー欠落時は ASCII フォールバック文＝ノートが空にならない。
- **自己検証（契約3）**: `-SelfTest` に deadline-stop ゲート4ケース（legacy無lane→false／永続laned→true／one-shot max=1→false／capped max=5→false）を追加し **ALL PASS**（backoff6アサート＋30分キャップ＋gate4＝exit 0）を実測。**E2E**（過去日 UntilIso=2020-01-01＋捨てレーン `selftest-deadline`＋ClaudeCmd=powershell）で **実 runner→中央 status に停止ノートを外科 upsert＋heartbeat 07:00:00→停止時刻(08:06:52)へ bump** を実測。先頭 break で停止＝**claude 非起動**、親コマンドラインにトークンを埋めない harness で **GUARD 誤爆なし**を確認。Parser::ParseFile PARSE OK。検証 harness は logs/（gitignore）に置き実行後削除。
- **無回帰**: 変更はすべて**期限break経路とオフライン純関数/-SelfTest**に閉じ、毎イテレーションのホットパス（claude起動・backoff・単一インスタンスガード）は不変。既存 backoff 6アサート＋30分キャップは従来通り PASS。
- **ゲート（契約3）**: web/src 不触＝tsc/lint/vitest/build 対象外。ps1 + strings + BACKLOG/cycle-log のみ。PowerShell は -SelfTest（ドライラン相当）＋実 E2E で自己検証。
- **BACKLOG-ops 更新**: 本タスクを [x] で追加、補充理由（静穏窓ブロックで3件未満→報告系欠陥）を明記。

## 2026-07-03 報告系補充 `最終更新:` を毎イテレーションの正直なハートビート化

BACKLOG-ops の未着手[ ]が line12（掃除系）/line14（perf第2弾実測）の2件のみで、両方とも「他レーン停止＝静穏窓待ち」でブロック（現に6レーン全稼働＝ops07:02/ux-tools07:04/ux-hub06:52）。3件未満につき契約どおり運用4系統（点火・補給・点検・報告）の欠陥から**補充**（機能改善は積まない）。**発見した報告系の欠陥**: 監視ポイント#1「ループが生きているか」の一次シグナルである先頭 `最終更新:` 行を刻印するのは launcher のみ（ログオン＋毎日07:00）。ところが `loop-report-status.ps1` は毎イテレーションこのファイルを書換えるのに `最終更新:` を更新しない＝launcherパス間（07:00〜次回ログオンで最悪ほぼ丸1日）は全レーンが生存していても時刻が凍結。社長は「生存」と「無言の死」を区別できず、実際に本イテレーション冒頭の実ファイルは `最終更新 06:38`（O17前の launcherパス）で凍結し、既に解消済みの「管理者を待っています」大声バナーが偽装表示されていた。**修正**（`loop-report-status.ps1` 当班所有・自己完結）: 書込み直前に region 外の `最終更新:` 行を label 接頭辞（`{NOW}` 前の文字列）一致で**その場更新のみ**（新規作成せず launcher の所有を尊重／strings 欠落時は接頭辞が非日本語キーになり無マッチ＝安全 no-op）。これで `最終更新:` は「少なくとも1レーンが X 時点で生存」の真のハートビートになり、どのレーンかは既存の per-lane `最終稼働` 行が示す。launcher バナー（復活/期限）の一時的staleは次の定期パスで自己修復するため本PRでは触らない。**実測**: (1)`-WhatIf` がハートビート予告を出力しファイル無変更・ロック無取得。(2)実ファイルの temp コピーへ実書込み→`最終更新` 06:38:53→現在時刻へbump、launcher の点火行（`- ops : model=...`・prefix衝突）/他レーン報告行（ux-hub 06:52/#587）/region markers はすべて不変、ops 報告行のみ外科upsert を確認。web/src 不変につき tsc ゲート対象外。

## 2026-07-02 S13-b プロンプト鮮度是正＋バックオフ3段化（5/10/30分）

BACKLOG-ops S13-b を実装＝(1)プロンプトのモデル非依存化 と (2)runnerバックオフ上限の10分→30分延長。**(1)モデル非依存化**: 6プロンプト（data/seo/ux-records/ux-tools/ux-hub の各1行目「あなたは Claude Opus 4.8。」＋各 Co-Authored-By 行のモデル名焼込み、legacy `loop-prompt.txt` は加えて【再開直後の最優先(2026-06-13)】節・「Opus 4.8時代/Fable 5期」の歴史記述・「大きい仕事に向くモデル」の自己申告）を、ops/planner/critic と同じ「モデル名はrunnerの -Model が正であり、自分のモデルを名乗る必要はない」＋「commit(末尾に Co-Authored-By 行・モデル名はセッションの実モデル)」へ置換。処理済みの再開節はloop-prompt.txt からのみ存在したので同時に除去。**実測**: `Opus|Sonnet|Haiku|Fable|4.8|claude-[a-z]|再開最優先|再開直後` を全 loop-prompt*.txt に Grep してマッチ0＝焼込みゼロを確認。**(2)バックオフ3段化**: インライン計算を純関数 `Get-BackoffSeconds($fails,$interval)` へ抽出し 3連続=5分/4連続=10分/5連続以上=30分(1800s)キャップの3段へ拡張（従来は4+で10分キャップ）。normal intervalを下回らない性質は維持。`-SelfTest` スイッチを新設＝Claude CLI/スケジューラ非依存でスケジュール表(fails 0/2/3/4/5/9)＋30分キャップ到達を assert し PASS/FAIL を exit code で返す（契約「PowerShellスクリプトは-WhatIf/ドライラン相当の自己検証」を充足）。**実測**: `powershell -File loop-runner.ps1 -SelfTest` が全6ケースOK＋「30-min cap reached: YES」＝PASS(exit0)、fails=5で1800s到達を確認。web/src 不変につきtscゲート対象外。

## 2026-07-02 S13-a 報告の一元化（loop-status.md 自己報告区画）

BACKLOG-ops S13-a（報告の一元化）を実装。診断08 §D「各レーンがイテレーション末尾に最終稼働・直近マージPR・残open・判断1行を1ファイルへ書く」を、O16逸脱2の「launcher単独生成で概ね不要」判断を覆して正式実装した（理由: launcherは点火/日次でしか更新できず、permanent runnerが回る大半の時間は行が固定＝§Dの per-iteration 鮮度を原理的に出せない）。新規 `loop-report-status.ps1`（レーン別に最終稼働/直近PR(git log origin/main の`(#NNN)`・レーンscope優先)/残open(BACKLOG-<lane>.mdの`- [ ]`数)/判断1行を、`<!-- LANE-REPORT:BEGIN…END -->`区画へ**自レーン行だけ外科upsert**・ロックファイルで6レーン直列化・全失敗を非致命化・ASCIIソース＋`-WhatIf`）。集約は launcher が点火時に環境変数 `SAFE_AI_LOOP_STATUS`＝本体リポジトリの docs/loop-status.md 絶対パスを子へexportし、別クローンのレーンも**1ファイル**へ書く（loop-status.md は gitignore なので追跡churn無し）。launcher は全面書換え時に区画を**逐語保存**（`Add-LaneReportSection`）＋初回は enabled レーン数だけプレースホルダ行。loop-status-strings.txt に reportHeader/reportLine/reportEmptyNote/reportPlaceholder を追加。6レーンプロンプトに step5.5（報告一元化）を追記。**実測**: launcher `-WhatIf` で scaffold生成＋生きた行(ux-hub PRESERVE-TEST-ROW)の保存を確認、reporter で ops/data 2レーンが環境変数経由で同一ファイルへ書込み・ops再報告が行を重複せず置換（count=1）を確認＝S13-a完了条件充足。規約の正本は O16-implementation-notes.md「逸脱2の解消」。

## 2026-07-02 O16 一括（点火の恒久解＋補給＋点検）

BACKLOG-ops O16-a/b/c を1PRで実装。運転計画(11-operation-plan.md)手順①。

- **O16-a 点火の恒久解**: `loop-config.json`（untilIso/lanes×model/intervalを1ファイル化）＋ `loop-launcher.ps1`（引数ゼロ・configを読み6レーンをレーン別`-RepoPath`で冪等起動。ops=main tree、5コンテンツレーン=../safe-ai-lanes/<lane>クローン）。タスクスケジューラ `safe-ai-loop-runner` を launcher(引数ゼロ)＋「ログオン時＋毎日07:00」で再登録（`loop-launcher.ps1 -Register`）。**期限焼込みを排除**＝延長は loop-config.json の untilIso 1行のみ、スケジューラ再登録は二度と不要。期限超過時は `docs/loop-status.md` に停止バナーを書いて起動せず終了、毎日07:00再判定でバナーが更新され続ける＝「静かな死」を可視化。近接期限は稼働中に予告（warnWithinDays）。
- **O16-b 補給**: `loop-prompt-planner.txt`。launcher が各レーンBACKLOGの open<3 を検知したら、当該レーン起動直前に planner ワンショット（別タグ`<lane>-planner`・-MaxIterations 1・対象レーン注入の一時プロンプト）を先行実行。水増し禁止・実測完了条件必須をプロンプトで強制。
- **O16-c 点検**: `loop-prompt-critic.txt`。lastCriticIso（gitignore の loop-state.json）から criticEveryDays 超過でレーンループ前に critic ワンショットを1回起動→ critique docs 生成＋S/A級を該当レーンBACKLOG冒頭へ注入。
- **設計逸脱2点**（理由は O16-implementation-notes.md）: (1)可変状態 lastCriticIso は追跡config でなく gitignore の loop-state.json へ（opsレーンのclean-tree契約維持）。(2)loop-status.md は各レーン書込みでなく launcher単独生成＋gitignore（クロスクローン衝突回避、S13-aを概ね置換）。
- **検証**: PSParser構文チェック緑、`-WhatIf` スタブ試走（本物のclaude不使用）で config parse→期限判定→critic/plannerゲート→6レーン起動計画→status生成を確認。期限切れconfigで停止バナー分岐も確認。
- 次: 手順③（常設レーン点火）＝ launcher 実起動で data/seo/ops=Opus・ux系3本=Sonnet5 を立ち上げ。手順②(Fableデイリー)は社長の手動運用。

## 2026-07-03 O17: 自動復活の管理者不要化（Startup フォルダ経由・admin待ちゼロ）

BACKLOG-ops 未着手の最上位は「掃除系一括」だが web/src を触る掃除で他レーン全6本が稼働中（loop-status.md 実測=全レーン起動）のため契約通りスキップ。運用4系統の欠陥から補充＝**点火系の最大欠陥「PC再起動後の自動復活が admin 待ちで数週間未有効」**を着手。

- **実測で前提を検証**: 現行スケジューラ タスク `safe-ai-loop-runner` は RunLevel=**Limited**/LogonType=Interactive/UserId=kanet（＝非elevated）だが action は旧 `loop-runner.ps1 -UntilIso "2026-06-12T08:00:00"`（3週間前に失効した焼込み期限＝O16が殺すはずの失敗が現存）。launcher の -Register 拒否メッセージの理由「既存タスクがelevatedだから更新に admin 必要」は**虚偽**。ただし非admin での Register-ScheduledTask を Limited/Interactive タスクで実プローブ→**Access Denied**＝この環境ではタスク登録自体に admin が要る（＝「残1手動 admin」は不可避）ことを確定。
- **admin不要の本線を新設**: `loop-launcher.ps1 -InstallUserStartup`＝現ユーザー Startup フォルダ（`[Environment]::GetFolderPath('Startup')`・既に note-worker-startup.cmd の先例あり・書込み可を実測）に `safe-ai-loop-launcher.cmd`（`powershell -WindowStyle Hidden -File <launcher>`）を配置。Task Scheduler も admin も不要で PC再起動→ログオン時に launcher が起動し全レーン冪等復活。冪等・-WhatIf対応・post-verify（ファイル存在＋launcherパス参照を確認、無ければ throw）。
- **status の警告を2分岐**: 復活経路（admin の scheduler / admin不要の startup）が**両方無い時だけ大声バナー**（-InstallUserStartup を案内）、**startup済みなら静かなINFO**（再起動復活はOK・-Register は毎日07:00点検の任意上乗せ）。`Get-StartupResurrectionHealth` 追加。-Register 拒否メッセージも正確化し admin不要の -InstallUserStartup を第一推奨に。
- **検証**: PSParser 構文緑／`-InstallUserStartup -WhatIf`（書込みゼロ・would 表示）→実インストール（post-verify PASS・cmd 実配置確認）→ launcher 通常 `-WhatIf` で banner が「startup済み=静かなINFO・scheduler=stale」へ分岐することを dryrun status で実測。web/src 不触＝tsc/lint/build 対象外。
- **効果**: 「残1手動＝管理者で -Register を1回」の admin 依存を解消。Startup エントリ実配置済みのため、次回ログオン以降は admin 操作ゼロで自動復活が効く（毎日07:00の定期点検だけは任意で admin -Register を上乗せ可能）。

## 2026-07-03 ops イテレーション — 横断計測: モバイル Lighthouse 第2弾ハーネス整備（PR: ops/mobile-lighthouse-harness）

自ブランチ未マージ PR ゼロ・main clean を確認。BACKLOG-ops の残タスクは2件とも P2 で、**両方とも静穏なマシンを要求**するため今は実行不可: (1) 掃除系一括＝web/src を触るため他レーン全6本稼働中（loop-status 実測=全レーン起動）は契約通りスキップ、(2) モバイルperf第2弾＝6レーン同時稼働下では CPU 競合で Lighthouse 数値が無効化（第1弾の完了条件は「3回連続安定」）。→ 契約「3件未満なら運用4系統の欠陥から補充」に従い、まず4系統を実測点検した。

- **4系統の健全性を実測**: 点火=launcher/config 正常＋runner の同一レーン多重起動ガード（process scan・fail-open・PID衝突で自exit, loop-runner.ps1:150-178）が launcher の無条件 Start-Process を冪等化＝欠陥なし。補給=planner の open<3 ゲート結線済。点検=critic の criticEveryDays/loop-state.json 結線済。報告=loop-report-status.ps1＋launcher の LANE-REPORT 逐語保存 稼働。**O17（自動復活 admin不要化）も実配置を実測確認**＝Startup フォルダに `safe-ai-loop-launcher.cmd`（launcher 参照）が 06:44 に配置済み（現 loop-status の大声バナーは 06:38 の旧 launcher 生成＝install 前のスナップショットで、次回 launcher pass で静かな INFO へ分岐する）。→ 明白な機構欠陥は無し。
- **補給として横断計測ハーネスを新設**: 保留中のモバイルperf第2弾（line 13）を静穏窓で誰でも1コマンド実行できるよう `scripts/mobile-lighthouse.mjs` を整備。第1弾はアドホックに lighthouse を叩いていたため同じ引数を毎回書き起こす必要があった。計測条件は第1弾と同一（`--only-categories=performance --form-factor=mobile --screenEmulation.mobile`・headless Chrome・localhost 本番ビルド・各3回中央値）。既定11ページ（第1弾達成済 /accidents・/laws・/whats-new を除外し、診断07 C-1 残課題で名指しの /equipment-finder・/chatbot・/law-search を核に実在ルートを採録）。lighthouse は npx 都度取得で **package.json 依存追加なし＝Path A 非侵**。summary.md 末尾に perf<90 ページを起票候補として列挙（起票自体は運用者/planner 担当）。`docs/perf/mobile-wave2-README.md` に手順と保留理由を明記。
- **ゲート（契約3）**: web/src 不触＝tsc/lint/vitest/build 対象外。ドライラン相当の自己検証を実測＝`node scripts/mobile-lighthouse.mjs --dry-run` が Chrome/サーバ無しで 11対象・出力先・lighthouse可用性・サーバ到達性を表示し **exit 0**。異常系も確認（--help=0／unknown arg=FATAL exit 1／不正 targets=明示エラー／--targets 上書き=解釈OK）。
- **BACKLOG-ops 更新**: ハーネス整備を [x] で追加、line 13 を「**実測実行のみ**（ハーネス整備済・静穏窓待ちで保留）」へ書き換え、実行手順と保留理由を明記。

## 2026-07-03 ops イテレーション（復活バナーの正直化＝Set-ResurrectionBanner）
- **PR #602 を squash マージ**（報告系ハートビート#602）→ main を ff-only 同期・clean 確認。未着手[ ]は line14/16 の2件のみで両方とも静穏窓待ち（他5レーン稼働中）＝3件未満につき運用4系統（報告）の欠陥から補充。
- **偽の大声アラームを発見・根絶**: loop-status.md 冒頭に大声バナー「## 注意: スケジューラが未更新（…-Register必須）」が居残る一方、#602 のハートビートが `最終更新` を毎イテレーション現在時刻へ更新するため **新しく見える偽アラーム**として監視ポイント#1を汚染していた。真因＝O17 で Startup エントリ（`safe-ai-loop-launcher.cmd`・06:44 配置）を置いても `-InstallUserStartup` は status を再生成せず `exit 0` する設計のため、install 前に旧 launcher が刻んだ大声バナーが凍結。scheduler は依然 stale だが Startup で PC再起動→復活は担保済＝O17 で admin は「任意」に降格したのに、バナーは admin 必須を煽り続けていた。
- **修正（loop-launcher.ps1 のみ・153+/24-）**: `Set-ResurrectionBanner` 新設。`Get-SchedulerHealth`／`Get-StartupResurrectionHealth` を `-InstallUserStartup` パス手前へ前方移動（重複定義は下部から除去）し、install 成功直後に status のバナー区画だけを外科置換（sched≠ok∧startup=ok→静かなINFO／両ok→バナー除去／両不可→大声）。`-AssumeStartupInstalled` で -WhatIf プレビューが install 後の状態を反映。
- **ASCII 厳守の構造的検出**: この .ps1 は純ASCII（PS5.x が BOMなし日本語を Shift-JIS 誤読）方針のため、日本語リテラルでの照合を排除。バナー区画を **deadline 行接頭辞・lanesHeader・warn/stopped 除外**の全アンカーを loop-status-strings.txt（UTF-8）から実行時読取りして構造的に特定＝現行文言も旧文言も deadline バナー混在も安全に処理。初回試作の日本語リテラル照合は二重バナーを生む不具合を dry-run で検出し撤去。
- **並走安全性**: 6レーンのハートビートと同じ `<status>.lock` を CreateNew+リトライで取得し read-modify-write を直列化（Preview は private dry へ書くため lock 不要）。finally で解放。
- **ゲート（契約3）**: web/src 不触＝tsc/lint/build 対象外。Parser::ParseFile で PARSE OK。-WhatIf ドライランで **大声→静かの1区画のみ置換**・レーン行/LANE-REPORT/監視ポイント/`最終更新`/deadline を diff で逐語保存を実測。full -WhatIf でも同一の静かな INFO 分岐（Scheduler=stale, Startup=ok）を確認。**実 install** で live dashboard の偽アラームを解消＝他5レーン稼働中でも lock で無衝突（並走 heartbeat の 07:23/07:26 の新しい行が残存）・lock 非リークを確認。
- **BACKLOG-ops 更新**: 本タスクを [x] で追加、補充理由（静穏窓ブロックで3件未満→報告系欠陥）を明記。

## 2026-07-03 (O・報告系補充) — 状態ファイルロックのstale-orphan回収（偽「ループ死亡」アラーム予防）

- **背景**: BACKLOG-ops 未着手[ ]は掃除系/perf実測の2件のみ・両方とも全6レーン稼働中で静穏窓待ちブロック（3件未満）→補充の指針に従い運用4系統(報告)の欠陥から補充。#602(ハートビート正直化)/#608(復活バナー正直化)が偽アラームを掃討してきた延長線上の残穴。
- **欠陥（実測で構造確認）**: loop-report-status.ps1 と loop-launcher.ps1 は同一の `docs/loop-status.md.lock` を `CreateNew` で取得するが、holder が mid-write（Ctrl-C／スリープ／クラッシュ）で殺されると `.lock` が finally に到達せず**永久に残存**。以後の全報告・バナー整合がロック取得を25回リトライ（5s）→失敗→非致命スキップ＝毎イテレーションのハートビート(`最終更新`)が凍結し、監視ポイント#1で「生存」と「無言の死」が区別不能に＝**偽の大声アラーム**を生む（#602/#608と同族）。
- **修正（loop-report-status.ps1 +51/-13）**: ロック取得を `Get-StatusLock($lockPath,$StaleSeconds=60,$Tries=25,$DelayMs=200)` に関数化＝正当な保持は全6レーン直列でも sub-second なので **60s 超のロックは live でありえない**→`Get-Item.LastWriteTime` で齢を測り stale なら**1回だけ外科回収して即retry（sleepなし）**、fresh lock は尊重して回収せず（過剰回収を防止）。二重回収レースは last-writer-wins に縮退（lock が既に許容）。
- **修正（loop-launcher.ps1 +12/-3）**: バナー整合の同一ロックループに同じ stale 回収をインライン適用（launcher は単発利用なので関数化せずインラインで launcher 流儀に合わせた）。
- **自己検証（契約3）**: `-SelfTest`（Claude/実status/ネット不要・temp限定）新設。(A)ロック無しからの clean acquire→true＋lock残置、(B)**fresh** foreign lock は回収されず→false＋残存、(C)**stale**（-120s backdate）は回収されacquire→true、の3系5アサート。`-Lane selftest -SelfTest` が **ALL PASS(exit 0)** を実測。
- **無回帰**: reporter `-WhatIf`（lock非取得のまま行計算）・launcher `-WhatIf`（banner reconcile を dry へ・7レーン点火予告）とも従来通り clean 実行を実測。
- **ゲート**: web/src 不触＝tsc/lint/build 対象外。ps1 のみ＋BACKLOG/cycle-log。
- **BACKLOG-ops 更新**: 本タスクを [x] で追加、補充理由を明記。

## 2026-07-03 (O・報告系補充) — 設定破損時の沈黙死を大声化（Write-ConfigErrorStatus）

- **背景**: 自ブランチ未マージPR ゼロ・main clean を確認（#611 を squash マージ済）。BACKLOG-ops 未着手[ ]は line15(掃除系一括)/line17(モバイルperf第2弾 実測)の2件のみ・両方とも **全6レーン稼働中**（`Get-CimInstance` 実測=ops/data/seo/ux-tools/ux-records/ux-hub の6本）で静穏窓待ちブロック（3件未満）→補充の指針に従い運用4系統(報告)の欠陥から補充。#602(ハートビート正直化)/#608(復活バナー正直化)/#611(stale-orphanロック回収)が掃討してきた「沈黙死・偽アラーム」系の残穴。
- **欠陥（実測で構造確認）**: loop-launcher.ps1 の config 読込は、欠落（Test-Path 失敗）でも JSON parse 失敗でも **私的ログ（logs/launcher-*.log）にだけ ERROR を書いて `exit 1`**、docs/loop-status.md を一切書かずに終了。→ 社長が唯一見る監視ファイルが最後の正常描画のまま**凍結**し、監視ポイント#1で「生存」と「無言の死」が区別不能。しかも社長の**最頻の手動操作＝untilIso の手編集**こそが JSON を壊す最有力トリガー＝O16 が殺すはずの沈黙死そのもの（カンマ/引用符/日付の fat-finger で launcher が起動直後に沈黙）。
- **修正（loop-launcher.ps1 +151/-22, loop-status-strings.txt +6）**:
  - `Write-ConfigErrorStatus` 新設＝設定3失敗（①欠落 ②parse失敗 ③parseできたが lanes 配列が空/欠落）で loop-status.md を大声バナー「## !!! 起動失敗: loop-config.json を読み込めません（全レーン未起動）!!!」＋直し方（JSON構文・untilIso 確認→次ログオン/07:00で自動復活 or 手動再実行）＋**エラー全文**＋`最終更新=now`（launcher は起動した＝正直な鼓動）で再生成。レーン**自己報告 region（LANE-REPORT:BEGIN..END）は逐語保存**＝前回起動から生存中のレーンの報告を破壊しない。ASCII厳守（日本語は全て loop-status-strings.txt から実行時読取り）。
  - ロックの三重複製を解消: Set-ResurrectionBanner のインライン stale-orphan 回収を **`Get-LauncherStatusLock`** へ関数化（#611 と同一意味論・60s超を live不能として1回回収＋fresh尊重）し、Set-ResurrectionBanner／Write-ConfigErrorStatus の双方が共用。reporter の Get-StatusLock と同じ 60s/25回/200ms 既定で2writer が回収基準を一致。
  - `-SelfTest` 新設（launcher にも初のオフライン自己検証）＝Get-LauncherStatusLock の (A)clean acquire→true+lock残置 (B)fresh foreign→回収せず false+残存 (C)stale(-120s)→回収しacquire true の3系5アサート・temp限定（Claude/実status/config/ネット不要）。
- **自己検証（契約3）**: `-SelfTest` = **ALL PASS(exit 0)**。`-WhatIf` で config **欠落 / JSON破損 / lanes空**の3系すべてが dryrun（logs/loop-status.dryrun.md）へ大声バナーを書き、**実 docs/loop-status.md は MD5 byte不変**を実測（AE4C…→AE4C…）。dryrun の UTF-8 描画（Read tool）で日本語バナー＋直し方＋エラー全文＋保存された6レーン LANE-REPORT region を目視確認。**正常 config の -WhatIf 無回帰**＝6レーン idempotent 起動予告＋Set-ResurrectionBanner Preview clean（両健全→バナー無し。scheduler が -Register 済で loop-launcher.ps1 を指す＝sched/startup とも ok を実測）。PSParser 構文 OK。
- **ゲート**: web/src 不触＝tsc/lint/vitest/build 対象外。ps1 + strings + BACKLOG/cycle-log のみ。
- **BACKLOG-ops 更新**: 本タスクを [x] で追加、補充理由（静穏窓ブロックで3件未満→報告系欠陥）を明記。

## 2026-07-03 (O・点検系補充) — 定期点検(critic)の沈黙死を正直化（Resolve-CriticStamp + criticFail バナー）

- **背景**: 自ブランチ CI緑の未マージPR #619（稼働期限の沈黙停止=Report-DeadlineStop）を squash マージ→main を ff-only 同期・clean 確認。BACKLOG-ops 未着手[ ]は line17(掃除系一括)/line19(モバイルperf第2弾 実測)の2件のみ・両方とも **全6レーン稼働中**（tasklist 実測＋loop-status.md=ops/data/seo/ux-tools/ux-records/ux-hub 全「起動」・heartbeat が直近1分内）で静穏窓待ちブロック（3件未満）→補充の指針に従い運用4系統の欠陥から補充。**直近5補充が全て報告系(#602/#608/#611/#616/#619)に集中していたため、意図的に別系統=点検(critic)を実測点検**した。
- **欠陥（実測で構造確認）**: loop-launcher.ps1 の critic gate（旧 738-745）は weekly critic を起動する前に `$state.lastCriticIso = now; Save-State` を**無条件 pre-stamp**し、`Invoke-OneShot`（旧 586-608）は (a)timeout→Kill+WARN (b)finished→exit code をいずれも**私的 launcher ログにだけ**書いて**戻り値を返さず**捨てていた。→ critic one-shot が 30分 timeout kill / 異常終了 / runner欠落 で失敗しても、社長が唯一見る loop-status.md（監視ポイント#1）には**一切表示されず**、criticEveryDays（既定7日）分は `criticDue=false` で**再起動もされない**＝site-critique-<date>/ が生成されず S/A級指摘が各レーン BACKLOG に補充されないまま**点検系が沈黙で停止**（失敗が続けば恒久停止）。#602/#608/#611/#616/#619 が掃討してきた「報告系の沈黙死・偽アラーム」の**点検系版の裏返し（真の停止が無言）**。
- **修正（loop-launcher.ps1 +40/-9, loop-status-strings.txt +3）**:
  - `Invoke-OneShot` を outcome 返却へ＝`skipped`(runner/prompt欠落)／`whatif`／`timeout`(kill)／`ok`(exit0)／`nonzero`(exit≠0)。既存の planner 呼び出しは戻り値を無視するだけで無回帰。
  - 純関数 `Resolve-CriticStamp($Outcome,$Now,$CriticEveryDays,$RetryDays=1)` 新設＝ok→`$Now`（full interval）／失敗→`$Now.AddDays(-max(0,every-Retry))` へ **back-date**（全期間ブラックアウトも毎ログオン30分再点火も避け、約 RetryDays 後の次 launcher pass で自動再試行）。every≤Retry の縮退は clamp で `$Now`。
  - critic gate 改修: **concurrent launcher（07:00 pass 中のログオン等）の二重 critic 起動を防ぐため now を先行 pre-stamp** した上で `Invoke-OneShot` の outcome を捕捉し、`-not $WhatIf ∧ outcome≠ok` のとき back-date へ **再 stamp**＋loop-status.md の `## 各レーン` 直上へ大声バナー（criticFailHeader に {REASON}=timeout/nonzero/skipped を差込）を Add-Status。LANE-REPORT region はバナーの下でそのまま逐語保存。
  - ASCII 厳守＝.ps1 に日本語リテラルを埋めず criticFailHeader/Body1/Body2 を loop-status-strings.txt（UTF-8）から実行時読取り。
- **自己検証（契約3）**: `-SelfTest` に Resolve-CriticStamp の 5 アサート追加（ok=now／failure が12h後は非due・1日後はdue／nonzero=timeout同値／every≤Retry clamp）→ `powershell -File loop-launcher.ps1 -SelfTest` が **ALL PASS**（lock 5 + critic 5 の計10）。PSParser 構文 OK。
- **E2E（隔離 temp・実 launcher）**: `$PSScriptRoot` を temp へ向けた隔離ハーネス（fake `loop-runner.ps1`＋全レーン disabled＝**実 claude/実レーン非起動**）で実 launcher を非 WhatIf 実行。(1)fake exit3→ログ `critic one-shot degraded (nonzero)`／docs/loop-status.md に日本語バナーが `## 各レーン` 直上へ描画（mojibake なし・{REASON}=nonzero 差込）／loop-state.json の lastCriticIso が **now-6d へ back-date**（=1日後再試行）／LANE-REPORT region 保存を実測。(2)fake exit0→degraded ログ無し・バナー **0件**・lastCriticIso=**now**（full interval）を実測。
- **ゲート**: web/src 不触＝tsc/lint/vitest/build 対象外。loop-launcher.ps1 + loop-status-strings.txt + BACKLOG/cycle-log のみ。
- **BACKLOG-ops 更新**: 本タスクを [x] で追加、補充理由（静穏窓ブロックで3件未満→点検系欠陥）を明記。

## 2026-07-03 (O・報告系補充) — 個別レーンの沈黙死を正直化（Get-StaleLanes + region健在性バナー）

- **背景**: 自ブランチ CI緑の未マージPR #624（critic沈黙死=Resolve-CriticStamp）を squash マージ→main を ff-only 同期・clean 確認。BACKLOG-ops 未着手[ ]は line19(掃除系一括)/line21(モバイルperf第2弾 実測)の2件のみ・両方とも **全6レーン稼働中**（loop-status.md=6レーン全「起動」・heartbeat 直近1分内・data が #626/#595 を並走報告中）で静穏窓待ちブロック（3件未満）→補充の指針に従い運用4系統の欠陥から補充。直近5補充(#602/#608/#611/#616/#619)＋#624 は**集約ハートビート/他系統**を扱ったが、その裏に残る**個別レーン粒度**の穴を実測点検して着手。
- **欠陥（実測で構造確認）**: トップの `最終更新` ハートビート（loop-report-status.ps1 が毎イテレーション in-place bump）は **生きている任意の1レーンが更新**する設計。→ 6レーンのうち **1本だけが黙って死ぬ**（Report-DeadlineStop が撃つ期限停止でもなく、Write-ConfigErrorStatus が撃つ config破損でもなく、runner プロセスが wedge/クラッシュ）と、残り5本が heartbeat を新鮮に保つため監視ポイント#1では**「生存」と表示**され、社長は region の6本の `最終稼働` を1本ずつ「今」と手引き算しない限り**どのレーンが落ちたか判別不能**。検知は次 launcher pass（ログオン／毎日07:00）まで最悪 ~24h 遅延＝#602/#608/#611/#616/#619 が**集約レベル**で掃討した沈黙死の**個別レーン残渣**。
- **修正（loop-report-status.ps1 +153, loop-status-strings.txt +4）**:
  - 純関数 `Get-StaleLanes($Rows,$Now,$StaleMinutes,$RowTemplate)` 新設＝region行を **reportLine テンプレ由来の実行時 regex** でparse（`[regex]::Escape` 後、`\{LANE}`→`(?<lane>\S+)`・`\{LASTRUN}`→ts捕捉に置換。**注意: Escape は `{`→`\{` にするが `}` は非エスケープ**＝閉じ側は素の `}` で照合）。日本語「最終稼働」リテラルは strings のテンプレ側にのみ存在＝**.ps1 ソースは pure ASCII**（WinPS 5.x の Shift-JIS 誤読を回避）。`$StaleMinutes` 超のみ抽出。
  - `Get-LaneHealthLine` 新設＝Get-StaleLanes を2回（`[int]::MinValue` で全 parseable 行を数え、`$StaleMinutes` で沈黙分を選別）。stale有→`健在性 !!! 停止疑い: <lane>(最終.../約Nh前)…復活=該当runner再起動 or 次回ログオン／毎日07:00 launcher`、無→`健在性: 全Nレーンが直近{MIN}分以内に自己報告`。
  - 書込みパス: region upsert 後・heartbeat pass 前に **region 先頭（BEGIN直後）へ管理1行を再計算**。冪等＝両テンプレの placeholder 前 prefix 一致で旧 health 行を除去してから再挿入（重複しない）。**報告する=生きている**任意のレーンが毎回再計算するので、死んだレーンは**生きたレーンの1インターバル(数分)以内**に名指し検知（~24h→~2h）。**自己修復**＝死んだレーンが復帰報告すると自レーン行が新鮮化しバナーが OK へ自動遷移。
  - しきい値既定 `[int]$StaleMinutes=120`＝重い単一イテレーション(実装+build+lint+vitest+PR≒30-40分・CI待ちは翌回送り)の **>3倍ヘッドルーム**で誤アラーム実質ゼロ。
  - old strings（health キー欠落）は **安全 no-op**（`$LS.ContainsKey` ガードで全パス skip）。launcher の region 逐語保存（`$old[$b..$e]`・loop-launcher.ps1:489/681）で health 行は launcher pass も生存→次 reporter が再計算。
  - `-WhatIf` は read-only で既存 region＋自レーン新鮮行から health 行を**予告**（ファイル/ロック不触）。
- **自己検証（契約3）**: `-SelfTest` に Get-StaleLanes の **8アサート**追加＝**ASCII 合成テンプレ**（`- {LANE} : LR {LASTRUN} / ...`）で Japanese を source に持ち込まず regex-build path を実測（fresh非flag／stale2本flag／名指し／age分計算／全fresh=0／MinValue全数=3・junk除外／placeholder無テンプレ=no-op）→ `-Lane selftest -SelfTest` が **ALL PASS**（lock3+stale8）。両 ps1 とも PSParser 構文 OK。launcher `-SelfTest` 無回帰（lock5+critic5 ALL PASS）。
- **E2E（隔離 temp・実 reporter）**: fresh(ops/seo)＋stale(data 4h前)の region を持つ status に対し実 reporter を非 WhatIf 実行＝(1)ops報告→`健在性 !!! 停止疑い: data(最終.../約4h前)` を BEGIN直後へ描画（mojibake なし）・ops行新鮮化、(2)seo報告→`健在性` 行 **count=1**（重複せず）、(3)data復帰報告→`健在性: 全3レーンが直近120分以内` へ **自己修復**を実測。`-WhatIf` で同一 health 行の予告出力を確認。
- **ゲート**: web/src 不触＝tsc/lint/vitest/build 対象外。loop-report-status.ps1 + loop-status-strings.txt + BACKLOG/cycle-log のみ。
- **scope 注記（正直）**: 一度も自己報告していないレーン（launcher の `（まだ自己報告なし）` placeholder）は timestamp を持たず staleness 判定の対象外＝**出生時未起動**は launcher の「起動」表示側の責務で、本 fix は「**一度報告した後に沈黙した**」個別レーン死の検知に限定。
- **BACKLOG-ops 更新**: 本タスクを [x] で追加、補充理由（静穏窓ブロックで3件未満→報告系欠陥）を明記。

## 2026-07-03 健在性バナーのroster対応化＝一度も自己報告していないレーンの名指し検知（PR#631）

- **前段（契約1）**: 自ブランチ #628（個別レーンの沈黙死正直化＝Get-StaleLanes）が CI緑(Vercel SUCCESS)＝squashマージ→`git checkout main && git pull --ff-only`→clean 確認。マージ済みブランチは削除済み。
- **着手判断（契約2）**: 未着手 `[ ]` は掃除系(line20)/モバイルperf実測(line22)の2件のみ・**両方とも全6レーン稼働中**（loop-status heartbeat 直近1分・data/seo/ux 各レーンが並走報告）で静穏窓待ちブロック＝3件未満 → 運用4系統（**報告**）の欠陥から補充。
- **欠陥（監視ポイント#1の最後の穴）**: `Get-LaneHealthLine` は region に**行があるレーンしか数えず** TOTAL=region行数だった。そのため **loop-config.json で enabled だが一度も timestamped 行を書いていないレーン**（出生時死＝起動直後にクラッシュ／報告経路の破損）が完全に不可視で、OKバナーが `健在性: 全Nレーンが直近120分以内に自己報告（個別レーンの沈黙停止なし）` と**全roster健全を誤主張**。#628 の scope note が「出生時未起動は launcher の起動表示側の責務」と明示的に繰り延べた盲点だが、launcher の「起動」表示は**起動試行**でしかなく liveness ではないため、起動直後に死んだ/報告経路が壊れたレーンは**二重に不可視**（起動表示は 起動 のまま・健在性は数から除外）。実際 live `docs/loop-status.md` は config**6レーン**中 **seo/ux-records** が region未登場のまま `全4レーン…停止なし` と表示していた。
- **修正（loop-report-status.ps1 + loop-status-strings.txt のみ）**:
  - `Get-EnabledLaneRoster` 新設＝loop-config.json（RepoPath直下）を ConvertFrom-Json し `enabled` かつ `name` を持つレーン名を roster として返す。config 欠落/不読/lanes 欠落 → **空 roster**（try/catch で例外を握り潰す＝非致命）。
  - `Get-LaneHealthLine` を roster対応化＝2つの失敗モードを分離: **stale**（報告実績あり・閾値超で沈黙＝report-then-silent死・大声）と **missing**（roster にあるが timestamped 行なし＝一度も未報告・注意）。優先度 **stale > missing > ok**。TOTAL は region行数でなく **roster数**。`$ExpectedLanes` が空（config不読）なら **旧来の報告数ベースへ完全 fallback**（誤アラーム0）。
  - 新バナー文字列 `laneHealthUnreported`（ASCII厳守・{MISSING}/{TOTAL}/{LANES} 差込）を strings に追加。strip の冪等ロジック（WhatIf preview・本書込みの2箇所）に `unrepPre` を追加し **3種prefix**を除去→再挿入＝健在性行が重複蓄積しない。
- **自己検証（契約3・-SelfTest）**: roster **7アサート**追加。E: `Get-EnabledLaneRoster` が enabled∧named のみ抽出・disabled除外・欠落ファイル→空。F1-F4: **実 reportLine テンプレ**で行を合成（regex-build path と健在性文字列を end-to-end で実測しつつ、アサートは Fmt 往復比較で source を ASCII に維持）＝未報告 seo→注意バナー名指し／全報告→roster数TOTALのOK／stale優先／空roster→legacy OK。→ `-Lane selftest -SelfTest` **ALL PASS(計20＝lock3+stale8+roster7+既存)**。
- **E2E（隔離temp・実 reporter・非WhatIf）**: enabled 3レーン(ops/data/seo)の config＋seed region に対し data→ops→ops を実書込み＝(1)health行 **count=1**（strip冪等）、(2)`- ops :` 行 **count=1**（upsert・二重書込みでも重複なし）、(3)health = `1/3レーンが未報告: seo`（roster突合で正しく名指し）を実測＝**RESULT PASS**。
- **live file 実測（-WhatIf・read-only）**: 本番 `docs/loop-status.md` に対し `-WhatIf` → `健在性 注意: 2/6レーンが未報告: seo、ux-records` を出力＝**偽の「全4レーンOK」を正しく置換**（ファイルは未書込み）。次回 step5.5 の実書込みで live dashboard の誤全緑が是正される。
- **ゲート**: web/src 不触＝tsc/lint/vitest/build 対象外。変更は loop-report-status.ps1 + loop-status-strings.txt + BACKLOG/cycle-log のみ。両 ps1 とも PSParser 構文 OK（SelfTest 実行成功が保証）。
- **scope 注記（正直）**: fresh ignition 直後は全レーンが missing になり得るが、reporter は自レーン報告後に呼ばれる（自行は常に present）＋missing は **注意レベル**で数分で自己解消するため、大声の偽アラームにはならない。config が clone 間で同一である前提（全レーンが main の同一 loop-config.json を読む）に依存。

## 2026-07-03 planner one-shot の同一クローン並走gitレース封止（補給系）

- **前段（契約1）**: 自ブランチ #631（健在性バナーroster対応化）が CI緑(Vercel SUCCESS・ops-only=web-ci path-filtered)＝squashマージ→`git checkout main && git pull --ff-only`（main→7ac8cb16・seo並走分も統合）→clean 確認。マージ済みブランチは --delete-branch 済み。
- **着手判断（契約2）**: 未着手 `[ ]` は掃除系(line20)/モバイルperf実測(line22)の2件のみ・**両方とも全6レーン稼働中**（loop-status heartbeat 08:57・6レーン起動）で静穏窓待ちブロック＝3件未満 → 運用4系統の欠陥から補充。**報告系は #602/#608/#611/#616/#619/#628/#631 で沈黙死検知を飽和済**＝限界効用低下のため今回は手薄な**補給(planner)系**の実欠陥を選択。
- **欠陥（既存破壊のgitレース）**: launcher の supply gate（`$open -lt 3`）は当該レーンの **persistent runner が既に稼働中か**を見ず planner one-shot を発火していた。`$running = Get-RunningLanes` は line695 で planner gate(820) より前に算出済みなのに gate では未使用＝idempotent guard(841)は persistent runner の二重起動しか防がず one-shot は無防備。冷起動（reboot/logon＝何も走っていない）では正しく「起動前に補給」だが、**O16 の中核＝毎日07:00/logon の自己修復re-run** ではレーンは既に稼働中で、同一クローンに **2本目の claude+git プロセス**が同時発火＝`index.lock` 競合／ブランチclobber＝ops レーンなら main ツリー、他レーンなら各clone で working-tree 破壊。設計コメント(line758)の「critic runs BEFORE the lane loop so it never overlaps the ops lane」は**冷起動時のみ成立**する誤った不変条件。
- **修正（loop-launcher.ps1 のみ）**: 純関数 `Test-ShouldFirePlanner($Open,$LaneRunning,$HasPrompt)` 新設＝`HasPrompt ∧ ¬LaneRunning ∧ 0≤Open<3` でのみ true。gate を `$laneAlreadyRunning = $running.ContainsKey($lane.name)` で分岐＝稼働中レーンは「補給skip（同一クローン並走git race回避・稼働レーンは step5.5 契約で自己補給）」を launcher ログに明記。`Get-OpenTaskCount` が -1（backlog欠落）を返す場合も `Open≥0` で確実に非発火。
- **critic の同族レースは別タスク化**: critic one-shot も repoRoot(main)で ops runner と同ツリー並走＝同じレース。だが planner と違い「稼働中skip」だと ops がほぼ常時稼働のため critic が恒常starve＝skip不可。正解は git worktree 隔離（大きい別変更）のため BACKLOG-ops に `[ ] 【Opus・P1】点検系: weekly critic one-shot を隔離 git worktree で実行` を新規起票。
- **自己検証（契約3・-SelfTest）**: planner **5アサート**追加＝発火(drained∧not running∧prompt)1本＋skip4本(running/未drain open=3/欠落backlog open=-1/prompt無)→ `-SelfTest` **ALL PASS(計16＝lock3+critic5+planner5+既存)**。
- **live config 実測（-WhatIf・read-only・プロセス起動なし）**: 全6レーン稼働中の本番 config に対し `-WhatIf` → **ops(open=2)/seo(open=1)/ux-tools(open=1)** の3レーンが**まさに今**drained∧稼働中＝旧コードなら3本の並走planner発火（3クローンで同時git race）の所を全て「skipping planner one-shot (avoids concurrent-git race)」で封止を実測。data/ux-records(open=5)/ux-hub(open=3)は gate 非該当で無変化＝回帰なし。EXIT=0。
- **ゲート**: web/src 不触＝tsc/lint/vitest/build 対象外。変更は loop-launcher.ps1 + BACKLOG/cycle-log のみ。`-SelfTest` PASS が PSParser 構文健全性を保証。
- **scope 注記（正直）**: 稼働中レーンの backlog 補給は当該レーンの step5.5 自己補給（open<3→運用/機能タスク補充）に委譲＝launcher の planner one-shot は**冷起動専用の補給**に役割限定。稼働中に backlog が枯れても live runner が自力で埋めるため機能欠落なし。

## 2026-07-03 — 点検系: weekly critic を隔離 git worktree で実行（PR: ops/critic-isolated-worktree）
- **着手判断（契約1・2）**: イテレーション開始時、main の working tree に**前回の未コミット実装**（loop-launcher.ps1 +108行 / loop-prompt-critic.txt）が残存＝内容は BACKLOG-ops 未着手 `[ ]` **最上位**の「weekly critic 隔離 worktree」そのもの。中断イテレーションの完成品と判定＝**捨てずに完遂**（open `ops/` PR は無し・他5レーンの PR は不可侵）。
- **欠陥（既存破壊のgitレース・critic版）**: weekly critic one-shot は `repoRoot`（main ツリー）で `-Lane site` として `git checkout main / commit / push` するが、ops persistent runner も同 main ツリーで常時稼働＝**同一 working tree で git 並走**（#634 で planner を封じたレースと同族）。だが planner の「稼働中ならskip」を critic に適用すると **ops はほぼ常時稼働**のため critic が恒常 starve＝weekly site-critique が二度と走らない＝skip 不可。隔離 worktree（独立 index+HEAD）だけが正解。
- **修正（loop-launcher.ps1 + loop-prompt-critic.txt）**: 純関数 `Get-CriticWorktreePath`（repo の**兄弟**＝nested禁止・launch stamp scope）/`Test-WorktreeIsIsolated`（正規化フルパスで main ツリー内・同一を弾く）/`Get-WorktreeAddArgs`（`worktree add --detach @origin/main`＝git は同一 branch の二重 checkout を拒否するため detached）/`Get-WorktreeRemoveArgs`（`--force`）/`Invoke-Git`（throwしない `git -C` wrapper）を新設。critic 起動を「fetch origin/main→prune→orphan 除去→detached worktree add→Invoke-OneShot(LaneRepo=worktree)→**finally で強制 remove+prune**」へ置換＝main ツリーの index/HEAD を一切共有せず ops runner の同時 commit と index.lock 競合ゼロ。isolation invariant 違反時は `worktree-unsafe` で**in-place 実行を拒否**（レース再開より未実行を選ぶ）。critic プロンプトも「隔離 worktree に detached で置かれている＝`git checkout main` するな・fetch/pull 不要・その場で `ops/critique-<date>` を切れ」へ改訂。
- **当イテレーションでの追加是正（WhatIf の正直化）**: WhatIf 分岐が phantom worktree（未作成）へ `Invoke-OneShot` を呼ぶと内部の `Test-Path $runner` guard が発火し**誤った `skipped, no runner` を dry-run に出す**ため、WhatIf は Invoke-OneShot を呼ばず「fetch→add→run→remove」の正確な計画のみ出力＝`$criticOutcome = "whatif"` へ修正（ドライランの honesty を担保）。
- **自己検証（契約3・-SelfTest）**: worktree **6アサート**追加＝stamp scope/sibling-isolated/nested→fail/repo自身→fail/add は detached/remove は forced → `-SelfTest` **ALL PASS(計22＝lock3+critic5+planner5+backoff... 既存+worktree6)**。
- **live 実測（-WhatIf・read-only・プロセス起動なし）**: 本番 config へ `-WhatIf` full run＝**無エラー・EXIT=0**。critic は「not due(last=07-02, every 7d)」で分岐非到達だが構文/実行時健全性を確認、全6レーン alreadyRunning=True → web/src 非侵を実測。worktree 経路のドライラン検証は `-SelfTest` で担保（Chrome/git/network 不要のオフライン純関数）。
- **ゲート**: web/src 不触＝tsc/lint/vitest/build 対象外。変更は loop-launcher.ps1 + loop-prompt-critic.txt + BACKLOG/cycle-log のみ。`-SelfTest` ALL PASS が PSParser 構文健全性を保証。

## 2026-07-03 ops イテレーション: 個別レーン沈黙死の「復活」を常駐watchdog化（launcher -HealOnly）

- **契約1（回収）**: 自ops PR #640（点検系 critic 隔離worktree化・SelfTest22 ALL PASS・Vercel緑）を squashマージ→`git checkout main && git pull --ff-only`→clean 確認。
- **タスク選定**: BACKLOG-ops.md の未着手[ ]は掃除系(line23)/mobile-perf実測(line25)の2件のみ・両方とも**全6レーン稼働中**（loop-status heartbeat 直近1分・data #636/ops #634 等が並走）で静穏窓待ちブロック＝3件未満→契約通り運用4系統（点火）の欠陥から補充（機能改善は積まない）。
- **欠陥（検知はできるが復活が遅い）**: #628/#631 で個別レーンの沈黙死を**検知**（loop-status の健在性バナーが死んだレーンを名指し）できるようになったが、**復活**は次のログオン/毎日07:00の launcher pass 頼りのまま。-Register の07:00 は admin 未登録＝実質次ログオンまで最悪~22h の穴。O16 の「止まったら即見える・即戻る」の後半（復活）が個別レーン粒度で欠けていた。
- **修正（loop-launcher.ps1）**: `-HealOnly` 新設＝**死んだレーンの runner 再起動だけ**を行い deadline/planner/critic/status/git worktree は一切触らない軽量 watchdog。冪等性は loop-runner.ps1 の per-lane 単一インスタンスガードが担保（生きているレーンを再起動しても新 process が即 self-exit）。純関数 `Get-LanesToHeal`（enabled∧`-not Running.ContainsKey(name)` のレーン名を返す・disabled は復活対象外・**plain return + caller は必ず @() でラップ**＝unary-comma `,$out` は inline `@(...)` で二重ラップするため不採用）＋実行ブロック（config load 直後・deadline/status 機構の手前に early-exit で配置）。
- **2つのfail-safe**: (a) config の untilIso 超過なら復活せず＝停止意図を尊重（runner 自身の deadline-break と ops watchdog が綱引きして期限切れ run を永久復活させる事故を防ぐ）。(b) **process scan が0件なら復活拒否**＝WMI outage 時は Get-RunningLanes が空を返す**と同時に** runner 側のガードも fail-open するため、ここで再起動すると全レーン二重起動になる。0件は「本当に冷えている（=logon launcher の責務）」か「WMI 障害（=二重起動の罠）」のどちらかなので、いずれにせよ heal は動かない＝heal が直すのは常に「一部生存・1本だけ死」だけ。
- **配線（loop-prompt-ops.txt step5.6）**: **常時生存する唯一の ops** が毎イテレーション末尾（報告直後）に `-HealOnly` を実行＝個別に死んだ他レーンを ~1 ops interval（数分）で復活（~22h→数分）。capability を誰も呼ばなければ dead code になるため、検知（バナー）と対で復活を hot loop に接続。
- **自己検証（契約3・-SelfTest）**: heal **4アサート**追加＝全生存→空／1レーン死→名指し／disabled は不在でも除外／空scan→全enabled返却（caller が refuse する前提）→ `-SelfTest` **ALL PASS(計26)**、既存22（lock3+critic5+planner5+worktree6+backoff等）に無回帰。
- **live/temp 実測（-WhatIf・read-only・プロセス起動ゼロ）**: **本番 config** へ `-HealOnly -WhatIf`＝全6レーン process 生存を確認し「nothing to resurrect」（健在性バナーの「停止疑い: ux-hub/ux-tools」は**報告 stale** であり process は生きている＝heal は正しく no-op＝バナーの検知粒度と heal の復活粒度が別物であることを実証）。**隔離 temp config**で死んだ fake レーン(repoPath ".")→「[HEAL][WHATIF] would resurrect dead lane 'zzzdead'」＋disabled 'zzzoff' 除外＋過去 untilIso→「healing nothing (run is meant to be stopped)」の各分岐を dry-run 実測（実 launch/status 書込みゼロ・config-error 分岐は dryrun ファイルへ隔離を確認）。
- **ゲート**: web/src 不触＝tsc/lint/vitest/build 対象外。変更は loop-launcher.ps1 + loop-prompt-ops.txt + BACKLOG/cycle-log のみ。`-SelfTest` ALL PASS が PSParser 構文健全性を保証。working tree clean で終了。

## 2026-07-03 ops iteration — 未報告レーンの沈黙死を正直化（born-dead 大声化 / PR after #644）
- **回収**: 前イテレーションの #644（-HealOnly 常駐 watchdog）を CLEAN/MERGEABLE で squash マージ（ops-only 変更のため web-ci skip・Vercel 緑）→ main を ff-only 同期・clean 確認。
- **欠陥**: `Get-LaneHealthLine`（#628/#631）は「一度も自己報告していない enabled レーン(missing)」を検知できるが、rowless=**timestamp が無い**ため永久に Quiet な注意バナー「起動直後なら数分で解消」でしか出せなかった。→ **出生時死（runner が起動直後に落ちる crash-loop）や報告経路の恒久破損**が2時間でも1日でも soft-worded のまま＝監視ポイント#1 の最後の**偽の安心**（#602/#608/#611/#616/#619/#628/#631 が集約〜個別で掃討した沈黙死の**未報告レーン残渣**）。live status も seo/ux-records が「未報告」のまま何時間も Quiet で居残る恐れ。
- **修正（reporter 単独・launcher 非改変）**: 未報告レーンに**時計**を与える。純関数群 `Get-MissingLanes`（roster − 報告済み行＝Get-StaleLanes MinValue で reported を引く）/`Get-MissingSinceMap`（region 内 ASCII マーカー `<!-- lane-missing-since: seo=ISO ... -->` を parse）/`Update-MissingSinceMap`（依然 missing=旧 stamp 維持で時計継続・新規 missing=now・復帰/roster 外=drop）/`Format-MissingSinceMarker`（空 map→行なし・lane 昇順で diff 安定）を新設。時計は**region 内マーカー**に持つ＝(a) 既存 status lock で6レーン直列化（side file の並走破損を回避）(b) launcher の verbatim region 保全に相乗り（別 file 不要）(c) レーンが報告した瞬間 stamp が drop して**自己修復**。
- **バナー2分岐**: `Get-LaneHealthLine` に `-MissingSince` を追加し、未報告レーンを stamp 経過で分類＝**≥StaleMinutes(120分)→born-dead(大声 laneHealthBornDead)**／grace 内→従来 Quiet(laneHealthUnreported)。優先度 **stale > born-dead > missing(quiet) > ok**。**fail-safe**: born-dead 文字列が無い旧 strings では missing を全て Quiet に倒す＝rowless レーンを絶対に falsely-OK バナーへ落とさない。stale(報告→沈黙)は born-dead(一度も報告なし)より上位＝メッセージ単機能を維持。閾値は stale と同一 120分＝「重い1イテレーション30-40分の>3倍」ヘッドルーム＝2時間 ZERO 報告は Quiet 卒業に妥当。
- **書込み経路**: write / WhatIf の両方で region を「マーカー行」と「実レーン行(health/marker strip 済)」に分解→missing 算出→旧マーカーから map 復元→Update→health 行 + マーカー行を BEGIN 直後へ再挿入（idempotent strip で重複しない）。
- **自己検証（契約3）**: `-SelfTest` に **G群12アサート**追加＝G1 grace内→Quiet／G2 120分超→大声で seo 名指し／G3 stale が born-dead に優先／G4 map keep-stamp・now-stamp・drop-recovered／G5 marker round-trip・empty→行なし／G6 Get-MissingLanes → **ALL PASS(計32)**、既存20(lock3+stale8+roster+F群)に無回帰。
- **E2E 実測（隔離 temp）**: ops 報告→data/seo を now stamp・Quiet ／マーカーを200分 back-date + data 報告→**seo だけ born-dead 大声(!!!)・data は報告済で drop** ／seo 報告→**マーカー消滅・OK** を banner の ASCII アンカー(`!!!`)分類で全 PASS。
- **live 実測（-WhatIf・read-only）**: 本番 loop-status.md へ `-WhatIf`＝md5 **byte 不変**、health 行「健在性 注意: 2/6レーンが未報告: seo、ux-records」＋マーカー「seo=... ux-records=...」を now で刻む予告＝以後120分沈黙で **Quiet→大声へ昇格**、報告すれば即 drop を実証。
- **ゲート**: web/src 不触＝tsc/lint/vitest/build 対象外。変更は loop-report-status.ps1 + loop-status-strings.txt + BACKLOG/cycle-log のみ。launcher 非改変（マーカーは verbatim 保全に相乗り）。`-SelfTest` ALL PASS が PSParser 構文健全性を保証。補充理由=未着手[ ]は掃除系/perf実測の2件のみ・両方とも全6レーン稼働中で静穏窓待ちブロック（3件未満）→運用4系統(報告)の欠陥から補充。working tree clean で終了。

## 2026-07-03 ops iteration — 健在性roster の読取り元をクローン→中央status同一ツリーへ是正（PR after #649/#654）
- **回収**: 前イテレーションの #654（-HealOnly 常駐 watchdog で ops SPOF 封止）を CLEAN/MERGEABLE で squash マージ（ops-only 変更のため web-ci skip・Vercel 緑）→ main を ff-only 同期・clean 確認。
- **欠陥（報告系の roster 一貫性）**: loop-report-status.ps1 は自レーン行を **SAFE_AI_LOOP_STATUS 経由で main ツリーの中央 loop-status.md に書く**のに、roster（enabled レーン一覧＝健在性バナーの分母・未報告/born-dead 判定の母集団）は `$configPath = Join-Path $RepoPath "loop-config.json"`＝**自クローンの config**から読んでいた。非 ops の5レーンは `..\safe-ai-lanes\<name>` の別クローンで走り**それぞれ自分の commit 済み config** を持つため、健在性バナーの roster が「**どのレーンがたまたま報告したか**」に依存。社長が最も触る main ツリー working-tree の config 編集（untilIso bump / レーン toggle）は**次の pull までクローンへ伝播しない**ので、報告元次第で分母がズレる＝#628/#631/#640 が積み上げた未報告検知の**土台**が報告元 non-determinism で揺らぐ（監視ポイント#1 の roster 一貫性欠陥）。書込み先（中央 main ツリー）と読取り元（roster＝ローカルクローン）が**別ツリー**という非対称が根本。
- **修正（reporter 単独・launcher 非改変）**: 純関数 `Resolve-RosterConfigPath($StatusPath,$RepoPath)` 新設＝roster config を**中央 status ファイルの居るツリー**へアンカー＝`<maintree>\docs\loop-status.md` の grandparent（Split-Path -Parent ×2）＝`<maintree>`＝launcher が実際に読む main working-tree config。anchored 不在時のみ `$RepoPath\loop-config.json` へ安全 fallback（standalone/manual・異常な status path＝空 roster で false-alarm 0 の既存契約を保存）。解決値を `Get-EnabledLaneRoster` へ渡す1行差替え。→ **書込み先（中央）と読取り元（roster）が同一ツリーに一致**＝全レーンが「launcher が実際に起動した roster」を共有し、報告元に依らず決定的。
- **自己検証（契約3）**: `-SelfTest` に **E2群4アサート**追加＝(1)非 ops クローン模擬（StatusPath=main ツリー docs、RepoPath=別 clone・両者に異なる config）で resolver が**main の config を選択**（clone を選ばない）／(2)anchored roster が main の3レーン（clone の1レーンでない）／(3)anchor 不在→RepoPath fallback／(4)blank StatusPath→RepoPath fallback・throw なし → **ALL PASS(計36)**、既存32(lock3+stale8+roster+F群+G群)に無回帰。
- **live 実測（-WhatIf・read-only）**: 本番中央 loop-status.md へ `-WhatIf`＝roster が **6レーン（ops/data/seo/ux-tools/ux-records/ux-hub）に解決**＝main ツリーからは従来と同一（無回帰）、health 行「健在性 注意: 2/6レーンが未報告: seo、ux-records」も不変。効果は非 ops クローンからの報告時に発現（そこで従来はクローン config を読んでいた）＝self-test E2 でヘルメティックに実証。
- **ゲート**: web/src 不触＝tsc/lint/vitest/build 対象外。変更は loop-report-status.ps1 + BACKLOG/cycle-log のみ。launcher/strings 非改変。`-SelfTest` ALL PASS が PSParser 構文健全性を保証。補充理由=未着手[ ]は掃除系/perf実測の2件のみ・両方とも全6レーン稼働中で静穏窓待ちブロック（3件未満）→運用4系統(報告)の欠陥から補充。working tree clean で終了。

## 2026-07-03 ops: logs/無制限肥大の封止＝launcher full pass に log 保持 sweep 新設（報告系の human surface 保全）
- **欠陥（報告系のログ肥大）**: launcher / runner / watchdog はいずれも `logs/` へ実行毎にログを吐く（launcher-*.log / loop-*.log / watchdog-*.log / planner-*.txt）のに、**3スクリプトのどれも1件も剪定しない**。常時ログイン機では毎 full pass（logon / 07:00）＋ #654 watchdog の heal spawn が launcher-*/watchdog-* を、runner 再起動毎に loop-*.log を無限に積む（現に logs/ は90件超）。操作者が死レーンを診断する**第一の面＝報告系の human surface** が読めなくなり、いずれディスクも侵す＝O16「止まったら気付いて即戻る」の検知面が自らのログ litter で曇る。log rotation はどこにも存在しなかった。
- **修正（launcher 単独・pure 関数＋full pass sweep）**: 純関数 `Get-LogsToPrune($Files,$Now,$RetentionDays,$KeepMin)` 新設＝**ループ自身が生成した4種のみ**（launcher-*.log/loop-*.log/watchdog-*.log/planner-*.txt）を対象に絞り、手書きの progress/audit .md（loop-status.dryrun.md 等）と非該当ファイルは**不可侵**、**最新 KeepMin 件は年齢無関係に必ず保持**（burst 下でも直近診断が残る）、残りのうち `Now - RetentionDays` より古いものだけを剪定対象に返す（IO なし＝offline self-test 可能）。実行は launcher の **full pass 末尾**（-HealOnly は早期 exit で対象外・deadline 無関係＝掃除は常に安全）で **14日保持 / 最新20件保持** で sweep。**開いている runner ログは Windows write lock で Remove-Item が失敗し skip**（走行中プロセスのログを消さない）、per-file try/catch で1件の失敗が sweep 全体を止めない、sweep 自身も try/catch で非致命。
- **自己検証（契約3）**: `-SelfTest` に **L群7アサート**追加＝(1)古 launcher ログ（keep-min 超）を削除／(2)古 planner one-shot を削除／(3)recent ログは絶対消さない／(4)手書き .md（audit-progress.md / loop-status.dryrun.md）不可侵／(5)非 loop の .txt（note.txt）不可侵／(6)KeepMin 最新は古くても保持／(7)空 logs dir で no-op → **ALL PASS(計43)**、既存36に無回帰。
- **live 実測（-WhatIf・read-only）**: 本番 logs/（91件）へ `-WhatIf` full pass＝6/8〜6/14 の古 loop-*.log **9件を「would prune」**（14日超・最新20件外のみ）と正しく名指し、**削除ゼロ（file 数不変＝WhatIf 自身の +1 ログのみ）**を実測。recent ログ・手書き .md は対象外を確認。
- **ゲート**: web/src 不触＝tsc/lint/vitest/build 対象外。変更は loop-launcher.ps1 + BACKLOG/cycle-log のみ。`-SelfTest` ALL PASS が PSParser 構文健全性を保証。補充理由=未着手[ ]は掃除系/perf実測の2件のみ・両方とも全6レーン稼働中で静穏窓待ちブロック（3件未満）→運用4系統(報告)の欠陥から補充。working tree clean で終了。
