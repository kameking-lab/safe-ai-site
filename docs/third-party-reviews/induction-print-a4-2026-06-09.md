# 受入教育記録 印刷物（A4）第三者レビュー — 2026-06-09

対象: `/site-records/induction`（新規入場者 受入教育 実施記録）の **印刷物（A4）**
方式: 第三者ペルソナ → Playwright で実データ入力 → `emulateMedia("print")` + `page.pdf({format:"A4"})` でA4実機プレビュー → 崩れ・はみ出し点検 → 是正 → 再評価

## ペルソナ
**桑原さん（48歳・ゼネコン作業所の元請安全担当）**
- 協力会社から新規入場者の受入教育記録を集め、作業所の安全書類ファイルに綴じる。労基署の臨検・本社安全監査で提示する。
- 我慢の限界:「**署名も押印もない、チェックだけの紙は証跡として弱い**。本当に教育したのか・本人が受けたのかを後から証明できない。監査で突き返す」。提出物の体裁にうるさい。

## 指摘（A4プレビュー実測・忖度なし）
全体のレイアウトは健全だった（@page A4・margin 15/12mm 設定済、操作ボタン/ナビ/保存一覧は `print:hidden`、横はみ出し0px、`docScrollW==docClientW==1200`、印刷時 visibleButtons=0）。一方、**提出書類としての体裁**に3点の欠点を実測:

1. **【致命】署名・押印欄がない。** 「実施者確認／本人 受講確認」はチェックボックスのみで、誰が教育し誰が受講したかの自筆署名・押印の記入場所が紙に存在しない。桑原さん「これでは監査で証跡として認めない」。
2. **標題が小さく弱い。** 印刷物の先頭が `h2`「受入教育 実施記録」（12pt）止まりで、提出書類として「何の記録か・法的根拠」がパッと分からない。サイト共通ヘッダーは `print:hidden` のため標題が実質ない状態。
3. **実施日が日付入力ボックスのまま印刷。** `<input type="date">` のカレンダー/スピナー(▾)が印刷にも出て、清書された帳票でなく入力途中のフォームに見える。

## 改善（既存破壊なし・ロジック不変）
- **署名・押印欄を印刷時のみ追加**（`hidden print:grid`）。`教育実施者 署名` / `受講者（本人）署名` の2枠を新設し、氏名（`educator`/`workerName` 入力値）をプレフィルした下線＋㊞枠を配置。自筆署名・押印が可能。純表示用 `SignBox` コンポーネントに切出し（状態・保存スキーマ不変）。
- **印刷専用の帳票タイトルを追加**（`hidden print:block`・中央寄せ `h1`「新規入場者 受入教育 実施記録」＋「労働安全衛生法第59条・労働安全衛生規則第35条」）。画面用の `h2` は `print:hidden` にして印刷時の重複を回避。
- **印刷時に日付入力のカレンダー/スピナーを非表示**（`globals.css @media print` に `input[type="date"]::-webkit-calendar-picker-indicator/-inner-spin-button { display:none }`）。日付文字列だけ残り全印刷ページ共通で清書見えに。

いずれも `print:` バリアント／印刷専用要素のため**画面表示・保存・CSV・既存導線は不変**（画面スクショで確認済）。新規依存0・データ/localStorageスキーマ不変。

## 再評価（A4プレビュー再実測）
- 印刷物の先頭に大きな中央標題＋法的根拠が出る。
- 実施日は「2026/06/09」のみ（▾消失）で清書帳票に。
- 末尾に `教育実施者 署名: 職長 山田 太郎 ㊞` / `受講者（本人）署名: 新規 花子 ㊞` の2枠が並ぶ。
- 横はみ出し0px・操作系0個を維持。
- 桑原さん「**標題・署名・押印がそろった。これなら作業所ファイルに綴じて監査に出せる**」=提出ライン到達。

スクショ: `induction-print-a4-after-2026-06-09.png`（印刷メディア・全入力済）

## ゲート
tsc0 / lint errors0(warn47は既存) / vitest 1105 pass / build成功。temp(_induction_print_review.mjs/_shot.mjs/png/pdf)削除済。架空0・水増し0・既存破壊0。env/DB変更なし。
