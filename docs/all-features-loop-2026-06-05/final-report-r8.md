# R8 トップ動線ゼロベース再調査 最終レポート（2026-06-05）

社長の不満「すぐ機能に行かないのが腹立つ」を起点に、トップ画面を含めゼロベースで再判断。これまで
「各機能ページ単体」で採用判定し、**トップ→機能の到達体験という軸を甘く見ていた**ことを認め、是正した。

## トップページの「すぐ機能に行ける」改善（本番モバイル実測）
- **改善前**: トップ構成= Hero(キャッチ＋研究系CTA) → 4ペルソナ帯 → 3速報pillar → FlagshipGrid(機能・4番目)。
  主要機能の縦位置(viewport 844px): チャットボット500・KY672の他は **事故DB2424 / 法改正3358 / 打合せ書3639 /
  化学RA3997 / サイネージ4177px**（最大**約4.7画面スクロール**）、**労災裁判例・新着はトップにカード無し**。
- **改善後(#355)**: トップ最上部に「**現場ですぐ使う**」9機能の1タップカード(HomeQuickAccess・モバイル3列)を新設。
  本番モバイル実測で **9機能すべてファーストビュー内・0スクロール・1タップ**（grid先頭y=227・allAboveFold=true）。
- クリック/スクロール: 化学RA まで **約4.7画面スクロール → 0スクロール・1タップ**。労災裁判例・新着 も **到達不能 → 1タップ**。
- SEO両立: Hero(h1/キャッチ/統計)・ペルソナ・速報pillar・FlagshipGrid はグリッド直下に温存。h1・meta description・
  JSON-LD の存続を実機確認（h1はファーストビュー内 y=615）。動線(配置/見た目)のみ改善し、検索流入の重要要素は非毀損。

## 全9機能ゼロベース再判定（前回→今回・トップ到達の観点を加味）
- いずれも機能ページ自体は稼働(前ループで本番検証済)。今回の核心は「トップからの到達」で、これは上記#355で全機能 0スクロール・1タップ化。
- KY/打合せ書/化学RA/事故DB/チャットボット/法改正/サイネージ/労災裁判例/新着 = すべてトップ最上部から即到達可能に。
- 「機能はあるがトップから遠い」という摩擦＝今回の発見。是正済み。

## 実装した改善・採否
- 採用: #355 HomeQuickAccess（トップ最上部 9機能直接導線）。見た目だけでなく到達距離を実測で 4.7画面→0スクロール に短縮＝本物の摩擦解消。
- 却下: なし（今サイクルは1件の高インパクト改善に集中）。

## マージPR・品質
- マージ: **#355**（HomeQuickAccess）＋ docs（本レポート）。
- 最終テスト **1019 pass**・lint 0 error・tsc 0・build OK・CI(e2e/smoke) green。
- main HEAD = c69d1261（=origin）、working tree clean、env追加なし、実DB変更なし。安全タグ all-features-loop-start-2026-06-05（b18842f3）健在。全否定の復元: git reset --hard all-features-loop-start-2026-06-05 && git push -f origin main。

## 安全停止設計の機能
- 全サイクルで main は常時デプロイ可能（直接コミットなし・ブランチ→CI緑→マージ）。
- 強制終了されても直前クリーン点が保たれる構成を維持。今サイクルも開始/終了をクリーン状態で確定。

## 社長が朝イチで見るべき本番URL
- トップ https://www.anzen-ai-portal.jp/ → 開いてすぐ「現場ですぐ使う」9機能カード（スマホで0スクロール・1タップ）。

## スマホでの改善確認
- 390px 本番実機: グリッド先頭 y=227、9機能すべて 278〜460px＝ファーストビュー内。bottom nav(KY/AIチャット/打合せ書/もっと)と併せ、現場が即起動できる。

## まだ残る摩擦・構造的限界（正直に）
- デスクトップは 9カード1行(lg:grid-cols-9)でやや密だが機能的（主戦場はスマホ）。
- 各機能内の到達済（前ループ検証）。元請様式の多様性・公式DB網羅性・毎朝の習慣 は引き続き構造的（コード外＝運用/営業領域）。

## ビルドコスト改善
- なし（横ばい）。今サイクルは機能PR1本＋docsのみ。無駄な再ビルドなし（1挑戦1ブランチ）。
