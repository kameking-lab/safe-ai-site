# スクリーンショットについて

本監査はコード読解ベースで実施した（前回監査と同様、本番URL `https://www.anzen-ai-portal.jp/` は外部到達制約、ローカルLighthouse/Playwright実機キャプチャは dev サーバ起動コストのため本セッションでは未取得）。

捏造防止のため、撮影していないスクリーンショットはプレースホルダを置かない。実機確認は Phase B のPRデプロイ後に Vercel Preview 上で行う方針。

評価の根拠はすべて `../01`〜`../12` に file:line 参照付きで記載。
