# 01. 前回（5/24）P0/P1 の解消確認（軸1）

判定根拠: main マージ済みコードの静的確認（ファイル名・型・コンポーネント）。実機挙動・印刷見た目は「未検証」と明記。
凡例: ✅解消(コード) / ⚠️部分・要実機 / ❌未解消 / ♻️一本化で別問題発生

## P0（致命的）8件

| # | 前回の問題 | 現状 | 根拠（コード） |
|---|-----------|------|----------------|
| P0-1 | 現場名・工事名の欄が無い | ✅解消 | `lib/types/operations.ts` に `siteName`/`projectName`、`ky-paper-view.tsx` に「現場名」「工事名・工区」欄。印刷見た目は⚠️未検証 |
| P0-2 | 昨日コピーが引き継がない | ✅解消 | `lib/ky/copy-latest.ts`（`copyKyForToday`）＋ `/ky/paper`「前回を複製」ボタン |
| P0-3 | 保存KYを開き直せない | ♻️**UI消失（後退）** | クラウドに履歴は保存（`ky_records`、`GET /api/ky/records` は一覧返却）。だが **一覧表示＋「開く/再編集」UIが Phase 7 で削除**（旧 `ky-page-content.tsx`/`KyRecordList` が消えた）。`/ky/paper` は「最新1件の引き継ぎ＋前回複製」のみ。**任意の過去KYを開く画面が無い** → 09・10で新規P0として再掲 |
| P0-4 | 保存キー不一致で空転記 | ✅解消 | `lib/safety-diary/from-ky.ts` が手動/自動両キーfallback、Phase 7 `lib/ky/storage-migration.ts` で旧キー→`ky-record` 引き継ぎ |
| P0-5 | 行動目標・指差呼称の欄が無い | ✅解消 | `teamGoal`/`priorityItems`/`pointingCall` 追加、`ky-morning-signage.tsx` で行動目標・指差呼称を大表示 |
| P0-6 | 印刷が見慣れたKY用紙にならない | ⚠️部分・未検証 | `/ky/paper` は枠線つき「用紙」レイアウト＋`window.print()`＋`print:` ルートで操作系を隠す＝旧「カード積み」より用紙寄り。ただし **A4の表組み・確認印枠・元請確認欄は未実装**。実際の印刷見た目は **未検証（ブラウザ必要）** |
| P0-7 | 詳細モードでL・Sが印刷に出ない | ✅解消(コード) | 新UIは L・S を `<select>`（値が印刷に残る）＋「評価値N（ラベル）」をテキスト表示。旧 `<button>` チップ非表示バグは該当コンポーネント自体が削除済。印刷実物は⚠️未検証 |
| P0-8 | 職長名欄が無い | ✅解消 | `foremanName`＋「職長（リーダー）」欄 |

P0: **6 解消 / 1 部分(未検証) / 1 後退（新P0として再掲）**。

## P1（重要）8件

| # | 前回の問題 | 現状 | 根拠 |
|---|-----------|------|------|
| P1-1 | サイネージが作成端末でしか映らない | ✅解消 | Phase 6 `api/ky/signage`＋6桁コード、`/ky/morning?code=`。実DB越し共有は⚠️未検証 |
| P1-2 | サイネージ自動更新が無い | ✅解消 | `/ky/morning` 約8秒ポーリング（remoteCode時） |
| P1-3 | 元請確認・承認が無い | ❌未解消 | 承認/提出フローのコードは追加されていない。競合との主要差として継続（04・09・10） |
| P1-4 | クラウド保管が無い | ✅解消 | Phase 4 Supabase（`ky_records`/`worker_master`、service_role サーバー経由）。実稼働は⚠️未検証 |
| P1-5 | 擬似AIを「AI」と表記 | ✅解消 | Phase 5 実Gemini（`gemini-2.5-flash`）。UIは「本物のAI（Gemini）の提案」/「定型提案（フォールバック）」を明示表記。実応答は⚠️未検証 |
| P1-5b | サイネージに行動目標が出ない | ✅解消 | 同上 P0-5 |
| P1-6 | 参加者削除で署名がズレる | ✅解消 | Phase 1-3（PR #285）で対応（`lib/ky/signatures.ts`）。※今回の再レビュー範囲では静的確認のみ |
| P1-7 | 一覧に検索/並べ替え/現場名が無い | ♻️**UI消失（後退）** | `KyRecordSummary` に `siteName` はある（データは充実）が、**一覧UI自体が P0-3 と同根で消失**。検索/並べ替え以前に一覧が出ない |

P1: **6 解消 / 1 未解消(元請) / 1 後退(一覧UI)**。

## P2/P3 抜粋
- **P2-5 全員サインの時短** → ✅ **設計で解消**：Phase 3「作業員マスター」で署名を「登録者からチェック選択」に置換（毎朝の手書き・指サイン回しが不要）。前回最大級の紙ペイン「全員サイン」に効く。
- **P3-5 デッドコード整理（915行フォーム等）** → ✅ Phase 7 で削除（915行＋1623行）。

## 総括（軸1）
- 前回 P0 8 / P1 8 のうち、**コード上 12 件が解消、元請(P1-3)が継続、そして一覧・再編集UI（P0-3/P1-7）が一本化で"後退"** という構図。
- **最大の収穫**：継続運用（現場名/目標/指差呼称/クラウド/別端末）の穴がふさがった。
- **最大の宿題**：①過去KYの一覧・再編集UIの復活（一本化の副作用）②元請提出 ③実機での稼働確認（全項目"未検証"のまま）。
