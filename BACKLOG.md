# BACKLOG — 無人自走ループのタスクキュー（Fable 5・2026-06-10〜）

このファイルは loop-runner.ps1 が各イテレーションで参照するタスクキュー。
各起動で **未着手(`[ ]`)の最上位タスクを1つ**取り出して実行し、完了したら `[x]` に更新する。
**未着手が3件未満になったら、実行前に柱1〜3から新タスクを自分で起こして補充**（空にして止めない）。
ミッション全文・契約・絶対ルールは loop-prompt.txt を参照。

## 運用ルール（毎イテレーション厳守）
- まず CI緑の自分の未マージPRをsquashマージ→`git checkout main && git pull --ff-only`→clean確認（前イテレーションのPR回収）。衝突時は origin/main をブランチへ通常マージ。
- 次に未着手タスクを1つ実行。1ブランチ→ゲート(tsc0/lint errors0/test全pass/build成功)→push→PR作成。
- 完了タスクを `[x]` にし、cycle-log(docs/court-cases-db-2026-06-06/cycle-log.md)に記録。
- main常時デプロイ可能・各始終clean・安全タグ fable5-loop-start-2026-06-10（既存タグも）維持。捏造0・水増し0・既存破壊0。env/DB独断変更禁止(Path A)。main直接コミット禁止。CI緑のみマージ。

## 第三者レビュー方式（柱3および機能改善タスクの標準手順）
1. 対象機能に対し、厳しい第三者ペルソナを具体的に生成（性格・前提・我慢の限界）。
2. そのペルソナになりきり Playwright で実操作（dev server か本番URL）。screenshotで確認。
3. 「面倒/分かりにくい/紙の方が速い」点を忖度なく全部書き出す。
4. インパクトの大きい欠点を改善実装。
5. 再度ペルソナ目線で操作し「これなら使う」と言えるまで磨く。
6. docs/third-party-reviews/ に（ペルソナ・指摘・改善・再評価）を記録。

## 回収待ち（タスクではない。イテレーション冒頭の PR回収で処理）
- PR #459: フルハーネス形状指定時のランヤード降格（旧タスク「ハーネス×ランヤード混在スコアリング是正」に対応）
- PR #460: 化学物質RA→保護具ファインダー連携の「必要な保護具一式」可視化（旧タスク「chemical連携レビュー」に対応）

---

## 柱2: ダッシュボード強化（重点テーマ・上から実行）
- [ ] /site-records ライブダッシュボードを「安全担当が毎朝最初に開く1枚」へ強化: 現状は未是正指摘・対応中ヒヤリ・使用不可点検・今月委員会の4枚だが、健診期限・資格期限・点検NG・カレンダー今月タスクなど他ツールの「要対応」が横断集約されていない。全記録ツールを走査する「今日やること/期限切れ」横断パネルを設計・実装（既存の各ツールの純関数を再利用、作り直し0）。厳しいペルソナでPlaywright実機レビュー込み。
- [ ] /signage ダッシュボードに /site-records の現場記録データ（未是正指摘・要対策ヒヤリ等）を表示する「現場の安全状態」パネルを検討・結線: サイネージは気象・事故・法改正など外部情報中心で、自現場の記録キットのデータが一切出ていない断絶を解消。localStorageの共有可否（同一端末前提）を実機検証し、可能な範囲で1画面フィットを壊さず実装。
- [ ] ダッシュボード系の情報設計を全体最適: /site-records ハブ・/signage・/signage/display・トップの「現場ですぐ使う」の4箇所で「どこで何が分かるか」の役割分担を明文化し、相互導線を整備（重複・迷子の解消）。docs/fable-reexamination-2026-06-10/ に設計記録。

## 柱1: 前任(Opus 4.8)の全面再精査（発見したら是正タスクを起こす）
- [ ] 採用レビューの「構造的限界」「コード上限」結論をゼロベース再検証: docs/all-features-loop-2026-06-05・docs/adoption-re-review(PR#346)・docs/meeting-distributed-input で「これ以上はコードでは改善不能・オーナー判断待ち」とされた結論を列挙し、Fable 5の目で本当に手が無いか再評価。手があるものは是正タスク化し、docs/fable-reexamination-2026-06-10/ に「前任の判断→Fableの発見→是正内容」を記録。
- [ ] チャットボット残課題(doc13のP1/P2)の再精査: 前任が「P1/P2残し」と記録した項目を読み直し、lexical RAG・ハルシネーション3層の設計自体に Fable 5 視点での改善余地（検索精度・条文提示・回答速度）が無いか実機評価→価値ある是正のみ実装。
- [ ] 判例DB「歩留まり低下→コツコツのみ」結論の再検証: 前任は確証2出典の確定判例の追加歩留まりが落ちたと結論。政府公開ソース（裁判所サイト・労働委員会DB・官報等）で未踏の確証ルートが無いか調査し、取れるなら追加・取れねば結論を追認して記録（架空0厳守）。
- [ ] 参照0件の死蔵コード横断棚卸し: 前任はsignage配下のみ実施（4部品中2結線・1削除）。web/src/components・lib 全域に広げ、参照0件の部品/関数を依存グラフ込みで棚卸し→価値あるものは結線・不要は削除（保守負債削減）。判定根拠を docs/fable-reexamination-2026-06-10/ に記録。

## 柱3: 全機能の第三者レビュー巡回（継続）
- [ ] 点検記録(/site-records/inspection)を「重機・電動工具の始業前点検を毎日回す職長」ペルソナでレビュー→NG項目の追跡・未是正の可視化
- [ ] 月次報告(/site-records/monthly)を「本社へ毎月提出する元請安全担当」ペルソナでレビュー→転記の手間削減・前月引き継ぎ・提出体裁
- [ ] Eラーニング(/learning)を「新人に受講させたい安全担当」ペルソナでレビュー→受講のさせやすさ・記録の残り方
- [ ] トップページを「初訪の一人親方(スマホ)」ペルソナでレビュー→自分に関係ある機能への最短到達
- [ ] リスクマップ(/risk)を「台風前日の元請安全担当」ペルソナでレビュー→警報・防災情報の実用性

## オーナー判断待ち（Path A・自走では着手しない）
- [ ] 【オーナー判断/Path A】サイネージ休憩時間ビューにアフィリエイト安全グッズ(SignageFeaturedGoods)を出す是非を確認。出す方針なら結線、否なら部品削除。

## 軸1: 判例コツコツ（確証取れる分のみ・架空0・優先度低）
- [ ] 確証(出典2つ以上・事件名/裁判所/判決年月日/確定)が取れる労災・刑事・元方/注文者の確定判例を1件検証→取れたら追加、取れねば見送り

## 補充の指針（未着手が3件未満になったら、ここから新タスクを起こす）
- 柱1の再精査で見つかった誤り・甘さ・見落としの是正を個別タスク化
- 柱2のダッシュボードで「目からウロコ」級の新価値（水増し・乱造は禁止）
- 柱3のレビューで見つかった具体的欠点の改善タスク化
- 既存機能の出力(印刷・PDF・CSV)の実務品質向上、機能間の一気通貫の不足箇所
