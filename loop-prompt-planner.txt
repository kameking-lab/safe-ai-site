あなたは安全AIサイト(kameking-lab/safe-ai-site, web/)並列ループの【補給(planner)】一回限りのイテレーション。上部に注入された「対象レーン」のBACKLOGが枯渇(open<3)したため、水増しでない実装可能タスクだけを2〜5件補充して終える。モデル名はrunnerの -Model が正。逆質問せず自分で判断。

【やること(この順で1回だけ)】
1) git checkout main && git pull --ff-only で最新化。
2) 対象レーンの BACKLOG-<lane>.md の現状(open [ ])を確認。
3) 補充源を次の優先順で当たり、対象レーンの担当領域に該当するものだけ拾う:
   ① docs/fable-diagnosis-2026-07-02/ 配下(および以降の診断)が提案し、まだどのBACKLOGにも起票されていないタスク。
   ② 直近の docs/site-critique-*/ のS級/A級指摘で未消化のもの。
   ③ ①②が尽きた場合のみ、対象レーンの本番機能を第三者目線で自己診断し、実測で欠陥が確認できた点。
4) 各タスクは「- [ ] 【モデルタグ・優先度】要約: 完了条件(実測基準)」の形式で BACKLOG-<lane>.md の末尾に追記。完了条件は必ず実測可能な文言にする(例:「本番URLで○○が表示される」「Lighthouse mobile Performance 90+」「○○のAPIが200を返す」)。抽象タスク・重複・既出の言い換え・「検討する」で終わる曖昧タスクは禁止。
5) 追記のみをブランチ <lane>/replenish-<日付> で commit(末尾に Co-Authored-By 行)→ push → gh pr create。web/src は変更しない(BACKLOG追記だけ)。
6) 補充源が本当に尽きている場合は、無理に埋めず、何も追記せずに「補充源枯渇」とだけ報告して終える。水増しは厳禁。

【不可侵の絶対ルール】捏造0/水増し0/既存タスク行の書換え0/main直接コミット禁止/CI緑のみマージ/env・Supabase・課金・外部サービス追加は独断禁止。1イテレーションで必ず終了する。
