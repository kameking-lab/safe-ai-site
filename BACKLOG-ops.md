# BACKLOG-ops — 運用・自律運転インフラ班のタスクキュー（2026-07-02新設）

担当領域: リポジトリ直下の loop-*.ps1 / loop-*.txt / loop-config.json / BACKLOG*.md の運用機構 / docs/loop-status.md / タスクスケジューラ登録 / 横断計測スクリプト。**web/src は原則触らない**（掃除系タスクのみ例外・他レーン停止時間帯に実行）。契約・絶対ルールは loop-prompt-ops.txt を参照。

## 未着手（上から処理）

### 2026-07-12 外部酷評第2ラウンド注入（出典: docs/site-critique-2026-07-12/ 02章）
- [ ] 【Sonnet・P1】CR2-O1: 数値申告のratchet化第1弾=first-load JS 上限テスト常設 — 完了申告の抜き打ち15PRで崩れていたのは「CIで機械固定されていない数値主張」のみ（#871: 申告1,720KBに対し実測2,488KB=後続PRで再増加）。next build の First Load JS を主要route（/chemical-ra・/chemical-database・/chatbot・/law-search・/search・トップ）についてスナップショットし、閾値超過でfailするテスト（増やすPRは閾値を明示更新=ratchet）を常設。完了条件=現状値で緑・+10%仕込みで赤の実証＋handbook へ運用1行追記。

### 2026-07-12 日本一スコアカード注入（正本: docs/nihonichi-scorecard.md／戦略: docs/nihonichi-strategy-2026-07-12.md）
- [ ] 【Sonnet・P1】NIQ-OPS1: 日本一スコアカードの四半期再計測（初回=2026-10月第1週。以降3ヶ月ごとに本タスクを複製して積み直す＝1回で消さない）。手順は docs/nihonichi-scorecard.md §10 の記載どおり: 自サイト側（plain:status／bench:field-terms／rag-metrics／eval:chatbot-gen 本番実測／npm run test のsite-stats検証／モバイルLighthouse静穏窓実測）＋競合側（§1〜8の再計測curl: e-Gov「ユンボ」0件のままか・CREATE-SIMPLE ver・あんぜんサイト死傷DB最新年・jaish Last-Modified）。スコアカードの現在値・判定・最終計測日を更新し、**判定が動いたセルをPR本文に明記**。生成物（.bench/*.json等）はコミットしない。GSC稼働後（NIQ-SEO1完了後）はS-1/S-2に実順位・indexed件数を記入。
- [ ] 【Sonnet・P2】NIQ-OPS2: CLAUDE.md「優先度の高い課題」の陳腐化更新。2026-07-12実ファイル確認で 4(Eラーニング編集=elearning-editor-panel.tsx実装済)・5(KY音声入力=voice-input-field.tsx/PDF出力=ky-print-sheet.tsx実装済)・6(チャットボット=本番稼働・51問eval 100%) が完了済みなのに未完リストに残存＝新規セッションの判断を汚す。残課題（通知=閉端末Push未実装・サブスク課金=PAID_MODE off等）と最新状態（docs/nihonichi-scorecard.md参照）に合わせて同セクションを書き替える。完了条件=CLAUDE.mdの課題リストが実装実態と一致（スコアカードへの参照付き）。

- [x] 【Opus・P0】O16-a: 点火の恒久解＝loop-config.json＋loop-launcher.ps1（引数ゼロ・configのlanes/model/untilIsoを読んで各レーンrunnerを冪等起動）＋タスクスケジューラ再登録（launcherを「ログオン時＋毎日07:00」・引数焼込みゼロ）。untilIso超過時は docs/loop-status.md に停止理由を書いてから終了（黙って死なない）。設計は docs/fable-diagnosis-2026-07-02/08-autonomous-operations.md §A。→ 2026-07-02完了(PR)。恒久解3層＋逸脱2点は docs/fable-diagnosis-2026-07-02/O16-implementation-notes.md 参照。
- [x] 【Opus・P0】O16-b: 補給の自動化＝loop-prompt-planner.txt 新設（レーンBACKLOG open<3 で launcher が当該レーンで planner を1回先行起動。補充源の優先順=①診断docsの未起票タスク②直近critique S/A残③第三者レビュー自己診断。水増し禁止・実測完了条件必須を強制）。→ 2026-07-02完了(PR)。launcherに open<3 ゲート＋対象レーン注入の一時プロンプト＋別タグ非衝突ワンショットを実装。
- [x] 【Opus・P1】O16-c: 点検の自動化＝loop-prompt-critic.txt 新設＋launcher連動（lastCriticIsoから7日超で通常レーンより先に1回起動→docs/site-critique-<date>/ 生成→S/A級を該当レーンBACKLOG冒頭へ注入→lastCriticIso更新）。→ 2026-07-02完了(PR)。lastCriticIsoは gitignore の loop-state.json に保持(逸脱1)。
- [x] 【Sonnet・P1】S13-a: 報告の一元化＝docs/loop-status.md 規約新設＋各レーンプロンプトへ「イテレーション末尾に自レーン行を更新」を追記。→ 2026-07-02完了(PR)。新規 loop-report-status.ps1（レーン別に最終稼働/直近PR/残open/判断1行を外科upsert・ロック直列化・非致命）＋launcherが `SAFE_AI_LOOP_STATUS` を子へexportし集約先1ファイルへルーティング＋`<!-- LANE-REPORT -->`区画を逐語保存＋6レーンプロンプトに step5.5 追記。O16逸脱2（S13-a不要判断）を解消＝launcher単独生成は per-iteration更新できず§Dを満たせないため正式実装。完了条件（2レーン→status反映・再報告で重複せず置換）を実測確認。詳細は O16-implementation-notes.md「逸脱2の解消」。
- [x] 【Sonnet・P1】S13-b: プロンプト鮮度是正＝loop-prompt*.txt から「あなたはOpus 4.8」等のモデル名直書き・処理済みの【再開最優先2026-06-13】節を除去しモデル非依存化（runnerの -Model が正）。バックオフ上限を10分→30分の3段（5/10/30）へ延長。完了条件=全プロンプトにモデル名焼込みゼロ・runnerのバックオフテスト（ドライラン）で30分到達。→ 2026-07-02完了(PR)。6プロンプト(data/seo/ux-records/ux-tools/ux-hub＋legacy loop-prompt.txt)の1行目モデル自己申告と Co-Authored-By のモデル名焼込みを「モデル名はrunnerの -Model が正」＋「Co-Authored-By 行・モデル名はセッションの実モデル」へ置換、legacy から【再開直後の最優先(2026-06-13)】節と Opus/Fable 期の歴史記述を除去。焼込みゼロを Grep で実測（マッチ0）。runner バックオフを Get-BackoffSeconds へ関数化し 3→5分/4→10分/5+→30分の3段化＋`-SelfTest` ドライラン新設（Claude CLI不要・6ケース＋30分キャップ到達をアサート）→ `powershell -File loop-runner.ps1 -SelfTest` が PASS(1800s到達)を実測。
- [x] 【Opus・P0】O17: 自動復活の管理者不要化＝launcher に `-InstallUserStartup` 新設（現ユーザーの Startup フォルダに loop-launcher 起動 cmd を配置・管理者不要でPC再起動→ログオン時に全レーン復活）。旧 `-Register` はスケジューラ登録に admin が要る事実を実測確認（Limited/Interactive タスクでも Access Denied）＝「残1手動 admin」は不可避と判明したため、admin不要の代替経路を本線化。loop-status の警告を「復活経路が両方無い時だけ大声・Startup済みなら静かなINFO」へ2分岐化＋Register拒否メッセージを正確化（虚偽理由「既存タスクがelevated」を撤去）。→ 2026-07-03完了(PR)。-WhatIf ドライラン＋実インストール＋post-verify＋banner分岐を実測。Startupエントリ実配置済み（PC再起動後の自動復活が admin 待ちゼロで有効化）。
- [x] 【Opus・P1】報告系補充: `最終更新:` を毎イテレーション更新の正直なハートビート化。従来は launcher（ログオン＋毎日07:00）のみが刻印→パス間（最悪ほぼ丸1日）は全レーン生存中でも時刻が凍結し、監視ポイント#1「ループが生きているか」で「生存」と「無言の死」を区別できなかった。loop-report-status.ps1（毎イテレーション書込み・当班所有）が region 外の `最終更新:` 行を label 接頭辞一致でその場更新（新規作成はせず launcher の所有を尊重・strings 欠落時は非日本語キーで無マッチ＝安全no-op）。→ 2026-07-03完了(PR)。-WhatIf でハートビート予告を出力・実書込みは temp コピーで launcher点火行/他レーン報告行/region markers 不変＆`最終更新`が06:38→現在時刻へbumpを実測（06:38凍結の偽装アラームの再発防止）。補充理由=未着手[ ]は line12/14 の2件のみ・両方とも静穏窓待ちでブロック（3件未満）→運用4系統の欠陥から補充。
- [x] 【Opus・P1】点検系補充: 運用機構(launcher/runner/reporter)の純関数 `-SelfTest` を CI 常設ゲート化。従来 loop-*.ps1 の -SelfTest はローカル手動実行のみが守り、PR時に誰も回さない＝機構リグレッション（生存スキャンの偽陽性#782/#784・wedge天井#789・報告区画upsert）が無防備に main へ着地しうる点検系の穴。直近ops修理は毎回 -SelfTest を足してきたが実行者が居なかった。→ 2026-07-03完了(PR)。`.github/workflows/ops-selftest.yml` 新設＝loop-runner/loop-launcher/loop-report-status.ps1 変更PRでだけ発火（パスフィルタで通常PRには走らない）、本番と同一の `powershell -File …-SelfTest` を windows-latest（Windows PowerShell 5.1）で3本起動し exit 0/1 を必須ゲート化。3本の -SelfTest がローカルで ALL PASS / exit 0 を実測、YAML構造（1 job・4 step・runs-on windows-latest・タブ無し）を実測。補充理由=未着手[ ]は2件のみ両方静穏窓待ちでブロック（3件未満）→点検系欠陥から補充。
- [x] 【Opus・P1】報告系補充: 復活バナーの正直化＝`-InstallUserStartup` 成功時に loop-status.md の復活バナーをその場で正へ是正。O17でStartupエントリを配置しても `-InstallUserStartup` は status を再生成せず `exit 0` するため、旧 launcher 実行が刻んだ大声バナー「## 注意: スケジューラが未更新…-Register必須」が居残り、毎イテレーションのハートビート(#602)が `最終更新` だけを更新するので**新しく見える偽の大声アラーム**として監視ポイント#1を汚染（scheduler は stale のままだが Startup で復活は担保済＝admin は任意）。修正: launcher に `Set-ResurrectionBanner` 新設＝Get-SchedulerHealth/Get-StartupResurrectionHealth を install パス手前へ前方移動し、install 成功後に status のバナーだけを外科置換（sched≠ok∧startup=ok→静かなINFO / 両ok→バナー除去 / 両不可→大声）。**ASCII厳守**＝日本語リテラルを .ps1 に埋めず、アンカー（deadline接頭辞・lanesHeader・warn/stopped除外）を全て loop-status-strings.txt から実行時読取りしてバナー区画を構造的に特定（現行/旧文言・deadlineバナー混在を安全に処理）。6レーンのハートビートと同じ `.lock` を取得し read-modify-write を直列化。→ 2026-07-03完了(PR)。-WhatIf ドライランで大声→静かの1区画のみ置換・レーン行/LANE-REPORT/監視ポイント逐語保存を diff で実測、実 install で live dashboard の偽アラームを解消（他5レーン稼働中でも lock で無衝突＝並走 heartbeat の新しい行が残存）・lock 非リーク確認。補充理由=未着手[ ]は line14/16 の2件のみ・両方とも静穏窓待ちでブロック（3件未満）→運用4系統(報告)の欠陥から補充。
- [x] 【Opus・P1】報告系補充: 状態ファイルロックのstale-orphan回収＝holderがmid-write（Ctrl-C/スリープ/クラッシュ）で殺されると `docs/loop-status.md.lock` が永久に残り、以後の全報告・バナー書込みがロック取得失敗→ハートビート(`最終更新`)が凍結し監視ポイント#1に**偽の「ループ死亡」アラーム**を生む（#602/#608の偽アラーム掃討と同族の欠陥）。修正: loop-report-status.ps1 のロック取得を `Get-StatusLock` 関数化＝正当な保持は全6レーン直列でもsub-second→60s超のロックはliveでありえないので1回だけ外科回収して即retry（fresh lockは尊重・回収せず）。loop-launcher.ps1 のバナー整合ロックにも同じstale回収をインライン適用。`-SelfTest`（Claude/実status不要・temp限定）を新設し「clean acquire / fresh尊重 / stale回収」の3系5アサートを実測。→ 2026-07-03完了(PR)。`-Lane selftest -SelfTest` が ALL PASS・reporter/launcherの `-WhatIf` ドライラン無回帰を実測。補充理由=未着手[ ]は掃除系/perf実測の2件のみ・両方とも静穏窓待ち（全6レーン稼働中）でブロック（3件未満）→運用4系統(報告)の欠陥から補充。
- [x] 【Opus・P1】報告系補充: 設定破損時の沈黙死を大声化＝loop-config.json が欠落／JSON構文破損／lanes欠落のとき、旧設計は launcher の私的ログにだけ ERROR を書いて `exit 1` し **docs/loop-status.md を一切触らない**ため、社長が唯一見る監視ファイルが最後の正常描画のまま凍結（監視ポイント#1で「生存」と「無言の死」が区別不能）。しかも社長の最頻の手動操作＝untilIso の手編集こそが JSON を壊す最有力トリガー＝O16 が殺すはずの沈黙死そのもの。修正: launcher に `Write-ConfigErrorStatus` 新設＝設定3失敗（欠落/parse失敗/lanes空）で loop-status.md を「## !!! 起動失敗: config読込不可（全レーン未起動）」の大声バナー＋直し方＋エラー全文＋`最終更新=now` で再生成し、レーン自己報告 region は逐語保存（前回起動から生存中のレーンの報告を破壊しない）。ロックは既存の stale-orphan 回収を `Get-LauncherStatusLock` へ関数化（Set-ResurrectionBanner のインライン複製を解消し reporter の Get-StatusLock と同一意味論に統一）。launcher に `-SelfTest` 新設（clean acquire/fresh尊重/stale回収の3系5アサート・temp限定）。→ 2026-07-03完了(PR)。`-SelfTest` ALL PASS、`-WhatIf` で config欠落/parse破損/lanes空の3系が dryrun へ大声バナーを書き実 status は byte不変を実測、正常 config の -WhatIf 無回帰も確認。補充理由=未着手[ ]は line15/17 の掃除系/perf実測の2件のみ・両方とも全6レーン稼働中で静穏窓待ちブロック（3件未満）→運用4系統(報告)の欠陥から補充。
- [x] 【Opus・P1】報告系補充: 稼働期限の沈黙停止を正直化＝loop-runner.ps1 が UntilIso 到達で `break`→exit 0 する際、従来は **私的なレーンログにだけ「Deadline reached」を書き** docs/loop-status.md を一切触らなかった。稼働中に期限が過ぎると（再起動なしのセッション中＝launcherの停止バナーは次のログオン/07:00まで出ない・admin未登録なら実質次ログオンまで）全レーンが黙って exit 0 し、社長の監視ファイルは「起動」表示のままハートビートが**凍結・理由なし**＝O16 が殺すはずの沈黙正常停止そのもの（#602/#608/#611/#616 が掃討した**偽アラーム**の裏返し＝真の停止が**無言**）。修正: runner に `Report-DeadlineStop` 新設＝期限breakの2箇所（ループ先頭/反復後）で当レーンの自己報告行を loop-report-status.ps1 経由で「停止:稼働期限到達。延長は loop-config.json の untilIso を書換え→launcher再実行」に更新（→ハートビートも停止時刻へ正直にbump・凍結の理由が残る）。ゲート `Test-ShouldReportDeadlineStop`＝**永続laned runのみ**（one-shot planner/critic=MaxIterations≥1 は誤stampせず・legacy無laneは行なし）を純関数化し `-SelfTest` に4ケース追加。日本語ノートは loop-status-strings.txt の `runnerDeadlineStop`（ASCII厳守・{UNTIL}差込）から実行時読取り＋ASCIIフォールバック。→ 2026-07-03完了(PR)。`-SelfTest` ALL PASS（backoff6＋gate4）、過去日UntilIso＋捨てレーンで**実runner→中央statusへ停止ノート書込み＋heartbeat 07:00→停止時刻bump**をE2E実測（claude非起動＝先頭breakで停止・GUARD誤爆なし）。補充理由=未着手[ ]は掃除系/perf実測の2件のみ・両方とも全6レーン稼働中で静穏窓待ちブロック（3件未満）→運用4系統(報告)の欠陥から補充。
- [x] 【Opus・P1】点検系補充: 定期点検(critic)の沈黙死を正直化＝loop-launcher.ps1 が weekly critic を起動する際、旧設計は `lastCriticIso=now` を**起動前に一括stamp**し、`Invoke-OneShot` は timeout強制終了/異常終了/起動不可を**私的launcherログにだけ**書いて戻り値を捨てていた。→ critic one-shot が失敗しても社長が唯一見る loop-status.md には**何も出ず**、full interval(既定7日)分 site-critique が生成されず S/A級指摘がレーンに補充されないまま**点検系が沈黙で停止**（#602/#608/#611/#616/#619 が掃討した「報告系の沈黙死」の**点検系版**）。修正: `Invoke-OneShot` を `ok/nonzero/timeout/skipped/whatif` の outcome 返却へ変更＋純関数 `Resolve-CriticStamp`（ok→now＝full interval / 失敗→now-(every-Retry)日へ**back-date**＝全期間ブラックアウトも毎ログオン30分再点火も避け約1日後に自動再試行）新設。critic gate は**concurrent launcher の二重起動を防ぐため now を先行stamp**した上で、`-not $WhatIf ∧ outcome≠ok` のとき back-date へ再stamp＋loop-status.md 冒頭（`## 各レーン`直上）へ大声バナー（理由=timeout/nonzero/skipped）を出す。ASCII厳守＝日本語は loop-status-strings.txt の criticFailHeader/Body1/Body2（{REASON}差込）から実行時読取り。→ 2026-07-03完了(PR)。`-SelfTest` に Resolve-CriticStamp の5アサート追加→ALL PASS(lock5+critic5)、**隔離temp**で実launcher×fake runner(exit3/exit0)をE2E実測＝失敗→バナー描画＋lastCriticIso now-6d(1日後再試行)、成功→バナー無し＋lastCriticIso=now(full interval)。補充理由=未着手[ ]は掃除系/perf実測の2件のみ・両方とも全6レーン稼働中で静穏窓待ちブロック（3件未満）→運用4系統(点検)の欠陥から補充。
- [x] 【Opus・P1】報告系補充: 個別レーンの沈黙死を正直化＝トップの `最終更新` ハートビートは**生きている任意の1レーンが毎イテレーションbump**するため、6レーンのうち**1本だけが黙って死ぬ**（期限停止でも config破損でもない・runnerがwedge/クラッシュ）とハートビートは新鮮なままで、社長は**どのレーンが落ちたか**を6本の `最終稼働` を目視で「今」と引き算しない限り判別不能＝#602/#608/#611/#616/#619 が**集約レベル**で掃討した沈黙死の**個別レーン残渣**（監視ポイント#1の最後の穴・検知は次launcher passまで最悪~24h）。修正: loop-report-status.ps1 に純関数 `Get-StaleLanes`（region行を reportLine テンプレ由来の実行時regexでparse＝日本語リテラルはソースに置かず strings 由来・{LANE}/{LASTRUN}を捕捉）＋`Get-LaneHealthLine` 新設。**報告する=生きている**任意のレーンが毎回 region 先頭に管理1行を再計算＝stale（既定120分超）なら「健在性 !!! 停止疑い: <lane>(最終.../約Nh前)…復活=runner再起動 or 次回07:00 launcher」、無ければ「健在性: 全Nレーンが直近120分以内に自己報告」。→ 生きたレーンの1インターバル(数分)で死んだレーンを名指し検知（~24h→~2h）。冪等（prefix一致で旧health行を除去し再挿入・重複しない）＋自己修復（死んだレーンの次回報告で自レーン行が新鮮化しバナーがOKへ）。しきい値120分は重い1イテレーション(実装+build+lint+vitest+PR≒30-40分・CI待ちは翌回送り)の>3倍ヘッドルーム＝誤アラーム実質ゼロ。launcher の region 逐語保存(`$old[$b..$e]`)で health行はlauncher pass も生存。ASCII厳守＝日本語は loop-status-strings.txt の laneHealthOk/Stale/Item/Sep から実行時読取り。→ 2026-07-03完了(PR)。`-SelfTest` に Get-StaleLanes の8アサート追加（ASCII合成テンプレで regex-build path を実測）→ALL PASS(lock3+stale8+既存)、**隔離temp**で実reporter E2E＝stale=data(4h)をバナー名指し／2回目報告で重複せず1行／data復帰報告でOKへ自己修復を実測、`-WhatIf` はread-onlyで health行を予告。補充理由=未着手[ ]は掃除系(line19)/perf実測(line21)の2件のみ・両方とも**全6レーン稼働中**（loop-status.md heartbeat直近1分内・data #626/#595等が並走報告）で静穏窓待ちブロック（3件未満）→運用4系統(報告)の欠陥から補充。**scope注記**: 一度も自己報告していないレーン(launcher placeholder)は timestamp が無く staleness 判定外＝出生時未起動は launcher の「起動」表示側の責務（本fixは「報告後に沈黙した」個別死の検知）。
- [x] 【Opus・P1】報告系補充: 健在性バナーをroster対応化＝一度も自己報告していないレーンを名指し検知。従来 `Get-LaneHealthLine` は region に**行があるレーンしか数えず** TOTAL=region行数だったため、**enabledだが一度も報告していないレーン**（出生時死＝起動直後クラッシュ／報告経路の破損）が完全に不可視で、OKバナーが「全Nレーン…**個別レーンの沈黙停止なし**」と**全roster健全を誤主張**（#628 scope noteが繰り延べた盲点＝監視ポイント#1の最後の穴）。実際 live loop-status.md は config**6レーン**中 seo/ux-records が region未登場のまま「**全4レーン**…停止なし」と表示。修正: `Get-EnabledLaneRoster`新設（loop-config.json の enabled∧name を roster読取り）＋`Get-LaneHealthLine`を roster対応化＝**stale**（報告後に沈黙>閾値=大声・report-then-silent死）と**missing**（一度も未報告=注意・起動直後は数分で自己解消）を2分岐、優先度 **stale>missing>ok**。TOTALは region行数でなく**roster数**。config不読/欠落→空roster→**旧来の報告数ベースへ安全fallback（誤アラーム0）**。新バナー `laneHealthUnreported` を strip対象に追加し3種prefix冪等除去→再挿入を維持。ASCII厳守＝日本語は loop-status-strings.txt から実行時読取り。→ 2026-07-03完了(PR#631)。`-SelfTest`に roster7アサート追加（E:roster抽出/F1:未報告→注意名指し/F2:全報告→roster数TOTALのOK/F3:stale優先/F4:空roster fallback）→ALL PASS(計20)。**live file**への`-WhatIf`（read-only）で「健在性 注意: 2/6レーンが未報告: seo、ux-records」を正しく出力（偽の全4レーンOKを置換）を実測、**隔離temp**で data→ops×2 の実書込みE2E＝health行1本・ops行重複なし（strip+upsert冪等）を実測。web/src非侵。補充理由=未着手[ ]は掃除系(line20)/perf実測(line22)の2件のみ・両方とも**全6レーン稼働中**で静穏窓待ちブロック（3件未満）→運用4系統(報告)の欠陥から補充。
- [x] 【Opus・P1】補給系補充: planner one-shot の同一クローン並走gitレース封止＝launcher の supply gate（`open<3`）は**当該レーンの persistent runner が既に生きているか**を見ずに planner one-shot を発火していた。冷起動（reboot/logon＝何も走っていない）では正しく「起動前に補給」だが、O16 の中核＝**毎日07:00/logon の自己修復re-run** ではレーンは既に稼働中で、同一クローンに **2本目の claude+git プロセス**が同時発火＝`index.lock` 競合／ブランチclobber（**既存破壊**のgitレース）。idempotent guard（line841）は persistent runner の二重起動しか防がず one-shot は無防備。修正: 純関数 `Test-ShouldFirePlanner($Open,$LaneRunning,$HasPrompt)` 新設＝`HasPrompt ∧ ¬LaneRunning ∧ 0≤Open<3` でのみ発火。gate を `$running.ContainsKey(lane)` で分岐＝稼働中レーンは「補給skip（レーン自身が step5.5 契約で自己補給）」を launcher ログに明記。→ 2026-07-03完了(PR)。`-SelfTest` に planner 5アサート追加（発火1＋skip4：running/未drain/欠落backlog/prompt無）→ALL PASS(計16)。**live config への `-WhatIf`（read-only・プロセス起動なし）で ops(open=2)/seo(open=1)/ux-tools(open=1) の3レーンが**まさに今**drained∧稼働中→旧コードなら3本の並走planner発火の所を「skipping…avoids concurrent-git race」を実測、data/ux-records/ux-hub(open≥3)は無変化。web/src非侵。補充理由=未着手[ ]は掃除系(line21)/perf実測(line23)の2件のみ・両方とも全6レーン稼働中で静穏窓待ちブロック（3件未満）→運用4系統(補給)の欠陥から補充。
- [x] 【Opus・P1】点検系: weekly critic one-shot を隔離 git worktree で実行＝critic は `repoRoot`（main ツリー）で `-Lane site` として走り、ops persistent runner も同 main ツリーで稼働＝**両者が同一 working tree で git を並走**（planner と同族のレース）。planner は「稼働中ならskip」で正しく封止できるが、critic を同様に skip すると **ops はほぼ常時稼働**のため critic が恒常 starve し weekly site-critique が二度と走らない＝skip は不可。正解は critic one-shot を `git worktree add` の隔離ツリーで実行（main ツリー不可侵・生成した site-critique/BACKLOG注入は別ツリーで commit→PR）。設計は docs/fable-diagnosis-2026-07-02/08-autonomous-operations.md §C 突合。完了条件=critic実行中に ops runner が同時 commit しても index.lock 競合ゼロ・`-SelfTest` で worktree 経路のドライラン検証。→ 2026-07-03完了(PR)。純関数群 `Get-CriticWorktreePath`（repo の**兄弟**＝nested不可・launch stampでスコープ）/`Test-WorktreeIsIsolated`（正規化フルパス比較でmainツリー内・同一を弾く）/`Get-WorktreeAddArgs`（`--detach @origin/main`＝branch名衝突を回避＝git は同一branchの二重checkoutを拒否するため）/`Get-WorktreeRemoveArgs`（`--force`）/`Invoke-Git`（throwしない薄wrapper）を新設。critic 起動を「fetch origin/main→prune→orphan worktree除去→detached worktree add→Invoke-OneShot(LaneRepo=worktree)→finally で強制remove+prune」へ置換＝**main ツリーの index/HEAD を一切共有しない**ため ops runner の同時 commit と index.lock 競合ゼロ。isolation invariant 違反時は `worktree-unsafe` で**in-place実行を拒否**（レース再開より未実行を選ぶ）。critic プロンプトも「隔離worktreeにdetachedで置かれている＝`git checkout main`するな・fetch/pull不要・その場で `ops/critique-<date>` を切れ」へ改訂。→ `-SelfTest` に worktree 6アサート追加（stamp scope/sibling-isolated/nested→fail/repo自身→fail/add detached/remove forced）→ALL PASS(計22)、`-WhatIf` full run 無エラー＝critic dry-run は phantom worktree への Invoke-OneShot を避け「fetch→add→run→remove」の正確な計画のみ出力（`skipped, no runner` 誤ログを排除）、全6レーン alreadyRunning で web/src 非侵。補充理由=本タスクが未着手[ ]最上位。
- [x] 【Opus・P1】点火系補充: 個別レーンの沈黙死の**復活**を常駐watchdog化＝#628/#631 で個別レーンの沈黙死を**検知**（loop-status の健在性バナーが死んだレーンを名指し）できるようになったが、**復活**は次のログオン/毎日07:00の launcher pass 頼りのまま（-Register の07:00はadmin未登録＝実質次ログオンまで最悪~22h）。検知と復活の間に丸一日近い穴が残る＝O16 が殺すはずの「止まったまま放置」。修正: launcher に `-HealOnly` 新設＝**死んだレーンのrunner再起動だけ**を行い deadline/planner/critic/status/git worktree は一切触らない軽量watchdog。冪等性は loop-runner.ps1 の per-lane 単一インスタンスガードが担保（生きているレーンを再起動しても新processが即self-exit）。純関数 `Get-LanesToHeal`（enabled∧未起動のレーン名を返す・disabledは復活対象外）＋実行ブロック（configのuntilIso超過なら復活せず＝停止意図を尊重／process scanが0件なら**復活拒否**＝WMI outage時はrunnerのガードもfail-openし二重起動になるため・冷起動はlogon launcherの責務）。ops プロンプトに step5.6 追記＝**常時生存する唯一のops**が毎イテレーション末尾（報告直後）に `-HealOnly` を実行＝個別に死んだ他レーンを~1 ops interval（数分）で復活（~22h→数分）。→ 2026-07-03完了(PR)。`-SelfTest` に heal 4アサート追加（全生存→空／1レーン死→名指し／disabled除外／空scan→全enabled返却=caller側でrefuse）→ALL PASS(計26)。`-HealOnly -WhatIf` を**live config**で実行＝全6レーンprocess生存を確認（健在性バナーの「停止疑い」は**報告**staleでありprocessは生きている＝heal正しくno-op）、**隔離temp config**で死んだfakeレーン→「would resurrect」＋disabled除外＋過去untilIso→「healing nothing」の各分岐を dry-run 実測（実launch/status書込みゼロ）。web/src非侵。補充理由=未着手[ ]は掃除系(line23)/perf実測(line25)の2件のみ・両方とも全6レーン稼働中で静穏窓待ちブロック（3件未満）→運用4系統(点火)の欠陥から補充。
- [x] 【Opus・P1】報告系補充: 未報告レーンの沈黙死を正直化＝#628/#631 は「一度も自己報告していないレーン(missing)」を検知できるようになったが、timestampが無いため**永久にQuietな注意バナー**（「起動直後なら数分で解消」）でしか出せず、**出生時死（runnerが起動直後に落ちる）や報告経路の恒久破損**が2時間でも1日でも soft-worded のまま＝監視ポイント#1の最後の偽の安心（#602/#608/#611/#616/#619/#628/#631 が掃討した沈黙死の**未報告レーン残渣**）。live status も seo/ux-records が「未報告」のまま何時間も Quiet で居残る恐れ。修正: reporter に純関数 `Get-MissingLanes`（roster−報告済み行）/`Get-MissingSinceMap`（region内ASCIIマーカー `<!-- lane-missing-since: seo=ISO ... -->` をparse）/`Update-MissingSinceMap`（依然missing=旧stamp維持・新規missing=now・復帰/roster外=drop）/`Format-MissingSinceMarker` を新設し、**region内マーカー**で未報告レーンに時計を与える（side fileでなくregion内＝status lockで直列化・launcherのverbatim保全に相乗り・報告した瞬間stampがdropして自己修復）。`Get-LaneHealthLine` を「stamp が StaleMinutes(120分)以上経過した未報告レーン＝**born-dead(大声 laneHealthBornDead)**／grace内＝従来Quiet」へ2分岐（優先度 stale>born-dead>missing(quiet)>ok）。born-dead文字列が無い旧stringsでも missing は必ず Quiet で名指し＝**falsely-OK には絶対に落とさない**。→ 2026-07-03完了(PR)。`-SelfTest` に G群12アサート追加（grace内→Quiet／120分超→大声名指し／stale優先／map keep-stamp・now-stamp・drop-recovered／marker round-trip／empty→marker無し／Get-MissingLanes）→ALL PASS(計32)。**隔離temp**でE2E＝ops報告→data/seo stamp・Quiet／marker200分back-date+data報告→seoだけbornDead大声・dataはdrop／seo報告→marker消滅・OK を banner分類で実測。**live status への `-WhatIf`（read-only・md5不変）**で seo/ux-records に時計を刻む予告＝以後120分沈黙で Quiet→大声へ昇格を実測。web/src非侵。補充理由=未着手[ ]は掃除系(line24)/perf実測(line26)の2件のみ・両方とも全6レーン稼働中で静穏窓待ちブロック（3件未満）→運用4系統(報告)の欠陥から補充。
- [x] 【Opus・P1】点火系補充: 復活watchdogのops単一障害点を封止＝#644 の `-HealOnly` は** opsだけ**が step5.6 で毎イテレーション実行するため、**ops runner 自身が死ぬ**と誰も ops を復活できず次のログオン/07:00 launcher pass まで穴（常時ログイン機では数日）。しかも復活は**mainツリーからしか正しく動かない**＝非opsの5レーンは `..\safe-ai-lanes\<name>` の別クローンで稼働し、そこから `-HealOnly` を撃つと loop-config.json の lanesRoot/repoPath を誤解決（最悪レーンを別ツリーへ起動）＝ops SPOF はアーキ内在。O16「止まったら即戻る」の後半が最も load-bearing なレーンで欠けていた。修正: 新規 `loop-watchdog.ps1`（**mainツリー常駐**・git/claude不使用の極小ループ・PURE ASCII）＝一定間隔で `loop-launcher.ps1 -HealOnly` を撃つ＝ops含む任意の死レーンを~1 watchdog interval で復活。launcher の**フルパス末尾**（`-HealOnly` は早期exitで対象外）で idempotent に spawn（自身の single-instance guard＋稼働中skip）＝既存の admin不要 logon→Startup→launcher チェーンが watchdog 自身の復活も担保（watchdog はレーンrunnerより遥かに単純＝死ににくい）。純関数 `Test-WatchdogPastDeadline`（blank/garbage untilIso→fail-open で healing継続・>=境界で停止）/`Get-WatchdogInterval`（60s floor で busy-spin封止）を `-SelfTest` で検証。deadline超過は watchdog 自身が clean停止（zombie無し）。→ 2026-07-03完了(PR)。watchdog `-SelfTest` 11/11 PASS・launcher `-SelfTest` 無回帰 ALL PASS、watchdog `-WhatIf` で `launcher -HealOnly -WhatIf` チェーンを read-only 実証(exit0)、launcher `-WhatIf` フルパスで「would spawn heal watchdog」を出力＋**loop-status.md byte不変**を md5 実測。web/src非侵。活性化=マージ後の次 launcher フルパス（既存 logon チェーン）。補充理由=未着手[ ]は掃除系/perf実測の2件のみ・両方とも全6レーン稼働中で静穏窓待ちブロック（3件未満）→運用4系統(点火)の欠陥から補充。
- [x] 【Opus・P1】報告系補充: 健在性rosterの読取り元をクローン→**中央status同一ツリー**へ是正＝loop-report-status.ps1 は自レーン行を **SAFE_AI_LOOP_STATUS 経由で main ツリーの中央 loop-status.md に書く**のに、roster（enabledレーン一覧＝健在性バナーの分母・未報告/born-dead判定の母集団）は **$RepoPath（＝自クローン）の loop-config.json から読んで**いた。非opsの5レーンは `..\safe-ai-lanes\<name>` の別クローンで走りそれぞれ**自分のcommit済み config**を持つため、健在性バナーの roster が「**どのレーンがたまたま報告したか**」に依存し、社長が最も触る main ツリー working-tree の config編集（untilIso bump / レーンtoggle＝#630系の enabled 切替）が**次のpullまでクローンに伝播せず**、報告元次第で分母がズレる（監視ポイント#1の roster 一貫性欠陥＝#628/#631/#640 が積み上げた未報告検知の土台が報告元non-determinism で揺らぐ）。修正: 純関数 `Resolve-RosterConfigPath($StatusPath,$RepoPath)` 新設＝roster config を**中央 status ファイルの居るツリー**（`<maintree>\docs\loop-status.md` の grandparent＝launcher が実際に読む main working-tree config）へアンカー、anchored 不在時のみ $RepoPath へ安全fallback（standalone/manual・異常 status path＝空roster false-alarm 0 の契約を保存）。→ 書込み先（中央）と読取り元（roster）が**同一ツリー**に一致＝全レーンが「launcher が実際に起動した roster」を共有。→ 2026-07-03完了(PR)。`-SelfTest` に E2群4アサート追加（非opsクローン模擬でmain 3レーン roster を選択／clone の1レーンを選ばない／anchor不在→RepoPath fallback／blank status→fallback・throwなし）→ALL PASS(計36)。**live 中央 status への `-WhatIf`（read-only）で roster が 6レーンに解決＝main ツリー無回帰**を実測。web/src非侵。補充理由=未着手[ ]は掃除系(line26)/perf実測(line28)の2件のみ・両方とも全6レーン稼働中で静穏窓待ちブロック（3件未満）→運用4系統(報告)の欠陥から補充。
- [x] 【Opus・P1】報告系補充: logs/の無制限肥大を封止＝launcher/runner/watchdogいずれもlogs/へ実行毎ログを吐くのに**どのスクリプトも1件も剪定しない**＝常時ログイン機では毎full pass(logon/07:00)＋watchdog heal spawnが launcher-*/watchdog-* を、runner再起動毎に loop-*.log を無限に積む（現に90件超）。操作者が死レーンを診断する第一の面(=報告系の human surface)が読めなくなり、いずれディスクも侵す＝止まったら誰も気付けない。修正: 純関数 `Get-LogsToPrune($Files,$Now,$RetentionDays,$KeepMin)` 新設＝**ループ自身が生成した4種のみ**(launcher-*.log/loop-*.log/watchdog-*.log/planner-*.txt)を対象、手書きの progress/audit .md と非該当ファイルは不可侵、**最新KeepMin件は年齢無関係に必ず保持**(burst下でも直近診断が残る)、残りのうち RetentionDays 超のみ削除。launcher の full pass 末尾(HealOnly非対象・deadline無関係＝掃除は常に安全)で 14日/最新20件保持で sweep、**開いているrunnerログはWindows write lockでRemove-Itemが失敗しskip**。→ 2026-07-03完了(PR)。`-SelfTest` に L群7アサート追加(古launcher/planner削除・recent不可侵・.md不可侵・非loop .txt不可侵・KeepMin保持・空dir)→ALL PASS(計43)。**live logs/への `-WhatIf`(read-only)で6/8〜6/14の古 loop-*.log 9件を「would prune」・削除ゼロ(file数不変＝WhatIf自身の+1のみ)**を実測。web/src非侵。補充理由=未着手[ ]は掃除系/perf実測の2件のみ・両方とも全6レーン稼働中で静穏窓待ちブロック(3件未満)→運用4系統(報告)の欠陥から補充。
- [x] 【Opus・P1】報告系補充: logs保持sweepを全レーンクローンへ拡張＝#661の穴を封止。#661 の log retention sweep は launcher の `$logDir`（=mainツリー `logs/`）だけを剪定していたが、`loop-runner.ps1:163` は自分の `loop-<lane>-*.log` を `<RepoPath>\logs` へ書き、非opsの5レーンは `lanesRoot` 配下の**兄弟クローン**で走る＝剪定はlog生成の**約1/6**（mainツリー＝ops runner＋launcher/watchdog/planner）しか覆わず、data/seo/ux-tools/ux-records/ux-hub の5クローンの `loop-*.log` は**永久に剪定されない**（実測: 各クローンに 2026-06-13 の古logが14日保持超で居残り）。#661 が「logs/無制限肥大を封止」と謳いつつ**5/6を放置**した穴。修正: 純関数 `Get-UniqueLogDirs` 新設＝候補log dir（mainツリー＋各レーン `Resolve-LaneRepo\logs`）を正規化フルパスで dedup（ops は `repoPath="."` で mainツリーへ解決＝二重sweep回避）、sweep を全dir反復へ書換え＝各dirで KeepMin=20/14日保持を独立適用（各レーンの直近診断20件は死守）・非存在dirは Test-Path/SilentlyContinue で無害skip・開いているrunner logはwrite lockでskip・集約ログに「across N lane log dir(s)」出力。→ 2026-07-03完了(PR#667)。`-SelfTest` に Get-UniqueLogDirs 4アサート追加（distinct全保持/大小・末尾slash重複collapse/blank除去/空入力）→ ALL PASS(計47)。**live への `-WhatIf`（read-only）で sweep が 1→6 lane log dir を走査**＝mainツリーの古log 9件は従来通り「would prune」・5クローンは現状<20件で保持（将来肥大を封止）を実測、docs/loop-status.md は md5 不変。ASCII厳守・web/src非侵。補充理由=未着手[ ]は掃除系(line28)/perf実測(line30)の2件のみ・両方とも全6レーン稼働中で静穏窓待ちブロック（3件未満）→運用4系統(報告)の欠陥から補充。
- [x] 【Opus・P1】報告系補充: レーン自己報告のエージェント依存を封止＝runner駆動のliveness heartbeat新設。実測でseo/ux-recordsが**6.5時間・30回超のイテレーションでPRをマージし続けながら中央loop-status.mdに一度も自己報告せず**、健在性バナー(#628/#631/#640)が**生きて働いている2レーンを未報告/born-deadと誤検知**（＝#602〜#654が掃討してきた沈黙死アラームの**偽陽性版**）。全クローンで loop-report-status.ps1/prompt/env(SAFE_AI_LOOP_STATUS)はbyte一致＝差はエージェントがstep5.5を実行するか否かのみ＝報告がエージェントの記憶に依存する構造欠陥。修正: reporter に `-HeartbeatOnly` 新設（純関数 `Set-RowTimestamp`＝{LASTRUN}位置の先頭timestampだけ差替えPR/OPEN/NOTE逐語保存／`Get-HeartbeatRow`＝既存行あり→timestampのみ更新・無し→正直な placeholder 行 `reportRunnerHeartbeat`「レーンは稼働中だがエージェントがstep5.5未実行」を作成）、runner が毎イテレーション末尾に `Report-Heartbeat`（永続lanedのみ・deadline stopと同一gate・SAFE_AI_LOOP_STATUS継承で中央ルーティング・非致命）を実行。→ **偽の沈黙死アラームを「生存＋エージェント非報告」の正直な信号へ変換**＝リッチなエージェント報告があればnote保存・自己回復。→ 2026-07-03完了(PR)。reporter `-SelfTest` にH群5アサート追加（timestamp swap/note内の日付不侵/既存→更新/未報告→placeholder/null安全）→ALL PASS(計37)、runner `-SelfTest` 無回帰PASS。**隔離temp**でE2E＝ops rich報告→ops heartbeatでnote逐語保存／seo未報告→heartbeatで正直placeholder＋健在性OK化／seo実報告→placeholder→rich自己回復 を実測。**live中央statusへの`-WhatIf`（read-only・md5不変）**でseoが「2/6未報告」から正直な稼働行へ・health線が「1/6未報告(ux-records)」へ更新される予告を実測。活性化=マージ後の次launcherフルパスでrunner再起動。ASCII厳守(日本語は strings 由来)・web/src非侵。補充理由=未着手[ ]は掃除系(line29)/perf実測(line31)の2件のみ・両方とも全6レーン稼働中で静穏窓待ちブロック（3件未満）→運用4系統(報告)の欠陥から補充。
- [x] 【Opus・P1】点火系補充: runnerコード/promptのホットスワップ＝**稼働中runnerが自クローンのloop-runner.ps1/prompt更新を無停止/無停滞で取り込む**。構造欠陥＝永続runnerは起動時に**スクリプト全体をプロセスへ読込み**＋promptも line220 で**一度だけ**読む→以後どちらの更新もプロセスが死ぬまで届かず、launcher の単一インスタンスガードは**生きているrunnerを再起動しない**ため、ops のrunnerコード修正（backoff #590/deadline-stop #619/**heartbeat #672**）とprompt編集（S13-b モデル非依存化 #590）が**最大5/6の稼働レーンへ数日～再起動まで届かない**。とりわけ**#672 は自分の標的 seo/ux-records の旧runnerでは活性化できない**（＝直前PRが理論上のみ有効）＝#602〜#672 のrunner側修正すべてを絵に描いた餅にする点火(配備)欠陥。修正2点: **(1)prompt毎イテレーション再読込**＝ループ先頭で `Get-Content` し直し（失敗/空時は直前promptを保持＝claudeへblank送出ゼロ）＝**無停止**で contract/steering 変更がレーンへ届く。**(2)スクリプトのhot-swap**＝起動時に自身の content hash を捕捉（`Get-ScriptHashSafe`）、イテレーション間（claudeターン完了・tree clean）に on-disk と比較（純関数 `Test-ScriptChanged`＝両hash非空∧相違でのみtrue・空/読取失敗→false＝**再起動ループ不能のfail-safe**）し、変われば**clean exit**→**既存の watchdog(#654)/launcher `-HealOnly` 再蘇生パス**（launcher:839 が clone の新 loop-runner.ps1 を spawn）が新コードで再起動。self-spawn はガードと競合するため**clean-exit＋既存heal**が唯一のrace-free配備＝プロセス消滅後に heal が競合なく新版を起動。永続lanedのみ（heartbeatと同一gate）。→ 2026-07-03完了(PR)。runner `-SelfTest` に hotswap 4アサート追加（不変→維持/変化→再起動/読取失敗→無再起動/baseline空→無再起動）→ALL PASS(計14=backoff6+gate4+hotswap4)、フルスクリプト PSParser パースOK・loop本体編集も無エラー。実IO E2E＝実 loop-runner.ps1 を SHA256 で hash→同一コピー無検知→実編集で変化検知→欠落ファイル空hashで無再起動 を実測。launcher heal パス(839)が clone の新runnerを spawn することを実コード確認（clean-exit が確実に新版へ再起動）。ASCII厳守（既存の #619 コメント内 `起動` 6byteは先行在庫＝本PR不介入）・web/src非侵。活性化=マージ後、各レーンの次git pullで clone の loop-runner.ps1 が更新→稼働runnerが hash差を検知し自動で新版へswap（#672 も同経路で seo/ux-records へ到達）。補充理由=未着手[ ]は掃除系(line30)/perf実測(line32)の2件のみ・両方とも全6レーン稼働中で静穏窓待ちブロック（3件未満）→運用4系統(点火)の欠陥から補充。
- [x] 【Opus・P1】点火系補充: 復活watchdog自身のホットスワップ＝#672はrunnerに hot-swap を与えたが、**同じく常駐する loop-watchdog.ps1（#654）は自コードを一度も更新できない**構造欠陥が残っていた。watchdog は起動時にスクリプト全体をプロセスへ読込み→以後ループ（loop-watchdog.ps1:176-184）で回り続けるが、毎tick spawn する `launcher -HealOnly` は**fresh process**なので launcher/lane 側の修正は届く一方、watchdog **自身**のループコード（deadline gate/interval/heal呼出し）は死ぬまで凍結。しかも #654 は watchdog を「far less likely to die」と**意図的に最も丈夫**にした＝watchdog-code fix が**全プロセス中で最も stranded**。runner との決定的非対称: runner は clean-exit すれば watchdog/heal が~1 interval で蘇生するが、**watchdog を蘇生するのは launcher フルパス（logon/07:00）だけ**（`-HealOnly` は #654 で意図的に watchdog を触らない）＝naive clean-exit だと最悪~24h 蘇生されず＝O16「止まったら即戻る」に穴。修正: runner と同じ純関数 `Get-ScriptHashSafe`/`Test-ScriptChanged` を移植し起動時 hash を捕捉→毎tick heal 後に on-disk と比較、変化時は**単なる clean-exit でなく self-respawn ハンドオフ**＝`-SupersedePid <outgoing pid>` 付きで新on-diskコードから後継を spawn→自身exit。新純関数 `Select-CompetingWatchdogPids`（single-instance guard を「self＋superseded predecessor は競合でない」へ拡張＝ハンドオフの数ms overlap を zero-watchdog gap に落とさない・heal は冪等なので二重は無害）＋`Get-WatchdogRelaunchArgs`（-File自身/-SupersedePid/interval/path をquote付きで組立＝spaces耐性）。spawn 失敗時は**現コードで生存継続**（exit しない＝zero-watchdog を絶対に作らない）。ASCII厳守（日本語リテラル不埋込）・web/src非侵。→ 2026-07-03完了(PR)。`-SelfTest` を 11→21 アサートへ拡張（hotswap4：不変維持/変化respawn/読取失敗no-loop/baseline空no-loop、guard4：self除外/superseded除外/real競合keep/no-supersede時self のみ除外、relaunch1：-File自身+-SupersedePid+interval を carry）→ ALL PASS、フルスクリプト PSParser 0-error、`-WhatIf`（read-only 1 heal pass）で loop-status.md md5 不変を実測、実ファイルIO E2E で unchanged→False/edit→True/missing→False（respawn loop 不能の fail-safe）を実測。活性化=マージ後、次に watchdog を蘇生する launcher フルパスで新 watchdog が起動し以後 loop-watchdog.ps1 の pull を無停止で self-swap。補充理由=未着手[ ]は掃除系(line31)/perf実測(line33)の2件のみ・両方とも全6レーン稼働中で静穏窓待ちブロック（3件未満）→運用4系統(点火)の欠陥から補充。
- [x] 【Opus・P1】報告系補充: レーン自己報告noteのGit-Bash/MSYSパス変換破損を正直化＝reporterの入口で無害化。**実測でlive loop-status.mdのux-tools行が `C:/Program Files/Git/stats に結論カード新設…` と表示**（レーンのnoteは `/stats に…` だったのが、step5.5の `loop-report-status.ps1 -Note "…"` をエージェントがBash経由で撃つ際、MSYSのPOSIX→Windowsパス変換が**先頭スラッシュのルートtoken `/stats` を Git インストール根 `C:/Program Files/Git/stats` へ黙って書換え**＝reporterがnoteを受け取る前のシェル層で破損）。社長が唯一見る監視面(#602〜#672が磨いてきた status surface)に**ファイルパスのゴミが混入**し意味が読めなくなる報告系欠陥。**MSYS_NO_PATHCONVはbash専用でシェル非依存でない**ため不採用＝reporterが全caller(エージェントはBash・runnerのheartbeat/deadlineはPowerShell)の唯一のchoke pointなのでそこで是正。純関数 `Get-MsysRoot`（`cygpath -m /`＝実MSYS根を実行時discover・forward-slash mixed形／fallbackは `EXEPATH`(=…\Git\bin)の親／drive-letter rooted検証で garbage排除・resolveできなければ$null＝**Git Bash外＝そもそも破損なし＝no-op**）＋`Repair-MsysMangledNote`（`<root><sep>` を単一の先頭 `/` へ復元・両slash形をマッチ＝MSYS2_ARG_CONV変種の `\` も保護・discover根にanchorするため他の本文は不可侵・正当なnoteは根の実パスを含まない＝**cleanなnoteは絶対に破壊しない**）。noteはWhatIf preview前に無害化＝dry-runと実書込みが同一の復元テキストを表示。ASCII厳守（コメント/テスト literalの日本語を除去＝Windows PS 5.x のShift-JIS誤decode回避）・web/src非侵。→ 2026-07-03完了(PR)。`-SelfTest` に I群6アサート追加（forward復元/backslash復元/mid-note復元/install-path無しは不可侵＝実 `/route` 温存/null根はno-op/空note安全）→ ALL PASS(計43)、PSParser 0-error・非ASCII 0行を実測。**E2E**＝実際にBash経由で `-Note "/stats …"` を撃つと raw arg が `C:/Program Files/Git/stats …` へmangleされる（別プロセスで実証）→ reporterのcomputed rowは `/stats …` へ復元、live status への `-WhatIf` は md5不変(read-only)を実測。活性化=各クローンの次git pullでreporterはfresh processとして常に最新on-disk版を読む（hot-swap不要）＝以後step5.5経由のnote破損が入口で自動修復・live ux-tools行はそのレーンの次回報告で自己修復。補充理由=未着手[ ]は掃除系(下記)/perf実測の2件のみ・両方とも全6レーン稼働中で静穏窓待ちブロック（3件未満）→運用4系統(報告)の欠陥から補充。
- [x] 【Opus・P1】報告系補充: 監視ファイルのtrailing空行 無制限肥大を封止＝報告系のwatch surface bloat（#661 logs肥大の同族）。**実測でlive docs/loop-status.md がレーン報告のたび末尾空行を1本ずつ増やし続けていた（19→20→21→22行/3write）**。真因: 5箇所の status writer が `($lines -join CRLF) + CRLF` を作った上で `Set-Content -Value` が**もう1つ**terminatorを付け足す→ファイル末尾がDOUBLE CRLFになり、次の `Get-Content` が末尾terminatorを1つだけ残す＝read-modify-writeの1往復ごとに+1空行（reporterは毎イテレーション×6レーンで恒常発火＝可視の無制限成長）。社長が唯一見る監視面が空行ゴミで埋まり本文が押し流される。修正2点: **(1)** 全status write（reporter 1＋launcher実書込み3＋dryrun3）を `Set-Content -NoNewline` 化＝ファイル末尾は合成テキストが既に持つ単一CRLFのみ（成長を発生源で停止・write が round-trip stable）。**(2)** reporter に純関数 `Remove-TrailingBlankLines` 新設＝合成前にEOF空行をtrim（既に溜まった21本を次回writeで自己修復）。EOF空行は無意味（最終実行はregion END markerかlauncherのwatch3行）＝trim常時安全。→ 2026-07-03完了(PR)。reporter `-SelfTest` に J群7アサート追加（EOF空行drop・内部空行維持・no-op・null安全・実ファイル往復で1回目healで0本＋2回目も0本維持＝再成長なし）→ALL PASS(計43)、launcher `-SelfTest` 無回帰 ALL PASS、両.ps1 PSParser 0-error。**実測E2E**＝live status形状のコピー(21空行)へ実reporterを3回撃つと1回目で0本へheal・以後0本で安定（旧コードの19→22成長を停止）、launcher `-WhatIf` で live loop-status.md は md5 byte不変(read-only)。live ux/ops行はマージ後の次回report（reporterはfresh processで常に最新on-disk版を読む＝hot-swap不要）で自己修復。ASCII厳守（追加分は全ASCII・既存の日本語コメントは先行在庫で本PR不介入）・web/src非侵。補充理由=未着手[ ]は掃除系(下記)/perf実測の2件のみ・両方とも全6レーン稼働中で静穏窓待ちブロック（3件未満）→運用4系統(報告)の欠陥から補充。
- [x] 【Opus・P1】報告系補充: born-dead誤検知をプロセス生存で封止＝#628/#631/#640/#654 の「一度も自己報告していないレーン(missing)」検知は、stampが StaleMinutes(120分)を超えると**LOUD born-dead**（「!!! 停止疑い＝runnerが起動直後に落ちている」）へ昇格するが、その判定は**中央status行だけ**を根拠にしていた。実測: seo/ux-records は**6レーンとも runnerプロセスが生存**（`-Lane` で確認）し午後中PRをマージし続けているのに、旧runner(#672 heartbeat未搭載・#690 hot-swap未適用で新コードを取り込めない)＋エージェントが step5.5 未実行のため中央statusへ一度も報告せず、missing-since=12:55 のため**14:55にborn-dead大声へ誤昇格寸前**だった（#602〜#672 が掃討してきた「沈黙死の偽アラーム」の未報告レーン版・生きて働くレーンを死亡扱い）。真因: never-reported は「本当に出生時死」と「生きているが自己報告しないだけ」の2状態を中央statusだけでは弁別不能。修正: **地の真実＝runnerプロセスの生存**をタイブレークに使用。reporter に best-effort `Get-AliveRunnerLanes`（launcher の Get-RunningLanes をミラー・WMIはマシン全域なので任意クローンから全レーン可視・失敗時は空配列→旧挙動へ安全degrade）新設し、`Get-LaneHealthLine` に純パラメータ `$AliveLanes`（既定空）を追加。never-reported レーンのうち**プロセス生存を確認できたもの**は stamp年齢に関係なく**born-dead へ昇格させず**、静かな新バナー `laneHealthAliveNoReport`（「健在性 情報: N/6が稼働中だが自己報告なし…runnerプロセス生存確認済＝死んではいない・復活不要」）へ。優先度 stale > born-dead > alive-no-report > missing > ok（真に死んだレーンの大声は維持）。fallback: alive-no-report文字列が旧stringsで無い場合も alive レーンは quiet へfold＝**絶対に falsely-OK にしない**。scanは `missingLanes>0` の時のみ発火＝通常経路は無コスト。ASCII厳守（日本語は loop-status-strings.txt 由来）・web/src非侵。→ 2026-07-03完了(PR)。`-SelfTest` に K群4アサート追加（K1:alive×past-grace→alive-no-report／K2:dead×past-grace→born-dead維持＝回帰ガード／K3:真の死レーンがalive-no-reportに優先＝born-dead大声維持／K4:alive×grace内→calm名指し）→ALL PASS(計47)、PSParser 0-error・追加分の非ASCII 0行を実測。**live中央statusへの`-WhatIf`（read-only・md5不変）**で「注意: 2/6未報告→14:55にborn-dead昇格」寸前を「情報: 2/6が稼働中だが自己報告なし: seo、ux-records（runnerプロセス生存確認済）」へ置換・missing-sinceマーカーは保全（真に死んだら正しくescalate）を実測。活性化=各クローンの次git pullでreporterはfresh processとして最新on-disk版を読む（hot-swap不要）＝以後どのレーンが報告しても健在性行が地の真実で再計算。補充理由=未着手[ ]は掃除系(line34)/perf実測(line36)の2件のみ・両方とも全6レーン稼働中で静穏窓待ちブロック（3件未満）→運用4系統(報告)の欠陥から補充。
- [x] 【Opus・P1】報告系補充: 健在性バナーstripのversion-skew漏れを封止＝shared-stem化。**実測でlive loop-status.mdが同一2レーン(seo/ux-records)に矛盾する2本の健在性バナー**（`健在性 注意: …未報告`＝laneHealthUnreported と `健在性 情報: …稼働中だが自己報告なし`＝#696新設 laneHealthAliveNoReport）を同時表示していた。真因: region健在性バナーの strip が `Ok/Stale/Unreported/BornDead/AliveNoReport` の**5固定prefixを列挙**して除去する設計で、非opsの5レーンは別クローンで**各自commit済みreporter**を持つため #696未pullの**旧reporterは新banner key(AliveNoReport)を知らず**新reporterが書いた「情報」バナーをstripできない→自分の「注意」バナーを並置＝社長が唯一見る監視面に矛盾2本が残る（banner keyを増やすたび＝将来も再発する構造欠陥・#602〜#696が磨いた監視面の最後の穴）。修正: 純関数 `Get-CommonPrefix`（全banner templateのpre-placeholder prefixの最長共通接頭辞＝全banner共通stemを実行時導出・ASCII厳守で日本語stemはソース非埋込・strings由来）＋`Test-IsHealthBanner`（**shared-stem ∪ 既知prefix列**で判定＝常に旧挙動のsuperset＝regressionなし・stemが旧/新どのreporter版でも任意banner variant(未知key含む)を除去してversion-skew封止・stem空→既知prefixのみへ安全fallbackで全stripしない）を新設し2箇所のstrip site（WhatIf preview/本書込み）を helper 呼出しへ統一。lane行(`- lane :`)・missing-sinceマーカー(`<!--`)はbannerと共通stemを持たず不可侵。→ 2026-07-03完了(PR#699)。`-SelfTest` に L群10アサート追加（共通接頭辞の純計算4＋旧prefix列でも未知bannerをstrip/既知banner維持/lane行・marker不可侵/stem空fallbackの6）→ALL PASS(計57)、PSParser 0-error・追加行の非ASCII 0。**live statusへの `-WhatIf`（read-only）で健在性バナーが1本へ収束**（旧コードの矛盾2本を解消）・md5 byte不変を実測。web/src非侵。活性化=各クローンの次git pullでreporterはfresh processとして最新on-disk版を読む（hot-swap不要）＝以後どのreporter版が書いても健在性バナーは1本に収束。補充理由=未着手[ ]は掃除系(line35)/perf実測(line37)の2件のみ・両方とも全6レーン稼働中で静穏窓待ちブロック（3件未満）→運用4系統(報告)の欠陥から補充。
- [x] 【Opus・P1】報告系補充: 健在性バナーstaleブランチにプロセス生存tiebreak追加＝report-then-silentの偽停止alarmを封止。**実測でlive loop-status.mdが `健在性 !!! 停止疑い: data(最終14:31／約2h前)` とLOUDな停止alarmを表示**していたが、WMIスキャンでdataのrunnerプロセスは生存中（6レーン全稼働）＝生きて働くレーンを死亡扱い。真因: #696 は never-reported（missing→born-dead）ブランチにだけ `Get-AliveRunnerLanes` の生存tiebreakを入れたが、**stale（report-then-silent）ブランチは `AliveLanes` を一切参照せず**、報告後に沈黙した稼働中レーンを無条件LOUD停止扱い（旧runner=#672 heartbeat未搭載／エージェントstep5.5未実行が真因）＝#602〜#696が掃討した沈黙死の偽alarmの**staleブランチ残渣**。修正: `Get-LaneHealthLine` の stale ブランチを `staleDead`／`staleAlive` へ分割＝runnerプロセス生存を確認できたstaleレーンは calm な `laneHealthAliveNoReport` へdemote（never-reported-aliveと同じbucketへseed）、`$canAliveNoReport` でgate＝旧strings（calmバナー無し）では全stale LOUD維持＝falsely-OKへ絶対落とさない、生存不明のstaleは従来通りLOUD、優先度 `staleDead(loud)>bornDead(loud)>aliveNoReport>missing>ok` を保存。→ 2026-07-03完了(PR#703)。`-SelfTest` に M群4アサート追加（M1:stale-alive→calm／M2:stale-dead→loud維持=回帰ガード／M3:2本stale中dead側のみ名指し／M4:真のborn-deadがdemoted-staleを優先）→ALL PASS(計72)、PSParser 0-error・追加行の非ASCII 0。**live statusへの `-WhatIf`（read-only・md5不変）** で `!!! 停止疑い: data` を `情報: 3/6が稼働中だが自己報告なし: data、seo、ux-records（runnerプロセス生存確認済）` へ置換・missing-sinceマーカー保全（真に死んだら正しくescalate）を実測。web/src非侵。補充理由=未着手[ ]は掃除系(下記)/perf実測の2件のみ・両方とも全6レーン稼働中で静穏窓待ちブロック（3件未満）→運用4系統(報告)の欠陥から補充。
- [x] 【Opus・P1】報告系補充: 健在性の生存tiebreakを「missing無し∧staleのみ」でも発火＝#703のgate漏れを封止。#696/#703 は stale/missing レーンの LOUD 停止alarm を **runnerプロセス生存**（`Get-AliveRunnerLanes`）で calm alive-no-report へ demote する tiebreak を入れたが、その scan を回す gate（reporter の2つの書込み site: WhatIf preview と本書込み）は **#696時代の `$missingLanes.Count -gt 0`** のまま＝**never-reported レーンが1本でもある時にしか scan しない**。#703 が stale ブランチを `$aliveLanes` の**新たな consumer**にしたのに gate は広げられず、**全enabledレーンが一度は報告済み（missing=0）で、その後1本が沈黙（stale）した瞬間** gate が scan をskip→`$aliveLanes` 空→`Get-LaneHealthLine` の stale-alive demote が**永久に発火不能**＝#703の修正が**静かに無効化**され稼働中レーンへ FALSE な LOUD「停止疑い」が再発（#602〜#703 が掃討した沈黙死の偽alarmの **gate残渣**）。live status は今 seo/ux-records が never-reported のため gate がたまたま通り**バグが遮蔽**されているが、両レーンが一度報告し1本が沈黙した途端に再発する latent regression。修正: 純関数 `Test-NeedsAliveScan($MissingCount,$Rows,$Now,$StaleMinutes,$RowTemplate)` 新設＝**missing OR stale** のいずれかで true（missingは呼出側の既算countを渡し region二重parse回避・staleは rows から `Get-StaleLanes` 導出）、2書込みsite の gate を helper 呼出しへ差替え。空rows/no-stale→false＝legacy no-scan を保存＝WMIコスト無増。→ 2026-07-03完了(PR)。`-SelfTest` に N群4アサート追加（N1:missingで発火=legacy trigger維持／N2:missing無し∧staleで**発火**=本regression fix／N3:missing無し∧stale無し→no-scan=legacy no-op維持／N4:空rows→no-scan・no-throw）→ALL PASS(計75)、PSParser 0-error・追加行の非ASCII 0を実測。**live中央statusへの `-WhatIf`（read-only・md5不変）** で健在性行が calm alive-no-report のまま無回帰を実測（現状は missing 有りで gate 既通・本fixは missing=0∧stale時の再発を封止）。web/src非侵。補充理由=未着手[ ]は掃除系(下記)/perf実測の2件のみ・両方とも全6レーン稼働中で静穏窓待ちブロック（3件未満）→運用4系統(報告)の欠陥から補充。
- [x] 【Opus・P1】報告系補充: 健在性 alive-no-report の分母を空roster時に正直化＝「N/0」ナンセンス分数を封止。#703 が stale-but-alive レーンを LOUD 停止alarm から calm `laneHealthAliveNoReport`（「N/TOTAL が稼働中だが自己報告なし」）へ demote する際、`Get-LaneHealthLine` の alive-no-report 分岐だけが **TOTAL=$ExpectedLanes.Count** のまま残っていた。この分岐は **空roster で到達可能な唯一の分岐**（`$missing` は roster を要するが `$staleAlive` は rows だけから導出される）＝クローンで config が一瞬読めない（mid git-checkout）間に stale-but-alive レーンがあると `$ExpectedLanes.Count=0` で社長の監視面に **「情報: 1/0が稼働中だが自己報告なし」** というナンセンス分数を刻む（#602〜#703 が磨いた watch surface の integrity を汚す残渣）。しかもヘッダ規約（L337-340）は「roster 不明→pre-roster 挙動へ revert＝TOTAL=報告数」と明記しており **契約違反**。born-dead/unreported 分岐は `$missing` 由来＝roster 非空でしか到達せず TOTAL=0 になり得ないため、本分岐が唯一の穴。修正: OK 分岐（L404）と同一の防御的 TOTAL（roster 既知→roster 数／空→`$all.Count`＝報告数）へ差替え。roster 既知時は挙動不変。→ 2026-07-03完了(PR)。`-SelfTest` に O群2アサート追加（O1:空roster+stale-alive→TOTAL=報告数2で「/0」封止／O2:既知roster→roster 数=無回帰=M1一致）→ALL PASS(計77)、PSParser 0-error・追加行の非ASCII 0を実測。**live中央statusへの `-WhatIf`（read-only・md5不変）** で roster 既知(6)のため健在性行「情報: 2/6…」が無回帰＝本fixは空roster edge のみ是正を実測。web/src非侵。補充理由=未着手[ ]は掃除系(下記)/perf実測の2件のみ・両方とも全6レーン稼働中で静穏窓待ちブロック（3件未満）→運用4系統(報告)の欠陥から補充。
- [x] 【Opus・P1】点検系補充: weekly critic の git worktree **実配管**（fetch origin/main → worktree add --detach 隔離checkout → remove --force → prune）に side-effect-free な事前検証 `-RehearseCritic` を新設。真因＝`lastCriticIso` が O16 seed のまま一度も本番実行されておらず初発火は **7/9 の無人**、既存 `-WhatIf` 分岐は意図的に worktree を作らない（launcher L1050）ため配管そのものは予行手段皆無＝無人初発火で壊れていれば静かに失敗する未検証クリティカルパス。修正＝critic が使う正確な git 配管を Claude one-shot 抜きで実行し fetch/add 失敗・checkout が自前の `loop-runner.ps1` を欠く・teardown 後 worktree 残留のいずれかでのみ FAIL、`"rehearsal-"` stamp で実 critic worktree（同一 launch stamp）と絶対衝突せず teardown は finally で漏らさない、`-WhatIf` 併用で plan のみ出力。→ 2026-07-03完了(PR#716)。`-SelfTest` に P群4アサート追加（隔離sibling/rehearsal-marker/実criticとの非衝突/dry-run が ok 且つ無git 且つ worktree 不生成）→ALL PASS(計40)、PSParser 0-error・追加行の非ASCII 0。**live実測** `-RehearseCritic`=fetch ok/add ok/loop-runner.ps1 True/loop-prompt-critic.txt True/teardown leaked=False・PASS＋main tree clean・worktree漏れ0、`-RehearseCritic -WhatIf`=plan出力・git不実行・PASS。web/src非侵。補充理由=未着手[ ]は掃除系(下記)/perf実測の2件のみ・両方とも全6レーン稼働中で静穏窓待ちブロック（3件未満）→運用4系統(点検)の欠陥から補充。
- [x] 【Opus・P1】点検系フォローオン: `-RehearseCritic` を critic gate（launcher L1037-）の**実発火前の自動 pre-flight** に配線＝criticDue 時にまず rehearsal を撃ち、PASS 時のみ本 worktree 生成＋one-shot へ進む。FAIL 時は本発火を skip し `Resolve-CriticStamp` で ~1日 back-date＋dashboard に劣化 note（=壊れた配管で無人 30分 one-shot を空振りさせない）。完了条件=`-SelfTest` に「rehearsal FAIL→本発火 skip＋back-date」アサート＋`-WhatIf` dry-run で配線経路を実測。※現状は #716 の手動 `-RehearseCritic` で 7/9 初発火の配管は実証済＝これは自動化の恒久化。→ 2026-07-03完了(PR)。criticDue の pre-stamp 直後に `Invoke-CriticRehearsal`（#716 と同じ fetch/worktree-add/verify/teardown を `rehearsal-<stamp>` で side-effect-free 実行）を撃ち、純関数 `Test-CriticFireAfterRehearsal($RehearsalOk)` で分岐＝PASS のみ本 worktree/one-shot へ、FAIL は `$criticOutcome="rehearsal-failed"` で本発火を skip し**既存の**「非ok→`Resolve-CriticStamp` back-date(~1日)＋criticFail dashboard バナー」ブロックへ合流（新経路を増やさず既存の劣化ハンドラを再利用＝rehearsal-failed も REASON として note に載る）。`-WhatIf` では rehearsal 自体が dry-run（git 不実行・plan出力）＝本発火の plan-only branch と併せて「rehearse→(pass)→fire」の全配線を出力。→ `-SelfTest` に Q群5アサート追加（PASS→fire可/FAIL→fire不可/rehearsal-failed が12h後not-due・1日後due=bounded retry/非ok判定でnote発火）→ALL PASS(計45)、PSParser 0-error・追加行の非ASCII 0を実測。**live実測**＝loop-state.json を7日超前へ一時back-date（`-WhatIf` は Save-State を撃たないため実state不変）して full `-WhatIf`＝「critic due→critic pre-flight [WHATIF] would fetch…rehearsal-<stamp>…verify loop-runner.ps1…remove→(本fire plan) worktree-<stamp>…run one-shot」の rehearse→fire 二段を出力、rehearsal stamp が real critic stamp と非衝突、docs/loop-status.md md5 byte不変・state不変を実測後に元stateへ復元。web/src非侵。補充理由=本タスクが未着手[ ]最上位。
- [x] 【Opus・P1】点火系: hot-swap 未搭載の**旧 runner floor** 調査＋設計。live telemetry で seo/ux-records が長期間「runnerが旧版で heartbeat 未搭載」表示。仮説＝runner の hot-swap（`Test-ScriptChanged`・#672 前後）**導入より前に起動した live runner は self-update コード自体を持たず、clone が新コードを pull しても永久に stale**（watchdog/-HealOnly は **DEAD** runner しか再起動しない＝live-but-ancient は対象外）＝再起動/ログオンまで旧コード続行。まず各レーン runner プロセスの起動時刻 vs hot-swap 導入コミット日で floor 該当を実測確認（過剰対応回避）、該当あれば version-floor marker＋**協調的**cycle（実行中イテレーションを壊さない end-of-iteration 合図）を設計。捏造・過剰対応禁止＝調査で floor 該当ゼロなら [x] クローズで可。→ 2026-07-03完了(PR)。**実測=全6レーンが floor 該当**（プロセス起動 06:38-06:39 JST vs #672 heartbeat 13:30・#675 hot-swap 13:43 の landing＝約7時間前起動）＝仮説確定・過剰対応でない。設計＋実装＝`loop-config.json` に version-floor marker `runnerFloorIso`（#675 landing 時刻）新設＋launcher に純関数`Test-RunnerBelowFloor`/`Test-ShouldCycleStaleRunner`＋live`Get-RunnerProcInfo`/`Get-ProcChildCount`/`Get-AncestorPids`＋`Invoke-StaleRunnerCycle`（**idle=子プロセス0 の below-floor runner だけを Stop-Process する kill-only**・relaunch は既存経路が担う）を新設し full pass と `-HealOnly` の両方へ配線。idle ゲート＝runner はイテレーション中 claude を子に持ち sleep 中は子0＝hot-swap 本体と同じ安全 exit ポイント＝in-flight を壊さない（既存破壊0）。drain は自己ブートストラップ＝watchdog が毎interval 最新 launcher を fresh spawn（watchdog:280）するため merge→pull 後の次 heal パスで各レーンを sleep 窓で1つずつ cycle→fresh コード復活・ops 自身も watchdog の別ツリーから cycle・無人 drain・手動再起動不要。多層安全（idle ゲート／先祖除外／marker 不在 no-op／scan 失敗は busy 倒し）。`-SelfTest`＝新規12アサート(floor 5・cycle 4・no-op 1・境界含む)追加で **ALL PASS(計57)**。**live 実測**＝`-HealOnly -WhatIf` で6レーン全て below floor 検出・5レーン BUSY で deferred・ops は先祖除外・何も kill せず／full `-WhatIf` も6レーン deferred・多層防御動作を確認・何も kill せず。設計/実測詳細=`docs/fable-diagnosis-2026-07-02/stale-runner-floor-notes.md`。web/src非侵。補充理由=本タスクが未着手[ ]最上位。
- [x] 【Opus・P1】点火系補充: 復活watchdog自身の常駐を`-HealOnly`で担保＝#727 drain のトリガー欠落を封止。**live実測でloop-watchdog.ps1プロセスが1本も走っていない**（6レーンrunnerは全て06:38-06:39起動＝#654 watchdog landing 前＝logon full passはwatchdogを産む前に走り終えた／以後 長時間ログイン継続でreboot/07:00 full passが再来せず＝watchdogは一度も spawn されていない）。watchdog は「full passでのみ spawn」（launcher L1454・`-HealOnly` はL1105で早期exitしspawnブロックに到達しない）＝**#727 の協調cycle drain と #654 の ops-SPOF 解消の両方がトリガー皆無**（ops自身を cycle できるのは watchdog だけ＝ops は自heal時に自分を先祖除外＋非idle・dead ops runnerを蘇生できるのも watchdog だけ）。これは watchdog が閉じるはずの「rare full pass まで何も戻さない沈黙gap」が**watchdog自身に反射**した欠陥（#602〜#727 の沈黙死掃討の最終残渣）。修正: 純関数 `Test-ShouldSpawnWatchdog($WatchdogExists,$AlreadyRunning)`＝script存在∧未起動でのみspawn（idempotent）＋live wrapper `Invoke-EnsureHealWatchdog`（running-scan＋watchdog自身のsingle-instance guardで二重防止・`-WhatIf`尊重・"spawned/running/absent/whatif"返却）を新設し、full pass の inline spawn を helper 呼出しへ統一＋**`-HealOnly` の「nothing to resurrect」早期exit手前へ配線**＝ops が毎イテレーション撃つ `-HealOnly`（step5.6）を**watchdog自身の蘇生トリガー**化（never-spawned/dead watchdog が最悪~22h不在→~1 ops interval で復活）。→ 2026-07-03完了(PR)。`-SelfTest` に R群4アサート追加（present∧未起動→spawn／present∧起動中→no-spawn＝idempotent／script欠落→no-spawn×2）→ALL PASS(計61)、PSParser 0-error・追加行の非ASCII 0。**live実測**＝`-HealOnly -WhatIf` が「all lanes alive; nothing to resurrect」でも **"would spawn heal watchdog"** を出力（旧コードは spawnブロック未到達で watchdog不在のまま）＝本fixの核・6レーンは below-floor BUSY で drain deferred（in-flight不破壊）・ops先祖除外・loop-status.md md5 byte不変、full `-WhatIf` も watchdog spawn 意図出力＋status byte不変。web/src非侵。活性化=マージ後 main pull で ops の次 `-HealOnly`（step5.6）が実 watchdog を spawn→以後 watchdog が drain/heal を無人継続。補充理由=未着手[ ]は掃除系(下記)/perf実測の2件のみ・両方とも全6レーン稼働中で静穏窓待ちブロック（3件未満）→運用4系統(点火)の欠陥から補充。
- [x] 【Opus・P1】報告系補充: 協調drainの沈黙stall（wedged runner）を正直化＝#727 drain の唯一の未検知death mode を封止。#727 の協調drainは**idle（子プロセス0）な below-floor runnerだけ**をcycleする（in-flight iterationを壊さない正しい設計）。しかし `loop-runner.ps1:352` の `$prompt | & claude | Tee-Object` は**claude呼出しにtimeoutが無い**ため、claudeがハング（dev-server-hang系の実在failure mode）すると子プロセスが永久に生存→runnerは恒久BUSY→drainが**永遠にdefer**→当該runnerは below-floor（heartbeat/hot-swap/self-report無し）のまま二度と更新されない。しかもreporterの健在性は**calmな** `laneHealthAliveNoReport`（「runnerが旧版＝自動解消」）を出すため、社長は「数分で解消する正常drain」と「永久にwedgeし手動介入が要る停止」を弁別不能（#602〜#731 が磨いた監視面の、最新機構=drainに残った silent-stall の穴）。修正: reporter の alive-no-report バケットに**2ゲートのwedge escalation**を新設＝(a)当該レーンが alive-but-silent >= WedgeMinutes(既定240分=StaleMinutesの2倍) **かつ** (b)復活watchdog(=drainエンジン)自身が >= WedgeMinutes 常駐、の両方成立時のみ LOUD `laneHealthAliveNoReportStalled`（「!!! 要確認: runner固着疑い・手動再起動」）へ。ゲート(b)が**watchdog起動直後の偽アラームを封止**＝レーンの沈黙clock(missing-since)がdrainの起動より前（live: 06:38 runner vs 18:09 watchdog）でも、drainが実際に働けるようになってからWedgeMinutes経つまでは calm を維持。watchdog不在(-1)は escalation せず（drainエンジン皆無＝cold state＝logon launcherの責務）。clock源=never-reported は missing-since、report-then-silent は自行のreport age。新banner は健在性stem共有＝#699のshared-stem stripが版差異を跨いで自動収束（旧reporterも新bannerをstrip）。純関数 `Get-WatchdogUptimeMinutes`（best-effort WMI・失敗→-1で安全degrade・Get-AliveRunnerLanes をミラー）新設、`Get-LaneHealthLine` に `-WedgeMinutes`/`-WatchdogUpMinutes` パラメータ追加（既定は既存呼出しを無改変＝K/M/O群回帰なし）。ASCII厳守（日本語は loop-status-strings.txt 由来・.ps1追加行の非ASCII 0を実測）。→ 2026-07-03完了(PR)。`-SelfTest` に W群6アサート追加（W1:silent+watchdog長期常駐+alive→loud wedge／W2:watchdog起動直後→calm＝**live-scenario偽アラーム回帰ガード**／W3:clock未満→calm／W4:watchdog不明(-1)→calm＝安全degrade／W5:真のborn-deadがwedgeを優先＝loud dead>loud wedge／W6:report-then-silent aliveも自行ageでwedge化）→ALL PASS(計83)、PSParser 0-error。**live中央statusへの`-WhatIf`（read-only・md5不変）**でwatchdog常駐23.8分(<240)→健在性は calm「2/6稼働中だが自己報告なし」を維持＝偽wedgeアラーム無し（gate(b)が正しく抑止）を実測。web/src非侵。補充理由=未着手[ ]は掃除系(下記line42)/perf実測(下記line44)の2件のみ・両方とも全6レーン稼働中で静穏窓待ちブロック（3件未満）→運用4系統(報告/点検)の欠陥から補充。
- [x] 【Opus・P1】点火系補充: 協調drainの沈黙stall（wedged runner）を**正直化(#736)から自己修復へ**格上げ＝根因を封止。#736 は wedge を LOUD 報告（4h後「手動再起動」バナー）できても**clearできず**＝社長の手動介入待ちが残っていた。真因は `loop-runner.ps1:352` の `$prompt | & claude | Tee-Object` が**claude呼出しにtimeoutを持たない**こと（claudeハングで子プロセス永久生存→runner恒久BUSY→#727 drainがidle-only設計ゆえ永遠にdefer→heartbeat/hot-swap/self-report全停止＝永久wedge）。修正: 既存の inline pipe（streaming＋UTF-8 stdin）は**byte不変で温存**し、その周囲に**out-of-band wedge monitor**を新設＝新param `-IterationTimeoutSeconds`（既定7200=2h・<=0で無効化する fail-safe kill switch）で `Start-WedgeMonitor` が sibling Start-Job を arm、`$TimeoutSeconds` 経過で**runnerのclaude子ツリーだけ**を name-filter（`claude|node|cmd`）付き `taskkill /F /T` で force-kill→blocked pipeがunblock→runnerがheartbeatを撃ち（reporterの4h wedgeバナーが出る前に自己修復＝2h<4hで#736を backstop 化）→次イテレーションがprompt再読込（hot-swap）で完全復帰。happy path は monitor を pipe 直後の finally で Stop-Job するため発火せず＝invocation は従来と同一。sentinel は kill 前に書込み（unblock→Stop-Jobのraceで喪失しないよう順序保証）。純関数 `Test-TimeoutMonitorEnabled`/`Get-IterationWaitSeconds`（<=0→無効・positive→passthrough）新設。ASCII厳守（.ps1追加行の非ASCII 0を実測）・web/src非侵（tsc/lint/vitest/build対象外）。→ 2026-07-03完了(PR)。`-SelfTest` に X群（wedge gate 4ケース＝7200/1/0/-5 で enabled/wait 検証＋Stop-WedgeMonitor(null) no-op）追加→**ALL PASS**、PSParser 0-error。**live実測**＝新設 `-RehearseTimeout`（#716 の -RehearseCritic 同様、claude不使用の dummy `cmd/ping` 子で本番と同一の Start-/Stop-WedgeMonitor 配管を予行）で (1)ハング子が2.7sで force-kill されpipe unblock＋sentinel生成 (2)高速子は無傷・sentinel無し (3)timeout<=0 で monitor job 不生成＝3ケース ALL PASS。活性化=マージ後 各クローンの次 hot-swap/relaunch で新runnerが monitor を arm＝以後claudeハングが2hで自己終了しwedgeが恒久封止（手動再起動不要）。補充理由=未着手[ ]は掃除系(下記)/perf実測の2件のみ・両方とも全6レーン稼働中で静穏窓待ちブロック（3件未満）→運用4系統(点火/報告)の欠陥から補充。
- [x] 【Opus・P1】点火系補充: 協調drainの恒久inert化を封止＝idle判定からconhostを除外。#727/#734 の stale-runner drain（`runnerFloorIso` 未満の旧runnerをidle窓でcooperatively cycleし fresh code へ復活させる機構）が**導入以来一度も発火せず**、全6レーンが06:38起動の下限未満版（#672 heartbeat・#675 hot-swap 未搭載）のまま滞留していた。真因: `Get-ProcChildCount` が直接子プロセスを raw に数え idle gate は `ChildCount -eq 0` を要求するが、**console接続のPowerShell runnerは生涯 `conhost.exe` を1本必ず子に持つ**（runnerと同時刻生成・runner死まで生存）ため子数は idle でも 1(conhost)・claude turn中で 2 となり `==0` に**絶対到達しない**＝drainが恒久 no-op。実測: `seo` runner は idle(claude不在)でも子=conhost 1本のみで「BUSY」誤判定→永久defer→heartbeat未搭載のまま健在性が「runner旧版」表示（live telemetry の seo/ux-records 滞留の正体）。修正: 純関数 `Get-WorkChildCount` 新設＝子名から永続console-host助手(`conhost.exe`/`openconsole.exe`)を除外し in-flight WORK 子(claude/node/git等)だけを数える。`Get-ProcChildCount` を「子名収集→`Get-WorkChildCount`委譲」へ書換え（`-1` scan-fail sentinel温存）。idle runner(conhostのみ)→0=cycle対象、claude turn中→1=BUSY defer=in-flight不破壊を維持。→ 2026-07-03完了(PR#745)。`-SelfTest` に N2群8アサート追加（idle=conhostのみ→0/conhost+claude→1/大小無視/openconsole除外/子なし→0/null安全/blank無視/複数WORK子→busy）→ALL PASS、PSParser 0-error・追加行の非ASCII 0。**live `-HealOnly -WhatIf`（read-only）**＝旧コードは6レーン全て「BUSY(children=2)」で無cycle→本fixで idle の seo/ux-records（滞留2レーン）が「would cycle」・claude稼働中の data/ux-tools/ux-hub は正しくdefer・ops はancestor除外。活性化=merge+pull後の次heal passでidle下限未満runnerをsleep窓で1本ずつ fresh code へ復活。web/src非侵。補充理由=未着手[ ]は掃除系(下記)/perf実測の2件のみ・両方とも全6レーン稼働中で静穏窓待ちブロック（3件未満）→運用4系統(点火)の欠陥から補充。
- [x] 【Opus・P1】点検/補給系補充: planner/critic one-shot のtimeout killを単一プロセス→**プロセスツリーkill**へ是正＝orphan claudeによるworktree破壊/gitレースを封止。#743 が永続runnerの claude ハングを `taskkill /F /T`（tree-kill）で自己修復した一方、**one-shot経路（`Invoke-OneShot`）のtimeout killは `$p.Kill()` のまま**放置されていた。真因: Windows PowerShell 5.1 は .NET Framework 上で走り `[System.Diagnostics.Process].Kill()` に **entireProcessTree オーバーロードが無い**（.NET Core 3.0+ のみ）＝**呼出したpowershell one-shotプロセスだけ**を殺し、その `& claude` 子ツリー（claude/node/git）を**orphanで生かしたまま**戻る。とりわけ**criticはこれが致命的**＝criticは隔離worktree(`$wtPath`)で走り、`Invoke-OneShot` が timeout("timeout"返却)した直後に **finally が同worktreeを `git worktree remove --force`**（launcher L1441）＝orphan claudeが**まだそのworktreeで書込み/commit中**にworktree削除が走る＝破損/worktree leak（`Remove-Item -Recurse -Force` がlive handleで沈黙失敗）/隔離が防ぐはずの main tree との git レース再燃。plannerも同型＝orphanがlane cloneに残りその後ignition loopが同cloneで起動するlane runnerとgit競合。#736/#743 が永続runner側で封じた「timeoutなしclaudeハング」の**one-shot残渣**。修正: 純関数 `Get-TreeKillArgs($ProcessId)`（`/F /T /PID <id>` の正確な引数vectorを構築＝offline self-test可能）＋薄wrapper `Stop-ProcessTree($Proc)`（`taskkill @killArgs` で tree-kill→`WaitForExit(10000)` で**tree消滅を待ってから**戻る＝caller の worktree teardown が dying tree と競合しない・null安全・throwしない）を#743 wedge monitorと同じ `taskkill /F /T` 意味論で新設し、`Invoke-OneShot` の timeout枝を `$p.Kill()`→`Stop-ProcessTree -Proc $p` へ差替え（healthy path/戻り値契約は不変）。→ 2026-07-03完了(PR)。`-SelfTest` に S群3アサート追加（引数vector `/F /T /PID 4242`／4トークンでPIDが文字列化／null handle no-op非throw）→**ALL PASS**、PSParser 0-error・追加行の非ASCII 0を実測。**live E2E**＝`& cmd`(claude同型のblocking invocation-operator子)で parent powershell→cmd.exe→ping grandchild の**本物の子ツリー**を立て、`Stop-ProcessTree` で parent+全descendant(cmd+ping grandchild+conhost)が survivors=0 で消滅を実測（単一 `$p.Kill()` なら cmd/ping が orphan する所）。`-HealOnly -WhatIf` 無回帰＝drain/heal path クリーン・loop-status.md md5 byte不変。活性化=各クローンの次git pull＋hot-swap/relaunchで新launcherが tree-kill を適用＝以後 critic/planner のtimeoutが orphan を残さずworktree破壊を封止。web/src非侵。補充理由=未着手[ ]は掃除系(下記)/perf実測の2件のみ・両方とも全6レーン稼働中で静穏窓待ちブロック（3件未満）→運用4系統(点検/補給)の欠陥から補充。
- [x] 【Opus・P1】点検系補充: critic rehearsal の**偽の安心**を封止＝injection-target 存在検査を追加。`-RehearseCritic`（#716）は「critic の plumbing が健全」と PASS を返すが、実際には git worktree の半分（fetch/add/verify loop-runner.ps1/teardown）しか検査せず、critic が S/A 指摘を**注入する先の各レーンBACKLOG**（loop-prompt-critic.txt step4＝BACKLOG-data/seo/ux-hub/ux-records/ux-tools＋ops-sink）が deployed HEAD に存在するかは**一切見ていなかった**。もしどれかのBACKLOGが rename/削除されれば、7/9 の無人 critic fire は注入に失敗するか repo root に**新規の stray BACKLOG を `git add`** するが、rehearsal は**依然 PASS を報告**＝偽の安心（点検系が de-risk すると主張する対象の半分が未検査）。修正: 純関数 `Get-CriticInjectionTargets`（注入先6ファイルの single-source-of-truth）＋ `Get-MissingCriticTargets($Root,$Targets)`（offline set-diff・null/blank安全・blank rootは全targetをmissing扱い＝沈黙passしない）を新設し、`Invoke-CriticRehearsal` の worktree add 成功後に checkout 内で全targetの存在を検査＝1件でも欠ければ rehearsal を hard FAIL（dry-run planにもtarget列挙を追記）。→ 2026-07-03完了(PR)。`-SelfTest` に J群5アサート追加（targetは5レーン+ops sink＝6/全present→0 missing/1件削除→そのファイルをmissing報告/blank root→全6 missing/blank入力→null安全）→**ALL PASS**、PSParser 0-error・追加行の非ASCII 0を実測。**live `-RehearseCritic`**＝origin/main checkout で新step「carries all critic injection targets [...]: True」を出力し PASS・worktree leak 0 を実測。活性化=マージ+各クローンの次git pullで、以後の rehearsal（毎criticdue前のpre-flight＋ops手動）が注入先欠落を注入前に捕捉。web/src非侵（tsc/lint/vitest/build対象外）。補充理由=未着手[ ]は掃除系(下記)/perf実測の2件のみ・両方とも全6レーン稼働中で静穏窓待ちブロック（3件未満）→運用4系統(点検)の欠陥から補充。
- [x] 【Opus・P1】点検系補充: critic rehearsal の**偽PASS**を#760の「ファイル存在」から「ロスター網羅」へ拡張＝config追加レーンの取りこぼしを封止。#760 は注入先6BACKLOGが deployed HEAD に**存在するか**は検査するが、`Get-CriticInjectionTargets`（と loop-prompt-critic.txt step4）が**ハードコードした6ファイル**である一方、レーンの正本は `loop-config.json`（設計§E＝社長は config編集でレーンを増減）＝**single-source-of-truth違反**。もし社長が config に7本目のレーンを足すと（§Eが明示する再構成経路）、critic は prompt もfunction も盲目でそのレーンに**永遠に注入しない**のに、#760 のファイル存在検査は（既存6件が実在するので）依然 **PASS を報告**＝#760 が封じた偽PASSの**ロスターレベルの双子**が再発。修正: 純関数 `Get-ExpectedCriticTargets($Lanes)`（enabledレーン→BACKLOG-<name>.md を導出・null/blank安全）＋ `Get-CriticTargetRosterDrift($Hardcoded,$Expected)`（大小無視の set-diff＝`UncoveredLanes`=hardcodedが漏らすenabledレーン／`StaleTargets`=どのenabledレーンも背負わないhardcoded＝無効化レーンで良性）を新設し、`Invoke-CriticRehearsal` に optional `-ConfigLanes` を追加＝呼び出し元が config lanes を渡すと（`-RehearseCritic` は best-effort読込・pre-flight は `$cfg.lanes` を渡す）、hardcoded target が**全enabledレーンを網羅するか**を pure に検査＝`UncoveredLanes` が1件でもあれば rehearsal を hard FAIL（「update Get-CriticInjectionTargets AND loop-prompt-critic.txt」を名指し）。config読込不可→null→ロスター検査スキップ（git plumbing は従来通り全予行＝config健全性は別ゲート）。→ 2026-07-03完了(PR)。`-SelfTest` に J2群8アサート追加（enabledのみ導出＝disabled seo除外／null/blank名レーン無視／hardcoded==expected→drift 0／新レーンは UNCOVERED 報告＝FAIL相当／無効化は STALE で良性／空入力null安全／**SHIPPED targets が loop-config.json の全enabledレーンを網羅＝本repoの live-drift 0 をアサート**）→**ALL PASS**、PSParser 0-error・追加行の非ASCII 0を実測。**live `-RehearseCritic`**＝origin/main checkout で新step「critic targets cover every enabled config lane [BACKLOG-data.md,...,BACKLOG-ux-tools.md]: True」を出力し PASS・worktree leak 0 を実測。活性化=マージ+各クローンの次git pullで、以後の rehearsal が config追加レーンの critic 未網羅を注入前に大声FAIL。web/src非侵（tsc/lint/vitest/build対象外）。補充理由=未着手[ ]は掃除系(下記)/perf実測の2件のみ・両方とも全6レーン稼働中で静穏窓待ちブロック（3件未満）→運用4系統(点検)の欠陥から補充。
- [x] 【Opus・P1】点検系補充: critic rehearsal の**偽PASS**の第3レグ＝**config↔prompt** を封止。#764 は hardcoded `Get-CriticInjectionTargets`（オフラインoracle）を config に固定したが、その FAIL 文言自体が「update Get-CriticInjectionTargets **AND** loop-prompt-critic.txt」と2箇所の同期を要求する一方、**prompt 側が同期したかは誰も検査していなかった**。無人 critic が実際に読むのは `loop-prompt-critic.txt`（launcher L1552＝`$criticPrompt` が one-shot の -PromptFile）であって oracle ではないため、社長が §E でレーンを増やし oracle だけ更新すると（rehearsal は GREEN＝roster drift 0）、**stale な prompt がそのレーンを step-4 の注入ロスターから落としたまま**で critic は永遠に注入しない＝#760/#764 が封じた偽PASSの oracle↔prompt レグでの再発。修正: 純関数 `Get-CriticPromptUncoveredLanes($PromptText,$Lanes)`（各enabledレーン名を prompt本文へ大小無視の substring 照合・null/blank安全・**blank prompt は全enabledレーンを uncovered＝沈黙passしない**＝`Get-MissingCriticTargets` の blank-root契約と対称）を新設し、`Invoke-CriticRehearsal` の roster-drift ゲート直後（`$ConfigLanes` 供給時・pure・git不要）で `$RepoRoot/loop-prompt-critic.txt` を実読し、enabledレーンが1件でも prompt に不在なら hard FAIL（「add them to the critic prompt」を名指し）。prompt 不在時は実 critic gate（`Test-Path $criticPrompt`）が発火しない＝注入drift無し→スキップ（偽アラーム抑止）。→ 2026-07-03完了(PR)。`-SelfTest` に J3群7アサート追加（named→0 uncovered／不在レーン→UNCOVERED＝FAIL相当／disabled不在は非flag＝良性／大小無視／blank prompt→全enabled uncovered＝never silently passes／null prompt・null lanes・nameless レーン null安全／**SHIPPED loop-prompt-critic.txt が loop-config.json の全enabledレーンを網羅＝本repoの prompt-drift 0 をアサート**）→**ALL PASS**、PSParser 0-error・追加行の非ASCII 0を実測。**live `-RehearseCritic`**＝origin/main で新step「loop-prompt-critic.txt names every enabled config lane (step-4 injection roster in sync): True」を出力し PASS・worktree leak 0 を実測。活性化=マージ+各クローンの次git pullで、以後の rehearsal（毎criticdue前のpre-flight＋ops手動）が config↔oracle↔prompt の三角形の最後のレグ（oracleだけ更新でprompt放置）を注入前に大声FAIL。web/src非侵（tsc/lint/vitest/build対象外）。補充理由=未着手[ ]は掃除系(下記)/perf実測の2件のみ・両方とも全6レーン稼働中で静穏窓待ちブロック（3件未満）→運用4系統(点検)の欠陥から補充。
- [x] 【Opus・P1】点検系補充: critic prompt-coverage の**偽PASS**を部分一致→ホールトークン照合で封止＝#765の substring-precision レグを封止。#765 の `Get-CriticPromptUncoveredLanes` は `.Contains()` の**生の部分一致**でレーン名を prompt へ照合していたため、**レーン名が既存の被ルーティング token の部分文字列にすぎない場合**（step-4 実在の `ux-records` 内の `records` レーン、`ux-hub`/`ux-tools` 内の `ux` レーン）を**偽covered＝GREEN**と報告＝critic はそのレーンへ永遠に注入しないのに rehearsal は通過＝#760/#764/#765 が封じた偽PASSクラスの substring-precision レグでの再発。修正: prompt を maximal `[a-z0-9-]` run へ**トークン化**（ハイフンは token char＝レーン名がハイフンを含むため `records` は `ux-records` token 内に一致しない）し、**bare token 一致**（step-4 slash-list の `seo`）**OR `backlog-<lane>` token 一致**（`BACKLOG-<lane>.md` ルーティング形 `backlog-data`）でのみ covered と判定。現行6 enabledレーン（ops/data/seo/ux-tools/ux-records/ux-hub）は全て被覆＝**無回帰**、`records`/`ux` 等の部分文字列レーンは正しく UNCOVERED へ、blank prompt→全 uncovered 契約も温存。→ 2026-07-03完了(PR#771)。`-SelfTest` に J4群4アサート追加（部分文字列 `tools`/`ux`→UNCOVERED＝偽PASS封止／`BACKLOG-<lane>` 形は covered 維持＝無回帰／`.md` 抜きの bare hyphen token 一致）→**ALL PASS**、PSParser 0-error・追加行の非ASCII 0（CRLFのみ）、SHIPPED no-drift アサート維持＝live prompt+config で 0 uncovered を機械保証。**live `-RehearseCritic`**＝「loop-prompt-critic.txt names every enabled config lane: True」・worktree leak 0 を実測。web/src非侵。補充理由=未着手[ ]は掃除系(下記)/perf実測の2件のみ・両方とも全6レーン稼働中で静穏窓待ちブロック（3件未満）→運用4系統(点検)の欠陥から補充。
- [x] 【Opus・P1】点火系補充: 復活watchdogのプロセス生存スキャンの偽陽性を`-File`精密照合で封止＝コマンドライン内の単なる**言及**によるspoofを排除。#654/#690/#748 が積み上げた watchdog は復活backbone（ops-SPOF解消・協調drainの唯一のトリガ）だが、その**単一インスタンスguard**（`loop-watchdog.ps1` 単一instance判定）と launcher の `Invoke-EnsureHealWatchdog`（`AlreadyRunning` 判定）は**両方とも `CommandLine -like '*loop-watchdog.ps1*'` の生の部分一致**でwatchdogプロセスを数えていた＝コマンドラインに `loop-watchdog.ps1` の文字列を**含むだけ**の任意のpowershell（オペレータ/エージェントが `-Command "... loop-watchdog.ps1 ..."` で撃つ点検コマンド＝**まさに本イテレーションで私が撃った診断が自己マッチした**）が liveness scan を**spoof**する構造欠陥。帰結2つ: (a) guard偽陽性＝正しくspawnされた新watchdogが「競合あり」と誤認して**self-exit→zero-watchdog gap**（watchdogの唯一の復活源=稀なfull launcher pass＝最悪~24h不在）、(b) ensure偽陽性＝`AlreadyRunning`が偽`$true`になり**本当に死んでいるwatchdogをspawnしない**。#602〜#771 が runner/lane scan で `-Lane` 正規表現＋config-lane keying により封じてきた「scan精度」欠陥の、二次フィルタを持たない**watchdog scan残渣**（`.Count -gt 0` の純粋部分一致でフィルタ皆無）。修正: 純関数 `Test-IsWatchdogProcess($CommandLine)` 新設＝**実launch2形（ensure-spawnの `-File <path>\loop-watchdog.ps1`／self-respawnの `-File "<path>\loop-watchdog.ps1"`）だけ**を `(?i)-File\s+"?[^"]*loop-watchdog\.ps1(?:"|\s|$)` で照合し、`-Command` 内の言及（`-File` が前置されない）を除外。両scriptはモジュール非共有のため局所コピー（`Get-AliveRunnerLanes` の先例に倣う・両所同一意味論）。→ 2026-07-03完了(PR)。watchdog `-SelfTest` に wd-id 7ケース追加（bare/quoted/trailing-args `-File`→watchdog／`-Command`言及→非watchdog＝偽マッチ封止／`-like`フィルタ文字列→非watchdog／lane runner→非watchdog／空→null安全）、launcher `-SelfTest` に R2群5アサート追加→**両ALL PASS**、両PSParser 0-error・loop-watchdog.ps1 非ASCII 0（pure ASCII維持）。**live実測**＝`-HealOnly -WhatIf` が実watchdog(pid 44156＝真の `-File` launch)を新predicateで正しく「already running; not spawning」検出＝無回帰・loop-status.md md5 byte不変、**live spoofer E2E**＝`-Command "... loop-watchdog.ps1 ..."` の実プロセスを起こし旧`-like`は**True(バグ)**／新`Test-IsWatchdogProcess`は**False(修正)**を実プロセスで実証。活性化=各クローンの次git pull＋watchdog/launcherのhot-swap/relaunchで新predicateが有効化。web/src非侵。補充理由=未着手[ ]は掃除系(下記)/perf実測の2件のみ・両方とも全6レーン稼働中で静穏窓待ちブロック（3件未満）→運用4系統(点火)の欠陥から補充。
- [x] 【Opus・P1】点火系補充: #777の`-File`精密照合を**lane runner**の生存スキャン4箇所へ横展開＝コマンドライン言及によるspoofを封止。#777 は watchdog scan の`-Command`言及spoofを`-File`精密照合で封じたが、その脚注が「#602〜#771 が runner/lane scan で `-Lane` 正規表現＋config-lane keying により封じてきた」と主張した**runner scan側は実は無防備**だった＝lane runnerの生存判定は依然 `CommandLine -like '*loop-runner.ps1*'` の**生の部分一致**でプロセスを数え、`-Lane data` を**含むだけ**の任意のpowershell（オペレータ/エージェントが `-Command "... loop-runner.ps1 ... -Lane data ..."` で撃つ点検＝#777の watchdog spoofと同族）が liveness scan を**spoof**する。`-Lane`正規表現は「どのlaneか」を抽出するだけで`-Command`言及を除外しない＝config-lane keying も無効化されず素通り。影響4箇所と帰結: **(1)loop-runner.ps1 単一インスタンスguard**（最重要）＝spoofプロセスが同lane競合と誤認され、hot-swap clean-exit(#672/#690)や heal後に**正しくrelaunchされたrunnerがself-exit→lane死亡**、**(2)launcher Get-RunningLanes**＝死んだlaneを「alive」と誤報告し `-HealOnly` が復活をskip（(1)と連鎖＝lane恒久死）、**(3)launcher Get-RunnerProcInfo**＝協調drainのPID/起動時刻マップ汚染、**(4)reporter Get-AliveRunnerLanes**＝#696/#703の健在性tiebreakが**死んだlaneをaliveと誤認し停止バナーを誤ってcalm化**（#777とは逆方向の害＝真の死を隠す）。修正: 純関数 `Test-IsRunnerProcess($CommandLine)`＝`(?i)-File\s+"?[^"]*loop-runner\.ps1(?:"|\s|$)` で**実launch形（persistent/heal spawn＋one-shot planner/critic＋手動＝全て`-File <path>\loop-runner.ps1`）だけ**を照合し`-File`前置なしの`-Command`言及を除外。3スクリプトはモジュール非共有のため局所コピー（#777の`Test-IsWatchdogProcess`／`Get-AliveRunnerLanes`の先例に倣う・全所同一意味論）＝legacy無lane runnerも`-File`形なので温存、watchdog `-File`は別filename＝非マッチ。→ 2026-07-03完了(PR)。runner `-SelfTest` に RID群7ケース（bare/quoted/one-shot planner/legacy無lane `-File`→runner／`-Command`言及＋lane→非runner＝spoof封止／watchdog `-File`→非runner／空→null安全）、launcher `-SelfTest` に R3群5・reporter `-SelfTest` に R群5アサート追加→**3スクリプト全ALL PASS**、3スクリプトPSParser 0-error・追加行の非ASCII 0を実測。**live実測**＝`-HealOnly -WhatIf` が実6レーン(全て真の`-File` launch)を新predicateで正しく「all enabled lanes alive; nothing to resurrect」検出＝無回帰・loop-status.md md5 byte不変、**live spoofer E2E**＝`-Command "... loop-runner.ps1 -Lane data ..."` の実プロセスを起こし旧`-like`は**True(バグ＝dataがalive誤報告)**／新`Test-IsRunnerProcess`は**False(修正)**を実プロセスで実証。活性化=各クローンの次git pull＋runner/launcher/reporterのhot-swap/relaunch(reporterはfresh processで即)で新predicateが有効化。web/src非侵。補充理由=未着手[ ]は掃除系(下記)/perf実測の2件のみ・両方とも全6レーン稼働中で静穏窓待ちブロック（3件未満）→運用4系統(点火/報告)の欠陥から補充。
- [x] 【Opus・P1】報告系補充: #782が積み残した**reporterのwatchdog生存スキャン**を`-File`精密照合で封止＝scan精度sweep（#777 watchdog／#782 lane runner4箇所）の最後の生substring残渣を排除。#782 は reporter `loop-report-status.ps1` の**runner** scan（`Get-AliveRunnerLanes`）を `Test-IsRunnerProcess` で精密化したが、**同一ファイル**の**watchdog** scan（`Get-WatchdogUptimeMinutes:610`）は依然 `CommandLine -like '*loop-watchdog.ps1*'` の**生の部分一致**のまま取り残していた＝#782 の双子欠陥。`Get-WatchdogUptimeMinutes` は wedge banner の**第2ゲート**（復活watchdogが常駐して drain が是正する時間を持ったか）で、`-Command "... loop-watchdog.ps1 ..."` の言及だけの任意powershell（オペレータ/エージェント診断＝#777でまさに私が撃った同族）が spoofマッチし常駐watchdog数と最古uptimeを汚染＝alive-but-silent laneの wedge判定（drainが時間を持ったか）を狂わせる。修正: 純関数 `Test-IsWatchdogProcess($CommandLine)`＝`(?i)-File\s+"?[^"]*loop-watchdog\.ps1(?:"|\s|$)` を新設（loop-watchdog.ps1:156／loop-launcher.ps1:429 の同族predicateの局所コピー＝モジュール非共有ゆえ・`Test-IsRunnerProcess` の先例に倣う）し、`Get-WatchdogUptimeMinutes` の scanを `$_.ProcessId -ne $PID -and (Test-IsWatchdogProcess ...)` へ差替え（reporter自身のプロセスも除外）。→ 2026-07-03完了(PR)。reporter `-SelfTest` に W群5アサート追加（bare/quoted `-File`→watchdog／`-Command`言及→非watchdog＝spoof封止／runner `-File`→非watchdog／空→null安全）→**ALL PASS**、PSParser 0-error・追加行の非ASCII 0を実測。**live spoofer E2E**＝`-Command "... loop-watchdog.ps1 ..."` の実プロセスを起こし旧`-like`は**True(バグ)**／新`Test-IsWatchdogProcess`は**False(修正)**を実プロセスで実証、**無回帰**＝実watchdog(pid 32896＝真の`-File` launch・up~19min)を新predicateで正しく1件検出。活性化=各クローンの次git pullで reporter はfresh processが最新on-disk版を読む＝即。web/src非侵。補充理由=未着手[ ]は掃除系(下記)/perf実測の2件のみ・両方とも全6レーン稼働中で静穏窓待ちブロック（3件未満）→運用4系統(報告)の欠陥から補充。
- [x] 【Opus・P1】報告系補充: runnerのwedge天井を中央status stale閾値**未満**へ引下げ＝wedge由来の偽「サイレント個別レーン死」アラームを封止。#602〜#688 が積み上げた沈黙死検知＝reporterは各レーンの`最終稼働`が `StaleMinutes(既定120分)` 以上古いと「健在性 停止疑い/手動再起動」の大声バナーを出す（`age -ge 120min`）。ところが `loop-runner.ps1` の**wedge監視の天井 `IterationTimeoutSeconds` が既定7200秒=ちょうど120分**で stale閾値と**同値=ヘッドルーム0**だった。wedge中はエージェントが自身のstep5.5報告に到達できず、`Report-Heartbeat`（唯一のタイムスタンプ更新源）はclaudeターン**終了後**にしか撃たれない＝1回の最悪wedgeがレーンの`最終稼働`を**丸ごと7200秒凍結**し、wedge kill直後のheartbeatが到達する時点で age がちょうど`-ge 120min`境界を踏む→**生きていて次イテレーションで自己回復するレーンに偽の「手動再起動」バナー点灯**（#628 のヘッドルーム論法「重い1イテレーション30-40分の>3倍」は**wedgeイテレーション（天井まで走る）を勘定に入れていなかった**＝当該コメント526-528「heartbeatが行を新鮮に保つのでreporterはエスカレートしない」を数値が裏切る構造欠陥）。修正: 既定 `IterationTimeoutSeconds` を **7200→5400秒（90分）** へ引下げ＝120分stale閾値に対し**30分ヘッドルーム**を確保（wedge kill後のheartbeatが必ず境界の手前に着地）。純関数 `Test-WedgeCeilingUnderStale($TimeoutSeconds,$ReporterStaleMinutes,$MarginSeconds)` 新設＝天井が`stale*60 - margin`以下かを判定（monitor無効`<=0`＝legacy無限待ちは免除＝真の無限hangはreporterが**正しく**死と判定すべき）。90分は単一claudeターンとしては依然極めて潤沢（通常30-40分）＝実wedge耐性の実害ほぼ無し・自己回復は次イテレーションで担保。→ 2026-07-03完了(PR)。`-SelfTest` に **Y群6アサート＋出荷defaultガード**追加（出荷5400s→合格／旧0ヘッドルーム7200s→**却下=本バグを捕捉**／境界6300s→合格・6301s→却下／無効0・負→免除／**shipping param default自体**が不変式を満たすことを実測）→**ALL PASS**、PSParser 0-error・**loop-runner.ps1 非ASCIIバイト0**（我が追加行の`最終稼働`を除去＋既存の孤立日本語リテラル`起動`(L476)もASCII化しpure-ASCII契約を回復）を実測。活性化=各クローンの次git pull＋runnerのhot-swap relaunchで新既定が有効化（launcherは`-IterationTimeoutSeconds`を渡さない＝常に既定を使用）。web/src非侵＝tsc/lint/vitest/build対象外。補充理由=未着手[ ]は掃除系(下記)/perf実測の2件のみ・両方とも全6レーン稼働中で静穏窓待ちブロック（3件未満）→運用4系統(報告)の欠陥から補充。
- [x] 【Opus・P1】点検系補充: #793 の CI常設ゲートに**復活watchdogの-SelfTest**を追加＝機構4スクリプトのうち唯一無防備だった watchdog を封止。#793 は ops-selftest.yml で launcher/runner/reporter の -SelfTest を機構ファイル変更時に緑必須化したが、**運用機構の第4スクリプト `loop-watchdog.ps1`**（復活backbone＝ops-SPOF解消・協調drainの唯一のトリガ／#654/#690/#748/#777）が path フィルタにも run ステップにも**不在**だった＝#777 が封じた `Test-IsWatchdogProcess` の scan精度（`-Command`言及spoof封止）等が**リグレッションしても誰も回さず main へ着地**しうる＝#793 が封じたはずの点検系の穴の watchdog残渣。修正: `push`/`pull_request` path フィルタ2箇所へ `loop-watchdog.ps1` を追加＋本番同一の `powershell -File .\loop-watchdog.ps1 -SelfTest` run ステップを追加＝loop-*.ps1 で `-SelfTest` を持つ**全4スクリプト**を gate へ被覆（完全化）。→ 2026-07-03(PR#797)。**自己検証**＝watchdog `-SelfTest` ローカル実測 ALL PASS(27ケース＝untilIso/interval clamp/hotswap/単一インスタンスguard/relaunch args/wd-id生存スキャン識別・exit 0)、FAIL枝が `exit 1` を返す exit契約(L243・他3本と同一でゲート成立)、`ops-selftest.yml` が YAML valid(`yaml.safe_load`通過)を実測。web/src非侵。補充理由=未着手[ ]は掃除系(下記)/perf実測の2件のみ・両方とも全6レーン稼働中で静穏窓待ちブロック（3件未満）→運用4系統(点検)の欠陥から補充。
- [x] 【Opus・P1】点火系補充: loop-config.json の**構造破損**をPR時に落とすCIゲート＝点火の唯一の正が無言で半点火する穴を封止。launcher の runtime config読込は「file欠落/JSON不parse/lanes空」の粗い3失敗しか見ず、**PR時に誰も検証しない**。loop-config.json は点火の唯一の正＝社長が最頻で手編集(untilIso)する正本なのに、**PARSEは通るが構造破損**した config（レーン名 空/欠落→Resolve-LaneRepo がゴミclone path＋runner が空-Lane＋heal/planner/critic routing 全て name キーで誤爆／model 空/欠落→runner が空-Model で無言の不良点火／intervalSeconds 非正/非数→sleep算出破綻／**レーン名 重複**→2 persistent runner が同一cloneを駆動＝index.lock競合/branch clobber＋liveness scan識別不能／untilIso 不parse→run全体が依存する期限判定が未定義）が全パスをすり抜け、次パス(logon/07:00・最悪~22h後)にゴミ/半数点火する無言の穴（#631/#634 が集約/個別で掃討した沈黙死の**config-time版**）。修正: 純関数 `Get-ConfigValidationErrors($Cfg)`（parse済objを取りIOなし＝-SelfTest全被覆）新設を runtime の lanes空 guard **直後**に配線＝構造破損を parse失敗と**同じ大声 config-error バナー**(社長のwatch-file)へ経由＝半点火せず可視停止。エラー文字列はASCII英字（parse例外文言と同列＝configErrorBody3 {ERROR} 差込・日本語リテラルは .ps1 に置かず strings 由来のヘッダ/本文のみ）。launcher `-SelfTest` に config16アサート追加（null/lanes欠落/空/blank名/model欠落/interval非正/非数/**ABSENT intervalSeconds/untilIsoは正常**/重複名(大小無視)/untilIso不parse/null lane entry/**live committed config構造健全**）。`ops-selftest.yml` パスフィルタ(push/pull_request)に `loop-config.json` を追加＝config を触ったPRで launcher `-SelfTest` がライブ config を構造検査＝fat-fingerを~22h後のprodでなく**PR時**に落とす。→ 2026-07-03完了(PR#800)。`-SelfTest` **ALL PASS**(config16含む)、**隔離C:\tmp**の構造破損config(untilIso不parse+重複OPS+model欠落+interval=0)への `-WhatIf` E2E＝4エラーを大声バナー「エラー内容」行へ集約→dryrunへ書込・exit1、正常configの `-WhatIf` は config-error **0件**(無回帰)を実測。web/src非侵＝機構ゲートのみ。補充理由=未着手[ ]は掃除系(下記)/perf実測の2件のみ・両方とも全6レーン稼働中(loop-status.md heartbeat直近1分内)で静穏窓待ちブロック（3件未満）→運用4系統(点火)の欠陥から補充。
- [x] 【Opus・P1】O18-a（点火/計測基盤・2026-07-04完了）: 生成品質evalの**夜間自動測定エンジン**＝`loop-eval-nightly.ps1` 新設。本番23問(`web npm run eval:chatbot-gen`＝本番URLへHTTP 23問・ローカルCPU非消費でperf静穏窓制約は非該当)を社長プロンプト無しで**1日1回だけ**自動測定。冪等ガード=当日分 `.loop-eval/genquality/<date>.json` が既存なら即no-op＝**Gemini消費は23問/晩に限定**（多重起動しても実測は1日1回に収束）。トリガは launcher/cron でなく**常時生存する唯一のopsレーン**の step5.7（loop-prompt-ops.txt）＝長時間ログイン機でも07:00 admin task非依存で毎日測定。結果は機械可読で `.loop-eval/genquality/`(gitignore・main tree・git非汚染/非レース) へ daily/`latest.json`/`history.jsonl` 追記。定義的失敗(evalプロセス起動済だが不成立)は当日markerを書いて予算再消費を防止・純setup失敗(web未install)はmarker書かず再試行。閾値(strictAccuracy floor 0.80＝baseline0.905の>2問下・誤アラーム回避)は record に belowTarget として刻むのみ。`-SelfTest`21アサート(日付stamp/冪等guard/回帰判定/record整形/history round-trip/失敗record)＋`-WhatIf`(guard判定と実行計画のみ・npm/network/書込みゼロ)。→ 2026-07-04(PR)。`-SelfTest` ALL PASS(19)、`-WhatIf` で当日未測定→実行計画出力かつ `.loop-eval/` 生成ゼロ・tree不変を実測。`ops-selftest.yml` パスフィルタ＋run stepに追加(機構第5スクリプトを常設ゲート化)。web/src非侵。閾値未達→loop-status警告バナー＋/about併載＋意図的劣化の点灯実測はO18-bへ分割。
- [x] 【Opus・P1】O18-b(1)（報告系・2026-07-04完了）: O18-aの計測結果を**社長の監視面へ surfacing**その1＝**閾値未達の夜は docs/loop-status.md に警告バナーを出す**（黙って劣化しない設計）。loop-eval-nightly.ps1 に純関数 `Get-EvalBannerLines`（record.belowTarget→localized3行／それ以外→空＝clear＝**record と同じ Test-EvalBelowTarget を再利用しダッシュボードと記録が矛盾不能**）＋`Set-EvalRegion`（`<!-- EVAL-QUALITY:BEGIN/END -->` を reportHeader直上に**region冪等**でraise/replace/remove・2回適用で不変・clearで原文完全復元＝自己修復）＋`Get-StatusLock`（reporter と同一意味論のstale-orphan回収）＋`Update-EvalStatusBanner`（**ロック直列**・非致命・latest.jsonから-WhatIf preview）を新設。日本語は loop-status-strings.txt の evalBelowHeader/Body1/Body2（{PCT}/{TARGET}/{DATE}/{CORRECT}/{SCORABLE}差込）から実行時読取り＝**.ps1 は純ASCII維持**（strings欠落時も生キー表示＝沈黙にはならない）。**3writerが全て銀行残**を実測封止＝(a)eval-nightlyがraise/clear (b)reporterのheartbeatは region外content逐語保存 (c)launcher full renderは新設 `Get-RegionLines` で EVAL region を reportHeader直上へ**preserve**（無ければlogon/07:00 full passがバナーを無言wipe＝この班が殺すはずの沈黙劣化の再来）。→ 2026-07-04(PR)。eval-nightly `-SelfTest` にH群18アサート追加（strings loader/banner body pct・15/21・ok以外は空/region raise・idempotent・clear自己修復・header無→top挿入/lock clean・fresh尊重・stale回収）→ALL PASS、launcher `-SelfTest` にGet-RegionLines3アサート→ALL PASS、reporter `-SelfTest` 無回帰。**隔離temp**でE2E＝degraded record→バナーraise(reportHeader直上)・-WhatIfは実status md5不変／recovered record→バナーclear（self-heal）／launcher full render(temp repoRoot)がEVAL banner+warn header+ops row全保存・banner is ABOVE report region／reporter heartbeatがEVAL banner保存を全て実測。web/src非侵＝機構のみ。(2)/about併載は下記O18-b(2)へ分割（越境+snapshot commit+git-race worktreeで独立PR相当のため）。
- [x] 【Opus・P1】報告/計測基盤補充（O18-a硬化・2026-07-04完了）: 夜間evalが**瞬間的HTTP不調で有効レポートを捨てる沈黙データ喪失**を是正。O18-a(PR#808)の初回本番実測(02:36)が「eval harness did not produce a parseable report」失敗マーカーを書き当日計測を喪失=**真因**は `chatbot-genquality-live.test.ts` が全23問レポートを書き出した**後**に「全問httpStatus=200」をassertするため1問でも瞬間非200(コールドスタート/レート制限/5xx)で harness 非0 exit → 旧wrapper(`loop-eval-nightly.ps1:564`)が `exitCode -eq 0` 条件で**有効な完成レポートを破棄**し失敗マーカー→当日予算消費=再測定も封止(この班が殺すはずの黙って劣化)。手動再測定(03:16)は**21/21=100%**成功=瞬間ブリップと確定。修正(全ops所有・web/src非侵): (1)パース可能レポートは exit code 不問で必ず記録(`-eq 0`ゲート撤去・真のハード失敗=レポート皆無のみ失敗マーカー)、(2)`Get-ReportHttpFailureCount`純関数で非200問数集計、(3)`ConvertTo-NightlyRecord` に ExitCode/HttpFailures(既定0=後方互換)追加し `incomplete`(exit≠0 or httpFailures>0)を刻む→incompleteの正答率は信頼不能(未到達=誤答扱い)ゆえ **belowTarget抑止**=可用性ブリップを品質劣化の偽アラームにしない(ok=true=データ保全)、(4)`Get-EvalBannerLines` に incomplete 別バナー(品質劣化でなく「測定不完全=品質判定保留」)追加=夜が黙って消えないが偽の品質警告も出さない・loop-status-strings.txt に evalIncompleteHeader/Body1({HTTPFAILS}/{EXIT}/{DATE}差込)新設・.ps1純ASCII維持。→ 2026-07-04(PR#820)。`-SelfTest` I群14アサート追加(http集計/incompleteでbelowTarget抑止/ok保全/後方互換/incompleteバナーは品質警告と別)→**全55 ALL PASS**、`-WhatIf`当日マーカー冪等no-op、PSParser 0-error・追加行の非ASCII 0を実測。補充理由=未着手[ ]3件(O18-b(2)は越境/掃除系・perf実測は静穏窓待ち)は全6レーン稼働中(loop-status.md heartbeat直近3分内)でブロック→運用4系統(報告/計測基盤)の欠陥から補充。
- [x] 【Opus・P1】計測基盤補充（O18-a兄弟修理＝no-report全焼・2026-07-04完了）: 夜間evalが**「parseableレポート皆無」のハード失敗を1回起こすと当日を全焼**する沈黙データ喪失を是正。O18-a硬化(PR#820)はparseable-but-HTTP失敗を救ったが、**同型の兄弟欠陥**が残存＝node/vitestがレポートを一切書かなかった場合(prodコールドスタート/デプロイ中/DNS瞬断が02:xxの唯一の試行に当たる)、旧ロジック(`loop-eval-nightly.ps1:652`)が即terminalな日次マーカーを書き当日の再計測を全ブロック→計測ゼロ。実測=本日`2026-07-04.json`が`ok:false / "did not produce a parseable report"`で全焼。修正(全ops所有・web/src非侵): (1)`Test-AlreadyRanTonight` を「**terminal**な日次記録があるか」へ厳密化＝`ok=true`(計測済/incomplete込)は常にterminal・`ok=false`失敗は`attempts>=MaxEvalAttempts`(既定3)で初めてterminal・未満はNON-terminal＝次opsティックが再試行、(2)`New-FailureRecord` に `attempts/maxAttempts/terminal` 追加＝失敗ブロックは前回attemptsを読んで+1し最終試行のみterminalマーカー、(3)legacy失敗記録(attempts欄なし)は0扱い=再試行可＝旧ロジックで誤って全焼した当日を回収・不明形状/破損マーカー(ok欄なし)はterminal扱い=破損ファイルでprodを叩き続けない保守的既定、(4)worst-case浪費は`MaxEvalAttempts x 23問`に有界・`-MaxEvalAttempts` param+doc新設。→ 2026-07-04(PR#824)。`-SelfTest` に retry境界8アサート追加(unknown-shape→terminal/ok=true→terminal/legacy失敗→retryable/attempt1・3のterminal境界/New-FailureRecordのattempts・terminal)→**ALL PASS**、`-WhatIf` で当日がretryable("today is NOT yet measured; would run")へ復帰＝次のstep5.7実evalで本日の計測を回収することを実測。補充理由=未着手[ ]3件(O18-b(2)越境/掃除系・perf実測)は全6レーン稼働中(runner6本live実測)でブロック→運用4系統(計測基盤)の欠陥から補充。
- [x] 【Opus・P1】計測基盤補充（history.jsonl 追記のBOM自己修復・2026-07-04完了）: 夜間evalの **trend履歴 `history.jsonl` が先頭にUTF-8 BOMを永久保持** し、O18(b)の/about consumer が line1 を JSON.parse すると SyntaxError になる沈黙欠陥を是正。真因=O18-a硬化(PR#820)は daily/latest.json を `Write-JsonNoBom`(WriteAllText=上書きゆえBOM除去)で救ったが、**追記専用の history.jsonl** は `Add-JsonLineNoBom`(`AppendAllText`)＝**新規行にBOMを足さないが既存BOMを剥がせない**。旧 `Set-Content -Encoding UTF8` が02:36失敗時に書いた BOM が byte0 に残存→以後 `Add-JsonLineNoBom` で何行追記しても line1 は `﻿{...}` のまま（実測=live history.jsonl 先頭 `EF BB BF`）。cycle-log自身が「BOMでSyntaxError」を O18(b)の危険として名指ししていたのに append 経路だけ未対処。修正(全ops所有・web/src非侵): 純関数 `Test-FileStartsWithBom`(先頭3バイト読取・IOエラーは$false＝append best-effort維持)新設＋`Add-JsonLineNoBom` を自己修復化＝**既存ファイルがBOM始まりなら ReadAllText(読取り時にBOM自動剥離)→WriteAllText(no-BOM)で全体を書き直し新行追記／それ以外は従来通り plain append**＝gitignoreのファイルを手で触らずとも次回追記で JSON.parse-safe へ収束。→ 2026-07-04(PR)。`-SelfTest` G2群7アサート追加(seed BOM→append後にBOM消滅・両行が順序保存・clean file は追記後もBOM無・欠落ファイルは非throーで無BOM作成)→**全PASS**、PSParser 0-error・追加行の非ASCII 0(既存3バイトはO18-a由来コメント・未編集)、**live history.jsonlのコピー**への実適用で `EF BB BF`→`7B 22 64`(`{"d`)・全行保存を実測。補充理由=未着手[ ]3件(O18-b(2)越境・掃除系/perf実測は静穏窓待ち)は全6レーン稼働中(loop-status.md heartbeat直近3分内)でブロック→運用4系統(報告/計測基盤)の欠陥から補充。
- [x] 【Opus・P1】計測基盤補充（latest.json は完全計測のみで前進・2026-07-04完了）: 夜間evalの **`latest.json`（O18(b)の/about が読む「現在の品質」ポインタ）が incomplete run で上書きされ、瞬間的HTTP不調由来の不当に低い正答率を公開面に晒す** 沈黙欠陥を是正。真因=`latest.json` の docstring は「overwritten each **success**（most-recent, for /about later）」だが、実コード(line795)は parseable なら **incomplete でも無条件で上書き**していた。incomplete run の strictAccuracy は未到達質問が誤答計上され不当に低い＝O18-aで record 側は `incomplete=true`＋`belowTarget` 抑止で正しく守ったのに、**latest.json という現在値ポインタ**が blip 由来の低値へ後退→O18(b)の/about が素朴に `latest.strictAccuracy` を読むと**サイトが健全でも偽の公開品質後退**を表示。修正(全ops所有・web/src非侵): 純関数 `Test-ShouldAdvanceLatest($Record)`＝**ok=true ∧ ¬incomplete のみ true**（null/failure/incomplete は false・no-throw）新設＋latest.json 書込みを本ゲートで条件化＝**incomplete run は daily marker(日ガード)と history.jsonl(trend)には従来通り記録するが latest.json は前進させず、最後の完全計測を現在値として温存**。初回計測が incomplete でも latest.json 欠損のまま＝O18-b(2)が既に「latest.json欠損時も破綻しない静的fallback付き」を要件化済ゆえ安全。→ 2026-07-04(PR)。`-SelfTest` I6群6アサート追加(clean at/above→前進・complete below-target→前進(真の値)・incomplete(exit≠0)→非前進・incomplete(httpFailures>0)→非前進・failure(ok=false)→非前進・null→非throーで非前進)→**全PASS(計38)**、`-WhatIf` は当日terminalで日ガード短絡=無回帰を実測、web/src非侵。補充理由=未着手[ ]3件(O18-b(2)越境・掃除系/perf実測は静穏窓待ち)は全6レーン稼働中(runner process 6/6実測)でブロック→運用4系統(報告/計測基盤)の欠陥から補充。次タスクO18-b(2)の土台を直接補強。
- [x] 【Opus・P1】報告系補充: 夜間evalログの無制限肥大を封止＝#655 の `Get-LogsToPrune` は launcher/runner/watchdog/planner の4種ログしか剪定せず、O18 で後から加わった `eval-nightly-*.log` を**allowlist漏れ**で放置していた。しかもこれは**全種で最速増殖**＝loop-eval-nightly.ps1 が start 行を already-ran ガードの**手前**でログするため、opsが step5.7 で~195s毎に叩く**当日no-opスキップ**でも毎回2行の新ファイルを1本落とす（~480本/日）。#655がkillした「操作者の一次診断面=logs/ の無制限肥大」の O18 残渣＝検知も剪定もされないまま main に着地していた。修正: `Get-LogsToPrune` の allowlist に `($n -like 'eval-nightly-*.log')` を追加（手書き .md / 非該当ファイルは不可侵の既存規律を維持・KeepMin最新は年齢無関係に保持）＋doc comment に「最速増殖型」の由来を明記。→ 2026-07-04完了(PR)。`-SelfTest` の L群に2アサート追加（古eval-nightly→削除／最新eval-nightly→保持）→launcher `-SelfTest` ALL PASS、実 logs/ に既に eval-nightly-*.log 7本が堆積（O18本日着地・以後~480本/日）を実測＝allowlist追加で以後の launcher full pass が14日/最新20件保持で sweep。web/src非侵。補充理由=未着手[ ]の最上位O18-b(2)は/about越境（ux班所有）ゆえ全6レーン稼働中は静穏窓待ちでブロック、line64掃除系/line66 perf実測も他レーン停止待ちで同様に即着手不可（3件ともブロック）→運用4系統(報告)の欠陥から補充。
- [x] 【Opus・P1】O18-b(2): O18-aの計測結果 surfacing その2＝生成正答率を **/about の品質表示へ併載**（2026-07-04完了・PR#842）。着地面は **/about/chatbot-eval**（/about配下の品質レポート専用ページ）＝同ページは自ら「Recall@5は検索ヒット率でありGemini生成回答の正答率ではない」と免責しており、まさにその開示の穴を埋める最も正直な位置。ux班所有の /about **本文(AboutBody)は無改変**＝越境を最小化（root textには触れずchatbot-evalページに新セクション追加）。追跡スナップショット `web/src/data/chatbot-genquality-latest.json` を **本番2026-07-04夜間evalの実結果**（strictAccuracy 0.9524=完全正答20/21・usefulRate 100%・.loop-eval/genquality/latest.json の公開安全projection＝baseUrl等の内部フィールドは除外）で seed し、`loadGenQuality()` が [0,1] 外・scorable非正・欠損で破綻せず **2026-07-03ベースライン19/21=90.5%**（docs/chatbot-genquality-eval-2026-07-03.md）へ静的フォールバック。既存Recall@5免責文2箇所を新セクションへの相互参照に更新。完了条件の実測=本番23問の夜間自動測定(2026-07-04 03:34・実eval)が生成した strictAccuracy を /about/chatbot-eval が読み 95.2% を表示（build prerender で静的描画を検証・値はeval:chatbot-gen実結果ゆえ自前フレーズ捏造ゼロ）。全ゲート緑=tsc(自変更clean)/lint(0 error)/vitest(2698 pass)/build(exit0・当該ページ静的prerender)。※スナップショット更新モデル=既存Recall@5(chatbot-eval-results.json)と同型の「静的commit」＝実eval結果をops PRで再commitして反映（夜間auto-PR refreshは採らない＝毎晩PR濫造とclean-tree契約違反を避ける・実測値の伝播は次opsティックの再syncで担保）。着手判断=全6レーン稼働だが/aboutを触るopen PRは0件（gh実測=衝突リスク皆無）＋直近7イテがeval-nightleyへの補充連発で水増しリスク＝starvation中の真の#1を実行。CPU競合はcorrectness変更を無効化しない（perf実測とは異なる）。
- [x] 【Opus・P1】報告系補充: reporter `-SelfTest` を**引数不要**で走れるよう是正＝機構4系統の唯一「自己検証が単独起動できない」スクリプトの穴を封止。contract-3 は運用機構の全PowerShellに `-SelfTest`/`-WhatIf` 相当の自己検証を課すが、`loop-report-status.ps1` だけ `$Lane` が **`[Parameter(Mandatory=$true)]`** ゆえ **`powershell -File .\loop-report-status.ps1 -SelfTest` が「Missing mandatory parameter: Lane」で即死**した（`-File` 非対話起動＝CI・launcher・本contractのゲートでは mandatory 欠落は**致命エラー**で、旧docstringが想定した対話プロンプトは出ない）。launcher/runner/eval-nightly/watchdog は全て `-SelfTest` を**ゼロ引数**で回せるのに reporter だけ magic な `-Lane <placeholder>` を要求＝自動ゲートで最も skip されやすい自己検証が事実上到達不能だった（#793/#797 が全4スクリプトの `-SelfTest` を ops-selftest.yml で緑必須化したが、reporterのそれは引数無しでは落ちる＝ゲートの実効性の穴）。修正(全ops所有・web/src非侵): (1)`$Lane` を param層の `Mandatory=$true` から `[string]$Lane=""` へ緩和、(2)純関数 `Test-LaneMissing($SelfTest,$Lane)`（SelfTest時は常に許可／実runは空/空白/null lane を欠落判定＝null安全）新設、(3)`-SelfTest` ブロック脱出直後に本ガードを配線＝実runは従来通り `-Lane` 必須だが**大声のmandatory致命エラーではなく親切なメッセージ＋exit 2**、SelfTestのみ免除。→ 2026-07-04(PR)。`-SelfTest` に N群6アサート追加（SelfTestは無lane許可／SelfTestはlane無視／実run+lane許可／実run空lane却下／空白lane却下／null lane却下=null安全）→**引数無しで ALL PASS・exit0**（本修正が直したまさにその起動法）、`-WhatIf -Lane ops` は従来通りdryrun計算のみ（無回帰）、`-File` 実run無lane→親切ERROR＋exit2、PSParser 0-error・追加行の非ASCII 0を実測。活性化=各クローンの次git pullで reporter はfresh processが最新版を読む＝即。補充理由=未着手[ ]2件（掃除系残/perf実測）は共に「他レーン停止の静穏窓」ゲートで全6レーン稼働中（runner process 6/6実測）ゆえブロック（3件未満）→運用4系統(報告)の欠陥から補充＝直近のeval-nightly補充連発とは別系統(reporter)の実欠陥。
- [x] 【Opus・P1】報告系補充: 夜間eval(O18)の**全滅（no-report）を沈黙で消さない**＝生成品質evalが解析可能レポートを1本も出せずその日の再試行予算（既定3回）を使い切った「terminal失敗」を、社長が唯一見る docs/loop-status.md にバナーで surfacing。従来 loop-eval-nightly.ps1 は (a) `Get-EvalBannerLines` が **`ok=false`のレコードを一律「バナー消去」**扱い（旧line293 `if($null -eq $Record -or -not $Record.ok){return @()}`）し、(b) no-report失敗パス（`$report -eq $null`）は失敗markerと history を書いて **`Update-EvalStatusBanner` を一度も呼ばず `exit 0`**していた。結果、本番 /api/chatbot 停止・デプロイ中断・DNS/ネットワーク障害等で eval が丸1日1問も測れずとも監視ファイルは無反応＝**「このレーンが殺すはずの沈黙データ喪失」の最外殻（測定そのものの全滅）が唯一無防備**だった（below-target=大声・incomplete=注意 は既に surfacing 済なのに、全滅だけ無音）。修正(全ops所有・web/src非侵): (1)`Get-EvalBannerLines` を「null→消去／`ok=false`かつ`terminal`→**可用性注意バナー**（品質アラームではない＝未測定ゆえstrictAccuracy不在）／`ok=false`かつ非terminal→消去(=再試行残りありは静音・単発blipでバナーをflapさせない)／`ok=true`→従来のbelow/incomplete/clean分岐」へ再設計、(2)no-report失敗パスに **terminalのときだけ `Update-EvalStatusBanner -Record $rec`** を配線（非terminalはバナー不変＝同日再試行がまだ走るので触らない）、(3)`loop-status-strings.txt` に `evalFailedHeader`/`evalFailedBody1`（ASCII厳守・{DATE}/{ATTEMPTS}/{MAXATTEMPTS}/{EXIT}差込・「未測定・直近測定値は据え置き・翌測定成功で自動消去」を明記）新設、(4)ログの action ラベルを below-target 専用→3系（below/incomplete/could-not-run）汎用へ正直化。→ 2026-07-04(PR)。`-SelfTest` に H2b群5アサート追加（terminal失敗→注意3行/ヘッダは可用性注意でありbelow-targetではない/attempts 3-3表示/body に日付+exit 7/at-or-above測定は無バナー不変）＋既存 H2 を「非terminal失敗は静音」の意味へ明確化→**引数無しで ALL PASS**、`-WhatIf` は当日terminal既済で idempotent skip(exit0) を実測、実 `loop-status-strings.txt` から新2キーが raw キーに退化せず {ATTEMPTS}/{DATE}/{EXIT} を実置換することを実測、launcher `-SelfTest` ALL PASS（共有 strings への追加が既存 banner 系に無影響）。活性化=各クローンの次git pullで eval-nightly は次夜間tickから新パスを読む。補充理由=未着手[ ]2件（掃除系残/perf実測）は共に静穏窓待ちで全6レーン稼働中ゆえブロック（3件未満）→運用4系統(報告/O18)の実欠陥から補充。
- [x] 【Opus・P1】計測基盤補充: 夜間evalの再試行budget(既定3回)が**~195sで密集**し10分超のprod停止で当日が全焼する欠陥を封止＝時間cooldown導入。O18-aの retry budget(PR#824)は no-report 失敗の**COST**を `MaxEvalAttempts x 23Q/日` に有界化したが**CLUSTERING**は未有界だった。ops loop が step5.7 で本script を **~195s tick** で叩くため、`Test-AlreadyRanTonight` が `attempts>=MaxAttempts` **のみ**で terminal 判定していた旧実装では3回の再試行が **~10分以内に密集**→prod の deploy/コールドスタート/DNS瞬断が~10分を超えると当日の試行窓を使い切り terminal 化、残り~21時間 prod が健全でも当日の生成正答率を1問も測れず**全焼**（O18-a が救った「沈黙データ喪失」の残り穴＝コストは守ったが密集は無防備）。修正(全ops所有・web/src非侵): (1)`Test-AlreadyRanTonight` に `-Now`/`-RetryCooldownMinutes`(既定40) 追加＝非terminal失敗が cooldown 窓内(`ranAt`から経過<cooldown)なら「このtickは実行不可」を返し再試行を後続 tick へ後ろ倒し＝3回が `~T/~T+40/~T+80` に分散し~80分の停止を橋渡し（**COST不変**＝依然 `<=MaxAttempts x 23Q/日`・ops-tick密集から再試行タイミングを分離しただけ）、(2)後方互換＝時計未指定/cooldown0/`ranAt` parse不能 は従来通り即再試行(fail-open＝詰まった日を作らない)・`attempts>=Max` の exhausted は cooldown 無関係に terminal 維持、(3)実行時 call site(line746) が `$now`/`$RetryCooldownMinutes` を引き渡し skip ログを cooldown 明記へ正直化、(4)`.PARAMETER RetryCooldownMinutes` doc 新設。→ 2026-07-04(PR#862)。`-SelfTest` に **B3群6アサート**追加（5分<40→hold/50分>40→runnable/時計無→OFF/cooldown0→OFF/exhausted→terminal維持/`ranAt`不能→fail-open）→**引数無しで ALL PASS・exit0**、`-WhatIf` は当日measured(95.2%)で日ガード短絡＝idempotent skip 無回帰、PSParser **0-error**・追加行の**非ASCII 0**・web/src非侵を実測。活性化=各クローンの次git pullで eval-nightly は次tickから cooldown 経路を読む。補充理由=未着手[ ]2件（line67 lint --fix 残／line69 perf実測）は共に全6レーン稼働中(runner 6/6実測)の静穏窓待ちでブロック（3件未満）→運用4系統(計測基盤)の実欠陥から補充。
- [ ] 【Sonnet・P2】掃除系一括（他レーン停止時間帯に実行・1PR/群）: lint警告46件（33件は--fix、ky-paper-viewのexhaustive-deps 2件は手当）／~~scripts/audit/internal-link-graph.json 再生成~~→**完了(2026-07-04)**＝手順は `node web/scripts/audit/internal-link-graph.mjs`（純静的パス＝src/をregex走査しJSON出力のみ・dev/build不要・CPU競合なし＝静穏窓不要と実証）。7週stale(2026-05-17・160route)を現mainへ更新(193route)。孤立routeは全てadmin/dev/auth/redirect stub/flow到達(print/success/error/result)＝意図的でユーザー向けIA欠陥なし（/quick-startは/quickへのredirect stub＝sitemap.test.ts:123で実体URL非該当と確認）＝他レーンへの起票不要／~~news-feed LLM判定基準の明文化（週次熱中症搬送記事の承認ゆらぎ是正）~~→**明文化パート完了(2026-07-04・docs/news-ai-judge-criteria-2026-07-04.md)**＝基準を実データで文書化し承認ゆらぎの根本原因（集計熱中症搬送記事のnewsType未規定＝approved「5月4176人」statistics vs rejected「週1100人余」general_newsの実揺れ）を特定・是正はプロンプト内容変更＝data班所有かつ社長編集判断(Path A)ゆえ選択肢A/Bを§5で提示しescalate（ops独断で確定しない）。残=lint --fix（web/src・静穏窓）のみ。診断 07-residuals-sweep.md P2群。
- [x] 【Opus・P2】横断計測: モバイルperf第2弾ハーネス整備＝`scripts/mobile-lighthouse.mjs`＋`docs/perf/mobile-wave2-README.md` 新設。第1弾のアドホック lighthouse 起動を固定化し、既定11ページ×3回中央値を1コマンドで記録（第1弾達成済 /accidents・/laws・/whats-new は除外）。`--dry-run` 自己検証（Chrome/サーバ不要・0終了）を実測確認。lighthouse は npx 都度取得で依存追加なし（Path A非侵）。→ 2026-07-03完了(PR)。line 13 の実測を静穏窓で実行可能にする補給。
- [ ] 【Sonnet・P2】モバイルperf第2弾の**実測実行のみ**（是正はしない・ハーネスは整備済＝`scripts/mobile-lighthouse.mjs`）: **他レーン停止した静穏なマシンで** `cd web && npm run build && npm run start` → `node scripts/mobile-lighthouse.mjs` を実行し `docs/perf/mobile-wave2-<date>/` を記録、perf90未満だったページだけを該当レーンへ【Opus】タスクとして分割起票。診断07 P1-6。※6レーン同時稼働下はCPU競合で数値が無効化するため実行不可＝静穏窓待ちで保留。

## 補充の指針
運用機構の欠陥（点火・補給・点検・報告の4系統）と横断計測のみ。機能改善は積まない。
