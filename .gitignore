# Claude Code local worktrees
.claude/worktrees/

# MHLW downloaded data (large, not versioned)
mhlw-data/

# Generated raw accident JSONL (~400MB, regenerate via scripts/etl/parse-shisho-db.py)
web/src/data/accidents-mhlw/

# Build artifacts
.next/
node_modules/
dist/

# Python ETL artifacts
__pycache__/
*.pyc
.mypy_cache/

# News-feed inter-stage handoff (regenerated each run by fetch-news-feed.mjs)
scripts/etl/data/news-feed-candidates.json

# チャットボット週次スモークプローブ (web/scripts/chatbot-prod-smoke-probe.mjs) の
# 実行結果。実行のたびに上書きされる一時レポートのため追跡しない
web/docs/third-party-reviews/scripts/chatbot-prod-smoke-latest.json

# OS / editor
.DS_Store
Thumbs.db
*.swp
*.swo
*.bak
*.tmp

# Package manager lock files (npm のみ使用。pnpm/yarn は禁止)
pnpm-lock.yaml
yarn.lock

# Env
.env
.env.local
.env.*.local
.claude/scheduled_tasks.lock
.vercel
.env*

# 無人自走ループ runner のログ
logs/

# 無人自走ループ launcher が生成するローカル専用の実行時アーティファクト
# (loop-config.json は追跡・人手編集。可変状態と現況ダッシュボードは追跡しない)
loop-state.json
docs/loop-status.md
# 夜間 生成品質eval(O18) のローカル専用 機械可読アーティファクト(main tree・/about用の追跡スナップショットは別途)
.loop-eval/

# マスコット画像生成パイプラインの生成物（採用分はweb/public/へ最適化して配置。生画像はコミットしない）
scripts/imagegen/output/

# 自動生成の法定対応表（npm run curriculum:status で再生成）
docs/education-curriculum-coverage.md
